# Libs Meeting 2026-08-18

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia Deckers, Chris Denton, simulacrum, Clar Fon, Amanieu, Josh Triplett, Josh Stone, The 8472, Max, Tomáš Šedovič

## Agenda

- LC & libs-FCP selections - call for candidates
    - Nia (pre-meeting): If you'd like to stand for either of those positions and haven't done so already, please [put yourself forward](https://rust-lang.zulipchat.com/#narrow/channel/253831-t-libs.2Fmeta/topic/Council.20rep.20selection.2C.20sept.20.2726/with/614640476) and/or [respond here](https://rust-lang.zulipchat.com/#narrow/channel/275122-t-libs.2Freviewers/topic/Library.20FCP.20team.20membership/near/616707147), respectively!
- Triage
- Anything else?

## Triage

### FCPs

1 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3983 *\`f16b\` type* - (4 checkboxes left)

[the8472 (1)](https://rfcbot.rs/fcp/the8472), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [Amanieu (1)](https://rfcbot.rs/fcp/Amanieu), [BurntSushi (1)](https://rfcbot.rs/fcp/BurntSushi)

### LC Nominations

Josh: I'm the only person nominated so far. If you are interested, please nominate yourself.

Nia: I put myself forward as willing to facilitate again if people want.

### FCPs

22 rust-lang/rust T-libs FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/134915 *Tracking Issue for ByteStr/ByteString* - (2 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/158186 *Guarantee 8 bytes of alignment of RawWakerVTable* - (3 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (3 checkboxes left)
  - merge rust.tf/157973 *Distinguish \`repr(C)\` ZSTs from others in ABI compatibility rules* - (4 checkboxes left)
  - merge rust.tf/158368 *std: make \`Instant\` a signed \`Duration\` on all platforms* - (2 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/157144 *Prevent mutating the global environment pointer in \`CommandExt::exec\` and opt to use execve and resolve path manually* - (3 checkboxes left)
  - merge rust.tf/160564 *volatile: allow accesses to non\-AM memory to trap* - (3 checkboxes left)
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (2 checkboxes left)
  - merge rust.tf/160534 *stabilize smart pointer map functions* - (3 checkboxes left)
  - merge rust.tf/161015 *Stabilize \`funnel\_shifts\` (including \`const\`)* - (5 checkboxes left)
  - merge rust.tf/160394 *Lang/libs coordination question: okay for libs to stabilize traits with \`final fn\` in them?* - (5 checkboxes left)
  - merge rust.tf/132484 *unix: Relax escaping in \`Debug\` impl on \`Command\`* - (0 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[saethlin (1)](https://rfcbot.rs/fcp/saethlin), [BurntSushi (1)](https://rfcbot.rs/fcp/BurntSushi), [digama0 (2)](https://rfcbot.rs/fcp/digama0), [BoxyUwU (1)](https://rfcbot.rs/fcp/BoxyUwU), [nikomatsakis (4)](https://rfcbot.rs/fcp/nikomatsakis), [the8472 (13)](https://rfcbot.rs/fcp/the8472), [Amanieu (7)](https://rfcbot.rs/fcp/Amanieu), [lcnr (1)](https://rfcbot.rs/fcp/lcnr), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [dtolnay (2)](https://rfcbot.rs/fcp/dtolnay), [joshlf (2)](https://rfcbot.rs/fcp/joshlf), [thomcc (3)](https://rfcbot.rs/fcp/thomcc), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [tmandry (2)](https://rfcbot.rs/fcp/tmandry), [joshtriplett (7)](https://rfcbot.rs/fcp/joshtriplett)


Nia: these are all things where we said we'll never call the global allocator that could under certain cases call it. E.g. for the thread-local allocator can panic and the panic can call the global allocator. I think we can mark them all high.

The 8472: Do we have some debug flag?

Clar: These are extremely difficult to deploy and also extremely difficult to actually create a test for.

Nia: 8472: having some idea of a critical section where you can't panic is something I've been hoping to get through. But that will require lang, it's akin to an effect system.

The 8472: I meant something akin to masking interrupts on the CPU

Nia: I have an ACP for setting a scoped panic hook. That does use thread locals, though so it won't help here. I don't see a nice way to do this at runtime withthout reimplementing thread locals.

Amanieu: Didn't we at some point decide thread locals should use system allocator?

Nia: They do, but they can panic. And that's a re-entrancy. We explicitly guarantee that the global allocator can for soundness reasons assume that calling a thread local will not be re-entrant. And if that panics.

Amanieu: How can thread locals panic?

Nia: `Thread::new` on certain unix platforms. `Thread::current` uses it. It can panic during TLS construction.

Amanieu: What happens when you call global allocator during TLS construction.

Josh: We have a way to make a function unsafe that was previously safe. We did this for `before_exec`. We have `rustc_deprecated_safe` and use that to say panic hook setting would be unsafe

Nia: I raised that. There was pushback. 

The 8472: Spelling out all the things global panic handler is not allowed to do might be an open set. We may discover more things we shouldn't do

Amanieu: We should say that the standard library's panic handlers be re-entrant

Nia: But we would have to say that global allocator relies on reentrancy now. That would be a breaking change.

Amanieu: It feels the simplest solution is to require global allocator to be reentrant

Nia: It would be if we didn't stabilise global alloc

Josh: (1) allowing global alloc reentrancy, (2) panic hooks, (3) we don't in practice allow re-entrance in global allocator. That seems preferrable if we could

Nia: I think that's tenable to see whether thread local panics and enforce that in CI

The 8472: We need to not panic but we also need to not allocate. We're forcing two properties that we don't have effect system for

Nia: Agreed, we should have effect system. We can verify that we don't reenter and we should audit that we don't panic

Clar: I don't think we should rely on linker shennanigans for linker reasons. They rely on LTO, but once build-std is there, we can't rely on that. It also makes the linker the load bearing aspect for soundness. It's better to put careful attention to the target-specific code: thread_local storage, park and unpark. Most of these bugs are target-specific.

Nia: This is probably something we can discuss async?

Chris: I don't think we'll design an enforcement mechanism in this meeting

Amanieu: I still think that if allocator is called, we require it to be reentrant. Rust code by default must be reentrant. 

Josh: You can write non-reentrant code without requiring unsafe. E.g. a lock.

Amanieu: But the reentrancy won't lead to unsoundness.

Amanieu: We shouldn't mix safe/unsafe into this discussion, that's orthogonal.

Nia: Regardless of what we do, something must happen. Something is broken and we should take one of these options. But regardless, the solution shouldn't affect the priority of these issues. I still advocate putting `high` on these and opening a thread on Zulip to discuss solutions.

The 8472: If nobody hits this in practice and it's been an issue for a long time, I'd go with `medium`

simulacrum: if we want to claw back the global alloc guarantees, we should do that sooner rather than later.

(we did a vote in the room, slight preference for `high`)


### (Prioritization Requested) rust.tf/160793 *Inappropriate \`panic!\`s in \`Thread::new\` on certain unix platforms*

### (Prioritization Requested) rust.tf/160815 *\`Condvar\` and \`Mutex\` in \`std::sys::pal::unix::sync\` violate aliasing rules*

### (Prioritization Requested) rust.tf/160837 *More global allocator reentrancy issues in \`park\` and \`unpark\`*

### (Prioritization Requested) rust.tf/160930 *More global allocator reentrancy issues; this time \`thread\_local\`*

### (Prioritization Requested) rust.tf/161091 *\`rtabort!\` can panic on xous*


### (Nominated) rust.tf/144426 *Tracking Issue for \`core::mem::DropGuard\`*

Nia: We were asked about a possible stabilisation. 

Clar: I propose to drop bikeshedding and keep the proposal as is

Nia: I agree

Nia: `dismiss` had the fewest amount of people who disliked it. There were a few people who *really* didn't like `disarm` because of association with weapons.

Clar: I think in this case was: "I'm not a native English speaker and this doesn't sound intuitive". For me as a native English speaker none of these are intuitive. People will have to learn it.

Josh: I agree with the notion of using something that's not analogous to a weapon, bomb etc. `dismiss` is fine, but "dismiss" as a verb is something one could do to an entire object, not just to the guard. That's why I suggested `unguard`

Nia: Dismiss is an associated function so you'd write `DropGuard::dismiss`

Josh: If you have a stable type that has `Deref`, you can't add a stable method to it. But DropGuard is not stable yet so we could add a method to it.

Nia: But people may already have such a method

The 8472: that's an argument for more obscure name

Amanieu: I'd leave it as is (dismiss)

Josh: I have a mild preference for making it a method

Nia: We could do add it at a method. But one of my main goals is to ship something without bikeshedding it

Josh: Ship it

Clar: Ship it

Nia: Would anyone be opposed to `dismiss`?

(crickets)

Nia: Does any one want to change the name `DropGuard`?

Amanieu: Please no. Ship it.

Josh: No.

Chris: Can we update the tracking issue to show the API?


### (Nominated) rust.tf/146407 *Tracking Issue for Windows' \`CommandExt::inherit\_handles\`*

Nia: I want to stabilise this and [opened a PR doing that](https://github.com/rust-lang/rust/pull/161163). Right now we don't have a nice way to say whether a child process should inherit a handle from the parent process. For Windows, the answer is "we shouldn't unless explicitly requested". And we do pass them on by default now.

(discussion about whethre these are owned handles)

Chris: We haven't defined the API yet.

Nia: I thought we did.

The 8472: There's a spawn_with attributes

Chris: You can use that, but we didn't do this specifically for handles

The 8472: Might a call rely on the address of the handle?

Chris: Yes

The 8472: So either you have to pass in owned handles or `Command` needs a lifetime

Chris: I think the boolean solution is still useful however we design the API

The 8472: I was not concerned about usefulness but more about adding one thing in isolation without thinking on how it affects other parts.

Chris: We have a lot of methods for setting low level flags and say this is another one. It just sets the flag on `Command`

Clar: The docs nowhere say what kinds of handles this applies to

Chris: All kinds of handles

Amanieu: It's in the same way as you'd say "any kind of file descriptors"

Chris: They're basically just IDs

The 8472: By default, they're set to not inherit.

Clar: There's no default, the API takes the value of a boolean that you need to pass.

Chris: We set them to not inherit by default when you create a file etc.

The 8472: So what do you need the flag on createprocess for?

Chris: For when you don't want to inherit anything at all or when you want to inherit everything or when you want to inherit a list.

Nia: On the grounds of exposing the underlying thing, I think it's good to stabilise this. Regardless of the final shape, this will likely be a part of it.

The 8472: I read a comment from PaulDance about the tristate of this. If we want to support the list, we'd have to pass in an `Option<[..]>` and set the bool.

Chris: We have a similar clear attribute.

The 8472: Wouldn't this clear attribute conflict with other stuff you could do?

The 8472: maybe it could be a `#[non_exhaustive]` enum and start with `None` and `MarkedForInheritance`?

Clar: This is specifically for Windows. And knowing how stable Windows API is, it's unlikely that we'd stabilise something and make it less usable than the Windows API. We could offer a nicer API, but I don't think it'd be bad to give them access to the actual function we'd call

The 8472: I thought we were doing other things in the background like setting the empty list.

Chris: We don't do anything like that at the moment. It would also allow us to elide the lock we currently have.

The 8472: In that case, it sounds fine

Nia: I'm fine with that as well

Nia: Would people like to open an FCP on the stabilisation PR?


### (Nominated) rust.tf/149745 *\`DebugStruct::field\_with\` and friends are code size footguns*

Nia: There was a problem with code size and now it's much less bad: https://github.com/rust-lang/rust/issues/149745#issuecomment-5308437830

Clar: I suggested to nominate it if you have the data

Clar: If somebody is trying to include these methods in a release version, that would be an issue. But now we got it to the code size being 2x, before it was ~13x

The 8472: Regarding debug vs release: those names are overloaded. `debug` means "this is meant for the developer"

Nia: I agree, it makes sense to ship debug stuff in release if it is useful for you as the programmer

Clar: There's a [note that we could make the code smaller](https://github.com/rust-lang/rust/issues/149745#issuecomment-5308599454) if we wanted it to be unsafe. That seems not worth it.

Josh: Do we expect in practice that a closure passed into `field_with` is going to move into?

Clar: Mutable references is one case. 

Josh: Strictly speaking it should be a `FnOnce` because we'll only call it once.

The 8472: "it optimises better and reducing people's code size" is an argument for doing this

Josh: There's another way to fix this: today `dyn` works with a `Fn` type bit it doesn't work with a `FnOnce` type. We could explore fixing that. If we made `dyn FnOnce` work, we wouldn't have to pass it as `dyn Debug`

Nia: I'm not sure how much work that'll be

Josh: Seems worth doing a quick investigation.

Nia: Would you reply and we can look at this next week?

Josh: Is there a change to the API of `field_with`? I think it'd be reasonable to ship it now and optimise it in the future.

Nia: `dyn FnOnce` sounded to me as something that would require a different signature.

Clar: The signature is an issue for FnOnce because it takes it by value and you can't have it unsized

Nia: I think we can implement something -- at least internally in the standard library -- that won't be exposed and would work here.

Clar: We could also make a dedicated API for a function that we'll only call once, but it's not a `FnOnce`

Nia: Sure. But it does sound to me as something we can address with the signature

The 8472: I think it's still worth evaluating which options we have and which ones would change the signature

(note for the minutes: Josh Stone just joined, every "Josh" mentioned until now has been Josh Triplett)


### (Nominated) rust.tf/156882 *alloc: stabilise \`Allocator\`*

Nia: We have the refactor of the traits merged on nightly & beta. Small questions came up: Jake proposed we stabilise a few more methods (breaking apart box and vec into pointer+allocator and reconstituting them along with `.allocator()` methods on both).

Nia: I raised concerns on whether we want `from_raw_parts` and `from/into_non_null`. I propose we deprecate raw parts and only stabilise the non_null one.

Nia: I think we're confident that we could do the `Allocator`, `StaticAllocator` (name tbd) and `GlobalAllocator`

Josh Triplett: I think it's appropriate for new types to just have the `non_null` variant. But existing types that only have the `raw_parts` method, we should provide that too.

Nia: The semantics of `from_raw_parts` are `NonNull::new_unchecked`. But it is a papercut

Josh Stone: just the non_null one is fine for me.

Chris: +1

Clar:  ??

Nia: Fair. Do we want to stabilise these in the initial batch? If people are okay then yes, go ahead

Nia: The other point is the APIs. StaticAllocator is an allocator that will only even free memory when you call deallocate/grow/shrink. It will never deallocate when being dropped. It's necessary for `Pin` to work. And it's also the semantics for the proposed new allocator intrinsics that LLVM is getting. The third one is something to replace GlobalAlloc which doesn't allow you to pass in zero sized allocations, doesn't allow you to express reentrancy etc. I kept the stabilisation doc at the very top of the PR.

Nia: Are there any concerns? If not, can we open FCP soonish? All the existing ones should be covered.

Amanieu: But now we're only stabilising `Allocator`, not StaticAllocator etc.?

Nia: Yes, just `Allocator`. None of the APIs on `Allocator` have changed other than the `chain_by_ref` which we agreed on at the All Hands.

Amanieu: This seems fine

Clar: I know me and Max are going to do a big audit on the final PR anyway

Clar: I don't think there are any blocking things, just nitpics

Amanieu: Is the fact that we're not switching to the new GlobalAllocator trait be a problem in the future?

Nia: I don't think so. It's something we'll need to consider when we'll stabilise the GlobalAllocator trait

Amanieu: This might require adding a blanket impl for GlobalAlloc of the GlobalAllocator trait

Nia: Which we have in nightly

Clar: GlobalAllocator has a few extra requirements that GlobalAlloc doesn't have. E.g. you added a Sync bound.

Amanieu: I thought we were going to do the blanket impl do the other way around: having the attribute require the ??

Nia: We can do that.

Clar: That feels like it might require an Edition redirect.

Nia: You'd have to write an impl of Allocator for every type that's GlobalAlloc

Clar: Weren't there some concerns with zero sized allocations?

Nia: Yes, effectively doing that, you're hurting everyone now who wants to use GlobalAlloc. I think we can keep GlobalAlloc as the thing that the attribute points to and change that over an edition.

Clar: global_allocator is only in the prelude. It wouldn't even require an edition redirect, just a new prelude for the edition

Amanieu: I'm not happy about changing the attribute over the edition because the allocators implementations will stop working

The 8472: We'd have to announce that early

Clar: You could have it work with both editions. It would still work

Nia: Yes. And cargo fix would do this for you

Nia: When do we want to do FCP? Now and open concerns for implementation nitpicks?

Amanieu: Fix these, then then we can start the FCP

Clar: The existing PR should be merged before start the FCP but we don't have to wait for the meeting next week

### (Nominated) rust.tf/158368 *std: make \`Instant\` a signed \`Duration\` on all platforms*

Josh Triplett: It has an FCP proposed. It was nominated to get more checkboxes. I filed an concern for waiting on BurntSushi as the author of jiff.

Clar: I'd say jiff seems unrelated. I think that's solely on clock time and not durations. It's looking at durations between datetimes.

Josh: https://docs.rs/jiff/latest/jiff/struct.SignedDuration.html

Amanieu: Duration is a scalar, it doesn't make sense for it to have a sign. I'd prefer TimeOffset

Josh Triplett: I get the philosophical objection

Amanieu: It's minor, people will understand if we use SignedDuration

Clar: This is purely internal. None of these naming decisions will be exposed publicly

Josh Triplett: I would love for us to ship a SignedDuration type in the standard library

Chris: So the public guarantee we are making is it has the same representation for all platforms? The documentation removes all the platform-specific docs

Clar: That wouldn't necessarily be a guarantee

Josh Stone: The default assumption should be that the implementation for types doesn't need to be specify

Clar: I believe this does allow some cases that weren't previously allowed.

Chris: We have been changing implementation specific details to allow negative times before this

Josh Triplett: Yes, we should support rust programs that run for longer than 2^32-1 seconds

Clar: I was saying that's a guarantee that many people didn't run into yet. Also, this is `Instant` not `SystemTime`. Instant is always going to be program runtime, so it's specifically running the program for more than 2^32-1 seconds

Nia: I still think this is something we should 

Chris: I'm in favour of instant running consistently on each platform

Clar: I'm in favour of replacing platform-dependent code with platform-independent code where possible

Chris: I'd still like more specific types like `Stopwatch`

Nia: I'd be happy to pick up the implementation

The 8472: Regarding performance, does this change on any platform?

Clar: I think it's only 32 bit platforms because of using u64 and it's relatively minor

Chris: We want Instant to support nanoseconds. So if you add 1 it's not equal to -1

Clar: I also believe we're trying our hardest to not emit division instructions here

The 8472: This is purely internal, we could revert it later, right?

Nia: Yes, if we don't make the promise to stable we could revert this


### (Nominated) rust.tf/159564 *Allow elided ('static) lifetimes in \`thread\_local!\`*

Clar: I nominated this. Making the syntax for the `thread_local!` macro match actual statics (allowing the `'static` lifetime be elided)

Nia: This is stably visible behaviour.

Clar: This is entirely libs, the question is whether Libs want to match Lang with this. The syntax is defined within the macro.

The 8472: If we're aligning more closely with Lang rather than deviating, then we probably don't need their input

Amanieu: This seems fine to me but I'd require an FCP

Nia: I agree



### (Nominated) rust.tf/160951 *Stabilize \`core::io::{Error(Kind), IoSlice(Mut), Result}\`*

Nia: We asked them to send a PR. We can FCP it.


### (Nominated) rust.tf/161015 `P-lang-drag-1` *Stabilize \`funnel\_shifts\` (including \`const\`)*

Nia: I think this is pretty stragihtforward. The only ask for us is to take a look and tick boxes if all is good. This is an intrinsic, but the intrinsics' behaviour is something you can entirely emulate in safe code.

Clar: We have an integer minimum intrinsic proposed which can be done without an intrinsic. But for float minimum does require an intrinsic.

### (Nominated) rust.tf/161153 *Unstable attribute behavior on re\-exports is surprising*

Nia: Is instability transitive and should that match the implementation of doc_hidden. Rustdoc plan to make doc_hidden transitive and Predrag is proposing we match that.

Clar: from discussions with Jana, the instability attribute is not doing what we want, we could lint that

Josh Triplett: Linting seems like a no-brainer. We should do that to catch the potential error. I'd like to change the attribute to do what we want because it affects name resolution

Nia: For the indeterminate future, do we want cargo-semver-checks to do what we expect

Clar: This really hurts on function re-exports. Because you can't change a stability on re-export, you have to create a wrapper function which messes with inlining

Amanieu: I believe that at crate boundary the final item in the redirect chain get's added to the crate metadata and everything in the chain is lost

Nia: We don't care about the crate boundary for libs

Clar: It does work in this particular case. But if we want to e.g. make a rename. We had to make it a function wrapper when moving spin loop to a hint.

Nia: If there are concerns, please tell me. Otherwise, I'm happy to unnominate the issue.

Amanieu: This should be tagged with T-compiler / T-lang as they know better how name resolution works


### (Nominated) rust.tf/161168 `P-lang-drag-1` *We should decide whether take\_mut is sound*

Nia: This is from Ralf.

Clar: Is this about moving out of a mutable reference, changing it and putting it back before anyone notices?

Nia: Yes

Josh Triplett: partial borrow is trying to solve the problem of: have a mutable reference of this half of a struct and a mutable reference to the other half of the struct

Josh Stone: Why does this matter?

Clar: It matters for opsem. When you take a mutable reference, you're tagging the entire thing as mutable. If you're doing a partial borrow, you must 

Amanieu: You have to use `&raw mut` to create a raw pointer from it, that allows you to have two raw pointers to access the same thing

Clar: I also believe there's a lang proposal for view types. Is this a libs concern or not? A lang feature doesnt' exist, therefore people made a libs solution that's unsound. The answer should be for lang to provide a solution

Nia: Ralf is asking both lang and libs

Clar: If it's fundamentally unsound from opsem perspective, that's a lang issue

bThe 8472: I think this is one of the cases where they have to chose one of the two possible models and they need to pick one and forbid the other

Nia: Yes, either `take_mut` or partial borrow must go

Josh Stone: Ralf said: "I suspect both lang and libs since this is about a decision on the safety contract of a built-in type (lang) that is best exposed via a std API (libs)."

Josh Triplett: because you have an `&mut` partial, you can't get an `&owned` partial, you can't satisfy ??.

Josh Stone: It sounds they ought to be handing out something pinned.

Amanieu: That's addressed in the 2nd paragraph. They give out mutable reference directly, because it has better ergonomics because of the implicit reborrowing.

Josh Triplett: That should be handled in the future. We'll have a reborrowing mechanism and also ship ViewTypes and then partial borrow won't be needed.

Josh Triplett: I do think ti's important to have an `&mut T` and temporarily get a `T`. That's what we should favour and ship in the stable library.

The 8472: If lang is okay with that, we should just discuss how that should look in the standard library.

Amanieu: The downside is you have to abort the whole process if this closure ever unwinds. But the original question: `take_mut` should be sound

Josh Triplett: I'm happy for us to add APIs that assume you don't recover from panics


### (Nominated) rust.tf/libs489 *ACP: \`#\[derive(Debug)\]\` on unions to print \`MyUnion { ... }\`*

Josh Triplett: It's useful to have a stub Debug for something that works on a struct where some fields don't implement Debug. But I think it's surprising that you'd derive a debug and get something that's worthless. So I think we should have a convenient way to do this, but we shouldn't just get it from `derive(Debug)`:

`#[derive(Debug(stub))]`

Josh Triplett: That would also be useful on a struct.

Clar: Would it work with the skip attribute? If you were to derive Debug on the union and mark every field as `skip` I'd have no issue with it

Josh Triplett: It would be uncomfortable if `derive(Debug)` could generate unsafe code for you that you haven't vetted. In general you can't read a union field without knowing something about the union. The only safe derive implementation we can write about the union is to emit nothing about the implementation.

The 8472: Even if we had an unsafe derive, even that would be footgunny. You'd need to know what's happening inside to know whether the Debug is safe

Josh Triplett: Yes, I'd prefer to have to implement debug yourself then in that case. So, we should either have a way to provide a stub Debug, or suggest implementing it yourself.

Clar: 

The 8472: "just print the name" as the only sensible thing you can do in an automated fashion ??

Josh Triplett: A agree there's only one sensible behaviour that compiles, but we could say the other ones don't compile. We made a particular decision on specific structs that say the only sensible implementation is to say this is a struct. But I don't think we should do that automatically when you do `derive(Debug)`

The 8472: This is still just developer information

Josh Triplett: If you do a `derive(Debug)` on a union, the compiler should fail and tell you to either use a different syntax to print out the stub or derive debug yourself and provide a stub of that.

Josh Triplett: The third alternative is: "oh, I shouldn't implement debug on this union at all, instead I should implement debug on the outward struct that has the union discriminant stored"

Amanieu: I agree, but we should make it convenient to use the stub output

Josh Triplett: That makes the consequence to runtime. I ran into things like this and would have appreciated the compiler to tell me when I wrote it

The 8472: I don't expect this will result in a critical issue

Nia: We could allow this and have a warn lint. I see the case for "I don't care" 

The 8472: I'd prefer the simple thing to just work, but if Josh wants this, how close are we to having a Debug modifier

Josh Triplett: This is not hard. We needed a motivating case for arguments to Derive for a while.

The 8472: In that case it's fine.

Clar: We could have this as a linter.






### (Chris) *Vibe check on enforcing more clippy lints in CI* (e.g. rust.tf/161017)

Nia: Do we want more clippy in CI? Would we like more safety comments etc. in std?

Josh Triplett: Thank you for going through the safety comments in std! And yes, I think we should have a goal of having a safety comment on every unsafe use in standard library. I have a moderate preference for us to not stub out "safety: untriaged" but instead to write an expect attribute.

Nia: We want this to be doable locally, so we're using `./x tidy` instead of clippy. I'm happy to switch out the `safety: untriaged`. The reasoning was not people copying that code to suppress the warnings

The 8472: For clippy, this should be rule-by-rule decision. 

Chris: Absolutely, I'm going thorugh them lint by lint

Nia: Notably, we went through removing useless referencing and dereferencing.

Clar: It can be a fun experience on a smaller codebase to just do `-W clippy_pedantic`. There are a few that are useful: using `&raw` references rather than just const pointers.

Josh: I've got a long list of rust and clippy lints I like to turn on as well. I'd be enthusiastic trying to turn them on.

Nia: I'm fan of integer_arithmetic

(everyone talks about their favourite clippy lints)


Josh's list of potentially useful lints:

```toml!
[workspace.lints.rust]
non_ascii_idents = "deny"
unfulfilled_lint_expectations = "deny"
unit_bindings = "deny"
unsafe_op_in_unsafe_fn = "deny"
unused_qualifications = "deny"

[workspace.lints.clippy]
allow_attributes = "deny" # force `expect` instead
char_lit_as_u8 = "deny"
ignored_unit_patterns = "deny"
implicit_clone = "deny"
manual_is_variant_and = "deny"
manual_let_else = "deny"
needless_borrow = "deny"
needless_borrowed_reference = "allow"
needless_question_mark = "allow"
needless_raw_string_hashes = "deny"
needless_raw_strings = "deny"
non_ascii_literal = "deny"
ptr_as_ptr = "deny"
should_panic_without_expect = "deny"
uninlined_format_args = "deny"
unnecessary_cast = "deny"
```



### (waiting on team) rust.tf/154715 *split try trait in twain*

### (waiting on team) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*

### (waiting on team) rust.tf/160436 *stabilize \`Box::take\`*


### (Regressions) rust.tf/139730 `P-high` *A case of compound x86\_64 performance regression caused by LLVM 20 and #124810*

### (Regressions) rust.tf/53340 `P-low` *Performance regression in tight loop since rust 1.25*

### (Regressions) rust.tf/119560 `P-low` *\`cargo test\` performance regression on Windows on version 1.75.0*

### (Regressions) rust.tf/124216 `P-medium` *Performance regression: rustc failed to optimize specific x86\-64 SIMD intrinsics after 1.75.0*

### (Regressions) rust.tf/130320 `P-medium` *stdlib code size increase in nightly\-2024\-09\-01 for aarch64\-linux\-android*

### (Regressions) rust.tf/142119 `P-medium` *Linking a Rust staticlib unexpectedly changes C math functions from libm to bundled ones from compiler\-builtins*

### (Regressions) rust.tf/142345 `P-low` *\`pin!()\` macro no longer constant\-promotes \`&mut\` references to empty arrays*

### (Regressions) rust.tf/145424 *rustc output no longer reproducible if it crashes*

### (Regressions) rust.tf/149752 `P-high` *TrivialClone is not derived for generic types (Performance regression)*

### (Regressions) rust.tf/152942 *Query depth limit overflow due to nested async fn when building matrix\_sdk*

### (Regressions) rust.tf/153425 `P-medium` *\`&pin!()\` stopped being deref\-coerced in rust 1.88*


### (new change proposal) rust.tf/libs857 *add a way to reuse a \`Box\` allocation*

### (new change proposal) rust.tf/libs856 *ACP: O\_DIRECTORY open flag on OpenOptionsExt*

### (new change proposal) rust.tf/libs855 *ACP: wrapping/checked/unbounded funnel shifts*

### (new change proposal) rust.tf/libs853 *Implement \`Write for Cursor\<W\> where W: AsMut\<\[u8\]\>\`*

### (new change proposal) rust.tf/libs851 *Short Duration constants*

### (new change proposal) rust.tf/libs850 *ACP: Add Instant::epoch()*

### (new change proposal) rust.tf/libs849 *Tuple \<\-\> Array conversion helpers*

### (new change proposal) rust.tf/libs847 *map on homogenous tuples*

### (new change proposal) rust.tf/libs846 *ACP: Add BTF relocation macros to core*

### (new change proposal) rust.tf/libs844 *Add methods for bounds\-checked raw slice pointer indexing to \`SliceIndex\` and slice\-like types.*


### (stalled change proposal) rust.tf/libs363 *Un\-specialize impl ToString*

### (stalled change proposal) rust.tf/libs191 *Add LocalWaker support*

### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs587 *ACP: \`try\_exact\_div\` method on \`NonZero\<{integer}\>\`*

### (stalled change proposal) rust.tf/libs322 *\`AssertThreadSafe\` (name TBD) – a more general API for lifting conservative \`!Send\` or \`!Sync\` implementations*

### (stalled change proposal) rust.tf/libs365 *ACP: \`TwoSidedRange\` trait*

### (stalled change proposal) rust.tf/libs347 *Context reactor hook*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
