# Libs-API Meeting 2026-06-23

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia Deckers, Chris Denton, Mark (simulacrum), Matt & Ben, Amanieu, The 8472, Josh Triplett, Nurzhan Saken, Tomáš Šedovič

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

16 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/69835 *Tracking Issue for layout information behind pointers* - (3 checkboxes left)
  - merge rust.tf/157226 *Partially stabilize \`box\_vec\_non\_null\`* - (2 checkboxes left)
  - merge rust.tf/143989 *Tracking Issue for LocalKey\<Cell\>::update* - (3 checkboxes left)
  - merge rust.tf/112811 *Tracking Issue for \`slice\_split\_once\`* - (4 checkboxes left)
  - merge rust.tf/156906 *Stabilize \`dyn Allocator\`* - (2 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (2 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[dtolnay (2)](https://rfcbot.rs/fcp/dtolnay), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [joshtriplett (8)](https://rfcbot.rs/fcp/joshtriplett), [the8472 (11)](https://rfcbot.rs/fcp/the8472), [BurntSushi (1)](https://rfcbot.rs/fcp/BurntSushi), [Amanieu (7)](https://rfcbot.rs/fcp/Amanieu)

### (nominated) rust.tf/112811 *Tracking Issue for \`slice\_split\_once\`*

Nia: BurntSushi started an FCP. Doesn't look controversial. There's a comment about possible open questions.

Nia: I'm in favor of merging as is. Any objections?

The 8472: The question is, can we have something pattern-like there? Some of the comments mention that too. Is adding a trait backwards-compatible, or would it cause inference problems? Isn't `Pattern` sealed?

Amanieu: I don't think it is.

The 8472: Maybe we can add an assoc type that generalizes to Haystack, or a generic bound to `Pattern` so the Haystack can be changed (str for str, subslice for slices)?

Nia: What would it look like? Put a bound on that trait?

The 8472: Instead of ... it would be ... If we could do that for slices, it would be nice. Breaking on more that one character seems useful. More generally, the splitting that excludes the delimiter (zero-sized matching would be nice but that's regex territory).

Josh: Solving the general problem seems if not intractable, a bigger design probelm. If we had a sealed trait that allowed passing a single value with eq or predicate would be fine. We'd have to choose that now, though, rather than stabilizing this API as is.

Nia: Would we need to change the other split methods for consistency?

Josh: We can't change the stable methods but could add `split_with`.

Nia: It'd be a point for stabilizing this *and* something else.

Josh: ... That's reasonable. We already have split methods that take predicates. If we want to add a new mechanism for patterns, even simplistic ones that don't allow for the multi-element case, we'd need separate methods for them. Having split_once and rsplit_once default to that and not be hardcoded to accept predicates would be odd.

Nia: That's what I was thinking. The 8472, does that address your concern?

The 8472: Kind of. Is it adding one method and then replacing it with more methods?

Nia: The idea is if we want to do multiple ways of splitting, we'd make separate methods and then change the name of ?? over an edition. It'd be weird is split_once took a slice instead of a predicate because of the inconsistency.

The 8472: Some take an index, some take a fn, others take a range (`split_off`). There's not one universal parameter, so I don't buy the argument that they're all the same.

Nia: I see. Reasonable. I'll link the docs: https://doc.rust-lang.org/std/primitive.slice.html#method.split_off.

Josh: Agree that they're not all conistent. We don't have a design for any kind of slice pattern abstraction, and in the absence of the design/unstable trait/method, we shouldn't block this.

The 8472: Don't we have `split_circumfix` that has `SlicePattern`? And `trim`.

Josh: This is implemented for slice or array, but not individual elements. Can't be implemented for `T` and `[T]` because of overlapping impls.

Nia: We could do negative bounds, but that's scary (impl for T if T is not a slice).

Mark: All of the existing SlicePattern stuff isn't doing ... so

Josh: Regular Pattern supports character, string or predicate, but predicate can only look at individual characters. No way to process a chunk of the string at a time. Same here, we can't make SlicePattern work for multiple elements. This would require more design. I don't think these methods should block on that design.

The 8472: Is this urgent? Would it be better to improve SlicePattern and apply it here?

Nia: Not necessarily a problem to stabilize as is. Most of the use cases should be covered. Maybe not urgent, but a quality of life improvement. I don't think an expanded design should be rushed because of that.

The 8472: It doesn't return the delimiter. If you're matching on one thing, it's trivial, but we have split_inclusive that doesn't eat the delimiter.

Nia: Isn't that an argument for split_once_inclusive?

The 8472: We could return a tuple of three elements, where the delimiter is in the middle.

Nia: I don't object, but people could complain about performance.

Mark: It's already 4 registers because it's slices.

Nia: Good point.

Mark: ?? feels awkward as BurntSushi points out, so may not make sense to ??.

The 8472: But strings are just characters, for slices you can have more complex things behind them.

Nia: 

The 8472: We could rename it to split_once_inclusive but why have...

Josh: Because you want that behavior sometimes.

The 8472: You could ignore the middle field.

Nia: Performance/ergonomics, don't have to remember the second item.

The 8472: It gets inlined?

Josh: On str we have split_once but not inclusive.

The 8472: Slices can have arbitrary types where the values are more than a character.

Josh: There's no mechanism to return the item on which you split.

Nia: Seems like most will prefer stabilizing not both split_once and split_once_inclusive?

Josh: No, fine to 

Nia: The 8472, you can raise a concern on the FCP. Would you be ok relegating the third value to a potential future method?

The 8472: Yes, but would like to avoid many variants of a method.

Nia: Would you be ok writing about that?

The 8472: OK.

### (nominated) rust.tf/129436 *Tracking Issue for \`string\_from\_utf8\_lossy\_owned\`*

Nia: Nominated for stabilization. Feels like it should be stable already. Has been open for a long time. Name's ugly, but it's whatever. One note is that the proposed guarantee is tighter, that we sometimes do guarantee Vec reuse. Worth noting. Otherwise, a trivial +1 from me.

Josh: Seems reasonable.

Amanieu: Not a fan of the name, but sure. The UTF8 error one seems unrelated? Where's the error used?

Nia: That's just a doc change.

Josh: Not a doc change, a new method... if you get an error, then you can say "make it work using the replacement character".

Amanieu: Seems fine. FCP?

Mark: One argument could be, if you want a lossy conversion, you could go ??.unwrap_or, which is as performant?

Amanieu: ?? than the owned version?

Josh: Reasonable to have a direct method to get there.

The 8472: Isn't this impl faster?

Josh: If you call from_utf8, it will walk the string and then notice the error, and then you'll have to redo it...

Mark: We return the point where it's not UTF8, so it's a single walk through the string.

Nia: We still need to rely on the optimizer, so this API is helpful. ... 

Josh: I can see the case for the 2-tep version if you need to warn about the encountered error. If you don't need that, this function seems like a straightforward approach.

Nia: What about the updated guarantee? I think it makes sense.

Josh: Reasonable.

Nia: FCP?

Josh: I can do it: https://github.com/rust-lang/rust/issues/129436#issuecomment-4780822636

### (nominated) rust.tf/156252 *Make \`Error::type\_id()\` an inherent method on \`dyn Error\`*

Nia (pre-meeting): See also [this ACP](https://hackmd.io/3juTaSYCTYST4VouUqih_A#new-change-proposal-rusttflibs819-ACP-Remove-static-from-Any-itself-moving-the-bound-to-the-operations-themselves)

Nia: We've been holding this off. One of the ACPs proposed was to add...

https://github.com/rust-lang/libs-team/issues/819

Nia: We should discuss the two together. Should we do it now or with ACPs, or defer?

Josh: Do we have enough info to decide now?

Nia: The ACP only has 1 comment on it, which is a substantive comment.

Josh: One thing I want to call attention to is the mention of "in the next edition we remove the 'static bound and add it as a ...". It's not obvious to me that it's doable over an edition.

Amanieu: Would need two traits and changing which one ?? gets resolved to over an edition. And I'm not sure if we want two versions of the Any trait in std.

Nia: We could do this if Any is two aliases over edition. We don't want two traits

Josh: This would be trivial with supertrait auto impl. Arguably we should wait until we have that.

Nia: So what should we do here? Block the APC on supertrait auto impl? If so we need to have Error::type_id now

Amanieu: Are you proposing for the new `Any` to be a supertrait of the old one?

Josh: Yes. In the new edition you'd have `Any` and a supertrait `AnyStatic`

Amanieu: We don't need the super trait. There's `impl Any (for any type)` so we'd just add a more concrete blanket impl

Nia: I think that's fine. I worry how we'll handle `dyn` objects. They're not handled by the impl

The 8472: Isn't `AnyStatic` the wrong name? It's not static.

Nia: If we have a `dyn NewAny` which doesn't have to be `'static` that won't be passable to old code.

Josh: I agree we wouldn't need supertrait auto impl

Amanieu: Any on the old edition would resolve to `NewAny + 'static` and on the new edition it would resolve to `NewAny`

Nia: this is my concern:
```rust
// old edition code
fn takes_any<T: AnyStatic>(_foo: T) {}

// new edition code
fn foo() {
    let obj = whatever as dyn NewAny + 'static;
    takes_any(obj); // error :(
}
```

Amanieu: They  could write `Any + 'statci` and that could be part of the migration guide

Nia: I'm happy to types-nominate this and ask them. And proceed based on their answers.


### (nominated) rust.tf/156269 *Implement \`Metadata:from\_statx\` for Linux \`MetadataExt\` trait*

Mark: While reviewing this, it's not clear to me how we evolve the API if we want to request more information from the kernel.

https://github.com/rust-lang/rust/pull/156269#discussion_r3368595954

Nia: I feel we discussed this before.

Nia: This is unstable for now.

Amanieu: The kernel people said they weren't going to increase the size of the struct.

Josh: The original thing we asked RfL folks was: how do we handle the scenario where we're asking for certain data from the kernel and we don't know if it'll be properly initialised. The conclusion was we can't rely on the defaults coming out of the kernel for anything it didn't tell us it intialized based on the flags we passed.

Josh: But for how do we handle extensions to the size question. If userspace passes a set of flags, the structure can get bigger and I think it has gotten bigger in the past. So we can pass back a set of flags that are how much data we want. The expectation is that you're passing a mask that fits in the structure that provide. There's a warning that says some flags might extend the buffer further. They do have spare space for future expansion. So if we did go this path, we might be the reason the kernel won't change the interface (because of their "don't break userspace" rule)

The 8472: We could provide the struct with the pre-filled mask that the user could provide their own mask on top of ours based on the kernel version they're using. But they could still break themselves.

Mark: We could ask them for the size of their struct so it's hard to get it wrong.

Nia: FWIW I'm not convinced they can change it now. It would probably require a separate syscall.

The 8472: No, they can put an array in the tail of the struct. C has flexible structure.

Josh: They would be allowed to extend `statx` to be bigger than it is now and specify that if you pass a specific flag the struct would be bigger. That could be dangerous because older code could break by passing flags it didn't understand. But if no such userspace existed, they could make this change.

The 8472: They did that to rseq but they pass the size field to that

Nia: we could mask out fileds we don't understand

Josh: But what if we give you flags you don't understand, you pass them to the kernel and the kernel writes over more than your small struct

The 8472: Is there any reason a user could support a statx struct that's larger than the stdlib understands? If we provide the opaque struct itself and you union it to statx, we can ensure the size. We could provide an opaque placeholder to make sure the struct is big enough.

Amanieu: We could. But realistically, it's unlikely to be an issue. If they make it bigger they'll probably add a size to it

The 8472: The manpage says that it goes through the flag

Josh: They say it goes through the flag. But it's reasonable to say that if they're going to extend the static structure could be the size of the new size being added in. They have 64 bytes of reserved space. We can ask them to add a length field at the end

The 8472: We could ask that. But users who want more can just union it to a struct of a larger size.

Josh: It's reasonable that someone working with this interface will want to pass a structure to allocate ??.

The 8472: You still need to be able to write the struct into the buffer to initialise the mask

Josh: You pass it as input to the syscall and then you get it back in the statx structure with fields the kernel set

The 8472: Ok.

Josh: I don't think we should do our own opaque structure. Last time we asked, I thought there was a message to one of the upstream mailing lists. Ask: We want to be able to hand back flags saying "this is the minimum that we want", can you make sure we can check if these were to overflow?

Tomas: TODO follow up -- has there been anything sent to the upstream kernel list about this (fs-devel)

The 8472: If they zero-initialise the struct, they're telling the kernel there should be no extra space.

Josh: Yes. You'd probably have to have a flag for "I'm passing in a length field". If not, you might not just have initialised the input field on the structure.

The 8472: Then we can't provide the flag, the user has to set it.

Josh: If you pass the flag to indicate the length, the kernel would clear that saying it didn't pass in that data

Amanieu: The kernel could work around this. Have a flag that means "this struct is biger" and a second field that says "extra flags".

Josh: Or the kernel could add a `statx2` that expects the length of statx as an argument.

The 8472: Then people need to update their seccopm filters, syscall probing etc.

Nia: I think we can leave this to the kernel (and treat them breaking us as breaking userspace)

Josh: Yes, but we need to communicate that ahead-of-time

The 8472: Yes

Amanieu: I'm happy with the API as it is

Nia: I'll reply and ping RfL

### (nominated) rust.tf/157168 *Stabilize \`RandomSource\` and \`DefaultRandomSource\`*

Josh: I submitted PR that renameb `RandomSource` to `Rng` and `DefaultRandomSource` to `SystemRng` and that was merged. And I rebased this PR on top. Based on our discussion last week, I think we're done bikeshedding names. Are we prepared to stabilize this? Do we have any objections we consider blocking?

The 8472: We shoudl document `SystemRng` panics if the OS has no suitable generator.

Amanieu: That's a divergence from the behaviour of the `HashMap` random source.

Josh: To the best of my knowledge on sane platforms (i.e. not wasm-uknnown-unknown) it panics if it can't get randomness

Nia: I don't see any note in the docs about `SystemRng` panicking.

The 8472: We need to tell people we'll fail loud and hard if we fail to get randomness.

Josh: I'll add documentation to that effect

Chris: Do we have any code examples in the docs?

Josh: We have an example of filling a buffer with random bytes. To generate a random UUID.

Chris: There should probably be module-level examples

Nia: We can land that as stabilization FCP is happening. It's not blocking on agreeing to stabilize it

Josh: We can have an example on how to use `SystemRng::fill_bytes` to fill 128-bit buffer and extract the bits as five different types and fill it into a UUID.

Chris: Are we not planning on stabilizing `random::random`?

Josh: That's not being stabilizing that one

Nia: This means we're ditching the next item on the agenda (`rust.tf/157573 *std: simplify the random feature*`)

Josh: Yes

Nia: Should we open FCP on this?

Josh: Should we open an FCP and immediately file concerns around adding docs and improving an example?

The 8472: what about fallible rng?

Josh: The stabilization report mentions a few ways of handling this. We decided that after the stabilization any of these is an option.

Nia: I'll try to start the FCP

***

Amanieu: I'd be more comfortable for the `SystemRng` constructor to return a `Result`.

Josh: That would imply that it would have a constructor at all.

Amanieu: Yes.

Josh: That's going to invalidate the premise of "we don't need `std::random::fill`" because we have something simple

Amanieu: I don't feel comfortable with having something that panics if it can't get the source of randomness

Josh: Hashmap does that too. It calls `getrandom` and if that falls, it'll look into `/dev/urandom` and if that doesn't work, it'll panic

Amanieu: On `sys::random::unsupported` it grabs random pointers

The 8472: Tier 1 platforms have other security considerations and failing properly may be a different concern?

Amanieu: That goes back to the difference between default random source and system random source

The 8472: I don't want to call it a system random source

Josh: It's an interesting question whether we treat `sys::random::unsupported` vs `sys::random::linux`. They set different precedents

The 8472: We don't want to maintain our own crypto-audited stack. So we go to the system and then we have to fail hard

Amanieu: Because failing is a possibility, this should return a Result

Josh: On every platform other than Linux and unsupported we implement getting the random keys to generate the keys. It will panic if you can't generate the data

Amanieu: On Windows it never fails.

Amanieu: SystemRng construction is the thing most likely to fail

The 8472: If it's a zero-sized struct we need to open `/dev/random` every time

Amanieu: you could have a global handle

Chris: Could we have a `try_init` method that returns a result?

The 8472: Some people said that construction-time initialization would not be enough for them

Amanieu: I think it's extremely likely on any platform for it to fail on any platform after construction

The 8472: There was a bug on AMD when this failed on wake-up

Amanieu: That's just a bug

Josh: That's not a path we're prepared to go down

The 8472: If you want to write code that's panic free you need these sanity checks

Josh: We're not prepared to do these sanity cecks

The 8472: We're also stabilizing the trait.

Amanieu: I think we should have `Result` on construction, not `fill_bytes`. If you successfully obtained `SystemRng` it means your system has an rng. And if it then fails, the system is broken and it makes sense to panic there. But if the system doesn't have an rng is a case that you want to know about

Nia: That can be handled in ways that are less annoying. Nobody's against adding a fallible Rng. If you're doing a println, you will unwrap on not having anything to print on. This is similar.

Josh: We could even make that a different type so you can statically show you can't have any panics after this point.

Matt & Ben: the fact that all the methods on the getrandom crate return result makes me sympathetic to returning a result here. people can write convenience APIs on top of this if they want to panic, surely 

Josh: Amanieu, Does that address your concern?

The 8472: That still doesn't handle the runtime failure case that can happen

Amanieu: Don't use a broken OS

The 8472: We also tell that about Instance and look how much time we spent making Instance not panic

Josh: There are several questions to handle runtime failures e.g. by seeding a runtime PRNG which you know cannot fail.

Josh: We could add a `TryRng` that has a `try_fill_bytes` method. That would tempt people to use it and how many people would use that and successfully get an error that they will successfully handle.

The 8472: I don't write crypto code. And there are places that have policy about not panicking and needing randomness.

Nia: Ultimately I think the answer there is to have a different type for that.

Chris: Are there platforms where filling can fail after successful initialization?

Josh: Strictly speaking the system call could return an error. In practice, can that call fail on real systems? If you pass in `O_NONBLOCK` and you don't have any random bits now, you'll get an error

Amanieu: I'm not concerned about post-init randomness. That's a pattern we have right now

Nia: That's something we may want to do but let's wait on someone who actually needs it.

Josh: If we're in a scenario where no panic maybe allowed, then in that scenario you need something capable of handling runtime failure. In thet case we ship a TryRng or point them to `getrandom` which exists in the ecosystem

The 8472: We should tell people that. Maybe that's sufficient?

Nia: That's what I'd do

Amanieu: I still would like constructing `SystemRng` to be fallible.

Nia: Why not have that be its own typeo or try_init method?

Josh: On such a system they don't get to call HashMap::new

Amanieu: I want a Result to say whether system randomness is unsupported. There's multiple platforms

The 8472: Would it be enough to give them the option to check?

Amanieu: I see this being the same as Instant on wasm-unknown-unknown. Would it be better for Instant to panic or to just return 0?

Josh: I think It's better to panic

The 8472: We also talked about getting more granular methods to get clocks but if you don't care, use `Instant::new`

Josh: We know how to handle this at compile time. Could we ask for a cfg?

Nia: Or add `is_available` method

Nia: summary, we're explicitly saying that the cases we support are that randomness are always or never avaliable on a given execution.

Josh: If you want randomness and don't care about panic, you do what we have. If you want to detect your OS won't give you randomness ever, then we consider giving an `is_available` or something or you use getrandom. And if you care about every individual call then you either use getrandom or ask us to add something like `TryRng`

Nia: I agree with that

Josh: Do we ship this and bikeshed an interface on making it available later or bikeshed it now? I'll review the ACP as soon as you file it.

(discussion about associated error type, we're not doing that now)


### (nominated) rust.tf/157573 *std: simplify the random feature*

### (nominated) rust.tf/157226 *Partially stabilize \`box\_vec\_non\_null\`*

Nia: Do we want raw_parts to be canonically NonNull?

Josh: We talked about how to name it. We should have a simplified name for what these should be. Let me drop my concern.

Nia: We're waiting on talking to BurntSushi


### (nominated) rust.tf/157728 *\`std::sync::mpsc::Sender::send\` can block the current thread*

Nia: We pinged the wasm and crossbeam people. We're removing a promise that's relevant but it's not been upheld in the first place. We're not breaking new code. Our docs were wrong and any code that'd be broken by this is already broken.

The 8472: No crossbeam specific response?

Nia: Alex Crichton is also a crossbeam maintainer and read the response as agreeing from that perspective too

Nia: My proposal is FCP to change the docs

The 8472: The reporter also agreed. Let's do that.


### (nominated) rust.tf/157734 *Stabilize \`local\_key\_cell\_update\`*

Nia: We stabilised `Cell::update` and we didn't stabilise this as well?

The 8472: I think this API is newer than `Cell::update`

Nia: I think this makes sense. It's a straightforward API. It's `replace` but with a function instead.

Josh: This seems fine. I can start the FCP.

### (nominated) rust.tf/157801 *Rewrite safety requirements for \`Allocator\` impls*

(skipped for now)

### (nominated) rust.tf/158057 *Don't escape U+FF9E and U+FF9F in \`escape\_debug\_ext\`*

Josh: We normally escape combining characters but these are kind of combining but not?

Nia: They don't look combining.

Josh: This needs an FCP. Seems fine. I'm inclined to start FCP and cc Manish

Nia: That's reasonable. We could do an FCP concern Manish signs off on

The 8472: Is this tracked in some unicode tables already or an ad-hoc fix?

Nia: Seems to be ad-hoc.

The 8472: Can we ping the Unicode consortium to track this better?

### (waiting on team) rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>*

Nia: This needs the crater run started.

### (waiting on team) rust.tf/157428 *allocator: refactor for stabilisation*

Nia: Skipping for today

### (waiting on team) rust.tf/157946 *Make \`char::is\_private\_use\` and \`char::is\_assigned\` unstably public*

Nia: This is an ACP for `is_assigned` and `is_private_use` These are unstable. This is proposing to make them public.

Nia: The names look fine to me

The 8472: `is_unassigned` is a shrinking set and `is_assigned` is a growing set. Shrinking could be more surprising

Nia: Negatives are harder to reason about 

Josh: Both methods seem reasonable to make public and stabilize

### (new change proposal) rust.tf/libs819 *ACP: Remove 'static from Any itself, moving the bound to the operations themselves*

Nia: We talked about this already. We pinged types on it

### (new change proposal) rust.tf/libs818 *ACP: Add TooManyOpenFiles variant to io::ErrorKind*

Nia: had 0 discussion, less than a week old

The 8472: This is fairly straightforward

Josh: The variant handles both `EMFILE` and `ENFILE`. Do we want to say that you need to check the syscall's errno if you want to distinguish? I think we should map them both to `TooManyOpenFiles`

The 8472: If there are no concerns I'd be in favour

Josh: Agreed

Nia: in that case we can reply "ACP accepted"


### (new change proposal) rust.tf/libs817 *ACP: Add OnceCell/OnceLock::new\_init*

Amanieu: This is to create a `OnceCell` that's already in an initialized state. Seems fine

Amanieu: But I thought we already had this. 

Nia: the author lists bikeshed: `new_ready`, `new_init` and `new_initialized`. `new_init` seems fine and this is not stabilization yet.

Josh: Re: https://github.com/rust-lang/libs-team/issues/817#issuecomment-4744434716 I disagree this is just `From`

Nia: Also, proposal to name it `with_value`

The 8472: `new_init` is more explicit that it's a constructor

Amanieu: `with_value` would be more consistent with the general pattern of having `with_` constructors (e.g. `HashMap::with_hasher`)

Josh: The `with_` values change the default type. This feels different

Nia: who's okay under `with_value`?

(josh, amanieu, nia)

Nia: Who's okay with `new_init`

(josh, amanieu, nia)

Nia: I'm more in favour about `new_init`

Amanieu: Sure

### (new change proposal) rust.tf/libs816 *ACP: Add \`fN::ordinal\`*

Josh: apparently you can reshuffle the bits in the float so that `total_cmp` sorts them correctly

Amanieu: That's what total_cmp does internally. But my personal opinion is this is the wrong way to do it. For performance reasons it's faster to compare and say "greater than and not NaN" or "greater than or NaN". That's two CPU instructions. Doing the int conversion and bit manipulation is more expensive.

Josh: The fact the proposal is "walk over the array, change them all to ordinal and change them back". They're claiming this is the faster way to do this. I doubt it?

The 8472: Doesn't your NaN proposal not properly order the Nans amanieu?

Amanieu: They are properly ordered either at the start or the end.

The 8472: Doesn't that collaps all the NaNs to...?

Amanieu: Yes, all the NaNs compare as equal. It's a different order than the bits but it's fastest

The 8472: I wonder if they need the specific bits order

Josh: I hope no-one is doing NaN boxing and then relying on a specific sort order of the NaN box

The 8472: We should ask

Nia: Yes. Also if we do this, there's no way to distinguish whether this is a slice of integers meaningful on its own vs. a slice of ordinal floats.

Josh: There's a comment in the thread 

Josh: it's not the fastest way to sort. But no objection from adding the method if it's useful unless it slows us down from adding ordered float or something. I'd be tempted to add this but also try to add ordered_float and stabilize both around the same time

Nia: That's convincing. We need to ask about NaN ordering and otherwise this is fine?

Amanieu: You put them all at the start or all at the end. Just pick one

Josh: Or you can have a floats with NaNs filtered out

The 8472: If performance is the only motivation and that's not true, then we shouldn't add it?

Josh: I could see the argument for getting a sort key for a total order of floats if you're sorting multiple things some of which are floats. But, meh.

Nia: I'll add a note that we're okay if there's motivation for it. But performance is not a motivation.

The 8472: We should ask whether they need total_cmp or if any fast order is ok.

Amanieu: This is how LLVM does float comparison: https://llvm.org/docs/LangRef.html#id320. There's 14 of them, all make sense and most if not all produce total order

(the team emits a collective groan)

### (new change proposal) rust.tf/libs815 *ACP: Add \`CovariantUnsafeCell\`*

Nia: UnsafeCell is invariant over the type currently. If you have a type `P: U`, you can coerce `P` to a `U` if it's covariant. If it's invariant, you can't. `'static` is covariant to `'a`

Amanieu: We only do covariance for lifetime.

Nia: Right now you can't coerce `UnsafeCell` to anything ever. We handle this based on the pointer. `*const` is covariant, `*mut` is invariant. 

Josh: What would happen if we just make UnsafeCell covariant?

Nia: I don't think it wolud be breaking to turn something that's invariant to covariant. You could observe it but only if you wrote a code that specifically observed it.

Josh: Is crater likely to catch it or is the type of code that would potentially broken is unsafe code?

Amanieu: Heres' why UnsafeCell needs to be invariant

https://doc.rust-lang.org/nomicon/subtyping.html


Nia: `&mut T + 'long` -> coerced to `&mut T + 'short` -> we write a `T + 'short` into the `mut` -> coercing back is unsound 

Josh: So writing into a mutable reference would be contravariant and teh intersection of contravariant and covariant is invariant

Amanieu: The UnsafeCell would be a covariant UnsafeCell plus a phantom variance(??) marker

The 8472: We don't have any bivariant type?

Nia: I don't believe we do.

Amanieu: What's the shape we want for this? Call it CovariantUnsafeCell? What about sync variant? We don't need it as a core primitive (you can wrap it)

Josh: Your argument is: this is a core primitive, if you need something more specific you can build it yourself?

Nia: You can express SyncUnsafeCell with proper unsafe code

Nia: Ship it. Is `SyncUnsafeCell` stable?

The 8472: It's unstable

Nia: We should note that so we don't have name explosion

Amanieu: You only need it when it's static. For every other case you can `impl Sync` on a wrapper

Nia: My proposal: accept ACP and leave a note that we need to consider what to do with `SyncUnsafeCell`

The 8472: The explicit motivation here is `LazyLock`.

Nia: If Waffle wants to add this to std, it's a breaking change we can make. Making something covariant can be a breaking change but it's hard to write code that compiles when invariance is upheld and not when covariant

Nia: I'm in favour of approving this. If Waffle wants to add LazyLock later, that would be cool. Everyone in favour?

The 8472: No strong opinion.

Amanieu: Sure


### (new change proposal) rust.tf/libs814 *\`OnlyNegative\` and \`NonNegative\` to compliment \`NonZero\` for signed integers*

### (new change proposal) rust.tf/libs811 *ACP: convenient numeric conversions dependent on overflow\-checks*

### (new change proposal) rust.tf/libs810 *ACP: Missing floating point conversions*

### (new change proposal) rust.tf/libs808 *Type\-smart \`intersect\` on ranges*

### (new change proposal) rust.tf/libs804 *ACP: Add allocation\-free owned downcasting for Any values*


### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs124 *Integrate \`Error\` trait with panic interfaces*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*

### (stalled change proposal) rust.tf/libs314 *Add security\_attributes() to windows::OpenOptionsExt*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs347 *Context reactor hook*

### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs397 *ACP: Add \`prev\_power\_of\_two\` and \`bit\_width\` methods for unsigned integers and \`NonZero\<uN\>\`*

### (stalled change proposal) rust.tf/libs544 *\`Arc\<impl !Sized\>\` constructors*

### (stalled change proposal) rust.tf/libs298 *Constructive/Destructive Interference Size Padding*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
