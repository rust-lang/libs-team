# Libs-API Meeting 2026-05-26

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia Deckers, The 8472, thomcc, Chris Denton, maxdexh, Amanieu, Orson, bstrie, kpreid

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

20 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/155750 *Document that \`ManuallyDrop\`'s \`Box\` interaction has been fixed* - (4 checkboxes left)
  - merge rust.tf/152193 *Tracking Issue for \`nonzero\_from\_str\_radix\`* - (3 checkboxes left)
  - merge rust.tf/147946 *Tracking Issue for \`strip\_circumfix\`* - (3 checkboxes left)
  - merge rust.tf/156906 *Stabilize \`dyn Allocator\`* - (4 checkboxes left)
  - merge rust.tf/147455 *Tracking Issue for \`path\_set\_times\`* - (3 checkboxes left)
  - merge rust.tf/127544 *Tracking issue for CommandExt::show\_window* - (3 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (0 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (3 checkboxes left)
  - merge rust.tf/153990 *Decide and document where stdarch intrinsics are allowed to diverge from asm behavior* - (3 checkboxes left)
  - merge rust.tf/126769 *Tracking Issue for \`substr\_range\` and related methods* - (3 checkboxes left)

[scottmcm (2)](https://rfcbot.rs/fcp/scottmcm), [Amanieu (8)](https://rfcbot.rs/fcp/Amanieu), [the8472 (14)](https://rfcbot.rs/fcp/the8472), [joshtriplett (6)](https://rfcbot.rs/fcp/joshtriplett), [dtolnay (2)](https://rfcbot.rs/fcp/dtolnay), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [BurntSushi (12)](https://rfcbot.rs/fcp/BurntSushi)

### (nominated) https://github.com/rust-lang/std-dev-guide/pull/76 Doc alias policy: aliases for crate names whose functionality we've added

Nia: Let's skip when Josh is here, defer to async discussion

### (nominated) rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16*

Nia: An old FCP was out for this, concerns weren't addressed until now. The concern is from Josh. According to the author the concern has been resolved, but Josh is not here. I'd defer

### (new change proposal) rust.tf/libs788 *Add missing integer casts*

(note, we have the ACP's author on the call)

Amanieu: after some in-person discussion at Rust Week we changed our mind and think this is better.

Nia: What was the reasoning?

Orson: The reasoning that allowed me to convinced was: starting from saturating_cast. I asked multiple people about the order they'd do if to make saturating_cast in two steps. And everyone got the order wrong. That suggests we want to have this in. And with that, it makes sense to have wrapping_cast and check_cast, etc.

Amanieu: That's roughly correct. If you look at the solution sketch, it's mainly about the `impl T` block.  This would be a replacement for the narowing/truncating methods. We would prefer more general solutions.

Orson: For truncate that would be wrapping cast.

The 8472: 

Nia: As long as we have semantics saying

Amanieu: 

Orson: The conversion is always well defined. The source type is irrelevant. You have an integer value and the only thing that matters is hte target type.

Amanieu: Conceptually, we're going from an infinitely-sized integer to something.

Orson: The implementation is also very efficient

The 8472: When you do a wrapping_cast from a signed integer to ??

Orson: wrapping_cast behaves as it is on std

The 8472: This would need to document that this is for the case that needs runtime logic

Nia: I don't think this replaces the statically compile-time methods

Amanieu: I'd remove the truncating one.

Amanieu: The trait is always implemented from the smaller type to the larger one. So it always preserves value

josh just posted this on zulip 
https://github.com/rust-lang/rust/pull/156974
 
Nia: This is possibly redundant if we accept the ACP for cast

Orson: There's overlap between narrow and cast. I see some people prefer to be explicit and say "I only want to widen" or "narrow". This should be compatible with that.

Nia: We take this ACP with the casts and we take the infallible extensions / widening methods. Leave `cast_signed` and `cast_unsigned` as they are. There's value in them

Amanieu: Yes, they can be useful in certain circumstances

Orson: They're better at inferrence too

Orson: The question is what to do about truncate.

Amanieu: I don't see any value in having narrowing/truncating ones. We should remove those.

Amanieu: We'll accept this. Do we move this ot the same tracking issue or have a new one?

Nia: Same as which one?

Amanieu: https://github.com/rust-lang/rust/issues/154330

Nia: I'd open a new one and point it at that one. Is that okay with everyone?

Amanieu + The 8472: +1

### (nominated) rust.tf/117224 *Tracking Issue for NEON dot product intrinsics*

Amanieu: Ship it

Nia:  we need to check the boxes.

### (nominated) rust.tf/136469 *Tracking Issue for algebraic floating point methods*

Nia: Some blockers were removed. Ralf nominated for us. We have to name this.

Orson: I originally proposed algebraics too

Amanieu: is this a stabilization request?

Nia: I think so

Orson: the unresolved question is about how this interact with optional-fused-multiply-add intrinsics

The 8472: Does chaniing the algebraic_* methods give you optionally fused? Their point is the compiler picks which one is faster

Amanieu: But even then it's nondeterministic

Orson: If you had algebraic operations, followed by this optional-fused-multiply-add and other algebraic operations, the block would be broken and might not be optimised. But if you replaced it with an algebraic one, it would be a single block

The 8472: If your bottleneck is multiply adds, it might make sense to expose the intrinsics but we should wait for someone to ask

Amanieu: I don't think there's a better name

Nia: Yes. Algebraic is weird, but we can explain it in docs. It's the best one I've got

The 8472: That's how one of the optimisation flags in LLVM is called

Nia: If so, then there's precedence

Amanieu: Happy to start FCP

The 8472: Same

Amanieu: Do we CC lang?

Orson: It's not 1-1 with a specifi LLVM flag

The 8472: https://llvm.org/docs/LangRef.html#rewrite-based-flags

kpreid: Some people might see this because they'll see "algebraic" and think it's better because it sounds mathy

Nia: This needs to be in the docs. People who care will look at the docs and who don't will use the regular methods

### (nominated) rust.tf/146561 *Change \`Location\<'\_\>\` lifetime to \`'static\` in \`Panic\[Hook\]Info\`*

FCP finished. Should be merged?

Amanieu: I don't think Jubilee's point is valid and we should override it and proceed with stabilisation.

Nia: What's her point?

Amanieu: "What if we want to generate location data at runtime? Then we can't give it a static lifetime." My answer is you just intern it.

Orson: What if it's a dylib that might get loaded later?

Amanieu: You can free it from the interning table if anyone cared.

Nia: If we start worrying about that, we have a lot of lang/internals to change

Nia: Based on that argument, the team signed off of this

Amanieu: We just need to say we discussed this in the meeting and we stand by the decision.

### (nominated) rust.tf/154330 *Tracking Issue for integer extension and truncation methods*

Defer until next week when Josh is here.

### (nominated) rust.tf/154374 *Rename \`MetaSized\` trait to \`SizeOfVal\`*

Nia: we discussed this and have a name.

### (nominated) rust.tf/154742 *Add APIs for case folding to the standard library*

Nia: This was a request from us? It doesn't have an ACP. We suggested this API to them. We could do an ACP for the formality of it but I think it looks fine and we did discuss this.

The 8472: They're unstable, right? We don't need an ACP for that. Just the tracking issue would be enough

Nia: I'll reply.

thomcc: one question is about exposing this at all without normalization (but i think it's still useful, personally) 

thomcc filesystem does casefolding and normalization, but unicode 6,0 

thomcc err apple ^ 

thomcc yes, it can be done separately 

thomcc although notably NFKC_CF (nfkc+casefold) needs to be applied in a loop 

The 8472: This is case folding without normalization. Some filesystem e.g. Apple's one do casefolding with normalization. So this could be relevant. Some usecases might actually need the folding with normalization and if they reach to the standard library, they may get the wrong result.

Nia: Who's our unicode expert?

The 8472: Manish

The 8472: If there's enough edge cases maybe we should point them to a more specialised crate

Nia: Directing them to open an ACP for tihs and letting the discussion bake a bit is worth it. Having it in the stdlib makes sense, we can implement the standard cases and document what we do

Orson: Assuming we don't get into something expensive (like adding large additional tables), I think we can do this but it should go through ACP

The 8472: I'm in favour of ACP. And it does add an additional tables

Amanieu: I'm opposed to requiring an ACP. All the needed information is in the PR. It's too much process

Nia: We could let it bake a bit more

Amanieu: Our responce should either be: "we'd like to make a change" or accept/reject

The 8472: My concern is we might have to reject this and there's no point in merging now and rejecting later

Nia: If we're not doing an ACP, ping Manish on unicode opinions here. I don't want to add another thing that's half-correct

Amanieu: Manish's opinion has already been requested and the response was :shrug:

Nia: Then I'd merge it and if there are concerns, we'll handle them at stabilisation

The 8472: We can still make this a tracking issue and reject this later

Nia: I think that's fine.

Amanieu: The concrete objection is "having this without normalization" is a footgun and we sholudn't have it

The 8472: Depends on how common/uncommon the footguns are

Nia: It's a good-enough footgun

The 8472: thomcc said the Apple standard needs normalisations from the Unicode 6 standard so not even the most recent one.

Orson: Python apparently also added casefold

Amanieu: do they have normalisation?

Orson: That's unclear right now

Amanieu: I could see changing the name to `unnormalized_equal_case`

Nia: I like that idea

Nia: I'll reply

Orson: I linked the unicode standard which has a section on the default case algorithm (this is also what Python uses) but it's not clear to me whether this does normalisation.

thomcc: some of the algorithms do normalisations


### (nominated) rust.tf/156270 `P-lang-drag-1` *Vibe check: can \`Simd\<T, N\>: Receiver\<Target = T\>\`?*

Nia: We need Josh for this one.

### (nominated) rust.tf/156629 *Stabilize \`core::range::{legacy, RangeFull, RangeTo}\`*

Nia: Moving the old Range types to a differnt module

The 8472: It's just reexpeorting them under the legacy name?

Nia: Yes.

The 8472: Didn't we have a PR like this before?

Amanieu: This is the old range types? I think some of the new Range types were stabilised

The 8472: We need an expricit path for the new edition to naem them. If we don't have that already, let's do that

Nia: What's the opinion?

The 8472: We need to FCP this?

Nia: Any issues with this? I don't see anything

Nia: FCP merge?

Amanieu: Yes. I think `RangeFull` is the same type in both editions. Is that just a reexport?

Nia: Yes it's just a `pub use` for `RangeFull` and `RangeTo`

Amanieu: And neither implements an `Iterator`. This seems fine, I'll start the FCP

### (nominated) rust.tf/156840 *Stabilize \`PathBuf::into\_string\`*

Nia: It's a stabilisation, so it needs FCP

Nia: This is a straight-up obvious way to do this. And I think we want this.

Amanieu: It's only been addeed 3 weeks ago.

The 8472: Shouldn't that be `try_into_string`

Amanieu: We already have `OsString::into_string`

The 8472: We can't make it infallible, right?

Amanieu: It could panic

The 8472: That's worse

Amanieu: I'll start FCP

### (nominated) rust.tf/156906 *Stabilize \`dyn Allocator\`*

Nia: `dyn Allocator` is fundamentally broken I think. It's unsound. We care about the implementation of `Clone` saying that if you have cloned the allocator to free the allocator's memory and vice versa. It's possible to construct an impl of clone that breaks this. You can implement a clone that gives you a different allocator.

thomcc: This is an issue for this: https://github.com/rust-lang/rust/issues/156920

Nia: The way around that is either make Clone bound to the allocator or have the semantics of a clonnable allocator depend on unsafe trait.

Nia: It seems the conclusion is: you can't have an unsafe trait where its semantics guarantee that it can rely on a correct implementation of a type. They can implement

The 8472: This is basically because `Box` is fundamental?

Amanieu: Yes.

Nia: We don't care about `Pin` or the `Fn` trait. So it's just about `Box`

Nia: ??

Amanieu: Ralf is still uncomfortable with this solution

https://github.com/rust-lang/rust/issues/156920#issuecomment-4543443329
> Ralf: I am somewhat uncomfortable with any mention of non-super-traits in the safety requirements for Allocator. 

Nia: Alternative, `Allocator` doesn't depend on anything and we have unsafe markers for it for `Clone`, `PartialEq` etc.

The 8472: Yeah, but we'd probably need allocator-specific unsafe crates. Because other unsafe things might have different invariants

Nia: Correct. That's why my proposal for lang to mark which traits it depends on

Orson: You can have a super trait that ??

Nia: But this doesn't prevent someone doing breaking safety invariants in safe code

The 8472: Would the traits also be inherited by dyn trait?

Nia: If it has Clone or PartialEq bound it also has a Sized bound

Orson: Allocator isn't the only unsafe trait that suffers from this problem

Nia: It's the only one that currently suffers from this. If an unsafe trait depends on a safe implementation, it should either have it as a bound or must not be dyn compatible

The 8472: Should we cancel the FCP due to currently unfixable safety issues?

Amanieu: I'd say "fcp cancel" and then "fcp close"

Nia: Given how ugly having a billion extra marker traits is, I'd say "no dyn allocattor until/unless we get support for having a language way to represent the semantic traits you care about"

The 8472: If we need to add additional constraits to make it dyn safe might break up users anyway

Nia: My proposal would not, it would only do this on dyn and right now you can't even make it dyn

The 8472: we need to run this by lang or types

Orson: It's not the end of the wold to make it dyn incompatible, you can write a wrapper type

Nia: I'll talk to lcnr about this tomorrow and I'll ask for types input. If the response from types is "this is possible", then I'll run it by the lang meeting tomorrow. If they say that might be possible, we'll keep dyn allocator as a possible future option. If not, we'll add AllocErr as an associated type, because that would break dyn compatibility

Orson: Is it a good idea? It makes it work with generics even more annoying

Nia: We could set the associated AllocErr to `!` and then have infallible allocators. The conclusion was "it's nice, but note nice enough to prevent dyn Allocator"

The 8472: The default associated type seems stuck

Nia: I'll talk to lcnr and give you an update at the libs meeting tomorrow

Nia: Conclusion for now: no dyn Allocator.

The 8472: Agreed

Amanieu: Yes, I agree

Nia: I'll have to talk to Josh. He was a fan of dyn Allocator

Orson: A concrete UB example like that's in the issue could be a good convincer for Josh too.

The 8472: He might argue for the traits solution.

The 8472: And we need to `fcp close` this

### (nominated) rust.tf/156908 *Tracking Issue for LoongArch CRC Intrinsics*

Amanieu: FCP started. It's just more intrinsics for loong arch

Orson: These names come directly from the instruction manual?

Amanieu: Yes, they come directly from intrinsics

### (nominated) rust.tf/156920 *\`dyn Allocator\` together with \`Allocator + Clone\` requirements is unsound, leading to UB with \`Arc\`*

(already discussed in the dyn Allocator section)

### (waiting on team) rust.tf/156025 *Support if\-let guards in \`matches\` macros by using \`:guard\` matcher*

Nia: The author expanded on what they think is breaking here. It feels like a lang thing.

The 8472: The guard macro matcher already exists? `matches!` is not a built-in macro, right? Then it's libs.

Orson: Is this unstable? Or is any change you make it to a macro instantly stable?

Amanieu: This is adding it to the `matches!` macro so it would make it instastable

Nia: I think a crater rune is worthwhile for this. This feels very arcane and I'd like to see if it breaks something.

The 8472: Agreed

Nia: If it's clean, we FCP this

Amanieu: I see no reason why this would ever break

Nia: Neither do I, but people writing this feel slightly unconfident. That's scary

The 8472: The guard fragment is still unstable, right?

Amanieu: Is it?

Nia: The guard fragment is unstable, but the support for guards that in the macro would be stable

The 8472: Should we use an unstable feature in a stable API?

Amanieu: In all cases this is followed by comma. I don't see why the follow even matters?

Amanieu: The guard matcher is unstable so this needs to go through lang

The 8472: And it has unresolved questions so we need to ask if they're comfortable in using it

Nia: I'll nominate this for lang

The 8472: And they can even check whether this even makes sense

### alloc: stabilise `Allocator` https://github.com/rust-lang/rust/pull/156882

(discussion about global alloc, the compiler doesn't notice the recursion limit that you can get to there)

Nia: We can have a blanket impl and tell people not to do that

Amanieu: You'll note this very quickly that it'll overflow. You'll notice very quickly (before `main` or the first time you allocate)

Nia: What happens if you're in `no_std`?

Amanieu: As a library you're not supposed to specify global allocator

Nia: You're probably right. And we can discuss the intricacies around global allocator when we're switching over the macro. But we should definitely not recommend using that macro.

Orson: I'm still unsire about the blanket impl. If I provide my allocator, I can also provide the global allocator that uses proper thread locals

Nia: You make a wrapper type

Orson: 

The 8472: Why are you bringing thread locals?

Amanieu: Thead locals alawys use the system allocator

Orson: In an allocator you want to use thread locals. But if you have an allocator object, you need a custom thread local object. And because that's less efficient, ideally you shouldn't be opt into the less efficient one already

Amanieu: I see your point. It boils down to: not all allocators are suitable for use as a global allocator

The 8472: Is'nt it more a usecase whether it's a singleton allocater vs. one that can be instanced?

Orson: The singleton one can be more efficient 

The 8472: You can also have singleton allocator trait. It's a property both global and custom ones can have. You can have jemalloc implement the Allocator trait, bind it to the C library

Amanieu: If it's a singleton allocator, it'll just be a ZST

The 8472: I think the discinction between allocator and global alloc is related, but it's not strictly the same property

Orson: Agree. But I'd like if you could advocate the ideal usage of your types. If I provide both a singleton and non-singleton allocator in my crate, it would be nice if they only implement the right trait

Amanieu: The nice thing is that you only need to implement a single trait

Orson: I understand where you're coming from but I don't know

Nia: We can remove the blanket impl rightn now

Amanieu: We need to make a decision whether we provide the blanket impl now

The 8472: Can we have a reservation impl?

Amanieu: then it's impossible to implement both allocator traits

Nia: We ship it fro now with a blanket impl, add a doc comment that you probably don't want to implement it and we could remove it in the future ... that would break stuff

The 8472: Yes

Amanieu: GlobalAlloc doesn't have an equivalent of grow_zeroth.

Nia: We can always change that over an edition

Amanieu: I suppose

Nia: We will be able to change name resolution depending on which edition you're in.

The 8472: Global itself does implement Allocator. Ideally we'd forward to the underlying global alloc trait. Can we do that?

Nia: What do you mean?

The 8472: Specialisation

Nia: I would love specialisation

Orson: Over an edition change you can change the allocator to require global alloc

Nia: The other option is: we make global allocator require that you implement allocator + a marker trait that says this is suitable for global

Nia: There probably are aclocators that aren't suited to be global allocator. So: no blanket impl and we'll see what we can do over an edition.

Nia: But I want team sign-off. Otherwise we'll assert your allocator must be suitable as a global allocator.

Amanieu: A marker trait would be ok but that'll have to go through another round of discussion and delay stabilisation even more. But I'd be happy with it.

thomcc: yeah that sounds good to me

The 8472: How does the magic work?

Nia: The compiler inserts the four functions and global is also a compiler magic at link time

Orson: Does global allocator check for `Sync`?

Amanieu: Yes

Orson: If you allocator is `Sync` that already implies you can use it from multiple..

Nia: `Sync` is an auto trait. I'm not putting global allocator safety behind an auto trait

Amanieu: Any global allocator will have pointers that make it non sync. And there's no such thing as global allocator safety. That's just a hint.

Nia: Fine, the blanket impl stays. We'll see what we can do over an edition

The 8472: Desn't the blanket impl mean that if you have an existing allocator that implements the alloc trait, doesn't it mean you can't implement global alloc directly?

Nia: There are no such existing allocators

The 8472: Should we suggest crates that want to use the API surface change their impls?

Nia: Before the edition

Orson: If you look at it from an implementing perspective, if you odn't have the blanket implementation, adding support for global allocator is additive. But if you have the blanket impl, it's 

kpreid: if you write `#[global_allocator] static mut ...`, then it's not checked to be `Sync`

Nia: You can still have multiple logical threads and state 

kpreid: \[unlike `static`\], `static mut` doesn't check for `Sync`. The idea is that it's unsafe to use anyway

Amanieu: You need a `cfg(version)` to use the new allocator trait anyway

Orson: Do you need to add the new impl and remove the old one or just add to it?

Amanieu: Add the impl and remove the old one

Nia: If you really want that, you can do the old macro expansion of the old global allocator yourself

Amanieu: I'd make it so that `#[global_allocator]` rejects `static mut`

Nia: Me too. And make it an unsafe attribute

Amanieu: `#[global_allocator]` should be a safe attribute. But it's completely unsound if you make it `static mut`.

The 8472: Can we slap a supertrait bound `Sync` on global alloc?

Amanieu: I'd just make it reject `static mut`

Orson: Is it unsound? Can you still access the object?

Amanieu: If it suppresses the `Sync` requirement it's unsound because `global_alloc` now can have a `Cell` in it.

The 8472: Let's try to fix this properly before we think about warnings etc. Let's forbid the `static mut` first and only if that breaks someone look at other ways

Nia: I'll check how many people opted into unsoundness

The 8472: Part of our safety rules allows us to break to fix unsoundness.


### (new change proposal) rust.tf/libs793 *\<\[T\]\>::get\_array() and \<\[T\]\>::get\_array\_mut()*

Amanieu: We have `as_array` already

kpreid: It's getting a subslice as an array

Amanieu: No, you're only specifying the start bound. You don't need to specify the end bound

kpreid: my comment: https://github.com/rust-lang/libs-team/issues/793#issuecomment-4525924831 if you want the API at all. 

The 8472: As something you pass into the index operator

Orson: I like it but not for the standard library

Amanieu: Isn't it equivalent to `slice[n..].first_chunk()`

kpreid: It's not equivalent if the input is too short. I think this could be useful for extracting info from binary formats

Amanieu: The motivating example is already using panicking indexing. Which means they know that both are in bounds

Orson: I like this. I run into this usage patters quite a bit.

Amanieu: Scott replied that try blocks would help. That is a project goal for this year

Nia: I don't know what to do with this one

Amanieu: I'm inclined to reject. Also `get_array` is a terrible name (we're calling this on an array). But we have enough ways to do this, we don't need ot add more. We have `as_array` and `first_chunk` and people need to get used to use them. They were stabilised recently and online documentation with examples may not be where it should be

kpreid: It would be nice if the response on the issue mentioned how to do this with `first_chunk`

Orson: The alternative already shows a `first_chunk` use case, but it's needlessly complicated

Amanieu: yes

The 8472: programmejoke also mentions it

Nia: So we just close it?

Amanieu: I'm closing it

### (new change proposal) rust.tf/libs792 *ACP: Add methods to get reference counts from raw pointers to \`Arc\\Rc\`*

Amanieu: Sure, add `strong_count`.

??: What about weak?

Amanieu: add `weak_count` too

Nia: We should add both

Amanieu: Why is it necessary to not turn it into a reference?

Orson: You'd have to manually drop the `Arc`

Amanieu: Right. In that case, sure. Im' inclined to accept and programmerjake already said `weak_count`

Orson: What should be the names? `strong_count` and `weak_count` already exist for a reference

Amanieu: `strong_count_from_raw`

Orson: `raw_strong_count`?

thomcc: I like `*_from_raw` more

The 8472: When the count is zero, it's deallocated

Orson: They said they were creating an `Arc` wrapper and wanted to provide their own strong count.

Amanieu: I definitely think we should have tihs method. Why don't we have `decrement/increment_weak_count`? Let's defer `weak_count` until someone has usecase for it?


### (new change proposal) rust.tf/libs789 *ACP: \`std::time::StopWatch\`*

Amanieu: I think we discussed this in the last meeting.

Chris: I'm coming around to the idea that it could just be a property of the clock. We require clocks to be 1ns precision.

Orson: If the only motivation is avoiding a cast to larger integer types

Chris: No, it means that you have to do the calculations up-front to work out what the count is in nanoseconds. Which is slow

The 8472: It does a multiplication and a division

Orson: That's a division by constant so that could be maed more efficiont

Chris: I'd like to have an instant that's just a wrapper around the QujeryPerformanceCounter

Amanieu: Could we have instant represented in ticks of QueryPerformanceCounter? Didn't we say some of these had platform-specific

Chris: The direction of previous libs-api decisions was to support people who wanted to do nanosecond adds

The 8472: If we want to make `Instant` generic over clock sources, the internal implementation must work for all of them

The 8472: What's the overhead of this calculation? Is'nt multiplication 3 cycles?

Orson: If the constant gets inlined, it's well optimised. The concern on whether instant should be suspend-aware is compelling. The performance concern is not that big of a deal

The 8472: Different clocks could 

Chris: https://gist.github.com/ChrisDenton/f692beb9d1921e624d7efd684723fafa#file-performance-rs-L50

Chris: I'd be happy with separate clocks

The 8472: The precision of the internal implementation when you manually add a nanosecond isn't necessarily consistent with the clock ??

Amanieu: If make Instant generic we can have the internal representation an associated type of the clock

The 8472: Ok

Amanieu: I'm inclined to try to make Instant fast

The 8472: people use it a lot already for performance sensitive work

Orson: We should make scheduling correct first

Amanieu: I'm hesitant to have two ways of measuring instants in two slightly different ways. Having Instant internally store units of ticks is fine. We just need to specify if you add 1ns whether you round up or downs. I'd round up.

Nia: I think that's correct

Amanieu: If we subtract 1ns how are we rounding?

Orson: Our API technically doesn't say ns, it says Duration. I think it has to be nanoseconds, I don't think we can change this.

Amanieu: We have add `Duration`.

Orson: Looking at the code, it does proper division at runtime and caches it. But you can precompute the inverse

Amanieu: api-wise, the minimum possible frequency is 1 tick per second

Chris: but's unlikely we'll see that in practice

Nia: I don't have strong opinions

The 8472: People want Instant to be fast for measuring how long e.g. a single method took. I'd assume they'd be reluctant to switch to a new clock source.

Orson: You could store Instant as a Duration component and a tick component. And only if you actually need to normalise to a duration, you'd do the division

Amanieu: Since Instant is os-specificic, we could use `u64` nanoseconds

Nia: Sounds scary, but I don't have a good argument against

Chris: The scheduling clock vs performance clock is an argument for them being different types

Orson: If we find a resolution to the suspend-aware problem I'd not be opposed to having 2 clocks

Amanieu: In terms of clock properties, there are several useful clock: default the system uses for sleeping, low latency high precision for perf measurements, boot time clock -- monotonic that matches real time (suspend aware)

Orson: I'm not a fan of adding type parameters to Instant. They have different potential needs.

The 8472: The clock sources can implement additional traits and we may let you convert to system time where it makes sense

Chris: Should we defer this after we have clock source figured out?

Nia: Yes.

### (new change proposal) rust.tf/libs787 *Add fN::replace\_nan*

Amanieu: replace NaN with a given value?

Orson: Yes. Naming-wise I'd prefer kennytm's proposal of `nonnan_or`/`finite_or`

Nia: I think this is good. The only problem I have with it is the name.

The 8472: `nonnan_or` would be different from `finite_or`?

Orson: Yes

The 8472: So you'd chain `nonann_or` and then `clamp`?

Nia: This is good, ship it, rename it to `nonan_or`

Amanieu: I'm not a fan of `nonnan_or`.

Nia: `non_nan_or`?

Amanieu: I'm fine with `replace_nan_with`?

The 8472: `_with` implies a closure

Orson: I don't want a closure API

Amanieu: I don't like the negation in the name. I want to do something with NaNs.

Orson: In numpy it's called `nan_to_num`, in polars it's `fill_nan` and tidyr it's called `replace_na`

Orson: `swap_nan`

The 8472: No

Nia: I like that

Orson: `unwrap_nan`?

The 8472: It never panics

Orson: `nan_to`?

The 8472: I'd be ok with it

Nia: ... sure

Nia: ACP accept, we'll call it `num_to`


### (new change proposal) rust.tf/libs786 *\`std::ops::RangeBounds::{start,end}\_bound\_inclusive\`*

### (new change proposal) rust.tf/libs784 *Float Exp Bias Constants*

### (new change proposal) rust.tf/libs783 *Add std::range::DecreasingRange\* structs*

### (new change proposal) rust.tf/libs779 *floating\-point NaN payload functions*

### (new change proposal) rust.tf/libs778 *ACP: Add \`char::to\_u32\` to access underlying value*


### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs334 *Customizing \`#\[derive(Debug)\]\`*

### (stalled change proposal) rust.tf/libs191 *Add LocalWaker support*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs462 *impl fmt::Write for BufWriter*

### (stalled change proposal) rust.tf/libs420 *Add basic tempfile API to stdlib*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs314 *Add security\_attributes() to windows::OpenOptionsExt*

### (stalled change proposal) rust.tf/libs525 *Vec::try\_remove*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
