# Libs-API Meeting 2026-06-16

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, thomcc, Mark, The 8472, Josh T, Nurzhan

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

16 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/69835 *Tracking Issue for layout information behind pointers* - (4 checkboxes left)
  - merge rust.tf/157226 *Partially stabilize \`box\_vec\_non\_null\`* - (3 checkboxes left)
  - merge rust.tf/156983 *Add \`io::Read::read\_le\` and \`io::Read::read\_be\`* - (4 checkboxes left)
  - merge rust.tf/156906 *Stabilize \`dyn Allocator\`* - (2 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (2 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[joshtriplett (7)](https://rfcbot.rs/fcp/joshtriplett), [the8472 (11)](https://rfcbot.rs/fcp/the8472), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [dtolnay (4)](https://rfcbot.rs/fcp/dtolnay), [Amanieu (7)](https://rfcbot.rs/fcp/Amanieu), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm)

### (nominated) rust.tf/libs806 *Request: guarantee that \`Vec\` field order is independent of type parameter*

([previous discussion](https://hackmd.io/C_EmyvSnQ5CWjlxgTGKI8A#nominated-rusttflibs806-Request-guarantee-that-Vec-field-order-is-independent-of-type-parameter))

Nia (pre-meeting): Taylor (author) didn't get to respond in the thread, but I discussed this with her in private - the answers to the questions I posed are (1) yes, an alternative repr would be okay and (2) no, she doesn't need method calls to work.

I also spoke to the compiler team during their office hours yesterday and was told that such an alternate repr would not be too difficult to implement, and we likely already do this.

The 8472: Still think that even with all the guarantees, they technically still can't do the thing they want, at least from API contract perspective. We could fetch the TypeId for every T and encrypt the pointer with that. Even if they had access to the fields, they're completely useless to C++ code.

Nia: They need guarantees that ?? are valid?

The 8472: ?? in a way that's polymorphic.

Nia: Yes. I guess there's a possibility we could restrict that in the future, but I don't see a meaningful gain.

The 8472: Sure, but they're asking for guarantees.

Nia: I'd signal that yes, we can make the guarantees... If you think it's a large consideration, I'm willing of asking for clarification.

The 8472: It's not about clarification. It's about what they're asking for being different from what they actually need.

Nia: ?? happy to clarify in our response that this is more expansive... Would you be pkay with that?

The 8472: Q is on which level we want to guarantee it, docs, API, under-the-table..

Josh: FWIW, I'd want to put it in the docs. The only ask that's reasonable is, between two vectors of types of the same alignment and same allocator, you can make this conversion...

The 8472: They want to directly access the fields and use the pointers, not conversion.

thomcc: Would be reasonable to say that `Vec<T>` stores the raw parts in some order.
    
The 8472: We couldn't shift the value by +1 or other things in the future.

Mark: My impression was that Taylor was happy with a guarantee of version-specific conversion when accessing, but the conversions need to be polymorphic. And that can be unstable. The guarantee is relatively weak and doesn't seem unreasonable.

The 8472: Okay.

Josh: My feeling as well. Not unreasonable to make this guarantee. (??) 

Nia (quoting): Tldr: I use the compiler to get the offsets of the fields and using it to get size and data without calling into rust, but if a future version of the stdlib required me to add or subtract five (or modulus something, or whatever) I'd do that, too. None of the particular methods of interpreting the fields are expected to be within regular stability guarantees, only that there is an ordering and it is possible to write some code which converts the fields into size and data

Nia: Agree. The quote addresses what The 8472 brought up.

The 8472: How do we want to guarantee it? Do we say what Josh said, that you can use the to_raw_parts for a specific instantiation and ...

Nia: What Josh said is a stronger guarantee. The strict guarantee being asked for is, the contents of Vec in memory are independent of T. The pointer will not be encrypted with TypeId-related stuff.

The 8472: What about ZST?

Nia: I think that's only a problem for method calls that they don't want.

thomcc: from_raw_parts and maybe to_raw_parts could be allowed, compared to len.

Nia: Right.

The 8472: from_raw_parts might be dangerous if we have UB checks there.

Josh: Would be hesitant about from_raw_parts. If you transmute and call into_raw_parts, the result will be the same values as if you haven't transmuted.

The 8472: That calls len under the hood. If you have LTO, ?? leads to miscompiles.

Nia: ??

Josh: I wonder if we could provide a method that could encode the polymorphism. A method on Vec that returns a raw pointer that doesn't have T, and separately a function on that to give the magic 3 fields. Sounds like what they're asking for. That seems reasonable.

Nia: Right.

The 8472: Digging deep into our internals isn't what you normally do, but I understand the use case.

Nia: Should we make a guarantee on memory layout? The guarantee is now a no-op...

The 8472: I think we need ?? repr before we can make this promise.

Nia: Compiler believed that the current layout generating code already makes this guarantee.

The 8472: No. If you recompile the std with ?? it should be changing something.

Josh: I don't believe layout randomization ??

The 8472: Using it to illustrate that the layout algo doesn't make this promise. layout randomization is a proxy for the freedom that the compiler theorretically has.

Josh: I think LR is stricter than what we should reasonably require from Rust code.

The 8472: I checked the unsafe guidelines and this is a guarantee that we make...

Nia: Would people be ok with making polymorphic layout a soft guarantee? (Agreement.) I'll reply.

Josh: Does this API seem reasonable?

```rust
impl<T> Vec<T> {
    pub unsafe fn get_poly_vec_handle(self) -> PolyVecHandle;
    pub unsafe fn from_poly_vec_handle(PolyVecHandle) -> Vec<T>;
}

impl PolyVecHandle {
    fn into_parts(self) -> (Option<NonNull<u8>>, usize, usize);
    fn from_parts(Option<NonNull<u8>>, usize, usize) -> PolyVecHandle;
} 
```

Josh: Correction: make it `*mut PolyVecHandle` to guarantee it's pointer-sized. 

Nia: No?

The 8472: I think it does.

Nia: Wouldn't it require a concrete type?

The 8472: Would need to somehow type-erase T. They should be able to call this without generic functions.

Nia: Should be isomorphic to the guarantee...

The 8472: Right.

(..)

The 8472:
> Generating per-type-parameter calls into Rust is not possible in a templated C++ definition of `Vec<T>`.

Nia: We can stick to the guarantee we previously discussed (poly layout).
    
### (nominated) rust.tf/libs813 *Should libs\-reviewers kick off FCP where they think it reasonable?*

Nia (pre-meeting): This is already what the proposed libs restructuring would do. We can say yes to this bit now, though.

Nia: And then we can figure out the RFC later.

Mark: Don't see anything in the RFC that changes the question.

Nia: One of the points was that anyone in libs broadly can start an FCP.

Mark: We didn't have the ability to do that previously.

Josh: rfcbot gave us that ability recently. Should say we want to take advantage of this new ability. Should address BurntSushi's concern about a flood of FCPs.

### (nominated) rust.tf/156252 *Make \`Error::type\_id()\` an inherent method on \`dyn Error\`*

([previous discussion](https://hackmd.io/C_EmyvSnQ5CWjlxgTGKI8A#nominated-rusttf156252-Make-Errortype_id-an-inherent-method-on-dyn-Error))

Nia (pre-meeting): @orlp said he'd put forward a proposal for this, but it's not here yet. Unnominate for now?

Amanieu: That was a proposal for a separate supertrait of Any that doesn't require static?

Nia: Think so.

The 8472: Didn't oli want that too? Non-static TypeId?

Nia: Just to have a non-static method(?).

The 8472: That's different but similar to the desire to have non-static TypeId.

thomcc: Orson is also interested in a non-static TypeId.

(Deferred.)

### (nominated) rust.tf/156737 *Implement \`DoubleEndedIterator::next\_chunk\_back\`*

Nia (pre-meeting): This might be worth deferring to the normal constification policy we adopted.

The 8472: He seems aware of the policy. Further down he posted a comment? Consistency with `next_chunk` could be an argument here.

Nia: If there's a proper case, maybe. Consistency alone isn't sufficient, I'd say. Will reply saying that.

### (nominated) rust.tf/157168 *Stabilize \`RandomSource\` and \`DefaultRandomSource\`*

([previous discussion](https://hackmd.io/C_EmyvSnQ5CWjlxgTGKI8A#nominated-rusttf157168-Stabilize-RandomSource-and-DefaultRandomSource))

Nia: Deep breath. We talked about this last week. Don't think there's been more discussion on this. Probably should discuss this toether with the next item.

Josh: From what I can tell, joboet is trying to propose not doing any of this and having a fill method. That feels like it's assuming never shipping an RNG or bikeshedding it long enough to have this separate mechanism. Meanwhile, the other proposal is in semantic bikeshedding. Efffectively, lots of discussion that boils down to people thinking there's a difference between RandomSource and Rng. Should we have randomness, random distributions, should we have this in std or crates. We'll have to make this call eventually. The call is affecting the naming. Rather than picking a name, we have to make the decisions on whether we want to have RNG in std (whether we want to change our decision to have it). Whether we want to encourage system RNG to be "default".

thomcc: Maybe neither? Is the distribution stuff part of the proposal?

Josh: No. It's for stabilizing the trait having fill_bytes and its default impl. An object with a trait giving fill_bytes that's equivalent to `std::random::fill`. It doesn't stabilize distribution but is saying "we'll have a rng that will give you bytes".

Nia: Bikeshedding aside, I'm in favor of stabilizing. Neutral between the naming schemes, I think they're synonyms.

Josh: One of them we can semantically ignore because most agree that they're synonyms (RandomSource vs Rng), but the other does have semantic implication (DefaultRng, SystemRng). We should pick between RandomSource and Rng based on bikeshed color, not semantics. I'd make a case for Rng because it's short and what the ecosystem already uses. We need to decide between System and Default (System we can guarantee that comes from the system, Default may not come from the system and it's the default because we can have a `fn random` using that)

Amanieu: Yes, it's an entropy source (the trait doesn't guarantee if it's bad or good).

Ben(chat): would it make sense to put this in std::io, so that people who want "random" don't have cause to object over any potential for more support for randomness? and name if with jargon that isn't "random", like "entropy", so that nobody conflates them? 

The 8472: Don't like Default.

Amanieu: Would expect Default to not do ??.

Josh: Doesn't have to make a syscall every time, might use a VDSO interface.

Ben(chat): `std::io::EntropySource`?

Ben(chat): I think the notion of a "default" rng is fraught, regardless of what users from other languages expect. simulation use cases and cryptography use cases both want rng with approximately opposite purposes 

Josh: Is that down the road going to produce an objection of ??.

Amanieu: For that, I'd expect it to not do a syscall. In favor of having both then.

Nia: If we'll have a semantic distinction, I'd have a SystemRng, and CspRng that's in user space...

Josh: Both Rng should be cryptographically secure because most people will use that. People who want speed, they should choose that.

Nia: I'm saying we should provide a fast Rng.

Josh: Yes. 

Amanieu: We can defer that.

The 8472: The problem is in the edge cases. If you have a system without a good entropy source (bpf or some isolated container).

Amanieu: System is the best your system can give you.

The 8472: No, if System guarantees security...

Amanieu: Then you shouldn't call it System but Secure.

Josh: We can provide a list of what it does on every platform...

The 8472: If ?? should we fail to compile? If we try to provide this in std, it has to serve a purpose. Is crypto in scope or should we tell users to figure that out themselves?

Josh: 1) My system dosen't have a (secure) randomness source, e.g. wasm or embedded; 2) My system does provide one, it's supposed to be secure, and sometimes it can't gather sufficient entropy (espf, probably secure, but if you build an esp that doesn't have RF capabilities, then there's no source of randomness in your system). If we have no SystemRng, it's reasonable to panic. In the (2) case, get it from the system and if it's not good enough, don't deploy on that csp.

Amanieu: These are the 3 possible behaviors we can have:

- DefaultRng: CSPRNG, but guaranteed to be reasonably fast (`[u32; 1000]::shuffle()` doesn't make 1000 syscalls)
- SecureRng: Most secure RNG available on the system. Fails with error if system doesn't provide secure random data.
- SystemRng: The system's entropy source, or a CSPRNG is there isn't one.

Josh: We could make shuffle more efficient by making a large fill_bytes call.

Nia: fill_bytes on a SysRng is a single poll of the system's RNG, what you build on top is up to you.

The 8472: Naive impl might ask for 4 bytes at a time and still be fast...

Josh: It's possible that it could be a SysRng that's capable of giving you a 1000 bytes at a time.

Amanieu: We should only have one of those but not two.

Josh: Naming aside, don't think we should have SecureRng, should just have it be secure. Agree with the options.

The 8472: For crypto we need System or die.

Josh: 1) system or die (current); 2) CSPRNG that you may choose to seed with the systemsource and run how you see fit.

Amanieu: Are you saying CSPRNG shouldn't be default?

Josh: Yes.

Amanieu: Then the shuffle method should take ?? arguments. And we'd not have a global `random` fn. I'd veto having these and requiring a syscall every time.

The 8472: Agree with Amanieu.

Josh: Disagree with the premise of "people using the defaults will care about perf of operations that will require a syscall per a block of numbers"

Amanieu: Perf matters, precedents. Don't want to repeat the mistakes. Shouldn't be slow for vague security reasons.

Josh: Don't think it's "unusably slow" as you say.

Amanieu: The exact same argument can be used against DefaultHasher being Sip... 

(..)

Nia: We could also not have a default.

Josh: I'd like the default RNG to support vmfork.

The 8472: Most use cases aren't that dependent on things being randomized. If you have two independent runs, ...

Josh: For anything that needs security on par with cryptography, it's possible for two forks to use the same random numbers...

The 8472: That's why I'm saying Default is a bad name.

Nia: Agree with The 8472. If we have a fn shuffle, we could have it a generic parameter of Rng + Default.

thomcc: Let's say you have a library and deep in the library you generate UUIDs. ?? The alternative would be to thread through the Rng from the user.

Nia: Similar to the clock discussion from earlier (strong property and reasonable defaults).

joboet: Pointing out that we don't use syscalls on nearly every platform. I chose RandomSource on the premise of the fastest of the sources the system says is secure. On linux we use getrandom, on BSD we use ??forrandom, on macos the same. On nearly every platform there isn't a syscall involved but a user-space rng seeded by the OS. Because it's in the C library it can depend on ?? that makes it secure. So I don't think perf matters so much.

The 8472: Sure but we have to consider niche platforms where this is not the case. Need to provide a Source that prioritizes security over speed.

joboet: I don't really think so becasue those systems are really rare.

The 8472: Sufficient to be on old Linux that doesn't have...

joboet: Shouldn't optimize the API for that.

Josh: Agree.

The 8472:

Josh: Hashing we can and should fix. If we provide a default, we make it hard to shoot yourself in the foot for security. Could we pick a name for which we can kick some of this decision down the road? Something like "If we choose to have a defualt, it will have these properties, otherwise, no default"?

Nia: I think if we want a default it would need to split the difference somewhat between security and speed, otherwise we need to set defaults only on some platforms and we get the worst of both worlds where other platforms don't which stops people from using the defaults at all if they want to write portable code.

Chris: ?? For shuffle, I don't think it needs strong crypto?

Josh: Would be skeptical about saying certain use cases don't need strong crypto. 

The 8472: It's about what properties you want.

Josh: We can't predict that. You might want X property.

The 8472: Picking defaults makes it problematic.

Josh: How much of this can we successfully manage to postpone?

Nia: Probably defaults would have to be later. Makes sense given the discussion. One of the earlier ideas people were okay with was a split between System (abort if missing) and fast rng.

Josh: Need some bikeshed on the fast version. Is it right that if we call it SystemRng, we're precluding the possibility of adding a DefaultRng, whether that's an alias for SystemRng or ??.

Amanieu: No... We should have ??

Josh: Does anyone feel like they'd have a blocking concern ... if we hadn't added SystemRng today?

Amanieu: Happy for Default and System be separate.

Josh: If I send a PR naming RandomSource -> Rng and Default... to SystemRng and try to stabilize that, would anyone have a blocking concern?

Amanieu: 1. What's the behavior on wasm-unknown-unknown?

Josh: Panic, stop using that target.

Amanieu: What about ??

Josh: Use getrandom.

Nia: Can't imagine a world where you can't interact with the outside world. You can probably mimic an RNG by using timing... If someone wants secure and they're on wasm-unknonw-un, the correct thing is to error.

Josh: And they can pull in some kind of wasm source or go run the CSPRNG with the seed that you need to get from somewhere...

The 8472: They can always bring their own impl of our trait.

Nia: Amanieu, does that resolve your concern?

Amanieu: Rng that panics and doesn't return results?

The 8472: Don't want to return Result maybe, can see pushback.

(..)

Josh: The other thing is the idea of having a "bad RNG" condition and handling that. How can you do that other than panicking? There's a mechanism for seeding an RNG for hashmaps, and if that fails, we die. We don't have handling for failure if some condition is failed.

Nia: Sounds like we may be haivng randomness. Send the PR, and let's see the next round of discussions.

Josh: Yay.

The 8472: Do we document that we panic? No. We should add it.

Josh: If someone wants a io::Result returning function, we can offer that.

The 8472: People who check for nopanic code might complain...

Nia: ??

Josh: That's one of the reasons I wanted to defer. We should have seedable RNG. Can you abstract over the seeding, etc. There are types for this in rand. Whether we want that too is a question for later.

The 8472: We can leave stuff for downstream crates to implement.

### (nominated) rust.tf/157573 *std: simplify the random feature*

Nia (pre-meeting): To be discussed together with the above.

### (nominated) rust.tf/157515 *Tracking Issue for \`xdg\_basedir\`*

Nia (sarcastically): Anothe lovely uncontroversial item on our agenda.

Nia: This got some discussion. I like it and think it's good. We should have this. Where to put it — under `unix` makes sense. Under `linux` is too strict. Under `??` doesn't make sense because ??. Concern on impl (home_dir instead of reading env) — not big of a deal because it's going to always be the same. Want this on stable.

thomcc: The fact that libxdg is 0.8 is concerning.

Josh: Few other issues with this that it hasn't learned on from previous attempts. The API in dirs crate is "give me an object representing i've retrieved these things and ..." can fail with io::Error, and the object represents you've read and cached it, which can fail in various OS-specific ways. We could cache this in std transparently, but we'd still have to return io::Result. It's satisfying to have error-free API.

Nia: 1) Do we want xdg handling in std? I think yes, if they break the standard, it's not unreasonable to move in under a different module over an edition. 2) API design. I agree that it shouldn't be fallible. println technically assumes that printing never fails. This API is not it.

Josh(chat): I would argue 3:
- Do we want to do XDG
- Handling Result/caching
- Do we want to do something higher-level that's cross-platform? 

joboet: I raised the whole thing because I was worried about macos. (??) Now we have two different ways to handle ??, and it makes me wonder if this is right for std. 

Josh: I'd lvoe to see us providing a higher levle API that's closer to dirs and tries to abstract over this and support portably asking for standard directories for things. Hesitant to provide xdg-specific things because in practice the # of programs that can say "im unconditionally using xdg dirs" is going to be low. Any program that can't do that and wants to do it portably will end up using dirs anyway, in which case the xdg API pulls part of the impl into the std in a way most shouldn't use.

Chris: We did actually try to make cross-platform dirs in std, and it fell aprat due to disagreements.

thomcc: On macos, it's opinionated where to put ??. If you're an app, ??, vs. if you're a CLI. I'd be in favor of having xdg exposed and having a darwin-specific api.

Nia: That's close to what I wanted to say — provide multiple things. Unsurprised to hear that attempt to ?? went nowhere. 

Josh: Similar proposal: what if we in the portable part of std provide a type similar to what dirs calls ProjectDirs (here are the pieces of what i might want -- configs, temp files). That type with a bunch of methods returning Option (if doesn't exist on your platform), and then... put in the docs that using this or that is a matter of taste...

Nia: Like that idea, probably should be a trait (Josh: a type). Would ask regarding macos, if Chris known, are there distinct standards if you're a CLI or ... where to put your config stuff.

Josh: 2: GUI app vs. portable unix app.

Chris: Hybrid choice of doing a cache directory and everything else xdg.

Nia: In that case I like it and think it could be nicely made cross-platform.

The 8472: How stable are ??.

Josh: The concept of having a folder to put Documents/Pictures in, the former will have some mapping between different OSes, and the latter is "as long as there are going to be formats to put pictures on a computer". The concept of "where should i put pictures/music" is sufficiently portable. The user is capable to use the defaults or fall back to their own heuristics.

thomcc: This is niche. Josh is probably right, but unsure if this meets the bar for std.

The 8472: ?? we can make it non-exhaustive and provide a default constructor... The niche stuff can be added if there's demand for it.

Josh: For unblocking that, we could add the stuff that many OSs have a default for...

The 8472: If we want a struct, it's just public fields?

Josh: Struct will have methods providing PathBuf, and additional ones providing Option of PathBuf.

The 8472: ?? customized...

Josh: Doesn't need to be fully constructible, ??. Would argue cache_dir/set_cache_dir,..., and the struct isn't specified to have pub fields.

The 8472: Many setters. Do we want that?

Josh: Seems fine. Doesn't seem like many people will create this. Users could manually set the fields they know about.

The 8472: Could have pub fields of `Option<PathBuf>` + non_exhaustive + default empty constructor

Josh: No way to have extension fields today.

The 8472: Some platforms will have None.

Josh: Trying to postpone the question of non-widely-used ?? having a field.

Nia: Sounds like there's appetite for experimenting with per-target defaults. We should respond with "yes, we awant exp, the api needs redesign, need cross-platform, would like to pursue". Is that right? No commitment to stabilize.

thomcc: Agree, covers some of that. Mention that we don't want a super-opinionated API that mirrors directories.

Nia: Sure.

The 8472: Skeptical how stable/portable this can be. OTherwise, okay with this.

Nia: Happy to include that porta/stability would be a blocking concern for stabilization.

Josh: Asking them whether they'd be ok with calling it 1.0...

(..)

Josh: xdg anticipates ?? actually... If an OS said it wants to put cache "here" ?? If none of the things are set, what would you fall back to?

The 8472: Yes, makes sense.

Josh: Would not be hard to reach out to ?? and ask them.

Nia: Happy to reach out to them, know some folks working on free desktop.

Amanieu: Unlikely to change because it would break a lot of things.

Josh: Probably, but we can ask them to document if this is stable.

The 8472: They could embark on a decades-long migration plan, again...

Nia: Fair, but ??. I'll reply.

Josh: Having been part of cargo dealing with xdg, this was the least painful bikeshed involving it I've had. That was really nice. I wish more things this controversial were as simple.

The 8472: I'm sure more people will come out of the woodwork with concerns.

### (nominated) rust.tf/157728 *\`std::sync::mpsc::Sender::send\` can block the current thread*

Nia: Mark nominated this (was here earlier but left). Feels more like a libs question? We have time. Not 100% sure what we're being asked to do.

joboet: Seems like a libs question.

Amanieu: For this one, we might want to see the wasm target maintainers.

The 8472: Might be libs api becasue it's asking for a doc update?

Amanieu: They want libs api to guarantee whether ?? uses Mutex internally.

Nia: We do guarantee it won't block which ??.

Amanieu: The intention here is it will return a Send error if the buffer is full or something.

Nia: Even querying the buffer requires checking the Mutex.

The 8472: Can we ??

Nia: Would be shocked if it's easily possible.

Amanieu: The channel has infinite buffer, so that will never happen. Has some shared state to ?? receivers which can in theory lock a Mutex. Considering you can't use channels on wasm, the maintainers are interested in this.

Nia: Their input would be relevant. I worry it might make it worse for other targets where it doesn't matter.

The 8472: Is it the code we copied from crossbeam?

??: Yes.

The 8472: We should check with upstream and see if they changed it since.

Josh: We shouldn't be maintaininig this ourselves. Need to ask crossbeam or switch to another impl.

Nia: Happy to cc wasm and crossbeam.

Amanieu: Main concern: is it ok if channel doesn't work on wasm.

### (nominated) rust.tf/157734 *Stabilize \`local\_key\_cell\_update\`*

Amanieu: Starting an FCP on this.

### (waiting on team) rust.tf/155421 *Document panic in \`RangeInclusive::from(legacy::RangeInclusive)\`*

Nia: Waiting on FCP.

Amanieu: Need to change to the correct label.

### (waiting on team) rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>*

Nia: Why is this waiting on us?

Amanieu: Mark changed the label, needs FCP. Needs crater run before FCP.

Nia: Yeah, feels scary.

The 8472: Are multiple applicable impls causing inference issues?

Amanieu: Need a crater run. I can do it.

Nia: Let's look at it next week.

### (waiting on team) rust.tf/156983 *Add \`io::Read::read\_le\` and \`io::Read::read\_be\`*

Nia: Waiting on bors.

Josh: BurntSushi thought this might need an FCP, but I pointed out it's adding an unstable feature, so he canceled the FCP and gave r+. I plan to add `read_ne` and then we can propose stabilization in 6 months or so.

### (waiting on team) rust.tf/157428 *allocator: refactor for stabilisation*

Nia: Main outstanding q is whether Allocator needs to return a slice or NonNull.

Amanieu: If we return a plain ptr, we'd be removing the excess storage logic?

Nia: Not necessarily. There are proposals on how to get excess storage...

Amanieu: Should be part of the allocation call...

The 8472: Agree. 

Nia: Agree, should keep it as a slice. Waiting for a big proper write-up so we can justify decision to people.


### ACP process

Nia: Should we go one by one? We want to make the ACP process more lightweight.

Amanieu: ??

Nia: Agenda gen needs changing.

Josh: Less than week old we should do a vibe check.

Amanieu: Separate sections in the agenda?

Josh/Nia: Good.

Nia: ACP process in meetings is most productive when the author has a weak motivation. Bikeshedding happens async mostly. So let's focus on the writing/motivation?

Amanieu: Meeting time is better used to provide final decision after there's already been some discussion.

Nia: Sounds good. Let's stick to the stuff that's had some discussion.

### (new change proposal) rust.tf/libs809 *ACP: library folder APIs for \`std::os::darwin::path\`*

Josh: This is relevant to the xdg proposal and could use a similar response.

Nia: Earlier we effectively asked for this (smth comprehensive for multiple platforms), it's part of that. Right? The sketch would subsume this ACP.

Josh: Both ACP.

Nia: The answer here then will point to the answer on the other ACP (accepted-ish). I don't like the API being proposed.

The 8472: Subsumed.

Nia: Yes. Will point them to the other xdg discussion.

(Agreement.)

### (new change proposal) rust.tf/libs814 *\`OnlyNegative\` and \`NonNegative\` to compliment \`NonZero\` for signed integers*

Josh: insert joke about `OnlyNans` here.

### (new change proposal) rust.tf/libs812 *ACP: Add \`HashTable\` to \`alloc::collections\`*

Amanieu: Ship it.

Nia: Why did I think we had this already?

Amanieu: We had HashMap.

Josh: This is to get the underlying thing...

Amanieu: Need to figure out how this works with hashbrown because it uses alloc.

Josh: Hypothetically, if we have this and HashMap, should we merge hashbrown into std and maintain it there? It's up to Amanieu. If we add 99% of hashbrown here, would it make sense to say it's the canonical version?

Amanieu: Yyyeah?

The 8472: Rayon integration.

Amanieu: I don't remember if we have serde integration?

The 8472: Anything else that's not exposed in the std?

Amanieu: HashTable has bucket API now.

Josh: If we move to std, then it's obvious whether serde or hashbrown should implement serde. And it's serde. Also avoids serde-vs-facet arguments.

Amanieu: Harder to evolve the HashMap API? And things previously available in hashbrown would now be unstable...

Josh: How hard would it be to have a directory in std that's the impl of hashbrown from which you both ship into the std and a standalone crate?

Amanieu: What we're already doing for backtrace.

Josh: Something like that. What if the canonical home became the std and we no longer had the circular deoendency.

Amanieu: Possible, need to discuss. Clar Fon is the main maintainer of hashbrown, need to check with them.

The 8472: JOSH subtree, separate git repo, CI, etc.?

Amanieu: In terms of tests, not much more interesting than the std tests.

The 8472: For unstable features...

Amanieu: Nothing that couldn't be moved to std. std already has benchmarks...

The 8472: Do we release any crates.io crates from rust-lang CI?

Josh: A handful of crates we release from rust-lang, not sure if from CI. Wouldn't be impossible.

Amanieu: Smth similar to backtrace could work where hashbrown is imported as a module?

Amanieu: The bucket API is the only unique thing to this repr. It means that the value can be accessed with a usize address. It won't work with a chaining table anymore.

The 8472: I don't think we'll ever want a chaining impl.

Amanieu: Sure but it restricts the repr.

Nia: Vibe?

Amanieu: Ship it.

Nia: Will you respond?

Josh: Some methods have K, V but HashTable takes T?

Amanieu: That should be T.

Josh: Needs fixing then.

(..)

Amanieu: Do we want to change it to K,V?

Josh: We should accept HashTable in its current state. Separately, we should put a design question on whether it's the right API. 

Nia: Amanieu, can you respond? I can.

Amanieu: Please do.

Nia: I will a bit later.

### (new change proposal) rust.tf/libs807 *ACP: Add \`BTree\` to \`alloc::collections\`*

Nia: This is basically the same, right?

Amanieu: That one's more complicated.

Nia: How?

Amanieu: This doesn't exist yet.

Josh: So HashTable is already in hashbrown, and this doesn't exist.

Amanieu: HashTable has a T, BTree keeps the key-value split. I'm inclined to reject this and have it be prototyped in a crate first.

Nia: I think there is a crate?

Amanieu: BTreeMap is fully impld in alloc.

Josh: Someone should copy-paste BTreeMap and prototype how to implement lower-level BTree?

The 8472: Not sure we need to mirror HashTable. There it's beneficial to calculate the hash separately, but here ?? compare function. You can't save efficiency by computing the hash externally and saving lookups. This could be impld on a regular BTreeMap?

Amanieu: No because if you have a type that impls Ord, we guarantee that elements of BTreeMap are always sorted. ?? elements not in order if you violate.

The 8472: We could say the guarantee only applies if it's a unit type (?)

Amanieu: How to guarantee that if the type is not unit?

The 8472: Trait?

Amanieu: Would like to see it prototyped first.

(Something about layout and having keys and values separate vs together, padding.)

The 8472: If this ties us down in a way we can't evolve the impl in the future, we shouldn't have this?

Amanieu: Impls in C++ behave similarly.

(..)

Amanieu: If we do have a key-val split, we still want the key to be fully mutable in this case because the way the API works is you provide a hash when you insert or do a lookup, and the lookup callback... There's no restrictions on modifying the key. It's the hash that matters.

thomcc: That's okay. ??

Amanieu: Rejecting this?

Nia: Yes.

### (new change proposal) rust.tf/libs811 *ACP: convenient numeric conversions dependent on overflow\-checks*

Nia: Oh god, another numeric conversions ACP, for floats.

(Deferred.)

### (new change proposal) rust.tf/libs810 *ACP: Missing floating point conversions*

(Deferred.)

### (new change proposal) rust.tf/libs808 *Type\-smart \`intersect\` on ranges*

(..)

Nia: My issue is that RangeInclusive can't be empty, that's why I want it to be fallible.

Amanieu: Possible to be empty is start > end.

(..)

Josh: This makes sense.

Nia: This make sense.

The 8472: We should provide an alias for a tuple of two bounds.

Nia: I like this, will let you decide.

Josh: +1 as proposed. You can do IntoBounds as Intersect. Going from pair of Bounds to some range, but...

Nia: That's a different ACP.

Josh: Want to approve as proposed and ask for various tests.

Amanieu: Confident that the empty cases work.

The 8472: Need to make sure it's covered.

Josh: Suggesting that we should have tests for them. Seems great. Like the idea of doing a bitand operator as well but not today. (bitor wouldn't be union because of gaps in between but something like hull)

Amanieu: I think you should never be mixing range types

Josh: What if trait method where combination will uniquely determine the output type... and where it's not feasible we can give you bounds? Range and RangeTo should be intersectable and produce a Range...

(..)

Amanieu: Should we move on?

Josh: Yes. I'll post a comment. No objections to either all 64 combinations or only those where there's a valid output.

Amanieu: Yeah... (Sigh.) What about any range intersecting a pair of bounds?

Josh: If you started with a pair of bounds, you should get a pair of bounds.

Amanieu: Yeah, but I'd prefer a simpler API.

The 8472: We already use RangeBounds...

Amanieu: We'd like to cc pitaj who proposed IntoBounds. Its entire purpose was to implement intersect, so the two authors should talk. scottmcm has already posted a comment... Josh can post a counter-proposal, let's move on.

### (new change proposal) rust.tf/libs804 *ACP: Add allocation\-free owned downcasting for Any values*

Amanieu: This is owned downcasting to a specific type...

joboet: You could use this for niche specialization...

Mark: https://doc.rust-lang.org/nightly/src/alloc/boxed/convert.rs.html#333

```
https://doc.rust-lang.org/nightly/src/alloc/boxed/convert.rs.html#333
    pub fn downcast<T: Any>(self) -> Result<Box<T, A>, Self> {
        if self.is::<T>() { unsafe { Ok(self.downcast_unchecked::<T>()) } } else { Err(self) }
    }
```

### (new change proposal) rust.tf/libs802 *ACP: give access to the inner iterator of \`std::iter::Peekable\`*

### (new change proposal) rust.tf/libs801 *ACP: add volatile operations to \`Atomic\*\` types*


### (stalled change proposal) rust.tf/libs452 *ACP: Implement \`TryFromIterator\<T\>\` trait to compliment \`FromIterator\<T\>\` trait.*

### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs523 *Add a deterministic constructor for \`RandomState\`*

### (stalled change proposal) rust.tf/libs296 *ACP: Designing an alternative \`FromStr\`*

### (stalled change proposal) rust.tf/libs544 *\`Arc\<impl !Sized\>\` constructors*

### (stalled change proposal) rust.tf/libs525 *Vec::try\_remove*

### (stalled change proposal) rust.tf/libs322 *\`AssertThreadSafe\` (name TBD) – a more general API for lifting conservative \`!Send\` or \`!Sync\` implementations*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
