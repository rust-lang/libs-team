# Libs-API Meeting 2026-07-28

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Nurzhan, Amanieu, David, Chris, Clar, Josh T

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

1 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3984 *RFC: Refactor the libs team* - (3 checkboxes left)

16 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/134915 *Tracking Issue for ByteStr/ByteString* - (3 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (3 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (3 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (4 checkboxes left)
  - merge rust.tf/117693 *Tracking Issue for \`core\_io\_borrowed\_buf\`* - (4 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[BurntSushi (1)](https://rfcbot.rs/fcp/BurntSushi), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [joshtriplett (8)](https://rfcbot.rs/fcp/joshtriplett), [dtolnay (5)](https://rfcbot.rs/fcp/dtolnay), [Amanieu (8)](https://rfcbot.rs/fcp/Amanieu), [the8472 (12)](https://rfcbot.rs/fcp/the8472), [thomcc (1)](https://rfcbot.rs/fcp/thomcc), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm)

### (nominated) rust.tf/libs830 *ACP: \`std::fs::UserDirs\` or similar*

Nia: Nominated last week.

Josh: We talked about this previously. We wanted to see the implementation of `Default` added and `take` removed. The author responded that they didn't think this should impl Default to avoid surprising people; instead, they wanted an `empty` method for this. Does the team think we should still ask for `Default`?

Nia: I see the point but also don't think we should replicate trait functionality as methods. Feels footgunny. Also means you can't derive Default on anything that contains this.

Josh: ... But we do have precedent in std that a type has multiple constructors and no `Default` because there are several potentially valid impls. Should we apply that logic here?

Nia: Fair. And I also think `take` should be removed.

Josh: Same.

Nia: Unless there are objections, we could tentatively OK this.

Josh: This is an ACP still and would need to get through a stabilization routine. Are people okay with the general direction?

The 8472: The main benefit of `Default` is having `mem::take`, and you can `mem::replace` instead.

Josh: True. You can `mem::replace` with `empty`.

Nia: So no `Default` and no `take`.

Josh: I'll respond.

### (nominated) rust.tf/libs833 *Meta\-ACP: Integer cast methods*

Nia: Fun. We've had a couple of proposals from Josh and Amanieu. Clar's original one is there as well.

Amanieu: The 2 points of disagreement were narrow (I think it shouldn't exist) and if widen should be able to change sign.

Josh: First, are we on the same page about the differences in Clar's tables?

Amanieu: Clar, what are your thoughts on the cast_signed/cast_unsigned thing?

Clar: TBH, that was a weak proposal. The idea was based on Josh's desire to not change from unsigned to signed... This was also before I realized there were stable cast_{unsigned, signed}. I'm less attached to that. The main thing is, I agree that widen should allow sign casting if there's enough space; feels natural. Everything else, seems reasonable.

Nia: Josh and Amanieu, your proposals mostly differ around `narrow`.

Josh: I agree with Amanieu's idea of the differences and stand by my version because narrow has value as a type-constrained version rather than following `cast` and similar. There's value in knowing that this operation will only do one thing.

Nia: FWIW, I read this as more of a conceptual difference on what widen means. Per Amanieu, narrow is a no-op if ?? whereas cast potentially lossily change the type. Josh's proposal is "make the type wider/narrower".

Josh: Reasonable. Amanieu would like an operation that's lossless-only and value-preserving (widen).

Amanieu: Yes. Is your counterargument that this is Into?

Josh: First counterargument is that, second is that yours is valid but I also want a sign-preserving method...

Clar: `lossless_cast`

Nia: I think keeping widen/narrow (not changing sign, extend); casts that??, and cast_signed/unsigned.

Josh: I like that also because the name lossless_cast parallels the other cast methods and answers "what will happen if this doesn't fit?". lossless_cast will not typecheck.

Amanieu: I think narrow is inherently redundant when cast exists. I think we should have one thing that allows sign changes. If we don't have narrow, we don't need widen.

Josh: Yes, but I want both.

Amanieu: We're proposing different versions of widen.

Josh: Your widen should have a different name (lossless_cast is a good one).

Amanieu: By your measure, widen would not allow ??.

The 8472: 1) Why lossless_cast and not Into? We could just cover more types. 2) Should widen be truly widening or have identity? Could be good for macros and generic code to allow identity (widen by 0).

David: Does that perspective hinge on ?? bounds? `T: Widen` ??. 

The 8472: `T: From<U>`.

David: From and Widen are redundant with each other?

The 8472: Yes.

Josh: This looks like my proposal. Amanieu's widen is ??.

The 8472: Yours is only if it goes to the same signedness?

Josh: Yes, in mine, u* can only go to u*. Amanieu's may map u* to i*.

Clar: I only put "noop" because "lossless" made the table wider.

The 8472: We should add `Into` to the table.

Josh: And `TryInto` by that argument.

Nia: That's just `checked_cast`.

Amanieu: You can't turbofish `TryInto` but can `checked_cast`.

The 8472: That's big.

The 8472: If `*Into` were turbofishable, this would be viable.

Nia: 1) checked_cast, 2) ??, and we can discuss those separately from explicit widen/narrow that preserve sign. The only blocker between them is if we want widen/narrow, then we should call the into-like one ??.

Josh: Agreed. There are additional reasons other than ??. That's a discussion we can have as two separate proposals.

Nia: Then I suggest to split up the issue. Is there anything else other than these where we're in disagreement? Seems like people only prefer signed/unsigned conversions only where it definitely changes the signedness.

Josh: ?? someone proposed `saturating` and `checked` variants for those. That'd be odd but fine? The current `cast` reinterprets the bits, and the other versions would be value-preserving.

Josh: Should widen/narrow exist and should there be an op for lossless_cast that preserves value (turbofishable Into)?

Nia: Sounds like people just want turbofishable Into.

Josh: ??

Nia: Amanieu, would you be ok with calling that lossless_cast?

Amanieu: Not a huge fan of the much longer name.

David: Should the `WidenTarget` trait be publicly nameable?

Josh: I'm a trait maximalist and in favor for a bound for "this can be widened to u64".

Nia: This runs into Widen semantics again, whether it allows changing signedness or not.

Josh: If you want to be able to change signedness, you could use `Into<u64>`.

Nia: ??

Clar: The only (weak) argument for `Into` for lossless cast is there are cases where `Into` can be implemented where you don't want it to be a lossless cast. (Socket example?)

Amanieu: Better example is `IpV4` which has `Into<u32>`.

Josh: In general, we don't have lots of numeric traits because we wanted trait evolution mechanisms first. I expect we'll want to seal the traits. If we have `Widen` or `LosslessCast`, people will want to impl them for their own types.

Nia: Either way, sounds like these traits are another pile of proposals.

The 8472: Are they designed for coherence, so user crate u32 can `Widen` into a std type?

Nia: I guess this would be a design question whether the traits need to be sealed. That's a separate discussion.

Nia: There are 3 different proposals. Let's split up the ACP into widen/truncate and traits, and keep going with the core methods where we agree.

Amanieu: We've already accepted ACPs and created tracking issues for these ACPs. 

Nia: Well, let's split up the discussion at least.

Josh: Shipping a set of cast methods we've agreed on. Writing up a list of the methods we agreed on and call that ACP acceptance? Let's start there?

Amanieu: Did we accept #811? Yes.

Nia: Yes but no tracking issue yet.

Nia: We can open zulip streams for these. Is there more to discuss next week?

Josh/Amanieu: No.

Josh: ??

Amanieu: Stuff we agree on: 
https://github.com/rust-lang/rust/issues/157388 and 
https://github.com/rust-lang/rust/issues/154650; Controversial one: 
https://github.com/rust-lang/rust/issues/154330

(Unnominated.)

### (nominated) rust.tf/libs839 *meta: Running cargo\-semver\-checks in reverse on std stabilisations*

Nia: I proposed this because it's happened a couple of times that something was stabilized accidentally. Given CSC allows us to see stability differences, that should be used. Do we want this?

Amanieu: I assume this will run on every PR and have a bot message "looks like you've changed the stable API surface", and it's up to the reviewer to check if that was intended?

Josh: Yes.

Nia: I'm a bit worried about it being noisy.

The 8472: If it runs in reverse wouldn't the message be confusing becasue it points in the unexpected direction?

Nia: They're working on lints working in forward direction as well.

David: Predrag was working on improving the message.

The 8472: ?? running it on stable, next beta,...

Nia:

David: (Golden file against which things are tested.)

The 8472: Would help to have an example for discussion.

Nia: [Main page for CSC](https://github.com/obi1kenobi/cargo-semver-checks) has examples.

The 8472: Ideally, would be something like an outline of what changed in the API shape.

Nia: So you want cargo-semver-checks to provide that?

The 8472: If it's not hard/too much work. Having an overview of how noisy the report is would help reviewers.

Nia: If it's not noisy, are you in favor?

The 8472: Yes.

Josh: We should make sure the output isn't excessibely confusing. Assuming the output is not confusing, I'd expect ??. If we're not thinking we're stabilizing anything, then having the message wouldn't be noisy but expected.

The 8472: That's not what I meant by noisy.

Clar: I like the idea of incremental report rather than on the whole release. If there were a version that provided a simplified list of added APIs and comparing that with what was actually stabilized...

The 8472: What I meant by noisy is that the report is long to read and explains why this is a breaking change and all the pass/fail info that's not needed. What Clar said sounded similar to the golden file idea.

Nia: We can probably make it less noisy (print less with some flag, etc.) Beyond that, I'm happy doing the golden file or conditioning it on an FCP label or something.

David: Golden file would let people use git history to compare.

Clar(chat): also it feels like it would be extremely common to stabilise extra things during stabilisation PRs, less likely to randomly stabilise in a PR that didn't intend to 

The 8472: Checking consistency with a label is insufficient...

Nia: I'll reply with that and let Predrag reply.

### (nominated) rust.tf/117693 *Tracking Issue for \`core\_io\_borrowed\_buf\`*

Nia: Nominated yesterday by Josh.

Josh: Was digging into this. Has been open for many years. We've wanted this for a long time and have been iterating on what it should look like. We made it generic, ?? also, and there's a separate feature flag for ??. I'm proposing to stabilize `BorrowedBuf`, `BorrowedCursor`, and the methods that use them. I reviewed the potential blockers and don't think there are any.

David: I thought there was a [blocker around `ReadBuf`](https://github.com/rust-lang/rust/pull/106643) related to implementing one trait or the other?

Amanieu: We have this?

Josh: The compiler added an attribute for "must implement one of"...

Amanieu: Do we stabilize this in core too or is this separate?

Josh: I expect we'd stabilize...

Amanieu: `core::io` is still unstable. ?? Or maybe we wait until we have the whole Read trait ready?

Clar: ??

Josh: The proposal doesn't cause us to stabilize `core::io`.

Clar: This can be stable in `std`.

Nia: And in `core`, we should stabilize it with `core::io`?

Josh: We'd no longer feature-gate these specifically, but `core::io` still is.

Clar: I wouldn't consider stabling even `alloc::io`...

Josh: That's also fully orthogonal to this.

Nia: Worth dropping a comment there, otherwise I don't have issues with this.

Clar: I'll write the comment on alloc/core.

Amanieu: B`orrowedCursor::written` tells you how many ??. It's the only thing on it that pretends that ??. The cursor is unconcerned with what was there before ??, only tracks the unfilled space. Feels out of place and I'm not sure if it's used anywhere.

Josh: I recall a discussion about the semantics of this method. There was confusion about ??. It's possible both should exist and maybe there is a better name for it.

Amanieu: Seems like we use it a lot. I'll review it async.

Nia: We can file a concern for that and figure it out. Looks out of place to me too, but it's used a lot.

Josh: Possible that we should have distinct method names (total_written) or something that makes it clear that it includes ??.

(..)

Josh: It's confusing that many parts of std pass around `BorrowedCursor` and call it `buf` and not `cursor`...

Nia: Any objections to start FCP?

Josh: Linked discussion about "must impl one of" probably blocks this. Need to get that PR merged. Items blocking that PR have been mostly addressed...

The 8472: Do we have approval from compiler to ship smth that relies on an internal feature?

Josh: That's lang. There was a policy that ?? need to be documented or stabilized. I'l make a note on that.

Clar: so lang would want to sign-off on that.

??

Nia: We can say we signed off as libs, and...

Josh: Lang gave the conditions, and we just satisfy those.

Josh: For simplicity, I started FCP for BorrowedBuf/Cursor/ReadBuf. The must-impl may take time. Should the FCP for the latter go ahead separately from ReadBuf?

Nia: We could do that, but I don't know if makes sense...

Josh: Is there any reason to not stabilize Buf/Cursor without waiting?

The 8472: Default implementation for `read_to_end` was changed when [initialization tracking was removed](https://github.com/rust-lang/rust/pull/150129), but it wasn't changed correctly it seems, so maybe the impacts of removing initialization tracking weren't fully considered? There's also an [open PR trying to improve that](https://github.com/rust-lang/rust/pull/158083), but there are regressions.

Josh: Removal of init tracking caused perf regressions, which is why we added tracking of the whole thing initialized as a bool.

Clar: `Read` was also moved to alloc, so it's probably not tracking that...

The 8472: https://github.com/rust-lang/rust/pull/150129/changes/df42b1908861f652ada10911074070962043640a#diff-3c029409e4e63a7e6e28fa9c43df76df8a6bc4bc946d42a75ff27bf448a00b3dL526 — `if !was_fully_initialized && consecutive_short_reads`.

Nia: How much does this impact the actual hypothetical stable API?

The 8472: Tracking of initialized bytes count was removed?

Josh: Leaving aside whether we want the API or not, ?? is under a feature flag, and byte-by-byte tracking ?? could also be under one.

Josh: From what I can tell looking at the PR fix attempt, it uses the bool init tracking.

The 8472: I was talking about the original code. ?? This is a potential oversight, and we should check if it can be fixed.

Josh: There've been several iterations trying to improve the fix, but I don't see anything that'd block stabilization or require changing the API surface.

Nia: That's my take as well.

The 8472: If init tracking could be added again if it was necessary, then that's ok.

Josh: Hopefully it doesn't need to be becasue it was removed for a reason.

The 8472: Yeah, but removal caused issues, so I want to make sure.

Nia: Happy to type the caveats out after the meeting. We can begin stabilizing if checkboxes are checked.

Josh: I've been writing it up.

Nia: Thank you.

### (nominated) rust.tf/159856 *\`core::derive\` was unintentionally stabilized*

Nia: A crater run finished. Of the 5 regressions, 3 actually regressed, and 2 were spurious. What do we want to do, keep it stabilized or break the 3 crates? I don't care too much, either seems acceptable. Don't see why we wouldn't have already stabilized it.

Josh: On the one hand, we shouldn't get into habit of retroactively stabilizing... ?? In general, if it were more non-trivial, I'd say let's claw this back. But in this case, I propose an FCP to stabilize this.

Nia: I generally agree. Should we FCP then?

Amanieu, Clar: +1

The 8472: Why is it on the top level? Seems unorganized.

Josh: Because it's on top level of std as well.

Clar: ship it, meaning, announce intent to ship, via FCP 

Nia: `core::prelude::v1::derive` is a mouthful...

Josh: ??

Nia: These are also the most commonly used macros.

The 8472: Yes, but usually you don't qualify them.

Josh: Usually people do when they make their own versions and need to call them while avoiding name clashes.

Nia: https://github.com/rust-lang/rust/issues/154645

The 8472: I'd prefer the path to be something else, but it's not that important.

Nia: We could change it and deprecate over an edition.

Josh: It's not as if we have an absurd number of macros/attrs. The only reason not to put them at the top level is ??. For example, `std::derive::Display`, but you expect to find them alongside the trait.

Clar: Is it okay to mark the revert PR as blocked because we're going to FCP?

Nia: FCP will be on the tracking issue.

Clar: Should we block the PR rather than closing it?

Josh, Nia: Yes.

Josh: If FCP finishes with merge, we'll close it, otherwise we'll FCP the revert.

Nia: I cannot start FCPs. Will someone do it?

The 8472: Will we have to add stable attributes as a separate PR?

Nia: We could do a spiritual stabilization PR.

The 8472: Then we can as well FCP on that one.

Nia: I don't mind but we can already start an FCP and why would be do it twice.

The 8472: Fair.

Clar: ??

Nia: The alternative would be doing something like the cargo-semver-checks thing above and checking every PR.

Amanieu: ??

Clar: For `core::derive`, the reexport is unstable... It's upsetting that it happened again.

The 8472: The compiler should be able to lint on ??. If we make an unstable alias to a stable thing, the compiler should lint against that.

Nia: If anyone wants to FCP this...

Amanieu: I'll do it.

### (nominated) rust.tf/159877 *Revert "Export \`derive\` at \`core::derive\` and \`std::derive\`"*

Clar: Left comment blocking on FCP.

### (nominated) rust.tf/159963 *core: implement FusedIterator for StepBy*

Amanieu: Let's FCP this.

The 8472: We can but there's an [open issue](https://github.com/rust-lang/rust/issues/159965) about questionable behavior that should probably be fixed beforehand.

Clar: This wouldn't be affected because it's only fused if the iterator it's wrapping is Fused...

The 8472: OK, nevermind.

Nia: Then we FCP this as well?

Amanieu: I'll do it.

### (nominated) rust.tf/160025 *Fix an edge case with \`StepBy::nth\` on non\-fused iterators*

The 8472: Wasn't there already a fix PR somewhere? That is the PR. Why was it nominated?

Clar: The author nominated it. Here's the [comment](https://github.com/rust-lang/rust/pull/160025#issuecomment-5097020679).

Nia: This is another what do we do with Fused iterators when in a wrapper... IIRC, the policy is that non-fused iterators are second-class citizens so ?? is weird but fine...

Nia: The bigger point to raise is that we don't promise this as a stability thing, so it's not a libs-api matter. It's an implementation detail that you can't rely on.

The 8472: Sounds like a good argument.

Clar: I think it's fine to leave this to whoever's reviewing this...

Nia: Would people be okay saying this? I don't think it's a libs-api thing.

The 8472: Sure.

Nia: I'll unnominate and reply.

### (waiting on team) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*

Josh: Haven't written anything up yet.

### (waiting on team) rust.tf/158271 *Add rem\_floor and rem\_ceil*

(pre-meeting) Clar: unmarked since this was waiting on author's ACP

Clar: Technically I promised an ACP a year ago and never followed through.

Nia: I will slap `needs-acp` on it.

### (old change proposal) rust.tf/libs641 *BorrowedFd view types*

The 8472: We discussed this, and there was some positive reception, but didn't really resolve in an approval. Some more discussion happened, and ?? proposed implementing AsRef on the types that can be turned into view, and that might work. Would people be on board?

Josh: So you want to treat a BorrowedFd as a BorrowedFile?

The 8472: Yes.

Josh: ?? getting a reference to the type rather than a wrapper than acts like one.

The 8472: ?? I think it's reasonable because each impl can justify why this is ok, so we don't need an unsafe trait.

Josh: Agree that we don't need an unsafe trait. Whether we want this to work on AsRef on File...

The 8472: It's in the other direction...

Josh: IF you have a method that takes AsrefFile, then you'd be able to pass a BorrowedFd to it and not do any conversion. Should we allow that? I think it's probably reasonable, just want to make sure we're okay with implicitness.

Clar: I hopped through the source code (messy) and verified that the impl of File is just a file descriptor. ?? You can do AsRef, and it will work. Wanted to see if File contains any extra data, but it's just the FD. It's not necessarily clear.

Josh: One possible reason for a dedicated method. Suppose I have BorrowedFd and want to call metadata on it. With the original proposal, I'd call `to_view` with a turbofish and then call `.metadata()`. ??

Clar: you can't turbofish `as_ref` 

The 8472: We could add this method as a helper...

Josh: You can add a method on BorrowedFd/OwnedFd such that ??, which is similar to into ??.

The 8472: An unsafe trait would let us document what it takes to implement this properly.

Clar: That doesn't feel particularly worth it because you'd have to ...

The 8472: No, if it's a trait, you can just implement the trait ?? (look at the solution sketch).

Josh: The actual operation would be done for you, and you'd just need to opt into that via an unsafe impl. AS long as we have an inherent method that could allow turbofishing... Shorthand `as_file` for `as_view::<File>()`...

The 8472: Personally, I use it to go to socket types, not files. Might be done as a follow-up. Can foresee people asking for `as_socket` and more.

Josh: We could provide similar methods on any type that's appropriately AsRef (File, Socket), or should we ??

Josh: Presumably, those will also exist on Handle?

The 8472: I think so, if Chris is ok with that.

Chris: Makes sense.

Nia: Should we go forward with the ACP?

Josh: Respond with something like "OK with AsRef, but also want a turbofishable method"?

The 8472: Can someone write it up so I'm not approving my own thing?

Josh: I can.

Clar: That's not you approving your own thing, we are.

### rust.tf/157428 Allocator status

Nia: Refactor PR: https://github.com/rust-lang/rust/pull/157428#issuecomment-5090752260. We have several use cases to accommodate. The plan for ini stabilization hasn't changed -- only one trait and a couple methods. The latest trait layout is `Allocator` (not unwinding out of methods and the drop impl promise), `StaticAllocator` (nothing will deallocate from an allocation except an explicit call), `AllocatorClone` (there's a use case for non-cloneable allocs and the supertrait would put a `Sized` requirement). `AllocatorClone` and `StaticAllocator` need signoff from opsem.

An arbitrary `Allocator` cannot be pinned (e.g. bump allocators). Requiring `'static` isn't enough because you can do (bad things). Need to block any clones of pinned non-static allocators.

Clar: I notice `AllocatorEq` is gone.

Nia: Amanieu wanted that, but I removed it for now. We can do that separately as an ACP. I wanted the bare minimum for now.

The 8472: BTreeMap could also benefit from it.

Clar: I'm a fan of leaving `LinkedList` alone for now because of the edge cases and how rarely it's used. (Something about cache locality for linked lists, finally.)

Nia: Initially I only want to propose `Allocator` and `Vec::new_in`/`Box::new_in`, and the second, allocator, field on `Vec`. Nothing about `Rc`/`Arc`. I appreciate a vibeck on whether this API looks good.

The 8472: Do we have a tracking issue?

Nia: Yes, a horribly out of date one. It covers the feature generally.

The 8472: Maybe there should be one for the stabilization specifically?

Nia: That's in the doc in the PR.

The 8472: We probably need one anyway because we need a feature flag for it?

Nia: I wanted to just name it `min_allocator` or something. Happy to do that.

David: The one ?? that's worth ruling out is the hashbrown arrangement.

Nia: Would this be blocking for `Allocator` stabilization?

David: It would be for the ?? parameter on collections.

Clar: Confused because hashbrown does have custom allocator support.

David: ??

Nia: I'd lean towards keeping the current impl and ??.

David: I agree with the direction, just bringing up the alternatives to rule out.

The 8472: Moving Vec out of std would be difficult for specialization reasons.

David: Allocators on String?

Nia: Would need a breaking change unless types can do something with default type parameter inference. We could do a breaking change over an edition and alias.

The 8472: We could avoid an edition change with something like `StringA`.

Nia: Worth looking at as an ACP. But we shouldn't change the default `String` right now.

David: Is that a new type for strings?

The 8472: Yes, one with a type parameter for the allocator.

Clar: the issue is that we can't have `String<A = Global>` work properly, whereas `Vec<T, A = Global>` works as expected 

Amanieu: I have a solution for ??, where `String` with `Allocator` is available on new edition. With edition redirects, we can have Allocator type that redirects to the ??.

(Discussion about `#[fundamental]`.)

Nia: https://play.rust-lang.org/?version=nightly&mode=debug&edition=2024&gist=dd9971fbbb4e05d62852852f9e040b25

Clar: http://github.com/rust-lang/rust/issues/157498

### (new change proposal) rust.tf/libs837 *Split out common behavior of \`FnStatic\` and \`FnPtr\` into a new trait: \`FnAddr\`*

Amanieu: With `FnStatic`, methods don't take `self`, do you can't call them with a dot syntax.

Nia: Is that a typo because I can't imagine how you'd implement this?

Amanieu: ??

Josh: Not sure we want to do that.

Nia: How does Addr not take any parameters and still produce a usize? Compiler magic?

Amanieu: It's not a typo.

Clar: Assuming it's similar to ?? where you'd just call the type as a function directly.

Nia: Either way, I like the solution sketch.

(..)

Clar: Should #836 be superseded by #837?

Nia: Yes. 

Josh: The proposal is to add FnAddr, but not other traits?

Amanieu: Not stabilizing anything, this is just an ACP.

Josh: But it's on a stabilization track separate from the rest?

Amanieu: I expect all of them to be stabilized eventually.

Josh: Will they be sealed?

Nia: They're builtin.

Amanieu: Do they have to be sealed?

Nia: Unless there's a good use case for them to be implementable, they should be.

Josh: Eventually we should unseal `Fn`, `FnMut` and `FnOnce` for user types. Implementing `FnMut` on something that doesn't have `FnAddr`, should that be true for `FnStatic`?

Amanieu: No? It can't hold any state.

David: It could be something like a transmute intrinsic that does extra checking statically.

(..)

Clar: can always unseal, not seal after the fact 

Nia: I want to see a concrete proposal.

Josh: Should `FnAddr` actually be a supertrait of `FnStatic`, or just another trait we blanket impl for `FnStatic`?

Amanieu: Good point.

Josh: Makes sense for fn ptr to have a fn addr.

David: (SIMD functions.)

Amanieu: Those don't implement `Fn` traits anyway. `Fn` is a supertrait of `FnStatic`. `unsafe fn`s don't implement `Fn`.

Josh: ??

Amanieu: The more I think about it, the more I think `unsafe fn`s should implement `FnStatic`, but they don't implement `Fn`.

Nia: ??

Amanieu: I'll reply.

### (new change proposal) rust.tf/libs836 *\`ptr::fn\_addr\`*

Amanieu: This adds a way to go from a fn ptr to a usize.

Nia: Yes, I need this.

Clar: closed in favour of 837

### (new change proposal) rust.tf/libs840 *Trait for safe all\-zero byte initialization (v2)*

### (new change proposal) rust.tf/libs838 *Add \`rem\_euclid\_unsigned()\` for signed integer types*

### (new change proposal) rust.tf/libs835 *Rename \`VecDeque::truncate\` to greatly reduce ambiguity*

Josh:

Nia: Back then we said truncate and truncate_back make sense.

Amanieu: No strong feelings.

Josh: We should do something with this ACP, either truncate/truncate_front, retain_? as an alias, or truncate_to_range. Or close?

Nia: I'd lean towards closing it.

The 8472: Do we have evidence that people are confused by it?

Clar: There was a [comment](https://github.com/rust-lang/rust/pull/151379#issuecomment-4998446984). The answer was that maybe we'd reconsider.

Josh: When we did `retain_back`, I think we felt that it was a better name than `truncate_front` and we kicked the can down the road. Now that we're here, sounds like now the reaction is meh rather than making a new name for this. So maybe we should close?

(??)

Nia: Happy to reply that we're happy to talk about this more, but the general opinion of the team is that the name is fine. Otherwise, message us on zulip.

### (new change proposal) rust.tf/libs832 *Type\-smart \`intersect\` on ranges \-\- v2*

### (new change proposal) rust.tf/libs828 *Separate \`Components\` iterator implementation into non\-prefixed & prefixed versions*

### (new change proposal) rust.tf/libs827 *ACP: add \`unsafe trait PureRef\<U\>\` to guarantee purity of the AsRef/Deref/Borrow traits*

Amanieu: Isn't there lang work on `PureDeref` right now?

Josh: `DerefPure`, yes. If you can point them to that, go ahead.

### (new change proposal) rust.tf/libs826 *Add \`core::mem::size\_of\_field!\`*

### (new change proposal) rust.tf/libs821 *ACP: \`impl From\<&Rc\<T\>\> for Rc\<T\>\`*


### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs124 *Integrate \`Error\` trait with panic interfaces*

### (stalled change proposal) rust.tf/libs331 *Support permanently skipping tests on a specific system*

### (stalled change proposal) rust.tf/libs523 *Add a deterministic constructor for \`RandomState\`*

### (stalled change proposal) rust.tf/libs363 *Un\-specialize impl ToString*

### (stalled change proposal) rust.tf/libs298 *Constructive/Destructive Interference Size Padding*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*

### (stalled change proposal) rust.tf/libs191 *Add LocalWaker support*

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
