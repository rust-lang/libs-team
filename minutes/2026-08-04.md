# Libs-API Meeting 2026-08-04

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Urgau, Nurzhan, Chris, David, Mark, Amanieu, The 8472, Josh

## Agenda

- Triage
- Anything else?

## Council representative selection

Josh: The September Council representative selection is coming up. We'll be selecting a new rep since I was serving a partial term (the remainder of Mara's term). We'l need a facilitator who isn't planning to stand for the position.

## Triage

### FCPs

1 rust-lang/libs-team T-libs-api FCPs

  - merge rust.tf/libs757 *Implement FusedIterator for core::iter::StepBy* - (3 checkboxes left)

16 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/134915 *Tracking Issue for ByteStr/ByteString* - (3 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (3 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (3 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (3 checkboxes left)
      - David: In the tracking issue, there's a link to the PR, and there is a list of the attributes that got changed.
      - Josh: A small part of me thinks mpsc in the stdlib was a mistake, but also, we should probably stabilize this.
      - Nia: I somewhat agree, but since we've commutted to offering channels, this should be useful.
      - Amanieu: Josh, are you objecting to channels in general?
      - Josh: No, it just seems like the API is kind of a disaster.
      - The 8472: Do we have to carry over anything we don't like about MPSC to this API?
      - Josh: My complaint wasn't specifically about the MPMC API...
      - Nia: Seems like MPMC is clearer than MPSC.
      - David: This module is envisioned as "lessons learned" over MPSC. (Not handling both sync and async ?? would be a mistake.)
      - Nia: You fundamentally need to implement sync vs. async channels differently, otherwise there will be perf consequences.
      - Josh: Flume channels have both sync/async methods.
      - Nia: That's because Flume busy loops instead of waiting?
      - Josh: That's an option but isn't required.
      - Nia: Benchmarks seem to be around the same as crossbeam's. Anyway, would this be a concern for you, ?
      - Josh: Haven't looked at this API closely in the past. Looking at it now, channel and sync channel return the same underlying types — what's the difference between them?
      - Nia: Worth flagging in the conversation. We can't resolve this now.
      - Josh: I can file a concern.
  - merge rust.tf/160136 *Add \`Default\` implementation for \`std::sync::Once\`* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[joshtriplett (8)](https://rfcbot.rs/fcp/joshtriplett), [BurntSushi (2)](https://rfcbot.rs/fcp/BurntSushi), [dtolnay (3)](https://rfcbot.rs/fcp/dtolnay), [the8472 (12)](https://rfcbot.rs/fcp/the8472), [Amanieu (8)](https://rfcbot.rs/fcp/Amanieu), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm)

### (nominated via Zulip) rust.tf/160432 *core: generalize \`BorrowedCursor::ensure_init\`*

Nia: Jonas brought this up on Zulip just now. The question for us is, if we're fine with default write for a slice of MaybeUninits. It'd be faster than doing it with a closure.

Josh: Seems reasonable.

Nia: Yes, and small enough that I wouldn't mind skipping an ACP.

Josh: Should go under the same feature gate as ensure_init, which is separate from BorrowedBuf and ??.

Nia: Makes sense because they're using it internally for this feature.

Josh: If there aren't any concerns, I'd be happy with r+-ing this.

Nia: Same. I'll reply.

### (nominated) rust.tf/libs131 *Add \`std::fs::rename\_noreplace\`*

Nia: Nominated after a couple of us were involved in a discussion. The question is...

Josh: The request is to have a rename when the target doesn't exist. Not all targets have a mechanism for this. This is not atomic, but rename also doesn't promise atomicity. ??

The 8472: Requires filesystem support, but not all FS support ?? links.

Nia: Hard-linking is semi-common to have unsupported, but not supporting renames is rare.

Josh: With rename_noreplace, you'd need to be ready for the possibility of failure.

Nia: Happy to go ahead with this. We always admit unsupported. The only thing is we should try not to fall back to weaker behavior because of insufficient atomicity (TOCTOU might be a security vulnerability). The more restrictive option should be the default, and if it's unsupported, you should be okay with TOCTOU.

Josh: The fallback would involve a 2-step process without races, becasue the 1st step is race-free, so the fallback shouldn't introduce TOCTOU.

The 8472: It's possible that the hard link succeeds, but the 2nd step fails because of permissions, then you'll be left with a duplicate hard link. We should document it.

The 8472: We shouldn't try to clean up because in other places we have partial failures...

Nia: We can accept the ACP.

Josh: Yes, and this is just an ACP.

The 8472: I thought we'd already accepted the ACP.

Josh: We've stalled on even accepting the ACP because we wanted enough confidence in target support.

The 8472: The caller can always decide what the fallback strategy is.

Nia: Accepted, will reply...

### (nominated) rust.tf/144419 *Tracking Issue for smart pointer (try\_)map*

Nia: Nominated because of a suggestion to stabilize. Marked as unimplemented, but there's a merged impl PR. Needs updating. Looked fine to me other than `try_map` being blocked on `Try`. `map` is probably fine to stabilize? I'm sure I've tried implementing this by hand.

The 8472: There's a crate for map. I used this before too. +1, but are there open questions? Yes, one by RustyYato: could it use `Cow<T>` instead of `&T`, if the refcount is 1? Since `map` takes ownership.

David: I think it makes sense to make them Clone??

Nia: Wouldn't be a fan of making in inconsistent with ??. I'm in favor of this.

David: The other thing is whether this does the same as vec collect where it checks that input and output are the same size, reusing the allocation. It does that optimization.

The 8472: ??

Amanieu: Wouldn't you use `Box::as_ref`?

The 8472: We can't get a `Box` out of the `Arc`.

David: Looks good. Let's ask for a stabilization PR for just `map` and then propose FCP on the PR, and keep the tracking issue for the rest of the API.

Nia: Will reply.

David: Do we do multiple FCPs on the same issue?

Nia: I don't think it's possible.

The 8472: previous borrowed or owned discussion: 
https://rust-lang.zulipchat.com/#narrow/channel/122651-general/topic/Why.20is.20there.20no.20BorrowedOrOwned.20type.20in.20core/with/504397139

### (nominated) rust.tf/146381 *Generalize \`impl\<T\> Clone for Box\<T\>\` to unsized types*

Nia: Mark nominated this. What's the concrete worry here?

Mark: This is proposing expanding cloning Boxes to all unsized Boxes by altering the impl to be more general. Lots of discussion on impl detials, but I don't know if we've had an ACP or anything. Wanted to get a confirmation from libs-api that this is desirable.

David: This wouldn't give you ??. This is the same trait bound that we already use for ??.

Mark: I think the trait is unstable. This is just generalizing it to prepare for a future where more people could implement it. In particular, some of the underlying impls are not accessible on stable. This is an insta-stabilization of those.

Nia: That would require an FCP.

Mark: This already does. I'm curious about the general direction.

David: When you said some impls aren't accessible, is it because it's not possible to create ??.

Mark: Might be because of ByteStr, which is unstable.

Nia: Clone to uninit is implemented for `[T]` when `T` is `Clone`...

David: Are you sure because ??.

Nia: (Checking.)

The 8472: On Mark's concern about accidentally stabilizing, maybe we could leave an internal comment of the trait itself?

Mark: We can do that.

Nia: Never mind, I don't think this is breaking.

David: ?? so I'm happy with reusing this bound.

The 8472: Can this be expanded to other things in the sized hierarchy like runtime sized values?

Mark: I don't think this causes any issues for that, and it's unstable, so we have flexibility. I'll leave a comment. If there's new stable surface, we'll FCP it.

David: We could consider if we're ready to make the trait stable.

Nia: Tracking issue: https://github.com/rust-lang/rust/issues/126799

(..)

Nia: Looking at the last comment on the tracking issue above, thinking if we can get around the ?? trait bound with some supertrait shadowing stuff. Seems like, for the most part, the blockers are around ergonomics and not fundamental.

The 8472: `Sized` is a specialization trait.

Nia: That makes our lives easier. You can just have a generic impl for that, and ??

The 8472: I think you have a base impl of `Unsized`, and then a spec impl on `Sized`.

Amanieu: This sounds similar to the `Any` situation. `Any` is only implemented for `static`, and we want to make it available on non-`static`.

(..)

David: Sounds like there's not an immediate easy stabilization we can do here.

(..)

Amanieu: Happy to just stabilize the impl for `Box`. The trait can be held back for now?

The 8472: Has anyone asked for a use case with a custom DST?

Nia: (Something about `dyn-clone`.) This is probably on the path to stabilization, just needs more work on ergonomics.

### (nominated) rust.tf/146882 *fully deprecate the legacy integral modules*

Nia: FCP finished. Should be merged?

Josh: I `r+`'d this. Everything's been addressed.

The 8472: 4 minutes ago Clar also reviewed this.

Nia: We're done here.

### (nominated) rust.tf/160436 *stabilize \`Box::take\`*

~~Josh: Returns a 3-tuple of the item, uninitialized box, and allocator. ...~~

Mark: It's a 2-tuple, if you look closely.

Josh: Did I misparse that? I see, nevermind.

Nia: The API looks good to me.

Josh: Looks perfectly fine.

Mark: Are we concerned about performance? I expect that this will force some extra copies. Maybe Amanieu's move stuff will help?

Amanieu: No, it only deals with locals, not boxes.

The 8472: What do you expect if you're pulling out a value out of a box?

Mark: Stuffing the value in a tuple...

The 8472: There's discussion about optimizing the return ABI of tuples to just return them to registers or something.

Amanieu: I think in this case, the fn is likely to be inlined, so it's not a big deal. The temporary copy should disappear, and MIR elimination should eliminate local-to-local copies. Even if it's not inline, this is fine because $REASONS$.

David: Is there a reason we might ant Mark's API mentioned anyway (`Box<T> -> Box<MaybeUninit<T>>` without moving the T out?)

Amanieu: That is Box::map.

Nia: Any other concerns? If not, let's start an FCP. Amanieu, can you start the FCP?

Amanieu: Yes.

### (nominated) rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`*

Nia: non_zero name consistency. We've had an agreement that nonzero is one word? Or was that nonnull? Yes, nonnull and non_zero. Or is it the other way around?

The 8472: non_null and nonzero, and nonoverlapping, non_exhaustive...

Nia: If nonzero has been consistently one word, let's keep it that way. I think we can resolve the concern. I'll ping BurntSushi and talk about this. Unless people have other things, I'm happy to continue async to get this unblocked.

The 8472: Sounds good.

### (nominated) rust.tf/154065 *Stabilize \`fN::BITS\`*

Nia: What are we waiting on here? There was a crater run.

The 8472: FCP? It's already complete (on the tracking issue).

David: The request is for us to look at the crater report and sign off on whether(??) there's a minimal amount of breakage. Some crates had their own assoc type in extension traits that we're breaking.

Nia: Looks like breakage is minimal, and major crates have fixed this and released a version. I'll +1 this and write a comment.

### (nominated) rust.tf/158580 *std: map ENOTSUP to ErrorKind::Unsupported*

Nia: There's been conversation.

Josh: This looks reasonable. It's a minor change about handling an additional errno option. It's a case of divergence between different Unixes. ?? different syscalls that mean the same thing. This is "this operation is not supported on this object", and I think it's correct to mark this as unsupported.

Nia: Mostly agree.

The 8472: I looked at linkat on FreeBSD and it doesn't say "on this object"...

Josh: This is one of the ways errno codes are fuzzy in how they're used. This is more or less, "this will not work". The only potential concern would be, is this an operation we'd want to map to a distinct error code? The only reason is if you'd like to handle it differently.

The 8472: Yes, it doesn't have different semantics, but if it's overloaded at least some times there's not much we can do anyway.

Josh: You might want to distinguish between the syscall that doesn't exist vs. it exists but doesn't apply to the object. But it'd be a programmer error or something that will never work.

Nia: I think this is correct. What happened to ENOTSUP before?

The 8472: Uncategorized.

Nia: I think this is strictly better than that.

Josh: And the other thing is ?? redundant. We should just approve this. Procedurally, this is an API surface area change. Should we FCP this?

Nia: This would be changing observable stable behavior. I don't know if we've made any promises about this.

Josh: I think we only promised that things can move from uncategorized to other things, but never change more.

Nia: Then no FCP.

Josh: We said we can do it, but we're committing to a one-way door, so let's do an FCP.

(FCP started.)

### (nominated) rust.tf/159856 *\`core::derive\` was unintentionally stabilized*

Nia: Last time we started an FCP to retro-stabilize this. Any comments?

Josh: This is not the last time we'll screw up in this way. WOuld be nice to detect this in the future.

The 8472: semver-checks?

Josh: Yes, but we could also ask rustc to flag that a stability attribute has been applied to re-export and will be ineffective.

Nia: I talked to Predrag, and the blessed file approach might not be ideal because its output may not be consistent between runs... I'll talk to him.

The 8472: How noisy?

Nia: Includes benchmarks, for example.

David: Can you opt out?

Nia: Need to find out.

The 8472: Are custom formats on the table?

Nia: If someone steps up to implement it.

The 8472: I like what we do in tracking issues where code lists the modules and signatures; useful for reviews.

### (nominated) rust.tf/159963 *core: implement FusedIterator for StepBy*

Nia: Is this a breaking change?

The 8472: It's a new API promise.

Nia: Needs FCP. But I think it's fine. The FCP is ?? from a while ago, has 3 missing checkboxes.

https://github.com/rust-lang/libs-team/issues/757#issuecomment-5106855839

Nia: I direct people to take a look and check the box.

Chris: (Issue about nth)

The 8472: That only applies when it's not fused. No overlap.

### (nominated) rust.tf/160108 *Stabilize \`windows\_process\_extensions\_main\_thread\_handle\`*

Nia: That's a hell of a name.

Chris: When you start a process, the OS gives you a handle to the main thread, and we want to pass that to the user.

The 8472: Should we have a process handle, can you do useful things with it?

Chris: Yes, the API is already based on the process handle.

The 8472: Are there other ways to obtain a process handle? A process can kill its children, but ?? and you can kill other processes too (not children).

Chris: I think that'd be new API. It makes sense on Windows.

Josh: We should in theory have a windows-spec API for converting handles to processed children, would that break things?

Chris: I don't think it breaks anything. I think people wanted to keep children children.

Josh: Main thread handle doesn't return the same as as_handle on a Child?

Chris: No. Process != main handle on Windows.

Josh: Child::as_handle => process handle, and this => main thread handle?

?: Yeah.

Amanieu: On windows, thread is a different kind of object from a process.

Chris: FCP?

Josh: Ship it.

Nia: Fine with this.

The 8472: Could you get the main thread from the process handle?

Chris: Not easily. You're not supposed to mess with another process in this way.

The 8472: Sounds fine.

Nia: Will let someone start an FCP.

### (nominated) rust.tf/160205 *core: make the \`Rng\` a by\-value parameter of \`Distribution::sample\`*

Josh: The proposal here, AFAIK, is to make `&mut T` impl `Rng` if `T` is `Rng`. This seems reasonable, but someone had a concern that you could end up with `&mut &mut &mut Rng`. I don't know if that's a problem, but it's worth evaluating.

Amanieu: Conceptually, it's similar to ??. This makes perfect sense.

Nia: Similar to Allocator.

Amanieu: https://doc.rust-lang.org/nightly/std/io/trait.Read.html#impl-Read-for-%26mut+R

Josh: Thoughts on TVU's feedback?

David: (References, Copy?)

Josh: I'd expect the compiler to be smart enough to not create extra indirection.

David: That's true if it's getting inlined, otherwise ??.

Amanieu: If fn is not inlined, the address you pass to it is observable so can't be optimized away?

Nia: Maybe not?

(..)

The 8472: https://github.com/rust-lang/rust/pull/160205/changes#diff-423005859f2e75bee2b161c8e03d8d927660de2a72231aa78aaf7b673bf6b677R92

The 8472: If it's just about ergonomics, I don't see how it's an improvement.

David: Agreed.

Josh: The only case where this will make your life simpler is a ZST representing a global RNG. ?? And so this is unnecessary. I can write a reply.

David: One more note is, the rationale hinges on this being a trait fn implemented in user code. This wouldn't apply to https://doc.rust-lang.org/std/io/fn.copy.html.

Nia: Agree.

### (nominated) rust.tf/160240 *Fix \`Path::with\_extension\` dropping the file name for \`..X\`*

Chris: This is about two things. 1) Bug with set_extension; 2) What should the behavior be when path starts with multiple dots.

Chris: Look at the 3rd table, the difference between old/new behaviors. The bugfix is just a bugfix, but the new behavior is relevant to libs-api.

Nia: The old behavior seems broken.

Chris: Yeah, we never considered the edge case.

The 8472: I wonder if other stdlibs handle this in some way. Who writes `...`?

Nia: It's probably legal.

The 8472: It is.

Nia: The new behavior is, if there's a number of dots followed by other characters, do not assume the dots are extension markers. Right?

The 8472: Looks reasonable as far as I can tell.

Josh: The claim here is that a series of dots will act as the delimiter of an extension?

Nia: Only a singular dot is treated as an extension delimiter.

The 8472: Does it change anything if the dots are in the middle of the name?

Chris: Only if it's leading.

Josh: So `...c` will be changed to `...ext`?

The 8472: I don't think there's a sane way to handle this. Makes sense to  ?? test cases?

Nia: We can bring up tests to the reviewer (Clar). The proposed behavior seems fine.

Chris: Is there a need for FCP?

Nia: I don't think so.

The 8472: We could discuss more whether it should reuse all the leading dots or add one more. This one proposes to add more.

Nia: I'll reply saying to add more tests, and otherwise looks good.

### (hand-nominated) https://github.com/rust-lang/std-dev-guide/pull/66 Add a policy for inclusion in the prelude - #66

Josh: This came up years ago. We'd been investigating adding things to the prelude 2024. This involved type system exploration on what can cause breakage and what can't. This is an attempt to document it. For these cases, it's rare enough that we could do a crater run.

Josh: It also mentions when something should/shouldn't be added to the prelude, like it has to be a name that makes sense on its own, unqualified.

The 8472: Is ?? import as _ possible in the prelude?

Nia: Method names can still conflict.

Josh: We could do that if we wanted the methods but not the trait, but ??

The 8472: So that resolution doesn't have lower priority.

Josh: Has lower priority than inherent methods, but not lower than ??.

The 8472: I was asking basically if import as underscore makes any difference.

Josh: No, it doesn't.

Nia: const/static wouldn't be breaking but macros/traits/types are?

Josh: Types are generally not-breaking but require a crater run due to a corner case with enums. Macros are, generally, breaking anytime someone declares a macro of the same name. Traits are always breaking, so we do the min editions.

(Josh explains the enum corner case, related to derive macros and name resolution...)

The 8472: What Nia asked about const/static would be good to add to the policy explicitly (the value namespace).

Nia: The policy, as written right now, has a catchall clause that adding items other than traits/macros/types will never produce conflicts.

The 8472: Might be useful to spell it out.

Josh: If you want to send a suggestion to the PR, I'd be happy to merge it.

The 8472: Apparently, constructors are also under that.

Nia: Modulo wording, are we in favor of adding this to the std-dev-guide? I think it's a good addition and helpful.

David: On board with adding the policy. To the extend that a future stabilization has a conflict between judgment of the team vs. the policy, it's more likely that the policy is wrong. (Josh: +1.)

Nia: https://en.wikipedia.org/wiki/Wikipedia:How_to_break_the_rules
 
Nia: We can add an explicit clause on that, that this is advisory and team can override this.

Josh: I can add a single sentence that this documents what we do, not tells us what to do.

The 8472: I just commented.

Josh: Looks reasonable, adding.

### rust.tf/160514 Extend `NumBuffer` API to allow casting

NumBuffer is in beta for 1.98

Josh: Is this an addition or is this fixing an existing API?

David: This is an addition. Just a heads up in case people have second thoughts.

Josh: Nothing in this needs us to make a change in a  timely fashion; we should just look at this and see if we change our minds on stabilizing NumBuffer.

Amanieu: It's a bit strange and assigned to me for review. Will look at it.

Josh: Is there a way for this to get cleaner if we waited for support for constants in trait arguments and return values?

Amanieu: ??. This is strange. I will prioritize this and look at it. Let's nominated it for next week, and if it's resolved by then, so be it.

### (waiting on team) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*



### (new change proposal) rust.tf/libs842 *Setting a \`File\` non\-blocking*

### (new change proposal) rust.tf/libs841 *Per\-thread panic hooks*

### (new change proposal) rust.tf/libs840 *Trait for safe all\-zero byte initialization (v2)*

### (new change proposal) rust.tf/libs838 *Add \`rem\_euclid\_unsigned()\` for signed integer types*

### (new change proposal) rust.tf/libs837 *Split out common behavior of \`FnStatic\` and \`FnPtr\` into a new trait: \`FnAddr\`*

### (new change proposal) rust.tf/libs835 *Rename \`VecDeque::truncate\` to greatly reduce ambiguity*

### (new change proposal) rust.tf/libs832 *Type\-smart \`intersect\` on ranges \-\- v2*

### (new change proposal) rust.tf/libs828 *Separate \`Components\` iterator implementation into non\-prefixed & prefixed versions*

### (new change proposal) rust.tf/libs827 *ACP: add \`unsafe trait PureRef\<U\>\` to guarantee purity of the AsRef/Deref/Borrow traits*

### (new change proposal) rust.tf/libs826 *Add \`core::mem::size\_of\_field!\`*


### (stalled change proposal) rust.tf/libs462 *impl fmt::Write for BufWriter*

### (stalled change proposal) rust.tf/libs314 *Add security\_attributes() to windows::OpenOptionsExt*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs476 *Parse IPv6 zone identifiers*

### (stalled change proposal) rust.tf/libs544 *\`Arc\<impl !Sized\>\` constructors*

### (stalled change proposal) rust.tf/libs365 *ACP: \`TwoSidedRange\` trait*

### (stalled change proposal) rust.tf/libs363 *Un\-specialize impl ToString*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*

### (stalled change proposal) rust.tf/libs124 *Integrate \`Error\` trait with panic interfaces*

### (stalled change proposal) rust.tf/libs549 *Add \`uNN::checked\_sub\_nonzero\`*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
