# Libs-API Meeting 2026-05-05

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: The 8472, Amanieu, Tyler B and bstrie, Josh Triplett, Chris Denton, Tomáš Šedovič

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

18 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/44930 *Tracking issue for RFC 2137: Support defining C\-compatible variadic functions in Rust (c\_variadic)* - (4 checkboxes left)
  - merge rust.tf/155750 *Document that \`ManuallyDrop\`'s \`Box\` interaction has been fixed* - (6 checkboxes left)
  - merge rust.tf/127544 *Tracking issue for CommandExt::show\_window* - (3 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (3 checkboxes left)
  - merge rust.tf/138215 *Tracking Issue for integer formatting into a fixed\-size buffer* - (2 checkboxes left)
  - merge rust.tf/153990 *Decide and document where stdarch intrinsics are allowed to diverge from asm behavior* - (4 checkboxes left)
  - merge rust.tf/126769 *Tracking Issue for \`substr\_range\` and related methods* - (3 checkboxes left)

[dtolnay (3)](https://rfcbot.rs/fcp/dtolnay), [nikomatsakis (2)](https://rfcbot.rs/fcp/nikomatsakis), [scottmcm (2)](https://rfcbot.rs/fcp/scottmcm), [BurntSushi (11)](https://rfcbot.rs/fcp/BurntSushi), [the8472 (12)](https://rfcbot.rs/fcp/the8472), [Amanieu (5)](https://rfcbot.rs/fcp/Amanieu), [joshtriplett (7)](https://rfcbot.rs/fcp/joshtriplett)

### (nominated) rust.tf/44930 `P-lang-drag-1` *Tracking issue for RFC 2137: Support defining C\-compatible variadic functions in Rust (c\_variadic)*

Josh: I jsut started fcp merge for being able to stabilize this. We're finally at the point where C-compatible variadics ar epotentially ready to stabilise. folkert has been putting a lot of effort into this. The language side of this is ready and there's an FCP for that in the tracking issue. I started an FCP for the parts that are in the library (`VaArgSafe` and `VaList`). I think it's ready.

Amanieu: That sounds fine to me.

The 8472: What about the unresolved question on the top comment?

Josh: The unresolved questions on the FCP are up-to-date. The top-level comment's ones are not up-to-date. The 128-bit types won't be part of this initially. The other item is whether we could let function pointers (as opposed to data pointers) to pass into something that's `VaArgSafe` so we can print them with e.g. printing `%p` in C. That's again being deferred to later. And we're also deferring certain targets that didn't have sufficient validation.

The 8472: The issue that's still in there is that passing a `VaList` to a function with a different ABI, would we need different types for different ABIs?

Amanieu: I think it's only for the C ABI. varargs are only supported on `extern(C)`

Josh: Right now, yes. If we wanted to support multiple arguments on different calling conventions, we'd possibly need a new type have to add that as a new feature. But I don't thikn we'd want to do it as a `VaList` generic over ABI

Amanieu: Regardless calling convention you use, C only has a single VaList type.

Amanieu: Do we stabilise `VaArgsSafe`?

Josh: It's sealed. And yes, that's the proposal. We're stabilising the existence of it so you can name it.

Amanieu: Sure. The fact it requires `Copy` -- is it intentional?

Josh: Anything that can be passed through the registers or stack needs to be `Copy`.

Amanieu: Checking the box.

### (nominated) rust.tf/152480 *\`format\_args!\`: only de\-duplicate captured identifiers that refer to places*

This needs being to be un-nominated.

### (nominated) rust.tf/154374 *Rename \`MetaSized\` trait to \`SizeOfVal\`*

Josh: We reported a proposal for waht we that it should be to the Zulip wheer this was discussed. How did that go?

The 8472: I started another hackmd with the extended hierarchy and a bunch of names but haven't got much feedback from people.

Josh: Why don't we leave this nominated, report back on the thread that this is going to need discussion and observe it might want to be discussed at RustWeek or similar. So we can grab the requisite library, language etc. folks in the room and come up with names.

The 8472: If nothing happens before RustWeek that sounds like a plan.

Josh: Yes. If we can solve it before then, great. But if we're going to do something before RustWeek, we ought to send an issue that will be scheduled this week. That's when the schedule will be finalised.

The 8472: How much time do we have?

Josh: Until Mara starts putting the schedule together. We need to file it today or tomorrow probably.

Amanieu: Is someone going to write up the bikeshed session for the AllHands?

The 8472: I'll open an issue

### (nominated) rust.tf/154665 *add safety sections for three functions in core::mem*

Amanieu: This was discussed last week.

The 8472: I posted a comment.

Josh: Is this just a stale nomination?

The 8472: I think we didn't reach a decision. But was a decision asked from us or just input? ... It's about a new guarantee. If we don't get a reply to my comment then maybe it wasn't definitive enough.

Amanieu: I think this is rarely going to come up in practice.

The 8472: But it's still an API change and we have to say something. We have the three options I listed there: https://github.com/rust-lang/rust/pull/154665#issuecomment-4336937080

> - keep it UB
> - guarantee the panic (needs an FCP)
> - make it a post-mono error. This would be a breaking change since it would leak out of dead branches - today's UB does not give us permission to do that because UB is a runtime property that requires the branch to be reached.

Amanieu: I'm happy to just guarantee the panic. It's strictly better than UB. There's no reason to make this UB when we can make it a panic. It's determined at compile time.

The 8472: The only reason why you would not want the panic at runtime, but a compile time error is if you wanted to have this error fired earlier.

Amanieu: I feel that should just be a lint not a post-mono error.

The 8472: But that's not a library concern.

Josh: I appreciate the point that if you use this as a post mono error, we won't have the option to use a conditional that doesn't actually hit the panic case at runtime.

The 8472: Is everyone in favour of panicking?

Amanieu: Sure. We can start the FCP.

The 8472: I'll start one.


### (nominated) rust.tf/155527 *Replace printables table with \`unicode\_data.rs\` tables*

Amanieu: This is a behaviour change for the Debug impls?

Josh: It is a behaviour change but only so that some of the characters we didn't escape we'd escape now.

Amanieu: Last week we said +1 for us but we wanted an option from a unicode expert. We now have +1 from Manish. Ship it.

The 8472: And it's debug change so we can change it later.

Josh: The only mildly questionable thing is that the variation selectors now get escaped.

The 8472: We checked last week that zero-width characters get escaped. A lot of the fancy unicode stuff gets mangled.

Amanieu: Do we need an FCP for this or just accept it?

Josh: It's not affecting anything that's API so we should just merge it.

Amanieu: I'll reply.

Amanieu: Technically, `escape_debug` is public API

The 8472: But it's like the `Debug` trait and that one is unstable.

Amanieu: I'll just do an FCP.

### (nominated) rust.tf/155924 *Make \`Infallible = !\`*

The 8472: We wanted more details on how big the impact is. We're in favour in principle but we wanted more details on the crater result. If that's within our usual acceptable limits then yes. Do we need to FCP this?

Amanieu: It needs to be FCPd by someone but I think we can either defer to Lang or ask for joint libs+lang FCP.

The 8472: Waffle wanted an ahead-of-time approval. So we just run the Libs FCP on this issue

Amanieu: Ok

The 8472: Waffle is working on breakages 1 and 4. For 3 there would be an additional change to remove the reservation impl.

Amanieu: Which crate is failing?

Josh: That's 14 crates including reverse dependencies.

The 8472: The breakage case 3 Waffle says we could "either introduce `impl<T> From<!>` for T with hacks in type check (making fixing the code as easy as removing the offending impl) or we can remove the reservation impl (if we can't introduce the impl, there is no reason to reserve it)"

The 8472: I'm not sure which one Waffle's referring to. But it seems some change is needed. I'll ask what the actual API change will look like and we can FCP that?

Amanieu: I'm concerned about the reservation impl. Are we giving up on not having `Infallible` for `<T>`?

Josh: I don't think we're giving up on it. Waffle says we could either introduce it with hack in type check or remove it if we can't introduce it.

Josh: We'd get the same set of errors if we had the reservation impl for `Infallible` from `T`

Amanieu: I'm inclined to add the right impl.

Josh: Part of the question is what's the hack. This is the crater run for reservation impls: https://github.com/rust-lang/rust/pull/155500#issuecomment-4326819086

Josh: One of them is on github, then three version of the same volo-http crate and then one crate yubirs-tools

Josh: This doesn't look like the crater run we need to stop and wait for.

Amanieu: I'm getting the imprenssion we probably should have had the reservation impl for `Infallible` as well

Josh: Sure, but we didn't know.

Josh: I think the right answer is to reply to Waffle's last comment and say that sounds potentially reasonable and the blast radius seems reasonable.

Amanieu: It's not clear to me why specialization is needed for the blanket impl?

The 8472: Because it conflicts with some existing `From` impl?

Amanieu: There's a blanket impl `From<T> for T`, that's why we need specialisation.

The 8472: `!` is a helper type, ??

Amanieu: do we keep the specialisation impl? I suggest we keep it.

The 8472: And the 14 crates would have to live with it?

Amanieu: Yes. I think overall people will be happy enough this lands and not care about the breakage. It's something people wanted for a while.

The 8472: We can send heads-up to the volo-http crate than's still active.

Josh: We should poke them and get it to fix it but we shouldn't block on it.

Amanieu: Would it help to add reservation impl for `Infallible`, fix for that and then make it `!`

The 8472: Isn't it better if they could implement this on the never type?

Josh: That was my thinking as well: give them the option to implement this with `Infallible` and have them use that and see it's better now

Amanieu: FCP?

The 8472: Yes.


### (nominated) rust.tf/155989 *Update \`transmute\_copy\` to ub\_checks and \`?Sized\`*

Amanieu: This is an alternative to the [one we just started an FCP on](https://github.com/rust-lang/rust/pull/154665). It changes the assert to `ub_check` and it's also an API   change because it allows transmuting from an unsized type. The reason for changing the assert to ub check is when it's a DST it doesn't know that at compile time. It's no longer a cont check, it's a runtime check. And I don't think we want runtime checks in these functions in release mode.

The 8472: Yes

Amanieu: But pe

Josh: Are we fine with the first two changes (making this a non-unwinding panic)? That part seems reasonable to me.

Amanieu: I like the idea of transmute_copy accepting an unsized as well.

Josh: Me too.

Josh: Using ub checks as opposed to doing this unconditionally is the one we're debating

Amanieu: If we accept unsized types we have to make this a UB check

The 8742: Can we use this new specialization machinery to check if it's sized and do tihs only if it's not sized?

Amanieu: Maybe?

The 8472: `try_as_dyn`

Amanieu: Can you even make it unsized?

The 8472: I think so. But `try_as_dyn` is for checking concrete types, not for checking dyns.

Amanieu: I don't think it works. You can't create a dyn Sized. The `Sized` trait is not dyn-compatible. A dyn trait implements the trait and `dyn Sized` cannot implement `Sized`. I don't think this is such a big issue.

The 8472: You could transmute a slice into an array?

Amanieu: Yes.

The 8472: Yes, then you don't want those sized checks either. You're asserting you know it's big enough.

Amanieu: I think what Scott is saying is that the last assert is useful because you're doing something UB anyway. I think the UB check is sufficient.

The 8472: You can have dynamic values and getting unfortunate outputs at runtime.

Amanieu: It's unsafe. You need to know what you're doing. I'm inclined to accept Scott's suggestion and reject the earlier one.

Amanieu: I'll FCP Scott's PR and cancel the FCP on the other one. ANd Il'l write up the comments.


### (nominated) rust.tf/156034 *Document which open commands will follow symlinks*

Amanieu: `create_new` doesn't follow a symlink

Josh: There are dangling symlinks, it could create a file there.

Chris Denton: This should be documented in `OpenOptions`

Amanieu: The PR as written saying "This call does not follows symlinks." is not helpful

Josh: I think adding the "This call follows symlinks" is good. But we should clarify what's going on in the `create_new`. And say "If a file (or directory or symlink or similar) exists at the target location, creating a new file will fail" on line 693

The 8472: Windows has symlinks too, we should check the behaviour is consistent across platforms.

Chris: Have we confirmed the unix behaviour?

Amanieu: Yes, I can confirm it.

Chris: Windows doesn't have symlinks. Symlink is just an extra metadata to a file. create_new will fail.

The 8472: It doesn't do dangling symlinks?

Chris: create_new will fail if you try to open it. But the other methods do follow symlinks.

Josh: Test program:
```rust!
use std::io::Write;

fn main() {
    let mut f = std::fs::File::create_new("test").unwrap();
    f.write_all(b"contents\n").unwrap();
}
```

The 8472: On the POSIX page for `open` doesn't mention symlinks

Josh: The Linux `open(2)` man page says if you point it at a symlink path, it will fail regardless of where the symlink points.

The 8472: Ah, I searched for "symlinks" but they call it "symbolic links". So POSIX also says it fails even if the symlink is dangling. Sounds good.

Amanieu: What's our response? We can say this isn't libs api behavior. This is just documenting existing behaviour. It explains to people how filesystems work.

Josh: These methods already implied certain behaviour and the fact we're documenting them isn't an API change, just writing down what these methods were created for.

The 8472: Sure.

Amanieu: I'd un-nominate libs-api and say this isn't a libs-api concern. Do we want these comments on `OpenOptions` as well?

The 8472: Not for all the methods. Just on `create_new` since that changes behavior.

Amanieu: Yes.

The 8472: If you set "write" or "truncate" or anything that changes the symlink behavior. Or we can put a summary on the `open` method itself. ... `OpenOptions::create_new` already mentions symlinks.

Amanieu: Do we say "only document this on `create_new`"?

Chris: We could add a discussion on the module to talk about symlinks.

Amanieu: That would be better.


### (nominated) rust.tf/156068 *Tracking issue for release notes of #156063: Map \`WSAESHUTDOWN\` to \`io::ErrorKind::BrokenPipe\`*

Josh: When you wrote to a socket on Windows on the write side that would shut down, you'd get a an `Other` error. Now it produces `BrokenPipe` and I wrote the rel note. I don't think there's any action unless people have concerns about making this change.

The 8472: Is this error code used just for sockets or something else too?

Josh: It's only used for Windows sockets I expect

The 8472: So we don't have the Unix problem where the error codes are overloaded a lot. Then sure.

Chris: Yes, it's documented having a specific meaning.

The 8472: Maybe we should update the documentation on the variant. It said it's because the pipe was closed but that's not what's happening here.

Chris: "pipe was closed or the socket was shutdown"

The 8472: I guess netcat relies on this. It is a bit weird but in the end the variants we have in the standard library aren't platform processes. You can always get the OS error code out. The question is: are we helping anyone with this?

Josh: It's making Windows match the Unix behaviour.

The 8472: If nobody has an issue with making something taht's not really platform native on Windows, then yes.

Chris: A lot of platforms support BSD sockets.

The 8472: Nothing for us to do here? This was just to have this discussion?

Amanieu: Agreed.

### (nominated) rust.tf/156142 *\[Windows\] computing an \`Instant\` from before program startup might panic*

Amanieu: On miri, Instant start counting from zero. If you try to do `now - 1 second` you get underflow. This only happens on Windows where it's an unsigned integer.

The 8472: On macos, you could also underflow. Since posix doesn't guarantee the epoch for boot time, we also can't guarantee it. We could use signed types and rely on platforms having some sane unspecified epochs to have enough room for negative and positive offsets.

Chris: We also have platforms that use Durations and would have to make them arbitrary large

The 8472: Or cut them in half

Amanieu: I think for all platforms to say use 64::MAX / 4

Josh: I don't think we should switch to signed where it's unsigned so picking a good range is a good way to go

The 8472: Posix is unspecified

Josh: If the point of miri is to surface bugs, but we don't treat this a bug if it underflows when created on epoch then we may not need to

Amanieu: I'm more concerneed about the reported bugs on hugging face because a program started on boot and panic

Josh: That sounds like a bug because it should be okay to compute now - hour

The 8472: But if you have scheduling you can have an initial value in the past

Josh: I'm saying either call saturating_sub or live with panics

Amanieu: I'm inclined to do the user friendly thing. It's the kind of bug developers are unlikely to detect but users are likely to hit.

Josh: In our code we're using a signed value on linux and using systems so we're converting to i64?

Amanieu: The value of the syscall is signed. Unix time overflows in 2038 because you reach i32::MAX not u32::MAX. time_spec uses time_t too

Amanieu: I'm happy to accept the PR from Ralf. Just when calculating the Instant add `u64::MAX/4`

The 8472: Sure.

Amanieu: I'll reply to this.


### (waiting on team) rust.tf/154905 *Derive \`Eq\`, \`Ord\`, \`Hash\` for \`MaybeDangling\`*

Amanieu: We have these for `ManuallyDrop`. I'm not happy to have `MaybeDangling` as a public type at all.

The 8472: Is there an actual demand for this or just completionism?

Josh: I'd guess someone putting ManuallyDrop in a struct and waned to derive.

Amanieu: I'm inclined to say sure. Based on teh fact they implemnt `Deref`. This is fine. There's no API change to `ManuallyDrop` only to `MaybeDangling`

Josh: Sure.

Josh: I'm trying to figure out Waffle's comment

The 8472: `ManuallyDrop` doesn't specifically imply `MaybeDangling`? So if the implication isn't there maybe the difference doesn't make sense?

Amanieu: The docs for `ManuallyDrop` say accessing the inside is safe. And as such I'm happy to say the same thing for `MaybeDangling`. Accessing the inside is safe but it's your responsibility to not do that once it's dangling.

Josh: I have the impression the plan is to stabilise this type eventually.

Amanieu: I don't see any value of this over ManuallyDrop

The 8472: There are some todos for using MaybeDangling for BTreeMap and quicksort implementation

Amanieu: The quicksort one was before ManuallyDrop had MaybeDangling. Now that it does that, we should be able to use ManuallyDrop. What you actually want is a `ManuallyDrop` that doesn't break when you put a `Box` in it.

The 8472: When would you use `MaybeDangling` instead of `ManuallyDrop`?

Amanieu: I don't know, I can't think of a use case.

The 8472: There's maybe an obscure Lang usecase we're not aware of but maybe we shouldn't just ignore that.

The 8472: So this removes the dereferencable attribute without suppressing drop. If you remove `MaybeDangling` it would still run the drop code.

The 8472: This might be for self-referential types?

Amanieu: Yes, it's useful for some sort of `OwningRef`. But most of the time you just need `ManuallyDrop`

The 8472: Do those traits make sense when you're using a bare `MaybeDangling` trait for self-referential reasons?

Amanieu: I don't feel too strongly about this. I'm struggling to imagine what the usecases for `MaybeDangling` are and therefore what API would make sense.

The 8472: There could be a more principled approached to which traits are implemented. If we deem implicit behaviour like `==` too risky on a dangling reference is too dangerous and every use of it should be explicit. Is that the concern? If so, then it makes sense to remove those trait impls.

Amanieu: You just have to be very careful around these types in general.

The 8472: I have used `ManuallyDrop`. You suppress Drop throughout the function and if you don't use any unsafe code there's no footgun. And if you do use unsafe it happens pretty much at the end. I don't know what the `MaybeDangling` uses would look like in practice.

The 8472: Since nobody uses it, we don't have much to study.

Josh: Do we have a post that says "here's the plan for `MaybeDangling` and why it needs to be stable"?

The 8472: When we discussed the RFC where the crates that have self-referential types (like `OwningRef`) could use this.

The 8472: Do we need this now? Even if we stabilized `MaybeDangling` itself, could we add this later without breaking people?

Amanieu: Yes, that's fine.

The 8472: So is there any point in adding this now if we don't even know if it's needed?

Amanieu: My overall feeling on this is "meh". I don't mind adding it or not adding it. Either is fine.

The 8472: If the concern is that it's a footgun, then we should look at how people are using it.

Amanieu: The way people are using it is a footgun already because they use it in self-referential types.

The 8472: Since we don't know how people would do it, maybe it's not a good idea to let operators touch it. E.g. calling `==` when you don't know whether it's dangling.

Josh: I'd agree with the "meh" assessment. I agree `ManuallyDrop` is a precedent but I agree with Chris's assessment that `MaybeDangling` could be more of a footgun. Adding them here could be more error-prone. Given that I'd hold back and wait before adding them.

The 8472: Also Waffle argues that `ManuallyDrop` having these traits is an issue too.

The 8472: I'm leaning towards deferring it until there's a concrete need to have these implementations.

Amanieu: I'll go with you and defer it

Josh: I'll +1 with what you're suggesting as well

The 8472: I'll reply


### (waiting on team) rust.tf/155153 *Ensure Send/Sync is not implemented for std::env::Vars{,Os}*

Amanieu: This is not waiting on team anymore.


### (new change proposal) rust.tf/libs786 *\`std::ops::RangeBounds::{start,end}\_bound\_inclusive\`*

Amanieu: This relies on the `Step` trait.

Amanieu: This is still under active discussion. I propose we skip it.

### (new change proposal) rust.tf/libs785 *\`VecDeque::truncate\_to\_range\`*

Josh: This one is a nice additional proposal: because it takes a range for what to keep, you can truncate both ends in once in a clean manner. And write both `truncate` and `truncate_front` in a simple fashion that shows exactly what you're doing. E.g. with `truncate_front` are you saying how many elements to drop vs how many elements to keep?

The 8472: `truncate_front` is still unstable?

Josh: It is so we could drop it or keep it for symmetry. I wouldn't be against either dropping or keeping it. But I want `truncate_to_range`

The 8472: If there's any performance issue we could specialise on half-open ranges to get exactly the same behaviour as `truncate_front`. But it should behave exactly like it. I think `truncate_to_range` is the better and we should remove `truncate_front`

Josh: I don't have any objection on that front.

Josh: If we could go back to pre Rust 1.0 I'd like for `truncate` to just take a range

The 8472: We could make `truncate` take a trait?

Josh: Probably not without widespread inference failure.

Josh: My inclination would be to approve this one and let others decide what to do about `truncate_front`

The 8472: Sounds good to me.



### (new change proposal) rust.tf/libs784 *Float Exp Bias Constants*

Amanieu: This should be the same type as `from_bits`/`to_bits`

Josh: Use exponent_mask to get out the bits and then subtract to get the exponent bias? Is that because the range of exponents here is that this is just the bias to make this a signed number?

Amanieu: Yes

Josh: In that case the most obvious math would be on `i64`. It's all fuzzy anyway and you can cast between the two. Or use a `wrapping_sub` and end up with the number you'd have had for signed and end up with unsigned. But I'm looking at this and people using it would have to be doing casts.

Josh: In an ideal world there would be a function that would do all this for you.

Josh: It's at least worth asking to show the code that would used it. Because the obvious code we're thinking of would need this to be signed because you'd need to be able to subtract it and be able to get a negative number.

Josh: I'll write a comment to that effect.


### (new change proposal) rust.tf/libs783 *Add std::range::DecreasingRange\* structs*

Amanieu: The 8472, you replied to this

The 8472: I haven't read the replies yet.

Amanieu: I think `DecreasingRange` is the wrong tool for the job. A Range doesn't have a direction. It's a low bound and an upper-bound. What you're trying to do is creating a range with an exclusive lower bound and inclusive upprer bound and then trying to iterate through that range from top to bottom.

The 8472: They probably have some usecase for that, the question is how often does that happen. We have the type that lets you specify this exactly. It's the tuple of `Bounds`.

Amanieu: Yes.

The 8472: We could have an `IntoIter` for that.

Amanieu: I'd like to not encode the direction of the iteration into the Range type.

The 8472: But if you want your lowest end element to be exclusive then we don't have a way to get that iterator now.

Amanieu: Then that's what we should solve instead.

The 8472: Then we should have a Bounds iter

Amanieu: Can we add methods to to tuples?

The 8472: They're both in core, we could solve this with coherence?

Amanieu: I wouldn't mind adding new Range types that are from exclusive

The 8472: I don't like how this multiplies. We have Inclusive/Exclusive. We have Inclusive/Inclusive.

Amanieu: We're adding three new Range types: excluvie end bound, inclusive end bound and unbounded end.

The 8472: I think for these explicit niche cases to have the swiss army knife by BoundsEnum.

Amanieu: I don't like the idea of a From impl to constructing an iterator.

The 8472: We have a bunch of trait impls on tuples already. So this seems okay to me.

Amanieu: I don't think we need to do this exploration ourselves. We can just say that we don't want the direction encoded in the type itself.

The 8472: Sonuds good to me


### (new change proposal) rust.tf/libs781 *ACP: Support \`NonZeroU16\` in \`ToSocketAddrs\`; \` SocketAddr\`'s \`From\` Implementation*

Josh: We talked about it last week, asked them to do a crater run and if there's zero breakage we could consider it. Otherwise it's not worth it. They put together a proposal and I kicked off crater run now.

Amanieu: Is about port number being 0 not being allowed?

Josh: In practice 0 can represent a specific action so people might want to represent it as `Option<NonZero<u16>>`.

Amanieu: I think the motivation is sufficiently weak that we should just reject it.

The 8472: Starting your application is not something you do thousands of time. You're not going to repeat this code so much to have this very concise way to express it.

Josh: This is about the type premise of making these states unrepresentable.

Amanieu: 

The 8472: The argument is stronger than that. The port 0 has a special meaning so it's not exactly like `u16`

Amanieu: I agree, but sometimes we want to create a sockaddr with port 0 to get this behaviour.

The 8472: But if your application doesn't want this behaviour you can encode that in the type system

Amanieu: Then encode this explicitly and return a u16 when you call sockaddr

Amanieu: If a function can accept a usize, we shouldn't change it to also accept `NonZero<usize>`.

The 8472: That's a useful intrinsic, but this case is more specific

Amanieu: I strongly feel this should be rejected.

The 8472: For an ACP, that's already sufficient.

Amanieu: Does anyone feel strongly about this?

The 8472: No

Amanieu: I can write this up.


### (new change proposal) rust.tf/libs780 *ACP: \`OsStr::{split\_at, split\_at\_checked}\`*

The 8472: This is related to the OMG-WTF-8 proposal. Do we still want to keep this option open?

Amanieu: Sure, we can add it.

The 8472: Did you read the alternatives section?

Josh: `slice_encoded_bytes` seems like it does the thing at which point we could consider adding `split_at_encoded_bytes`. I don't necessarily thing we need to add the `encoded_bytes` to the name there. This seems reasonable.

Josh: I'd have concerns about the u16 encoding case if not for the fact that Chris is the one who proposed this.

The 8472: What are the native OsStrings on Windows again?

Amanieu: The native ones are UTF-16

Josh: The native ones, yes. Ours are WTF-8

Amanieu: If you throw normal unicode at it, you get a UTF-8 string out of it.

Josh: The OMG-WTF-8 encoding, the only case that matters is the non-Unicode string where Windows have non-surrogate unicode points which UTF-8 doesn't allow. I don't think we have to care here.

The 8472: What I'm asking is: are we blocking anything with that? If we add the splitting here and then change the encoding to OMG-WTF-8, would that break anything?

Josh: My understanding is that the we could today just say "you're not allowed to split between surrogate pairs". That would be fine here and then later we could say "we've switched to OMG-WTF-8 and you're allowed to split along surrogate pairs". We can't do it the other way.

Josh: So we should say you're not allowed to split there

The 8472: Then this would be a platform-specific 

Chris: I think the only sane API is to only allow splitting at UTF-8 boundaries

Josh: I don't think we should be as restrictive. We should have the same behaviour as slice_encoded_bytes. Which says the valid boundaries depend on the encoding used by as_encoded_bytes and start and end must be valid boundaries. It makes sense for us to say we want to require the same boundaries there. If the encoding doesn't care where you split (like unix), you can do what you like. If it's like on windows where it requiers wher eyou can split, then we should panic there

The 8472: It lists the definition of what is the allowed boundary on a platform-independent manner

The 8472: You need to call the other methods, search for split points and then produce this range

Chris: Part of why I proposed thi sAPI si I was trying ot implement it manualyl and that's pain

The 8472: But if we have these restrictions, you'd still need to perform a similar search.

Chris: Yes.

Josh: I'm tempted to make it as capable as the platform. But in practice the legitimate places to split an OsString are boundaries that are character boundaries (`\`, `/`, `.`) etc. Splitting on a pathname that may not be a unicode pathname is questionable.

The 8472: `OsStraings` aren't always just paths. They could be env vars, socket names, registry keys etc.

Chris: In that case you have to know the platform you're using. You can't use the general API

Josh: Yes. And on the environment name case, the left hand side you'd want to split it on a character boundary.

The 8472: We apparently have `check_public_boundary` internal methods. I wonder if we should make those public for seaching.

Josh: I don't think it'sneeded. Whatever boundary you're searching for, it's either going to be a number or character. If you e.g. search for `:`, you won't find the `:` codepoint as part of something else.

The 8472: Ok.

Josh: It's not a big deal to say if you really want to pull stunts for things that aren't valid unicode and want to split on that, you have to do something else. This seems fine and we can just document that you're allowed to only split on valid character boundaries. Same way we did for `slice_encoded_bytes`.

The 8472: I'm in favour. I assume one of these will panic.

Josh: Yes, one will panic and the other one will return an `Option`

The 8472: Ok. ACP accepted then. I'm typing a reply.


### (new change proposal) rust.tf/libs779 *floating\-point NaN payload functions*

Amanieu: I feel like we don't want this. Oh, C actually has an API for this.

Josh: The only reason we've had that discussion was signalling vs. quiet NaNs. I think it's nice to have this. The one piece I'd question was `set_nan_payload` that takes `self`. I think there's noneed to take `self`, we can just take the payload. And then we should ditch `SetNanPayloadError`.

The 8472: The proposed thing says the payload doesn't include the quiet/signalling bit or the sign bit. Why is that?

Josh: That went back and forth several times. I think it should include the entirety of the payload.

Josh: I'd structure this: get_payload that returns Option. nan_with_payload that takes a payload and either gives you a floating point and panics. And possibly a version that takes a payload and gives you an option instead of panicking. And if we need one more constant for checking if something's in range, we can do that too.

Josh: The proposed API from programmerjake has a `nan_payload` that has `Option<NonZero<uint>>`. Why is that?

Amanieu: That would be an infinity.

Josh: If you're doing nan boxing, in theory you might want to allow representing "this doesn't have a payload, this is just a NaN".

The 8472: But that's your application domain logic, not the floating point domain.

Josh: I agree, but that's the same as infinity payload

The 8472: That's not a NaN then. Since the lowest fraction bit is reserved to indicate infinity, we can't use it as a payload.

Josh: I think it's the entirety of the payload being zero, not just the bit, right?

The 8472: Right.

Josh: The alternative of the mantissa being zero and ?? being set would be a negative infinity. Should that be reserved as well?

The 8472: If we're including the sign bit, then.. we'd need to split it into a `NonZero` and a `bool`?

Josh: I don't think so. The function to extract the NaN payload would give you a None if there's no payload. And you'd get that for negative and positive infinity. And if you encode something, you can encode something that includes the signed bit and you can't encode something that's all ones or zeros.

The 8472: But then you can't represent the range

Josh: I think there is a simple way to represent it. We could represent the range as NonZero. Then we don't need to distinguish negative and positive zero. And we just give you back a NonZero.

The 8472: So it's not really a non-zero, it's more of a custom range type.

Josh: It is a non-zero because it can't be a zero but also unsigned integer

The 8472: Would we shift it around or would they be two disjoint ranges?

Josh: We could encode it for you or give you the value for the full range of ??

The 8472: I guess people who do NaN boxing want to do as few instructions as possible

Josh: Agreed, people who do NaN boxing probably don't want us to shift the bits

Josh: The layout is signed exponent, then mantissa. So if you wanted to encode 53 bits into a 64-bit value

Josh: Option (a) we only givey ou the 52 bits. Option (b) we we give you the 52 bits and the top bits but they're separated. Option (c) we shift -- but we should reject that. ??

Josh: That seems fine because if you want to encode ??

The 8472: Or anything with exponent bits. The rules are a bit complicated.

Josh: The no exponent bits are part of the range. You have to handle the case that it can't be a positive or negative zero.

The 8472: Then it's no longer a compact range anymore.

Josh: It seems the simplest case is to just hand you back the 52 bits but I'm still tempted to give you the top bit as well.

The 8472: The nonzero doesn't really give you that much. This none check doesn't really bring you into the safe zone

Josh: True. Nonzero would make sense if you only had the mantissa.

Josh: So maybe we should just give you the 52 bits and if you want to set the signed bit as well you have to do that yourself?

The 8472: I haven't done any NaN boxing, I just know JS engines do it and I don't know whether they use the top bit.

Josh: Given they use it for pointers, I don't think they use the top bit as they only need to encode the 32 bits.

Josh: Can agree we don't want to do the self-based construction of NaNs and just say "build a new NaN"?

The 8472: Agreed

Josh: And then we just have to settle the queestion of handling of the signed bit

The 8472: And whether we want a checked version

The 8472: Would pattern types help here? This is what an or range would look like. That would be nice.


### (new change proposal) rust.tf/libs778 *ACP: Add \`char::to\_u32\` to access underlying value*

### (new change proposal) rust.tf/libs777 *ACP \`Vec\<u8\>::push\_utf8\` for no\-memcpy UTF8 in a general bytes buffer*

### (new change proposal) rust.tf/libs775 *\`unchecked\_wrapping\_rem\` and \`checked\_wrapping\_rem\` for \`iN\`*


### (stalled change proposal) rust.tf/libs523 *Add a deterministic constructor for \`RandomState\`*

### (stalled change proposal) rust.tf/libs131 *Add \`std::fs::rename\_noreplace\`*

### (stalled change proposal) rust.tf/libs287 *ACP: Add \`FromByteStr\` trait with blanket impl \`FromStr\`*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs462 *impl fmt::Write for BufWriter*

### (stalled change proposal) rust.tf/libs210 *Add associated consts to f32, f64 for mathematical constants*

### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs544 *\`Arc\<impl !Sized\>\` constructors*

### (stalled change proposal) rust.tf/libs347 *Context reactor hook*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_


