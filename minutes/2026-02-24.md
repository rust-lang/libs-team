# Libs-API Meeting 2026-02-24

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Amanieu, David, Nia, Josh, Chris, Nurzhan, Tomas

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

2 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3874 *build\-std: always* - (11 checkboxes left)
  - merge rust.tf/rfc3892 *Complex numbers* - (5 checkboxes left)

16 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/139087 *Fallback \`{float}\` to \`f32\` when \`f32: From\<{float}\>\` and add \`impl From\<f16\> for f32\`* - (3 checkboxes left)
  - merge rust.tf/99301 *Tracking Issue for \`error\_generic\_member\_access\`* - (2 checkboxes left)
  - close rust.tf/136638 *warn on empty precision* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`Exclusive\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/111688 *Tracking Issue for ExitCodeExt on Windows* - (4 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/119639 *Tracking Issue for tcp\_deferaccept* - (3 checkboxes left)
  - merge rust.tf/149537 *f\*::min/max do not behave as documented for signaling NaN on aarch64, or with optimizations* - (4 checkboxes left)

[tmandry (1)](https://rfcbot.rs/fcp/tmandry), [joshtriplett (7)](https://rfcbot.rs/fcp/joshtriplett), [the8472 (11)](https://rfcbot.rs/fcp/the8472), [Mark-Simulacrum (1)](https://rfcbot.rs/fcp/Mark-Simulacrum), [epage (1)](https://rfcbot.rs/fcp/epage), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [weihanglo (1)](https://rfcbot.rs/fcp/weihanglo), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [BurntSushi (8)](https://rfcbot.rs/fcp/BurntSushi), [Amanieu (5)](https://rfcbot.rs/fcp/Amanieu), [oli-obk (1)](https://rfcbot.rs/fcp/oli-obk), [estebank (1)](https://rfcbot.rs/fcp/estebank), [0xPoe (1)](https://rfcbot.rs/fcp/0xPoe), [Muscraft (1)](https://rfcbot.rs/fcp/Muscraft), [nikomatsakis (3)](https://rfcbot.rs/fcp/nikomatsakis), [dtolnay (3)](https://rfcbot.rs/fcp/dtolnay), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [traviscross (1)](https://rfcbot.rs/fcp/traviscross)

### (nominated) rust.tf/149926 `P-lang-drag-1` *Do not deduplicate captured args while expanding \`format\_args!\`*

Amanieu: This is in FCP, but we currently have a blocking concern.

Josh: I don't think there's a filed blocking concern, rather someone has expressed it.

Amanieu: There is an actual blocking one: it hurts optimization because of duplication. We can work around that by adding an extra opt to format_args lowering that deduplicates things when they're locals or statics. We have a PR for this: https://github.com/rust-lang/rust/pull/152480. I've sent a message to Mara.

(Mara joins)

Amanieu: The FCP is blocked on the optimization concern. The main concern that you had was that this makes the optimization too complicated. But the feeling here is that this would open access to things such as field access and the new semantics would be clearer.

Josh: I'd not consider the latter load-bearing -- the new semantics are easier to explain regardless of what else we do.

Mara: I don't agree that the situation after the PR is easier to explain. The current situation is that .., but after the PR you'd need a separate transformation to explain that:

Before this PR:

```rust=
format!("{x} {x}") // simply transforms to:
format!("{x} {x}", x=x)
```

After this PR:

```rust=
format!("{x} {x}")
format!("{x} {x}", x=x) // simple transformation
format!("{0} {1}", x, x) // more complicated transformation
format!("{uniq0} {uniq1}", uniq0=x, uniq1=x) // more complicated transformation
```

Mara: I'm still arguing that the extra transformation, though simple, makes things more complicated. The current optimization feels fairly usable. But it relies on what's an implementation detail today. If we change that, we'll have to bring along this dependency on the lang resolver. Today, if I look at the expansion of `format!` I know exactly what it expands to. After this, I can't tell without knowing the surrounding code.

Josh: FWIW, one aspect of this that's load-bearing is that semantically, it expands to the same thing. It evaluates each thing on each mention. The optimization that the compiler does is semantically invisible. A hypothetical implementation of Rust isn't required to make that optimization. I think this is what makes this optimization appealing.

Mara: That's reasonable, but it makes things more complicated. This PR tries to solve a very rare case, about a Cell in a const in a print statement. I feel like the only reason we're having this discussion is for the RFC for fields in placeholders. But that discussion should be held on that RFC about that feature, not as part of the weird cell-const-print thing.

The 8472: I pretty much argued the same but lang claimed it's their territory because it's their macro. And they argued this is essentially the same as a function call and should be evaluated the same way.

Josh: lang only said that as with any built-ins, this is an area of shared jurisdiction between lang and libs-api.

Nia: Regardless of what lang's doing, this is a libs-api issue. There are arguments for this optimization, but if we don't have consensus, why apply this change? Even the PR is in draft. At the minimum, it makes sense to leave this for later with a note on insufficient information — until a consensus forms.

Josh: I think we shouldn't keep delaying this. We have reached some form of consensus in the past for this. There were concerns about the performance, we now have a PR showing how it could be optimized. We should evaluate the optimization and figure out if it's reasonable.

Mara: If the PR is necessary to enable fields in placeholders, then sure. But currently, this PR is about solving the weird cell-const-print case. .. For that, it is not worth it. We should close that issue.  Separately, we should work on the RFC for fields and see how it works out.

Josh: This is not a blocker for the fields RFC. Either approach is provide a desugaring for fields. But there were cases where using the current behavior would allow things be observed.

Mara: I've been working on `format_args!` for five years, and every time the constraint has been that this should be a minimalist macro in terms of magic. I'm not against more magic, but is it really worth crossing that line just to fix a niche case?

Josh: I'm not against the compiler adding more magic that would make this easier to implement.

Mara: With the Libs hat, no but with the compiler hat, yes. We can add the magic, but the macro needs to be maintainable. It used to have a very messy implementation. I want to avoid adding more magic because it would make the macro less maintainable.

The 8472: Can we cite the previous decisions?

Amanieu: Those concerns were reasonable when we moved format_args into an AST node. But I don't think that's relevant now.

Amanieu: Even if we didn't have this optimization, explaining to users "if you use a name in a `format_args!` string that's not defined, it expands to creating an anon placeholder for it". That's easy to explain. I think the optimization isn't important in practice, and even without it it's worth explaining the semantics to users. That every time this expression is used, it's evaluated separately and it gets its own anonymous argument.

Mara: I'm checking what clippy does today. Clippy suggests inlining the argument, if you have a print statement in this form it will suggest that you put it in there. It also suggests that if you have multiple of the same argument.

Josh: Which is why we shouldn't suggest that.

Mara: Clippy should, because it's more efficient

Amanieu: We wouldn't need the lint due to the optimization.

Mara: The optimization doesn't apply to all cases. Consts, etc.

Josh: That problem already exists if you .. pass a constant 10 times. The compiler would find .. where it can avoid .. 

Mara: Who is going to maintain this? I'm currently maintaining this part of the compiler. I don't want to maintain this optimization.

Josh: I'm arguing most people won't care if the optimization is not present.

Mara: They do on embedded. I've been trying to get this optimized for their use cases for years, but this is a step away from that.

The 8742: It doesn't just evaluate multiple times, it also increases compilation size? the representation of `format_args!`?

Amanieu: Only the array size is relevant.

The 8472: So it's more about size bloat than evaluation.

Nia: The consensus last time was that the performance concern was relevant. If there is no feasible optimization, we probably don't want to go ahead with removing the deduplication.

Josh: I think the consensus was that if we have the optimization we'll have consensus.

Mara: What happens if there's a conflict between t-compiler and t-libs-api? If libs-api wants this and the compiler team will not want to maintain the optimization.

Josh: Then that takes us back to the question on what we should do when we don't do the optimization. Some people still want it then. 

The 8472: I think referring to past decisions would be helpful. If this will be blocked by the compiler we should bring them on board.

Mara: Whom do you want to bring in? I'm the compiler member who's maintaining this. 

Mara: As reviewer, I would reject a PR that starts using name resolution in a builtin macro for an optimization.

Mara: In the reviews of my earlier work on format_args, the reviewers laid out some boundaries for what is and isn't acceptable in terms of magic.

(we cut the discusison for time, no conclusion)

### (nominated) rust.tf/152480 *\`format\_args!\`: only de\-duplicate captured identifiers that refer to places*

(see above)

### (nominated) rust.tf/152304 *stabilize new RangeToInclusive type*

Amanieu: The FCP passed, but the PR added two impls that were not covered by the FCP: `SliceIndex<[T]>` and `RangeBounds<T>`. The SliceIndex one seems fine to me. I'm wondering what's up with the RangeBounds one.

Josh: Is it methods or impls that are being added here?

Amanieu: Looks like impls are being added.

(notes were corrected accordingly)

The 8472: tgross says that they were present in the RangeToInclusive, so this is just copying them over:

https://github.com/rust-lang/rust/pull/152304#pullrequestreview-3772288904

Nia: Seems reasonable, I don't see anyone raising concerns.

Amanieu: Do we need an FCP, or should we just review and merge? I think it should be fine.

Josh: This is specifically only for Inclusive, right?

Amanieu: Yes:

Josh: I just remember some discussion that would make it difficult for the exclusive case, but only inclusive seems fine.

Amanieu: I think we just forgot this impl, we had it for everything else.

Josh: There was something else that was different for inclusive and exclusive because inclusive had to handle the boundary point differently, but this seems fine.

### (nominated) rust.tf/152832 *Support if\-let\-guard in \`matches!\` and \`assert\_matches!\`*

Amanieu: The if-let guard was stabilized this week.

Josh: It's showing up in the next upcoming version. This makes sure that `matches!` and `assert_matches!` supports the same syntax as `match`

Amanieu: Do we already have if guards in `matches!` and `assert_matches!`?

?: Yes.

Amanieu: The concern is about significant additional complexity. We may need an full expression parser.

Josh: I don't think we need a full expression parser, but matching commas complicates this.

Amanieu: The alternative is to add a guard meta-variable type to macros.

Josh: That would be reasonably simple to add, might be a good approach.

Amanieu: We explicitly match an if guard with an if literal: https://doc.rust-lang.org/nightly/std/macro.matches.html. Proposal: we say "yes, we want this", just for consistency with the `match` syntax. If this requires extedning macro_rules with another meta-variable, so be it. This would be better than turning this into a compiler macro (?)

David: We want to see when something fails with multiple guard expressions, it would be great to show where exactly that failure is. It would be easier to do that as an built-in macro.

Amanieu: Let's focus on the base line functionality here. Moving it to a built-in macro, can be done later.

Josh: "We want to see this accepted in `matches` and `assert_matches`, we'd be open to making `macro_rules` more complicated or adding a new meta-var to `macro_rules` or whatever else makes sense" seems like the right response.

David: For this reason, `assert!` is already a built-in macro.

Nia: Regardless of whether we do this as a built-in macro or not, .. patterns seems useful.

### (nominated) rust.tf/152851 *Fix SGX delayed host lookup via ToSocketAddr*

Amanieu: Why is this controversial?
https://github.com/rust-lang/rust/pull/152851#issuecomment-3937266185

Amanieu: I'm confused, what's the problem here?

Josh: The original change was something in std that simplified lookup. They split .. and then called the same underlying impl to deal with that. What does the new code look like?

Amanieu: Lookup host on SGX just always returns an error?

Josh: Looks like it's unconditionally returning an error. It's confusing why it's ..

The 8472: Doesn't `..SocketAdds` support `String` too? I know it does for UNIX sockets, but what about TCP?

Amanieu: I see what the problem is. If you look [here](https://github.com/rust-lang/rust/pull/146541/changes#diff-914d1e5dc39224af8e8eaf48133b277dfd6c8d6eb08b261f66e502f4a6ed11ebL240), previously it would call resolve socket addr, which on SGX would copy the entire string unchanged into the lookup host struct.

Josh: So it didn't split out the port number, it just .. and stuck it in a struct.

Amanieu: And now lookup host is... SGX was passing in something with a name and not a port, and was expecting the return error type to contain the original string. The SGX constructor for lookup host is [here](https://github.com/rust-lang/rust/pull/146541/changes#diff-914d1e5dc39224af8e8eaf48133b277dfd6c8d6eb08b261f66e502f4a6ed11ebL240), and always returns an error with the original string inside. Now, instead of that, it's failing with invalid input because the string doesn't have a colon (a valid port number).

Amanieu: Lookup host changed to hold a string and an optional port.

Josh: I'm reading the original issue. It looks like one piece of info... there'a comment on line 2?39... if the string can't be parsed, it needs to return NON_SOCKET_ADDR error. And if the host & port need to be parsed, they have to be passed alongside the error.

Amanieu: I figured out exactly what SGX is doing.

Josh: I think this code is too complicated for what SGX is trying to do. The previous code was fine, but if we want to allow what SGX is doing, we want to do it in a simpler way. If we can't split these strings, we can call some other function. Even the SGX comment has "fixme this is a terrible terrible hack".

Josh: Proposal: rather than relitigating this inside this meeting, we should respond to the PR and say this is too much complexity to preserve what SGX itself describes as a terrible hack. .. that doesn't impact other targets.

Amanieu: the api doesn't expose a function to resolve a name to an address and port. In this hack, if this returns an error, the string is passed to ..connect.

The 8472: .. It's not quite a TCP socket, it's an SGX TCP socket.

Josh: Like talking to a service name essentially?

The 8472: Yes.

Amanieu: It's still TCP but it's doing name resolution separately.

Josh: How does it get the port?

Amanieu: It doesn't. It just connects and then passes in the string.

Josh: This sounds like a proprietary issue, then.

Amanieu: The concern here is that there are people relying on this behavior.

Josh: My proposed resolution: we respond to the PR saying that this is impacting other tagrets significantly and we don want to do that. If they want to revise this, they can come back to us for a vibe check.

Amanieu: Extension traits?

Josh: That would have been a good idea at the beginning. But doing it now will break their users.

Amanieu: But that's what the PR does.

The 8472: Since this is internal in the library, can we look in the string and pass it through? Could we do this through a specialization?

Amanieu: That's basically what it does. But it's all just a string. It doesn't have a concept of a port or address.

Nia: We should document this as special cases .. because this seems like proprietary vendor hell.

Josh: Yes. How self-contained and not affecting others can we make this?

Amanieu: there are two things you could do here:

  1) allow host-name lookup inside..
  2) add a connect_name method via extension trait

Josh: Taht's what they should have done, but this will break the existing users relying on the hack.

Nia: We can do a deprecation warning.

Amanieu: We could deprecate the connect method entirely.

Josh: That's not a bad idea, but in the meantime we should not sit here designing how to support having the connect method for a few more releases.

Amanieu: Do we maintain backwards compatibility or force a breaking change?

Amanieu: Proposal: add a connect_by_name via extension trait and deprecate connect?

Josh: I'd rather give the feedback to the .. and let them propose an alternative solution, but in the meantime urge them to come up with other workaround to ??

### (nominated) rust.tf/152855 *Do not emit separator as before elements*

Nia: Seems like the current behavior was an oversight, and the proposed one is right.

Josh: Is intersperse stable?

?: No, it depends on ..

The 8472: https://doc.rust-lang.org/nightly/std/iter/trait.FusedIterator.html

> Note: In general, you should not use FusedIterator in generic bounds if you need a fused iterator. 

David: That doesn't really apply here.

The 8472: Why not?

David: (explains)

Josh: I wish we had a type system feature that allowed consuming the iterator if `next()` returns `None`. But it's too late for that.

Nia: Since this is a 6-line change, we could merge this with .. that should get addressed before stabilization (?)

?: We need tests.

Josh: I don't think we should require `FusedIterator` as a bound on `Intersperse`. ..

Nia: There have been a couple of cases where I used unfused iterators that I ended up scrapping because I found better solutions.

Nia: Do we have some consensus?

Amanieu: No strong feelings.

Josh: No objections to adding tests and merging as is, but ..

The 8472: I don't agree. I think we should support unfused iterators in cases where it's not problematic.

Nia: It's probably a separate discussion whether Intersperse should fuse iterators.

Josh: We can decide later before we stabilize this, but add something to the tracking issue signaling that.

### (new change proposal) rust.tf/libs745 *ACP: Introduce something akin to reverse ancestor method/iterator for \`std::path\`*

Amanieu: We don't `ancestors.rev()` because ..

The 8472: Should we suggest that they implement `DoubleEndedIterator` for `Ancestors`?

?: Yes.

### (new change proposal) rust.tf/libs744 *Const path separators*

Amanieu: This is asking for const separators, which would give you all the supported separators (?)

Josh: Seems reasonable.

The 8472: What module do they go in?

Amanieu: The alternative is to make `is_separator` const, which we should do anyway.

Josh: They're explicitly saying they want this in the future.

The 8472: The only theoretical issue I can see is that .. are context-dependent. On Windows, .. doesn't work.

Nia: We could only support things that are always separators in const, and make the context-dependent parts runtime.

Chris: We already have the main separator concept -- the one you should use according to us.

Nia: But if you're parsing a string, you'd want to know what may be a non-standard separator. And then at runtime, those may or may not be separators.

Chris: When you're being generic about this, you really need generic interface. 

Nia: On most platforms it won't be an issue -- the lists will be the same. And then it's up to the implementor to figure out how to handle the cases where this is differennt.

Chris: There's a gap where you don't want enough about the platform to handle this itself. I'm not sure the stdlib can handle every possibility.

Nia: We can apprvoe as is and say the list of alt separators might not be fully comprehensive depending on the platform. And we'll include all the things that are always guaranteed on a platform.

Chris: There's cases when something not a separator on windows.

Nia: We do the same that separator does, we add a note in the documentation and let this go ahead.

Amanieu: Yes.

Chris: I'm fine with that.

### (new change proposal) rust.tf/libs743 *ACP: Opt\-in to calling GlobalAlloc in std's internals*

Amanieu: Is there an implemenation PR attached? How will this function be used?

Chris: Is the context here thread local allocation?

Josh: This is related to the change where we said to allow the GlobalAllocator to use these features, we'll make sure not to call the allocator from these things. We added SystemAllocator which solved issues, but people want to control all the allocations. This proposes saying the GlobalAlloc is okay to call from ThreadLocal because it won't call threadlocal

Josh: We allow ThreadLocal calling from GlobalAlloc

Amanieu: They could use the thread_local attribute if that becomes stable in theory

Josh: Sure. In general, telling std that the thread local allocator is fine in places where we'd use the systemallocator instead seems fine. I'm not sure this is the right implementation strategy. I like waiting on `try_as_dyn` and if you don't want to use std in your allocator, implement the trait and we'll check if we have it. I don't understand their comment on  that.

Josh: This seems fine.

Nia: This is probably the best we'll get. Everything else is uglier.

Amanieu: Is it too late to revert the guarantee now?

Chris: We used to guarantee the opposite, no?

Josh: If we want people to write allocators in Rust, we need them to be able to use thread locals to write pools in the allocator.

Amanieu: This was merged three months ago (in November)

Chris: Just one stable version.

Nia: I'd feel uncomfortable undoing the guarantee. It would make writing the allocator hell. I don't see how it might be done in a nicer way.

Amanieu: If the solution is "check if the allocator supports re-entrance and if not, use the system allocator" then it's the right shape.

Josh: That's exactly what is proposed. The stdlib pieces that currently say "don't call global alloc" would say "if the global allocator uses std, call system, otherwise call the thread local allocator"

Amanieu: Can we override the SystemAllocator on all platforms? You'd provide your version of malloc.

Josh: Yes, you override the symbols.

Amanieu: The issue is in wasm where we implement the SystemAllocator entirely in Rust and there are no symbols to override. Can we reframe this as providing a way to override the SystemAllocator?

Nia: Maybe, but that'd mess with how we link the Rust library. That feels much uglier.

Josh: We could change the attribute for setting the GlobalAllocator to have a different option/name e.g. system_allocator. It would say "please register me and call me when you call the SystemAllocator". But whether an allocator can be used as a system allocator is a property of the allocator itself.

Amanieu: I like the idea ??

Josh: If you have a crate providing an allocator that you'd say "register as global allocator as usual", you'd have to say "register this not as usual but with the extra attribute". You're putting the burden on the user.

Amanieu: the name `uses_std` is terrible

Josh: We could invert the sense of `bool` if that helps name it better.

Amanieu: I'd like to use something that invokes system allocater specifically

Josh: We may want to add more things in the future

The 8472: `is_reentrant`?

Nia: Yes. And that would be more future proof

Josh: It's not about reentrancy but about whether the invocation would recurse.

Amanieu: With reentrancy in generally, if I say my allocator is not reentrant, I could just use Vec and the standard library would make that work somehow.

Josh: Yes and if you used vec or hashmap you need to specify the allocator used

Amanieu: We'll have to put reentrant there somewhere

Nia: `reenters_std`? That only covers the properties we care about

Josh: We could invert this. Frame this as overriding the system allocator. `use_as_system` = `true`/`false`?

Amanieu: that becomes confusing. People won't know what global system even means anymore. There should be a clear list

Nia: We can say "tentative yes, we need to bikeshed name" and the default true/false depend on how we name it. Do we want this or consider other options e.g. an attribute?

Josh: Attribute would complicate it

The 8472: Not just a naming issue. We need to list the calls into the allocator.

Amanieu: I'd explicitly call it uses thread local storage

Josh: I'd say uses_std

Amanieu: that's too vague. You could take that to mean "uses path" etc. But you can't. You can only call things that use the thhread local storage. The flag really means "does this allocator need hepl from thread local storage"?

The 8472: If the user implements a custom allocator, does this function inline?

Amanieu: It should with LTO

Nia: Yes, but this is `inline` for that reason.

Amanieu: It should be explicitly scoped around thread local storage.

The 8472: And there's no way to fix wasm?

Amanieu: We could change wasm to use a no_mangle malloc we could link and you then override.

The 8472: That would match the other platforms

Amanieu: At which point you'd just override the system allocator directly

The 8427: Not just for Rust but also for all the C libraries.

Amanieu: For the WASI targets there's alerady a WASI libc that we call malloc on. The built-in malloc is only for WASM none I think.

Amanieu: So maybe having an overridable `malloc` symbol could be enough for people wanting to override this on wasm.

Amanieu: Seems on wasm we always use the built-in dlmalloc even on targets that have a libc.

Nia: Aren't those different situations though? We have the possibility we merge the global alloc method, and then there's the replacing libc allocator -- FFIing into some other code we could override it there. I'm not sure those are the same situations.

Amanieu: If you have libc and a system allocator, you can have libc use the thread local allocator. If you don't have a libc you override the malloc symbol.

Josh: The use case in the OP is not overriding the allocators but hooking into them so you can do malloc-tracking:

> perhaps just tracking statistics 

The 8472: That's not in the ACP, that's coming from wasm

Amanieu: ??

The 8427: And that does the wasm growing?

Amanieu: It does.

Amanieu: A similar issue: on unix you have a system break and if two allocators tried to move it you get problems

The 8472: But that's only doen when called by libc

Amanieu: Most allocators only use memmap and not break

The 8472: What about the wasm case?

Amanieu: In wasm you need to make sure you only use one allocator.

The 8472: But you get corruption so the system allocator is broken?

Amanieu: No, ??.

The 8472: How is that okay? It's ok to call system to allocate from multiple threads.

Amanieu: wasm doesn't have threads. But the system one has a lock.

The 8472: The comment https://github.com/rust-lang/rust/pull/144465#issuecomment-3786972215 says:

> .. In my multi-threaded WebAssembly application..
> 

Amanieu: This happens because you have two allocators: System and Global. ANd they both grow the memory. They're trying to replace System to do the grow themself manually.

Nia: That almost sounds like they want to build a patched libc. I'm not sure the ACP covers the usecase described.

Amanieu: If the threadlocal allocator can still redicet it to their allocator, than you have full control

Nia: I think the ACP makes sense. But it won't help this particular use case.

Josh: We could do a separate thing: allow overriding the allocator call system.

Nia: Yes.

Nia: Now there's no motivating example. What do we do with the ACP? What we care about is overriding the system allocator.

Josh: Recently we addressed this by having them in the meeting and came to a conclusion. And today we talked about this without a conclusion. We should table it for now and invite them again.

Nia: I'll invite @laurmaedje to the next meeting

### (new change proposal) rust.tf/libs742 *ACP: add \`str::split\_twice\`*

Josh responded two weeks ago: https://github.com/rust-lang/libs-team/issues/742#issuecomment-3879295422

Proposal: close ACP.

### (new change proposal) rust.tf/libs740 *ACP: add \`str::version\_cmp\`*

Josh: We talked about this one in a meeting. There is value in providing rustfmt-compatible version compare. Something designed to deal with writing macros, sorting etc. And separately, doing numeric-aware comparison in the standard library was something we're open to. But we don't want to use the mechanism from rustfmt. I sent that response out, looking at the following comments.

https://github.com/rust-lang/libs-team/issues/740#issuecomment-3879932738 and onwards

Josh: Looks like it's mainly nailing down the exact behavior rather than a fundamental disagreement. We can respond to programmerjake's question around what is the exact behavior.

Amanieu: This looks more like it should be a crate that gives you the exact numerical sorting you want.

The 8472: Agreed. Since some regions use non-ASCII numerals and they'll want to sort them too.

Josh: Yes, there are other languages using different characters. I don't think we should provide anything in those scenaries. But we should handle the common ASCII 0-9 cases

The 8472: But then we're playing favorites for Latin

Nia: Last time we discussed this to put this in the crate to do the version comparison we care about in Rust.

Josh: I thought that's what we said we wanted to see that for rustfmt. That we wouldn't want that in the standard library. I was talking about the other method not specific to rustfmt.

The 8472: And I think that should also go into a separate (but different) crate.

Josh: It sounds like we don't have a consensus to add this to the standard library. So we should go recommend this goes in a crate.

Amanieu: I agree. There are many slightly different ways that are reasonable and we don't want to pick a favourite.

Nia: Yes, either we make this a crate or we have complex long names to handle things like cross-locale comparison.

The 8472: It might not have to be locale-aware, but it'd have to be either all unicode numerals or only ascii.

Nia: Even within ASCII on different locals the same string can be interpreted differently depending on meaning of `,` or `.`

Chris: So for this one we can say "maybe in the macro crate" and for the other one we can ask them to create a new crate. For the standard library version they could come with another proposal.

Nia: That's reasonable.

Amanieu: Do we want another proposal?

Nia: For the proc-macro we'd want this in an internal crate and for the other one we don't want it in the standard library.

The 8472: Yes, that feels like an internalization issue and we don't do that for time etc.


### (new change proposal) rust.tf/libs733 *allow specifying whether \`Instant\` should be suspend\-aware*

### (new change proposal) rust.tf/libs711 *Add \`mem::needs\_clone\`*

Amanieu: I don't think this is implementable right now. It's no better than specialization.

The 8472: Couldn't the compiler do something..

Amanieu: If it could do something conservative, we'd do it for specialization.

The 8472: It could do something between returning `false` and strictly what specialization does.

Chris: That sounds more like RFC territory and getting compiler on board.

Amanieu: Should we close this and ask an RFC?

Nia: Yes.


### (new change proposal) rust.tf/libs710 *\`BinaryHeap::from\_raw\_vec\`*

Josh: we talked about it before. We want to do this but we have to specify the vector layout for the heap. If we do it now, we have to pin that it's a max heap. If we want to make it a min/max heap we have to do this ??

Nia: We know it's a max heap. We specify that in the docs.

Josh: If you have a max heap there's one standard layout in the vec that makes sense.

Nia: That means we can commit to that layout and forever.

Josh: Last time we weren't sure there was a guarantee we'll keep this forever. Now that we found that guarantee in our docs, we can just ship this.

Amanieu: I don't like the "raw vec" name.

Nia: `from_vec_unchecked` sounds what we're doing in the stdlib. And if we want a min-max heap that can be a separate ACP.

The 8472: I think `from_vec_unchecked`

Josh: +1

Amanieu: `from_vec` isn't checking, it's *fixing* the order.

Nia: It would be valid in the `from_vec` impl to check whether the vec is already in the right layout and only allocate if it's not

Josh: This function could be safe if we wanted it to be. It could be constant and O(1) if we're willing to say you'll just get misbehavior if you do.

Amanieu: That's opposite of the guarantee we've done in the past.

Nia: We did that for types where we don't know the `Ord` implementation but we guarantee that for types in the standard library that we know and control.

The 8472: If we stabilised that, we'd break unsafe users relying on the invariant.

Josh: Then we shouldn't make it safe. It's worth saying that now we have `From` which is complex. We could add a new way that takes a sorted vec.

Josh: I'm fine with renaming this to `from_vec_unchecked` and leave it unsafe. I just observed that there was a path to make it safe if we wanted to take it.

Amanieu: I think `unchecked` is good enough. The name directs you to reading the docs.


### (new change proposal) rust.tf/libs709 *Add \`str::copy\_from\_str\` and other mutating \`str\` methods*

Chris: I think copy_from_str was possibly useful for ascii but people had concerns with the other methods.

The 8472: The ACP author replied: the other methods are for consistencies. `copy_form_str` was for the case where both strings are known.

Nia: So do we say yes to `copy_from_str` and nothing else? That's the only one with reasonable soundness guarantees that don't have as much overhead as doing something safe.

The 8472: This is for convenience because you can always handroll this with slices.

The 8472: Couldn't we make it an `IndexMut` and assigment? Would that need language support? .. Yes, it's a built-in type. Ok, a method is fine.


### (new change proposal) rust.tf/libs700 *Mapped Arc and UniqueArc*

Development/discussion in progress.

### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs287 *ACP: Add \`FromByteStr\` trait with blanket impl \`FromStr\`*

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs131 *Add \`std::fs::rename\_noreplace\`*

### (stalled change proposal) rust.tf/libs298 *Constructive/Destructive Interference Size Padding*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*

### (stalled change proposal) rust.tf/libs540 *Add Unicode XID related functionalities to \`proc\_macro\` crate*

### (stalled change proposal) rust.tf/libs322 *\`AssertThreadSafe\` (name TBD) – a more general API for lifting conservative \`!Send\` or \`!Sync\` implementations*

### (stalled change proposal) rust.tf/libs296 *ACP: Designing an alternative \`FromStr\`*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_


