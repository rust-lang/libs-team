# Libs-API Meeting 2026-07-07

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Amanieu, Josh T, Chris, The 8472, Nurzhan, Mark, Giovanni Senzani, thomcc

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

16 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/157226 *Partially stabilize \`box\_vec\_non\_null\`* - (1 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (4 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (5 checkboxes left)
  - merge rust.tf/156906 *Stabilize \`dyn Allocator\`* - (2 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (4 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[BurntSushi (2)](https://rfcbot.rs/fcp/BurntSushi), [the8472 (10)](https://rfcbot.rs/fcp/the8472), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [dtolnay (4)](https://rfcbot.rs/fcp/dtolnay), [Amanieu (8)](https://rfcbot.rs/fcp/Amanieu), [joshtriplett (7)](https://rfcbot.rs/fcp/joshtriplett), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm)

### (nominated) rust.tf/146954 *Tracking Issue for vec\_try\_remove*

Nia: This is a proposed stabilization. Was proposed a while back. The ?? happened in September. No concerns.

Amanieu: This only fails on bounds checks failure, right?

Nia: Yes. Seems uncontroversial.

Amanieu: I'll start FCP.

### (nominated) rust.tf/151379 *Stabilize \`VecDeque::retain\_back\` from \`truncate\_front\`*

FCP finished. Should be merged?

Nia (pre-meeting): I very strongly assume this is fine to just +1. We're being asked if FCP should rerun because of a post-FCP name change that we agreed on in a past meeting.

Josh: When is the branch to beta?

Nia: 1 day.

Josh: The question is, should we wait longer? It's on nightly and not on stable. We have plenty of time before this is released. We won't change the checkboxes, so there's no point in redoing the FCP.

Nia: I'll write that we'll just merge this then.

### (nominated) rust.tf/156252 *Make \`Error::type\_id()\` an inherent method on \`dyn Error\`*

Nia (pre-meeting): No longer blocked on types; we have their [blessing](https://github.com/rust-lang/rust/pull/156252#issuecomment-4852026822) to do some hack to enable non-`'static` `Any`.

Nia: Based on that, I'd reject this PR and go for the alternative approach of relaxing `Any`, making it a supertrait of `Error`.

Amanieu: From this, I infer that trait aliases work well enough that we can use them for this.

The 8472: Says the feature used for this doesn't really matter. Maybe they have a different mechanism in mind.

Josh: Trying to understand one of the proposals here. The alternative of relaxing Any is based on the assumption that ?? because Any is impleented on every type?

Nia: Correct.

Amanieu: I've started working on the compiler impl of edition-dependent resolution.

Nia: Should I reply to that effect?

Amanieu: Yes.

### (nominated) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*

Josh: There's been a lot of back and forth on this. Effectively, people asking for top-level free fn rather than trait, continued to do so. People concerned about getting a Result, continue to be so. Unsure how to move forward with this. One thing that changed is that BurntSushi filed a fallibility concern. Wondering how we make progress in terms of types of concerns that we have. How do we talk through them? GitHub doesn't help.

Nia: Zulip? A thread somewhere under libs?

The 8472: We've had an extensive discussion on Zulip. The same points have been discussed.

Nia: In that case, ping BurntSushi and point to previous notes.

The 8472: I think he doesn't participate because of time zone?

Josh: I think it's primarily availability, not time zone. Might be worth checking in on that. I'm for setting up a one-off. A separate Zulip thread would probably retread a lot of the already discussed issues. We can't do something that everyone is happy with. I think we have all the input and need to make a call.

Mark: It doesn't seem that we have all the discussion put into a doc. We could point to one to prevent repeated takes.

Nia: Josh, would you be up to making sometihng like that?

Josh: Yes.

Nia: Feel free to link to previous conversations.

The 8472: Has there been an RFC on the topic? I can't find one. Would it make sense since this is so controversial? The document could be that.

Josh: We definitely had an ACP, not sure about an RFC.

The 8472: We've had several ACPs. The RFC format seems to fit this well.

Josh: I'm more hesitant about the direction of starting over with an RFC. More enthusiastic about putting together a document covering all the designs.

Mark: RFC sounds more like a stabilization report in this situation.

Nia: We could do it as a stabilization report. I don't care which repo it's part of, as long as the document exists. Just a list of the things we've considered. would go a long way to control the ocnversation.

Josh: +1 to that. Once I have it, I'll share it everywhere.

Nia: Perfect.

### (nominated) rust.tf/157226 *Partially stabilize \`box\_vec\_non\_null\`*

Nia: Haven't we discussed this last week? We deferred. There's a concern on from_parts and into_parts. And we need to talk to BurntSushi.

The 8472: We can also rename them. The methods aren't frequently used. We can rename to something more specific. Unless there's an opposing opinion.

Nia: I'm fine with changing it to from_non_null_parts.

Amanieu: I think non_null should be the default API people go towards.

Josh: There's a pattern... I think we should decide whether we're making it a default API and this should be the canonical parts.

Nia: There was a point that the "canonical" part would be NonNull aligned pointer. Basically like a Box that could be uninitialized.

The 8472: It could, e.g., provide extra niches.

Nia: From my limited knowledge, you can express a lot of these things separately (whether a pointer is dereferenceable or ??)...

The 8472: Wasn't sure whether "aligned" is a property of ?? or when you pass the pointer as a fn argument.

Mark: I think it can be both.

The 8472: ?? the question is whether it's important enough.

Nia: We can later make from_parts an alias for from_non_null_parts, but not the other way around...

Josh: ?? NonNull is a useful property to capture...

(..)

Josh: If you want a pointer that you know to be ?? level of valid, if you want a lower-level unsafe thing, there's value in NonNull... Not sure if it's worth representing "this can't be unaligned"...

The 8472: If you have an Option of a pointer that's NonNull and unaligned, ??. From a layout perspective, it would be useful, but...

Josh: If I'm at an FFI boundary, and I'm getting a ptr from a C program, I could write that type as `Option<NonNull>`... If I have a `NonNull` guaranteed to be aligned, then I can't ??. That's where people usually reach for from_raw_parts to work around the limitation.

(..)

Nia: I'm not arguing that we need this, but there are cases outside of FFI where this could be useful. E.g., in Allocator, a user might prefer an allocation around an address. If that's memory you own, you can represent that... otherwise, you need to take an extra step of aligning it. Same benefit as pattern types, but... I see a world where we decide this, in which case this would be the canonical parts. I don't think we lose anything by calling the thing non_null_parts.

Josh: If we call it parts, we steer people towards it as the canonical repr. from_non_null_parts is more verbose and might discourage people from using it because they might not be sure if that's what they want.

Nia: Fair. In that case, would you like to invite BurntSushi to this conversation?

Josh: Will write a long comment explaining why NonNull is the canonical representation. If it turns into a back and forth, will make sense to turn into a more real-time conversation. It's worth some discussion in the thread, before changing the conclusion we'd previously come to in favor of the feedback.

Nia: Then let's continue the discussion in that thread.

### (nominated) rust.tf/157573 *std: simplify the random feature*

(skipped)

### (nominated) rust.tf/157801 *Rewrite safety requirements for \`Allocator\` impls*

Nia: This PR is a wall of text. Regarding all the promises we already make about allocator.

Amanieu: I reviewed most of it already. I think it seems fine to just merge.

Nia: Agree, but this is worth discussion.

Amanieu: We already had the discussion two weeks ago. I think we concluded that this just needs a PR review. It's just explaining what the current behavior is.

The 8472: Why can't those be expressed as trait bounds? Sounds like it's conditional only when it's cloneable...

Nia: That's what I want to do. My PR, that's blocked on this, makes this change. I want a separate AllocatorClone trait.

The 8472: OK.

Nia: On my end, this is fine. If Amanieu doesn't have more review points, we can merge.

Amanieu: I'll merge it later today.

Nia: I'll nominate my PR afterwards.

***

Nia: There's a document on allocator with possible alternatives: https://gist.github.com/thomcc/9a328dee5ad8c6e9086942fdb1e4c2b4. I'm in favor of keeping it as a slice.

The 8472: So the argument is, implementation could return ?? but this is bad for implementers and so on... This seems to refine the communication between the collection and the allocator...

Nia: Yes, basically. There are ways to get around this, like the proposal to have behavior set in stone (the issue was it didn't play well with dyn allocators).

The 8472: Having a separate query would be more expensive. For allocators that can return it, would be cheaper to return it as part of the call.

(..)

Amanieu: Last time we discussed this 5 years ago, the consensus was, if it requires a separate call, it's not worth it.

Nia: The question remains, do we want this. I'm still arguing to do it this way. In the worst case, it remains a slight inconvenience. Not many people return a length different from what was requested.

Amanieu: Actually, ?? adds a division... The actual capacity might be more than requested, and there's no guarantee that it's a power of 2 (you can only do a mask if the element size is a power of 2).

The 8472: Can it work with multiplication and maximum? No, because you don't know how much more it is. Annoying. I thought we had this code in Vec, and I don't know if it used division.

Amanieu: We have the division in the current Vec code. The division is optimized away because ??.

The 8472: We are looking at the size? I thought there was a PR that removed that.

Nia: Right now, the capacity is passed in.

The 8472: I'm looking at grow_amortized, which takes the input capacity and doesn't look at the returned pointer's capacity.

Nia: There's a comment in raw_vec:

```rust
// Allocators currently return a `NonNull<[u8]>` whose length
// matches the size requested. If that ever changes, the capacity
// here should change to `ptr.len() / mem::size_of::<T>()`.
Self {
    ptr: unsafe { Unique::new_unchecked(ptr.cast().as_ptr()) },
    cap: capacity,
    alloc,
} 
```

The 8472: But it's by a constant amount, which should turn into a multiplication by a reciprocal?

Nia: Also, tbf, I don't think this is perf-sensitive.

The 8472: Currently, we don't have a stable allocator that returns... We could change it back for things that aren't Global that could benefit from it. Or we could find a way to bypass Global and get length from... As long as we have no allocator that provides this information, Vec doesn't use it.

Nia: Ultimately, this is a design taste decision. Do people have an opinion?

Amanieu: Happy to defer the whole allocator excess topic to next time. Would like to stabilize...

Josh: Having an interface that gives you back something that most things will give you back exactly what you asked for seems excessively complicated. If you want to ask for something possibly exceeding the expectation, that should be a separate thing.

(..)

The 8472: Would people complain about vtable size?

Nia: If people complain about that, I don't know what to say.

Amanieu: It's not a problem because you won't have many allocators.

Josh: And you don't pass around vtables but pointers to them.

The 8472: I think it's overstated how rare this is. ?? jemalloc ?? would be a common case for this.

Josh: The reason we shouldn't support this is complexity, not because it's rare.

The 8472: The complexity is if you want to use it. And the allocator isn't obligated to return the extra capacity unless it's cheaper to calculate.

Josh: So your argument that the default allocator method should return the extra capacity?

The 8472: The extra capacity is provided when there's not extra effort to do so.

Nia: And it might be costly to compute the capacity without the excess (??)

Josh: ??

The 8472: It's just a pointer to a slice, and a slice has a length, so it's implicit in the return type.

Nia: Right now we return a `NonNull<[u8]>` and I think we should return a `NonNull<u8>`.

Amanieu: We also have a lot of documentation on what it means for an allocation to ??.

Nia: Because of this, the language is "you can deallocate with any size between what you asked for and what you got".

The 8472: This is also in the Vec API that says something about growth/resize not giving you the exact capacity you asked for.

Josh: Curious if doing a prototype change to make allocator return a `NonNull<u8>` ??... whether that will show in benchmarks.

Nia: There's no perf impact, I've checked; it's noise. The only scenario where this would affect something would be dyn Allocator, where we have an enforced calling convention due to the type being opaque.

(thomcc joined.)

thomcc: My weak feeling is that the roundup method solves some of the issue. But it does incur some overhead.

Nia: That would be the method to see how much space you have before you do an allocate call.

Nia: Is there a semantic difference between ??.

thomcc: Yes, there is.

(..)

thomcc: For Vec, it'd be expensive to calculate the excess every time ??.

(..)

Nia: Should there be a marker trait for whether ??

Josh: Or a const fn that ??. For example, `fn can_return_excess -> bool` with `false` as a default.

The 8472: Would also make sense to check with allocator implementers to see if they've done that and if they've seen benefits.

Nia: Vec-like collections benefit from this. Vec and VecDeque are significantly more important than other collections. Most ecosystem libraries don't require it because they're often not Vec-y.

Nia: Based on everything, I'm still leaning towards keeping the slice return type. Later we can figure out what ? means.

The 8472: Agree.

Amanieu: There's a c++ library using jemalloc API to get full object size, and that's folly (facebook's stdlib replacement or extension) they use for...

thomcc: I thought they rounded up the size.

The 8472: I think Vec rounds up as part of its strategy...

thomcc: I think the rounding up has to do with avoiding locking.

Amanieu: They're using a function called n_alloc_ex.

thomcc: That is like a roundup method.

Amanieu: https://github.com/facebook/folly/blob/9874b4aa1f971459be6c0476c88f02c7ac8485b8/folly/container/FBVector.h#L1175

(..)

Josh: Is there a concrete proposal that we need to block on before shipping what we currently have? You mentioned shipping the slice API and considering extensions later.

Nia: Do we want to leave the slice there? That's been causing grumbles because what would it mean if we changed it in the future? It will be annoying (not too bad though)

thomcc: If it's only useful for something that can be computed cheaply, we need to communicate that...

Nia: To address everyone's concern, we specify that you should only return more than is requested if that's cheap, and then leave ?? to future extensions.

The 8472: Return if cheap, and if you want a precise thing, we can add additional excess methods in the future?

Nia: Yes, there's nothing blocking these.

Nia: Everyone's equally upset, so that's a good compromise.

Josh: So we still use the attribute that ?? dyn allocator. We're stabilizing in parallel with the discussions on ??.

### (nominated) rust.tf/158271 *Add rem\_floor and rem\_ceil*

Nia: Nominated because this is being submitted instead of ACP.

Amanieu: This is weird because they make sense on signed numbers. It's useful to have. There's rem_floor on unsigned numbers that's equivalent to rem, which is confusing.

Josh: Yes. rem_ceil isn't defined on unsigned numbers becasue it would always be negative... My first reaction is, I don't think we should add these on unsigned.

Nia: Yes, this needs an ACP, aka defer this.

Josh: Close, ask for ACP, and detailed explanation of how this fits together with other signed versions.

Nia: I'd just close and block on ACP. Should I reply? OK, I'll do it.

Amanieu: Isn't this covered by the existing reply?

Nia: I'm fine with the current reply standing in that case.

(Unnominated.)

### (nominated) rust.tf/158286 *Escape additional classes of characters in \`escape\_debug\_ext\`*

Nia: Nominated for FCP. It changes unicode stuff, so it's insta-stable.

The 8472: ?? Are they unprintable?

Nia: A lot of printable ones.

Nia: I'm in favor. It makes us more compliant, and we don't guarantee ?? for Debug.

The 8472: It's not really about compliance...

Nia: Makes sense that if you normalize a string, the unnormalized bits will be escaped.

The 8472: ??

thomcc: There's multiple normalization forms. Having non-normalized strings print weird sounds bad because there are different ways to be normalized...

thomcc: Reading the issue, my gut is that it's okay. It's probably rare to have a character that needs this. I'd have to look.

The 8472: My main concern is the second point.

thomcc: I think that'd still be characters that appear in NFD, not NFC. The weird case is partially composed characters (Korean?).

(..)

The 8472: Maybe I should ask in the issue. I'll write a comment.

Nia: Should we at least agree to solve these points, deprecated characters?

The 8472: No opinion on that. I'll ask why they should be escaped.

Nia: Manish's opinion used to be that this is probably fine.

thomcc: One deprecated character would be Kelvin (K)...

The 8472: To me it'd make more sense to escape the confusables to avoid someone being tricked than some weird categories that are deprecated but don't pose problems.

thomcc: Deprecated includes things that show up in a bunch of languages.

The 8472: I wouldn't want to escape things that appear in normal text.

Nia: Let's discuss async. Then I'll let people ask their questions and let's revisit this next week.

### (nominated) rust.tf/158654 *Variance's \`PrivateItems\` assoc const is accessible in subtraits*

Nia: Another reason not to stabilize the extra `Phantom*` markers.

Amanieu: Surprised that stability doesn't trigger here.

Nia: Add to the bucket of "stability attrs are screwed".

Josh: Trait items don't have visibility. They're always accessible if you can name the trait. We have a function that's accessing the value of a type that it has access to that matches this trait becasue there's a public item that implements the trait. ?? sealed. Trying to figure out how we'd do something about this. We could mark the value itself as perma-unstable because it's sealed already...

Amanieu: Have you read [Vadim's comment](https://github.com/rust-lang/rust/issues/158654#issuecomment-4855489282)? They basically say what I wanted to say.

Josh: It'd be easy for us to make a mistake of marking things stable... There are ecosystem crates where ??. The private module should be under a perma-unstable feature, not the same feature.

Amanieu: My conclusion is that this is not an issue (due to Vadim's comment), and I don't think this necessitates an action.

Josh: I'm requesting a PR doing that.

### (nominated) rust.tf/158712 *library: Make \`ProcThreadAttributeListBuilder\` more unsafe*

Chris: One of these things should be unsafe, which one? If the fn that uses them ??, or a builder function ??. The unsafe bit is the call to spawn_??.

Nia: I think the policy has been to let people build the unsafe thing and make actually using it unsafe (??).

Josh: The question is, what is the validity invariant of ?? (ProcThreadAttribute?)...

The 8472: Not sure if it's safe to handle it even if you build it...

??: ?? lifetime.

Nia: There might be reasons to construct the "wrong" thing...

Josh: ??

Amanieu: The unsafety seems to be in the attribute method.

Nia: Worried about the unsafety being cross-cutting across multiple attributes...

Amanieu: I think that's covered by the fact if you use an attribute you should know what it's doing. ?? is outside of Rust's safety guarantees.

Amanieu: So the only change is to make the attribute method unsafe.

Chris: Will report on that.

Josh: Additionally, we may want to ask for documentation on the proc attribute list that says that it contains only correct attrs or something. We should state the validity invariant.

Chris: I see what you mean.

### (waiting on team) rust.tf/157428 *allocator: refactor for stabilisation*

(Skipped.)

### (new change proposal) rust.tf/libs825 *Add is\_non\_null() to \`\*const T\` and \`\*mut T\`*

Amanieu: Why? `!is_null()` exists. Do we have something like `??`?

Nia: You can do (alternatives).

The 8472: `.is_null().not()`

Nia: Seems like sugar that's not worth it.

Josh: Can write a comment.

### (new change proposal) rust.tf/libs824 *ACP: Add an io::ErrorKind for low\-level I/O errors (EIO)*

Josh: They want something to map the generic IoError to. Seems reasonable enough.

The 8472: What do they want it for?

Nia: ??Device, which right now is turned into Uncategorized. Someone's bikeshedding the name, but that's a detail.

The 8472: Chris, do you know if the windows thing is comparable to Unix EIO?

Chris: Could be?

The 8472: On Unix, it's very generic...

Chris: On Windows it tends to be more specific.

Nia: I think it makes sense conceptually to have them together.

Amanieu: Usually, an EIO error means a panic (not Rusty, a literal one).

Josh: The current names are a bit unfortunate.

Nia: Let's ask to bikeshed, and otherwise accept the ACP. I'll reply.

### (new change proposal) rust.tf/libs823 *ACP: IpAddr::to\_unspecified() and SocketAddr::to\_unspecified()*

Nia: Seems like a nice convenience method.

The 8472: A bit weird. It's basically constructing it, but not from scratch.

Josh: `to_unspecified` is an interesting phrasing.

The 8472: The address family ??, and this is abusing the IP address enum to express the address... The correct way to phrase it would be ??.

Nia: ??, so it's a genuine conversion.

Josh: The idea makes sense. Name is questionable. Should it be `to_` and not "give me unspecified from the same family"?

Nia: I think it makes sense because ??.

(..)

Nia: Don't know what the right response is.

Josh: The address family concept makes sense. "Domain" is confusing in the socket context because it evokes DNS. That said, I think we should have that. I do have one question: somebody mentioned "give me the same thing but on localhost" — I wonder if it makes sense to have `.set_address` and have that be unspecified.

Nia: That almost works expect that we have ipv4 and ipv6 that are ??.

Josh: Right, you can't do that without knowing the kind...

The 8472: We need dependent types for that.

Josh: +1 to shipping as is.

The 8472: Maybe it would be better as a constructor? The phrasing seems weird to me.

Nia: We can also approve the ACP and ask for bikeshedding.

The 8472: I can reply.

### (new change proposal) rust.tf/libs821 *ACP: \`impl From\<&Rc\<T\>\> for Rc\<T\>\`*

### (new change proposal) rust.tf/libs820 *Method to invert/toggle a boolean*

Josh: I can see the argument where this would be nice with a struct full of booleans. But also, meh.

Nia: I don't see much here.

Josh: Let's ask, if you have a struct with 8 different toggles in it, what would we expect people to do?

Nia: We'd expect them to rely on bitflags or something.

Amanieu: I'd write a toggle helper method.

Josh: So this exact method?

Amanieu: It'd be a free function.

Josh: You can't mix `.toggle(&mut self)` up with `.not(self) -> bool`.

Nia: I'd call it `flip` and not `toggle`.

Amanieu: These don't tell you anything about in-placeness.

The 8472: Should be `invert_mut` or something.

Nia: That's verbose...

Amanieu: I'd be happy with a one-off helper for this.

Josh: I'm observing that there's precedent in other languages.

The 8472: Does Swift have extension traits?

Nia: I see a case for extending `Not` with something like this.

Josh: Sounds more complicated.

Nia:

```rust
pub trait Not {
    type Output;

    // Required method
    fn not(self) -> Self::Output;
    
    // Provided
    fn invert(&mut self) where Self::Output = Self {
        *self = self.not();
    }
}
```

Amanieu: So this is an equivalent to `AddAssign` in a sense.

Josh: What types would that match besides bool?

Amanieu: Ipv4Address.

The 8472: Integers.

Amanieu: I don't think we should do this on the trait. Just do it on bool.

Nia: We can either reject the ACP or do it on bool.

Josh: I'm warming to the idea of having this, but I don't want to bikeshed on the name.

Amanieu: There's a language that's called it toggle.

The 8472: I'd prefer that it made it explicit that it's in-place.

Amanieu: I don't think we can realistically put in-placeness in a way that's useful.

Nia: There's no way you'd not notice it because the signature is different.

Josh: That's my thought as well, also `#[must_use]` on `Not`. Kind of fine with `flip` too.

The 8472: Can `flip` cause inference issues related to the unit return type?

Josh: Further thought: `.not` is not an imperative operation so it's not in-place. `.reverse` is imperative, so it's in-place. `.toggle` and `.flip` both work well, though I'd avoid `.invert`.

Nia: Ship it then.

### (new change proposal) rust.tf/libs819 *ACP: Remove 'static from Any itself, moving the bound to the operations themselves*

Amanieu: We're approving it, though in a slightly different way.

Nia: We approve the thing this is trying to do. I'll link from the thing we want to close.

### (new change proposal) rust.tf/libs801 ACP: add volatile operations to \`Atomic*\` types

Josh: Seeing that it was proposed by Ralf resolved 3/4 of my concerns.

Amanieu: This allows you to include memory ordering with volative load store, and notably ensures that the ?? is an atomic operation.

Josh: Slightly confused by the distinction between doc comment and...

Amaneu: I think the ordering was added later.

Josh: Looks good, doc comment needs changing.

Nia: I agree with Josh. Most concerns are addressed by Ralf being the author.

Amanieu: I have concerns that can probably be deferred until stabilization.

The 8472: ??, but I think that's for the experts than for us.

(Discussion on a separate ACP to add the inverse API. The 8472 will make that.)

### (new change proposal) rust.tf/libs816 *ACP: Add \`fN::ordinal\`*

### (new change proposal) rust.tf/libs815 *ACP: Add \`CovariantUnsafeCell\`*

Nia: We talked about this?

Josh: I think people made a reasonable case that we need this. I'd defer to the folks who argued for it.

Nia: Nominated for next week.

### (new change proposal) rust.tf/libs814 *\`OnlyNegative\` and \`NonNegative\` to compliment \`NonZero\` for signed integers*

### (new change proposal) rust.tf/libs811 *ACP: convenient numeric conversions dependent on overflow\-checks*


### (stalled change proposal) rust.tf/libs514 *Add \`Rwlock\` \`try\_upgrade\` method*

Amanieu: I just closed that one.

Josh: Based on the parking_lot experience...

Amanieu: Because of variance...

Josh: I'll take your word for it. :)

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs549 *Add \`uNN::checked\_sub\_nonzero\`*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs397 *ACP: Add \`prev\_power\_of\_two\` and \`bit\_width\` methods for unsigned integers and \`NonZero\<uN\>\`*

Josh: I think the answer is: we already added bit_width and isolate_most_significant_one, and the author seems happy with that. I'd propose closing this in favor of saying that the former covers prev_power_of_two.

Nia: Agree. +1 to closing.

### (stalled change proposal) rust.tf/libs452 *ACP: Implement \`TryFromIterator\<T\>\` trait to compliment \`FromIterator\<T\>\` trait.*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs420 *Add basic tempfile API to stdlib*

### (stalled change proposal) rust.tf/libs462 *impl fmt::Write for BufWriter*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
