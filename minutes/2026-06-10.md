# Libs Meeting 2026-06-10

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-ujepnbwg2lzqgt6wvrndwimi
**Attendees**: The 8472, Josh Triplett, thomcc, Tomáš Šedovič, Amanieu

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

6 rust-lang/rust T-libs FCPs

  - merge rust.tf/153563 *Lint against iterator functions that panic when \`N\` is zero * - (3 checkboxes left)
  - merge rust.tf/155697 *Stabilize c\-variadic function definitions* - (3 checkboxes left)
  - merge rust.tf/155499 *stabilize never type* - (4 checkboxes left)
  - merge rust.tf/132484 *unix: Relax escaping in \`Debug\` impl on \`Command\`* - (0 checkboxes left)
  - merge rust.tf/149648 *Mark const SIMD intrinsics as indirectly stable* - (4 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (2 checkboxes left)

1 rust-lang/rust-forge T-libs FCPs

  - merge github.com/rust-lang/rust-forge/issues/1040 *Add an LLM policy for \`rust\-lang/rust\`* - (6 checkboxes left)

[joshtriplett (4)](https://rfcbot.rs/fcp/joshtriplett), [digama0 (1)](https://rfcbot.rs/fcp/digama0), [Amanieu (1)](https://rfcbot.rs/fcp/Amanieu), [nikomatsakis (3)](https://rfcbot.rs/fcp/nikomatsakis), [BoxyUwU (1)](https://rfcbot.rs/fcp/BoxyUwU), [joshlf (1)](https://rfcbot.rs/fcp/joshlf), [tmandry (3)](https://rfcbot.rs/fcp/tmandry), [the8472 (2)](https://rfcbot.rs/fcp/the8472), [scottmcm (3)](https://rfcbot.rs/fcp/scottmcm), [nagisa (1)](https://rfcbot.rs/fcp/nagisa), [traviscross (1)](https://rfcbot.rs/fcp/traviscross), [spastorino (1)](https://rfcbot.rs/fcp/spastorino)



### (Prioritization Requested) rust.tf/157363 *1.97 beta regression due to \`TryFromIntError\` changing size*

Josh: Somebody's trying to transmute `()` to `TryFromIntError` and expecting it to work. But it's no longer a ZST.

The 8472: Is there a full crater report?

Josh: It was found by the 1.97 crater run. This has been fixed in 2 months but the older versions will break. This was never meant to be a ZST. If they wanted a cleaner warning it was ZST, they could have used [conjure_zst](https://doc.rust-lang.org/std/mem/fn.conjure_zst.html) and they didn't 

Josh: Looking at the fix. They now just call "u8.try_from(-1).unwrap". I think our answer is: "yes, this is an acceptable breakage"

The 8472: Yes

### (Prioritization Requested) rust.tf/157408 *1.97 beta regression due to name collision with \`highest\_one\`*

Josh: If we had put this on a numeric trait, we would have been able to use item shadowing. It's acceptable breakage. This is a github, not a crates.io crate.

The 8472: It's just a single crate, there's nothing we can do

Josh: It's someone's 2-years-old test project with four commits. I sympathise with them but I don't think it's necessary to treat this as unacceptable breakage

The 8472: Yes, so close.

### (Prioritization Requested) rust.tf/157439 *1.97 beta regression due to name collision with \`bit\_width\`*

The 8472: That's also one github project. That one seems to have a bit of activity. Did they get a heads-up already? ... No. I think we should ping them.

Josh: Their version of it is equivalent to the `BITS` associated value. I think it would make sense to give this user a heads up saying that what they're trying to do exists as `{num_type}::BITS`

The 8472: What they have is a trait, ours is a constant

Josh: If they can't use ours, they can modify the trait and use `BITS` inside

### (Nominated) rust.tf/155625 *Move \`std::io::Error\` into \`core\`*

Amanieu: the primary concern is the `Display` and other impls need to call into std. And it's implemented via a static atomic pointer in core. It should probably implemented using EII.

Josh: That's an implementation detail though

Amanieu: Yes, which is why it's nominated for libs not libs-api. I'm happy to add this for now but the preferred mechanism is EII.

Josh: I'd agree with that. Once it's possible, EII with support for defaults would be preferrable.

The 8472: That (EII with default support) has some issues on Windows

Amanieu: I think we ping Jana and ask whether EIIs are good enough to use for now

thomcc: One thing about the current implementation is that it does a pointer call and that would be great to not do for contention reasons

Amanieu: That could be changed to read if not null

Josh: IT would be easy enough to change the inline set function to be read before write.

Amanieu: Based on the comments they've considered EII. I'm not sure it's usable yet

Amanieu: I'll reply: prefer using EII, ping Jana if it's ready. If she says it's not, go with this instead

Josh: I replied suggesting read before write and suggested a perf run

Amanieu: I don't think change will have any perf impact

The 8472: This mentioned some custom Box magic. What is that doing?

Amanieu: the Error can be `Box<dyn Error>` which is only available from alloc, not core

Josh: We have historically have core some knowledge of the OS. On that same basis, I think it might make sense for some of these OS functions on raw_os_error to just move into core.

Amanieu: ErrorKind might have some dynamic allocations. But sure

Josh: I think `ErrorKind` doesn't. It's just an enum

thomcc: I'm the one who implemented the current io::Error. I don't think there's anything we can move into core without causing problem.

Josh: I agree fmt_os_error probably couldn't. But default error kind could.

thomcc: It has a lot of different variants

Josh: In practice it might make difference as a function that always exists in core and can be inlined a trivial match

thomcc: My concern would be how gnarly is the cfg block for this

### (Nominated) rust.tf/156496 *Redesigned \`Components\` iterator to use front and back indexing instead mutating and subslicing path field*

The 8472: We discussed last week. Is there anything left? I think we were talking about performance last time and wanted to see discussion. Clar Fon related my comments in the thread. I'll take a look later today.

### (Nominated) rust.tf/156743 *std: store key\-based TLS variables in a hashmap*

Amanieu: Does that mean we need a hashmap lookup on every access? That seems slow.

The 8472: It's not comparing strings, riht?

Amanieu: It's address of a static.

The 8472: How much slower that the glibc implementation would it be?

Amanieu: glibc is indexing into an array for executables. On a shared library it has to go through tls which is slow

thomcc: This is definitely worse on MacOS. This is key-based and the keys are integers. For OpenBSD pthread get specific is 1-2 hashmap lookups and they're pretty inefficient. I feel doing this on our side

Amanieu: My main concern is performance. We should ask for benchmarking for performance on all the major platforms

thomcc: I think it'll be worse on every platform

Amanieu: This is just target_thread_local

thomcc: Which platforms is it targeting, then?

Amanieu: Anything using ELF should have it. Linux should have it.

The 8472: There's a big `cfg_select!` that has additional stuff

Amanieu: It's a fallback if you don't have `target_thread_local`

The 8472: The PR is motivated by an issue that happens on the Haiku OS. I'll ask about performance and blast radius

Amanieu: I think GNU windows is the main mainstream one



### (Waiting on team) rust.tf/154715 *split try trait in twain*



### (Regressions) rust.tf/139730 `P-high` *A case of compound x86\_64 performance regression caused by LLVM 20 and #124810*

### (Regressions) rust.tf/53340 `P-low` *Performance regression in tight loop since rust 1.25*

### (Regressions) rust.tf/119560 `P-low` *\`cargo test\` performance regression on Windows on version 1.75.0*

### (Regressions) rust.tf/124216 `P-medium` *Performance regression: rustc failed to optimize specific x86\-64 SIMD intrinsics after 1.75.0*

### (Regressions) rust.tf/130320 `P-medium` *stdlib code size increase in nightly\-2024\-09\-01 for aarch64\-linux\-android*

### (Regressions) rust.tf/142119 `P-medium` *Linking a Rust staticlib unexpectedly changes C math functions from libm to bundled ones from compiler\-builtins*

### (Regressions) rust.tf/142345 `P-low` *\`pin!()\` macro no longer constant\-promotes \`&mut\` references to empty arrays*

### (Regressions) rust.tf/145424 *rustc output no longer reproducible if it crashes*

### (Regressions) rust.tf/149752 `P-high` *TrivialClone is not derived for generic types (Performance regression)*

### (Regressions) rust.tf/152942 *Query depth limit overflow due to nested async fn when building matrix\_sdk*

### (Regressions) rust.tf/153425 `P-medium` *\`&pin!()\` stopped being deref\-coerced in rust 1.88*


## Actions

- [ ] Reply to all issues/PRs discussed in this meeting, or add them to the [open action items](https://hackmd.io/Uehlc0qUQfWfvY1swYWRgQ).

_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
