# Libs Meeting 2026-08-25

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Nurzhan, Clar, bendn, Chris, Frank, David, Amanieu, Guillaume, The 8472, Josh T

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

1 rust-lang/rfcs T-libs FCPs

  - merge rust.tf/rfc3983 *\`f16b\` type* - (4 checkboxes left)

27 rust-lang/rust T-libs FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/134915 *Tracking Issue for ByteStr/ByteString* - (2 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/158186 *Guarantee 8 bytes of alignment of RawWakerVTable* - (3 checkboxes left)
  - merge rust.tf/157973 *Distinguish \`repr(C)\` ZSTs from others in ABI compatibility rules* - (4 checkboxes left)
  - merge rust.tf/158368 *std: make \`Instant\` a signed \`Duration\` on all platforms* - (2 checkboxes left)
  - merge rust.tf/161520 *Stabilize \`core::mem::DropGuard\`* - (4 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/157144 *Prevent mutating the global environment pointer in \`CommandExt::exec\` and opt to use execve and resolve path manually* - (3 checkboxes left)
  - merge rust.tf/161531 *Remove \`T: Send + Sync\` bound from \`Arc::downcast\`* - (4 checkboxes left)
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (2 checkboxes left)
  - merge rust.tf/161015 *Stabilize \`funnel\_shifts\` (including \`const\`)* - (4 checkboxes left)
  - merge rust.tf/161163 *windows: stabilise inherit\_handles* - (4 checkboxes left)
  - merge rust.tf/161312 *libtest: Allow passing \-\-test\-threads and \-\-color multiple times, with later arguments overriding earlier* - (4 checkboxes left)
  - merge rust.tf/156882 *alloc: stabilise \`Allocator\`* - (7 checkboxes left)
  - merge rust.tf/161430 *Re\-export \`core::fmt::NumBuffer\` in \`alloc\` (and \`std\`)* - (3 checkboxes left)
  - merge rust.tf/160951 *Stabilize \`core::io::{Error(Kind), IoSlice(Mut), Result}\`* - (6 checkboxes left)
  - merge rust.tf/160394 *Lang/libs coordination question: okay for libs to stabilize traits with \`final fn\` in them?* - (5 checkboxes left)
  - merge rust.tf/159564 *Allow elided ('static) lifetimes in \`thread\_local!\`* - (4 checkboxes left)
  - merge rust.tf/132484 *unix: Relax escaping in \`Debug\` impl on \`Command\`* - (0 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[nikomatsakis (6)](https://rfcbot.rs/fcp/nikomatsakis), [scottmcm (2)](https://rfcbot.rs/fcp/scottmcm), [joshtriplett (13)](https://rfcbot.rs/fcp/joshtriplett), [joshlf (1)](https://rfcbot.rs/fcp/joshlf), [dtolnay (2)](https://rfcbot.rs/fcp/dtolnay), [thomcc (9)](https://rfcbot.rs/fcp/thomcc), [calebcartwright (1)](https://rfcbot.rs/fcp/calebcartwright), [Muscraft (1)](https://rfcbot.rs/fcp/Muscraft), [traviscross (1)](https://rfcbot.rs/fcp/traviscross), [cuviper (1)](https://rfcbot.rs/fcp/cuviper), [BoxyUwU (1)](https://rfcbot.rs/fcp/BoxyUwU), [digama0 (1)](https://rfcbot.rs/fcp/digama0), [Amanieu (7)](https://rfcbot.rs/fcp/Amanieu), [the8472 (18)](https://rfcbot.rs/fcp/the8472), [lcnr (1)](https://rfcbot.rs/fcp/lcnr), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [BurntSushi (8)](https://rfcbot.rs/fcp/BurntSushi), [weihanglo (1)](https://rfcbot.rs/fcp/weihanglo), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [tmandry (3)](https://rfcbot.rs/fcp/tmandry)


### (Backports) rust.tf/161430 *Re\-export \`core::fmt::NumBuffer\` in \`alloc\` (and \`std\`)*

Nia (pre-meeting): Vote seems to be in favour.

Amanieu: I've invited Guillaume to talk about NumBuffer.

Amanieu: (+1 to the backport)

### (Backports) rust.tf/161451 *Avoid arming the Windows TLS destructor guard in fibers*

Nia (pre-meeting): Vote seems in favour but too few have voted to be conclusive. Thoughts?

Clar: This mainly affected wasmtime. If we're doing a backport, might as well include this.

Nia: If it's a bugfix that's not going to change functionality, then yes, otherwise no strong opinion.

Amanieu: It's a very obscure thing.

Clar: ??

Amanieu: Probably fine to backport considering how limited the impact is. Pretty sure it's not just wasmtime that uses ??.

### (Prioritization Requested) rust.tf/161479 *Building std for wasm32\-wasip3 fails due to missing target\_env = "p3" in library/std/src/os/mod.rs*

before meeting, Clar: I think Alex is already working on a fix for this, so, does an issue in the woods need priority if it's fixed before anyone prioritizes it?

Nia: I guess P-high because we don't want to break people, but also it doesn't matter what priority we slap because it's getting fixed anyway.

Clar: I was going to say P-low beause it's a tier-3 target, and it's being worked on.

Nia: OK. Other opinions?

The 8472: Agree with low.

### (Prioritization Requested) rust.tf/161699 *Doc links to \`std::io::Error::last\_os\_error\` fail to resolve in 1.98*

before meeting, Clar: I think this is a rustdoc bug, and not a libs issue

### (Nominated) rust.tf/160514 *Extend \`NumBuffer\` API to allow casting*

Guillaume: Basically, currently the API provides a type and iterator on integes so you can generate a string representation of an integer. The issue is the API is fixed so you can't have ??. T oaddress this, I make a PR with a generic API. That approach had suboptimal compiler errors. The other approach was to cast between numbers first...

Josh: Only an issue if it was a cast in an ownership sense. `NumBuffer<u64>` -> `NumBuffer<u8>` would make it difficult to go back. But if it's a `&mut`, then we could support that.

Guillaume: It already takes &mut.

Amanieu: Post-mono error is another concern. Should we habe the cast approach at all, or should we just allow passing any compatible buffer?

Josh: Feedback was that pass in any compatible buffer approach was painful.

Amanieu: I thought you can do it without nightly features.

Guillaume: We discussed this with Clar, and it's problematic because ??.

Clar: That was the main point of contention, either not supporting user types, or supporting user types with an unsafe trait. There's also a quadratic macro problem. It's fine for std types, but users would have to deal with that.

Josh: User-defined types will also mean it will perpetually be incomplete.

Nia: Seems like good rationale to not make it implementable for user types.

Josh: Or just basing it on size.

Amanieu: If we base it on size, it needs to be a post-mono error.

David: We'd also need to enforce that the number for trait is a ?? array as opposed to an arbitrary type.

Josh: We can use a sealed trait to enforce that the second parameter is a `[MaybeUninit<u8>; N]` for some `N`.

Josh: With nightly features we could make the second parameter the `N` rather than `[MaybeUninit<u8>; N]`

Clar: The main issue is generic const generics, which is not done and hard to use. The other option is having a way to cast a `&mut [u8]` into a `NumBuffer`. ??

Guillaume: That seems like a good approach. Need to be careful about how we expect the size to be provided. We cannot make it generic. 

Nia: How feasible is it to say that we stick to std types and not user types for the foreseeable future?

Guillaume: The big limitation currently is compiler errors, not const generics.

Nia: We could also special case the error messages.

Clar: Currently, numbuffer.rs is 120 lines, a large portion of that is the doc comment. The API is WYSIWYG. That's why converting from &mut slice would be useful. ?? The main thing to add is a way to get the capacity. The user could probably figure it out beforehand. We could document the sizes for the various std types. It would not be a bad thing to guarantee.  ??

Josh: Sounds like we have a cast strategy. We could format_into_buf if we wanted a worse error message, but...

Amanieu: We could just avoid it entirely.

David: The problem is you have to specify the buffer size every time you create a new buffer.

Josh: 

Amanieu: We can't change that as it's already stable.

David: We said we would add some kind of cast method in the past.

Guillaume: I still think it's a lot of boilerplate.

Frank: I think having the buffer figure out the size for you is one of the main points of this API. (So convenience around inference seems relevant.)

Josh: With cast, do we have to track how much of the buffer has been initialized at that point?

??: No.

Amanieu: It's a one shot API...

Josh: So you can't format into the numbuffer and then print it. You can only format and immediately get back a thing you can get.

??

(Post-mono error?)

Frank: The API question boils down to having integer comparisons. I can imagine cast method with a trait bound, and a different one with max flexibility and post-mono error or even runtime.

Josh: It's useful to avoid post-mono, and a serious tradeoff to never allow doing it from outside of std. (Examples of crates.)

Amanieu: You don't need NumBuffer for hex.

Nia: Would you agree to go for the post-mono version unstably and seeing how uch we can clean up the error messages? Maybe there's a way to avoid too many post-mono errors for std types.

Amanieu: I'd prefer adding a trait version first, since we could change that to a post-mono error backwards-compatibly.

Josh: We can't extend that to 3rd parties in a backwards compabible way. Unless we also allow 3rd parties to impl the trait, but then we run into the combinatorial issue. Do we want the path of better errors in the short term at the expense of bad extendability, or do we want the version that takes more work but can be easily extended?

Nia: I feel if we can fix the error messages then it's big progress. Josh makes a convincing argument.

Guillaume: I discussed const generics with Boxy, and Boxy said if we needed something specific, it could be fast-tracked.

Nia: How much would that fix? Where is it on the spectrum?

Guillaume: It's about allowing formatting to work on any number that would fit into the buffer but cause?? a panic otherwise.

Clar: It's not the last time that post-mono errors will be a diagnostic issue. I think that's going to be solved over time.

Josh: 3rd option: use a trait that's size-based using the unstable const generics mechanisms...

Nia: Guillaume, could you approach Boxy with your last proposal and see how feasible it would be to go ahead with the trait proposal?

Guillaume: I have 3 different impls already, and can ask Boxy which one she approves of.

Amanieu: I still think this is a low-level API that doesn't need to be user extendable.

Guillaume: To counter this, pattern types. 

Amanieu: Which are still part of the library and we can make them work with the trait approach.

Guillaume: Some are, but soon you'll be able to have your own and extend things.

### (Nominated) rust.tf/61695 *Tracking issue for \`Result::into\_ok\`*

Nia: Nominated a day ago. theemathas left comments with a proposal for stabilization. There's a stabilization PR I was assigned to. I think it's fine. The tracking issue needs updating.

Clar: Assuming it does stick this time, ! is stabilized. So this would be a reason to stabilize this too.

Nia: This is straightforward. I think it's fine. I'd just FCP merge, but if you have objections...

David: Do we still need `Into<!>`? Are there still empty enums in the ecosystem that implement `Into<!>` that we  need to support here?

Amanieu: Infallible?

Clar: I think we do need it because of that.

Nia: And we don't have a trait for uninhabited types. I'd leave it in then. Should I FCP merge?

Josh: Does this include `into_err`?

??: Yes.

(FCP merge'd.)

### (Nominated) rust.tf/129036 *Additional NonZero conversions*

Clar: This is my old PR. I just rebased it. The primary motivation is improving the APIs for CStrings. There was one conversion that I couldn't implement (from NonZero bytes to regular bytes) because of a typeck regression.

Josh: There's ??, conversions of slices of NonZero to the corresponding T, and there's TryFrom in the other direction. Is that right?

Clar: That's also listed in the description. I didn't do anything with `&mut` because it gets complicated (you can end up with UB).

Josh: Pattern types have that issue too.

Nia: We can't have NonZero around non-Freeze types, right?

Clar: No, only primitives. I need to check ?? impl restrictions ?? coherence.

(..)

Josh: This seems fine. Shall we FCP?

Nia: I'd agree. I'm typing up a merge thing. Any objections?

Amanieu: For cases like this, when a team member is proposing stabilization, should they just immediately start an FCP? Since that's the equivalent of asking the team whether something should be stabilized.

Nia: We did extend FCP privileges. Usually we deemed FCP merge stronger than...

Josh: Ask-to-ask is a bad pattern. Reasonable to just open FCP. If anyone has a concern, they can open one.

Clar: FCP close isn't available in rust-lang/rust, only merge (only RFCs have that).

Josh: https://imgflip.com/i/azmhf9

### (Nominated) rust.tf/149745 *\`DebugStruct::field\_with\` and friends are code size footguns*

Nia: Oops, this was discussed last week. We should skip

### (Nominated) rust.tf/151973 *Stabilize \`proc\_macro\_value\` feature*

Nia: We can open an FCP and then talk about this? The ask was "libs, do you want to FCP this?" Seems like all concerns were addressed. I'll open FCP.

Clar: It's also from February and not 3 years ago.

### (Nominated) rust.tf/160436 *stabilize \`Box::take\`*

FCP finished. Should be merged?

Nia: We nominated it as a proxy for a bigger discussion about &own. See RFC #4000. It's long. There's proposal with lang support about adding &own (&mut but also runs the destructor when dropped and acts like a Box).

The 8472: So owns the value but not the allocation, right?

Nia: Someone brought up that this is a box with no opt-in allocator. One question was, would Box::take be sound since it leaves in-place and MaybeUninit. The example was with a niche (e.g. NonNull) wrapped in an Option. Is it sound to take &own to the NonNull inside of the container if you match on it? ?? It would be nice if &own had the same semantics as Box... We could treat them as differnt things, but it's sad to have 2 primitives for owning pointers.

Josh: Box::take takes self, not &mut self. You can only take from a Box if you own it. Same is true for an &own. If we pretend that &own is a box with no deallocator, then you would have to own the &own to consume it. That would be consistent. The enum case, I think the bug there is you shouldn't be able to get a &own to something you can get back later after having destructed the &own unless you have a MaybeUninit or ManuallyDrop or similar that can be uninitialized. ??

Nia: Yes, I agree that reading the same thing adfterwards doesn't make sense. We currently admit code that does this (enum discriminants). It's a bug and has unsettled semantics.  ?? Thankfully, it seems like enum discriminants are the only interaction like this. We also talked about smth like write-only places.

Frank: The curernt Allocator API says you get some memory and you can write into it. And &own sounds more like "some bit patterns are not allowed".

Nia: We don't need to worry about the allocation side. I'd argue for leaving behind an invalid bit pattern (which is what Ralf seems to be arguing for too).

Clar: What you said, and we need to keep in mind the distinction that the value of a Box != &mut to a Box because &mut implies hving a valid  value for its lifetime. And if we own the box, we can do whatever with it.

Nia: Do we want to say that a Box conceptually owns the entire backing allocation, or just the value? And that Box needs to leave behind a manuallydrop or something. I think either works, but it's useful for us to agree on the conceptual.

Clar: ManuallyDrop doesn't allow arbitrary uninitialization, only allows the drop...

Nia: We could ACP a notion of a write-only place. (...) And then Box could return that.

David: does this boil down to whether `fn turn_into_box(&'static mut T) -> Box<T, NoOp>` is legal? 

Nia: Kind of.

Clar: I don't think it can ever be illegal because while you have the &mut you can't drop the value. The only exception is &mut inside the drop function.

The 8472: `fn turn_into_box(&'a own T) -> Box<T, NoOp>` ?

Nia: `fn take(boxed: Box<T, A>) -> (T, Box<Dropped<T>>, A>)`

Frank: Recent lang discussion had a discussion about dropping values that are received via `&'static mut T` references. An API that came up there was https://docs.rs/replace_with and the soundness question that comes up there was if it's allowed to drop a value `T` behind a `&'a mut T` even when you don't have an immediate replacement value available (as long as you make sure your control flow can never exit/panic/etc before placing back a value). (If you have an immediate replacement value available, mem::replace can already allow dropping things, of course.) This is a concern independent of the "niches" issue around owning references discussed above. The question of `fn turn_into_box(&'static mut T) -> Box<T, NoOp>` being sound or not that david posted above is possibly incorporating multiple soundness questions then.

Josh: The case I realized is equivalent is, we don't want to provide a safe equivalent for drop_in_place. It's unsafe for a reason. If we give you a means of getting a `&own T` or `Box<T>` no-op, then that needs to be able to call drop_in_place or you have to completely consume the object. And it needs to be unsafe or has to consume. ??

Nia: Mostly yes. It'd be hard to explain the semantics of 2 distinct types for owning pointers.

Clar: To correct myself earlier and paraphrase Frank, you're not allowed to trigger language UB (writing invalid values to pointers), but you're allowed to drop things, which doesn't cause UB if nobody observes it after that. The main issue with take with regard to 'static mut -> Box is things like leak which lets you get the 'static mut back. ?? Whether you're allowed to drop &mut T, that part is fine.

The 8472: Frank said in the chat that mutual exclusivilty only applies to Boxes with actual Allocators. (1) and (2) operate on allocators. So we could make A: not-an-allocator (remove the bound from the struct). The issue then becomes: What does Drop do? We'd need specialization inside Drop to distinguish between those cases.

??

Frank: IMO, it makes sense that you can create Boxes with a parameter that doesn't allow allocating new things. You always need to destroy your Box, how to deal with deallocation and getting rid of the pointer.

Nia: Deallocator super trait + super trait auto impl.

Clar: If we want to allow other allocators other than Global, StaticAllocator would be a fair bet on this. 

Josh: If we did say that &own = Box + noop allocator would mean a specific noop allocator, not arbitrary. We just need to say that you can get a `Box<T>` ??

??

The 8472: So no-op allocator can't be an Allocator, or it can be a static allocator if the ?? bound is sufficient?

Clar: note the idea behind StaticAllocator is mostly to be overly restrictive, we can always remove that bound later if we find it's not needed 

(..)

Frank: Specifically, the posed property is: If you have a `Box<T, A>` with an `A: Allocator`, then you're allowed to (forget the value `T` in the box), treat the box as a pointer to raw byte data of the size of `T` and write whatever data you want (including unitialized data) into that memory. This would preclude an "owning reference" `&'a own` encoded as a `Box<T, NoOp<'a>>` for owning references to enum fields (since with niches, the niches can encode the enum variant, not just the field's data).

Nia: This assumes that we want to read the enum discriminant after the fact. If we don't, this discussion doesn't matter (doesn't matter how we resolve it).

Clar: In terms of allocators, allocations (Allocator) aren't the same thing as allocation (opsem/ptr docs/provenance) 

Nia: If people are feeling we should tighten the bound on Box::take, I'm happy to open a PR for that. If ??

Clar: and the restriction is the `StaticAllocator` bound? 

Nia: Yes

Josh: 1) We shuldn't tighten take. It should work on a Box with any Allocator. 2) It's usefully instructive to look at the case of Arc AlLocator (allocate extra memory beforehand for a potential refcount, and then you can freely convert between that and an Arc). ??

Nia: Makes sense. That's my mental model as well. If people think Box::take should leave behind a MaybeUninit, I'm still happy to work with that.

Frank: I thought that existing things that reuse allocations are probably not generic over allocations.

Nia: Based on this, I'll stick to leaving Box::take as is and see how much I can get out of opsem.

??

Josh: We don't have to decide between ruling out 2 or 3. We only need to rule out 1 to ship this, and then it's up to opsem to figure out the distinction between 2 and 3.

The 8472: So no bounds?

Nia: Yeah, seems we can do it without the bounds. I'll talk to lang about this tomorrow.

David: Nia's cpunterproposal to not restrict the Allocator and change the return type from MaybeUninit to Boxed or Dropped or `Boxed<ManuallyDrop>` which would achieve the same thing...

Nia: Worth exploring in parallel. Doesn't seem like we have that API right now, and that would mean delaying stabilization of Box::take for a bit. I think this would have been a nicer approach to go from the start. Also the delta between this and the current way isn't sufficiently large to stop the stabilization process. If there's appetite for this API, happy to proceed with it. That makes my life easier.

David: I tihnk it should be mentioned in the response.

Nia: Happy to reply with that. Another point is ?? not reading the enum discriminant after the fact ?? Box::map if size is the same ?? 

Clar: +1. Right now the MnauallyDrop API is very sparse. `Box<ManuallyDrop>` would be worthless unless you wanted to make additional assumptions and do pointer arithmetic, which we don't want to encourage. That could be a valid reason to allow ManuallyDrop to put back in a new value with the assumption that it would be ok ?? forgetting ??

Nia: That's part of my rationale for write-only places. I'll reply.

### (Nominated) rust.tf/160912 *Rename \`bstr\` module/feature to \`byte\_str\`*

Clar: This is "object now or I'll merge this".

Josh: Renaming the features is fine. Renaming the module is annoying, and I don't think we should. It's useful to have a short, evocative module name. And I hope we just add these to the prelude in the future.

Clar: The main reason is because we renamed bstr to ByteStr, and modules are named after the types.

Josh: I'm sure we have cases where names don't match.

The 8472: But those are stable and we can't fix them. This one isn't.

Clar: I'd be fine with calling it bstr, but I don't see a compelling reason to not rename.

Josh: 1) I originally called it bstr and someone asked it to be renamed. 2) If you look at the list of std modules, we have no stable multiword module names. This would be the first one?

The 8472: hash_map.

Josh: This wouldn't matter if we had an assoc type `HashMap::Entry`.

The 8472: I've also referenced `Occupied`.

Clar: There's also `async_iter`.

Josh: Fair.

Clar: It'd be fair to rename it to BString instead of ByteString. There are two competing ideals here: ByteString to avoid conflicting with bstr (but we want it in the prelude), or it's not in the prelude and we name it something shorter.

Josh: This had nothing to do with the bstr crate... I'd be in favor of renaming it back to bstr. I'm not going to strongly object to strongly object to us renaming it to byte_str. It's just obnoxious that I want to hurry up and add it to the prelude so that nobody has to use that module name.

??

The 8472: Either bstr::Bstr or byte_string::ByteString, mixing it and code-golfing the module name doesn't make sense since the type name is used more often.

Josh: No objections.

Clar: I can tell Mark we talked about it. We can merge this now under the condition that if before stabilization someone convinces us to rename it back, we could.

Josh: +1

Nia: +1

### (Nominated) rust.tf/161710 *Stabilize \`mem::conjure\_zst\`*

Nia: Straightforward.

Clar: The main reason was Scott bringing up there not being a dedicated, explicit way to do this and lots of other ways. We guarantee this way and don't bother with the other methods (MaybeUninit, transmute, zeroed...)

Josh: Seems fine.

Nia: The error message being different is slightly sad, but that's not a stable promise we make. I'll start FCP.

### (Nominated) rust.tf/libs789 *ACP: \`std::time::StopWatch\`*

Chris: To have Instant work differently depending on the clock... Currently it supports nanosecs, you can go back in time before the clock started, whereas Stopwatch is narrowly focused on performance.

(..)

bendn says:would Instant::elapsed still be useful after this? 

Clar Fon says:we would probably want to deprecate a few Instant methods 

Clar Fon says:or clippy lints 

The 8472 says:Just to avoid taking 2 clocks at the same time. Instant is "good enough" for performance most of the time 

The 8472 says:It only matters once you go below ~milliseconds 

bendn says:well you cant do arithmetic on these 

The 8472: What I mean is if you're using Instant for timestamping and want to print "step took n ms" too then you don't create both Instant + StopWatch 

(..)

Josh: How bad would it be if Instant were generic, and `Instant<Perf>` just didn't do some things? It's mentioned as an alternative.

Nia: Not sure how I feel about restricting the API based on the clock too much. I thought that the clock is an implementation detail.

Josh: If we made Instant generic over the clock, then Instant over perf clock doesn't have to implement any of the methods Stopwatch doesn't have.

Clar: ACPs are to prevent wasted work. Maybe we want to do the work? Focusing the ACP on insatnt being optimized for comparisons, and stopwatch for subtractions.

The 8472: What Josh said earlier about making Instant generic, and then making ?? depend on it. Part of the problem is that other PRs refactoring Instant right now ?? chaning the internal repr of Duration ?? We might have to hack something with a union if we wanted to do both.

Josh: Storing ?? in a different type makes sense.

(..)

Nia: Sounds like there's enough to accept the ACP. We'll see if this works well with the duration reworks and etc. I'll accept the ACP. We can note down that we'd like to figure out the interactions.

Josh: Document the requirements on duration that the internals are optimized for ??

### (Nominated) rust.tf/libs859 *ACP: Remove \`T: Send + Sync\` bound from \`Arc::\<dyn Any + Send + Sync\>::downcast\<T\> where T: Any + Send + Sync\`*

Nia: This has an implementation PR. I'm tempted to just accept this. Objections?

Clar: I don't think we need an ACP for this.

Josh: Does Send + Sync still flow through the downcast?

Nia: Yes because you're downcasting to a concrete type. There's no loss of Send or Sync.

Josh: Seems fine.

Clar: Send and Sync are always there, ??

### (Nominated) rust.tf/161612 *std: make a lot of items crate private*

Nia: This is one of the clippy lints. Would like to see a more concrete reason for this.

Clar: The reasoning is, I received a PR for Pattern. There were some things that were more public than needed. It's intrusive to ??, requiring pub and complicated parens paths to everything. It's unclear if it's helpful. Because there was breakage, I wanted to talk about it, but it seems like it's too much and we don't want to deal with it.

Nia: I'd say ?? semver-checks ?? so no to this change.

Josh: I kind of loved this change, but I'd maybe not delete unreachable pub afterward.

Nia: My worry is noticeable visual noise. It's not too bad.

Clar: To counter the stability argument, there are still some bugs where stability attrs don't propagate correctly, and this makes it more obvious that if something is public, you know there's no way that it could need a stability attribute.

The 8472: But we have cargo-semver-checks, and those holes are limitations that should be fixed. Like the module reexport thing we recently discussed. Can this be applied on a mod level...?

Clar: To clarify, this was a breakage with a PR for the pattern API, unverified ??

Clar: I don't think people like this enough to merge.

Josh: I liked the idea, but if others don't, let's not do it.

Nia: I'm ok with this, slightly negative because I don't find it helpful and it's 0.1% hindering.

Clar: I think we have this enabled in core and alloc, but not std.

Nia: That changes things.

Clar: And we added this for hashbrown recently, but they are different cases (not a lot of target-specific code). std has a lot of target-specific code, and having to write pub(crate) in many places isn't very useful.

Nia: In that case, I'd leave this to your discretion.

Clar: So the meeting has delegated authority to my opinion.

Nia: I don't speak for everyone else, but I delegated my decision to you.

### (Nominated) rust.tf/libs864 *Extra tracking labels for libs team*

Nia: See #t-libs/meta thread.

### (waiting on team) rust.tf/154715 *split try trait in twain*

### (waiting on team) rust.tf/156882 *alloc: stabilise \`Allocator\`*

### (waiting on team) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*

### (waiting on team) rust.tf/161153 *Unstable attribute behavior on re\-exports is surprising*



### (Regressions) rust.tf/139730 `P-high` *A case of compound x86\_64 performance regression caused by LLVM 20 and #124810*

### (Regressions) rust.tf/53340 `P-low` *Performance regression in tight loop since rust 1.25*

### (Regressions) rust.tf/119560 `P-low` *\`cargo test\` performance regression on Windows on version 1.75.0*

### (Regressions) rust.tf/124216 `P-medium` *Performance regression: rustc failed to optimize specific x86\-64 SIMD intrinsics after 1.75.0*

### (Regressions) rust.tf/130320 `P-medium` *stdlib code size increase in nightly\-2024\-09\-01 for aarch64\-linux\-android*

### (Regressions) rust.tf/142119 `P-medium` *Linking a Rust staticlib unexpectedly changes C math functions from libm to bundled ones from compiler\-builtins*

### (Regressions) rust.tf/142345 `P-low` *\`pin!()\` macro no longer constant\-promotes \`&mut\` references to empty arrays*

### (Regressions) rust.tf/145424 *rustc output no longer reproducible if it crashes*

### (Regressions) rust.tf/149752 `P-high` *TrivialClone is not derived for generic types (Performance regression)*

### (Regressions) rust.tf/152942 *Query depth limit overflow due to nested async fn when building matrix\_sdk*

### (Regressions) rust.tf/153425 `P-medium` *\`&pin!()\` stopped being deref\-coerced in rust 1.88*


### (new change proposal) rust.tf/libs862 *ACP: Add a downcasting method for \`Arc\<dyn Any\>\`*

### (new change proposal) rust.tf/libs861 *ACP: Add \`Unsize\`\-based downcast methods to combat combinatoric explosion*

### (new change proposal) rust.tf/libs860 *ACP: \`impl From\<usize\> for u{64, 128}\`*

### (new change proposal) rust.tf/libs857 *add a way to reuse a \`Box\` allocation*

### (new change proposal) rust.tf/libs856 *ACP: O\_DIRECTORY open flag on OpenOptionsExt*

### (new change proposal) rust.tf/libs855 *ACP: wrapping/checked/strict/unbounded funnel shifts*

### (new change proposal) rust.tf/libs853 *Implement \`Write for Cursor\<W\> where W: AsMut\<\[u8\]\>\`*

### (new change proposal) rust.tf/libs851 *Short Duration constants*

### (new change proposal) rust.tf/libs850 *ACP: Add Instant::epoch()*

### (new change proposal) rust.tf/libs849 *Tuple \<\-\> Array conversion helpers*


### (stalled change proposal) rust.tf/libs523 *Add a deterministic constructor for \`RandomState\`*

### (stalled change proposal) rust.tf/libs191 *Add LocalWaker support*

### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs124 *Integrate \`Error\` trait with panic interfaces*

### (stalled change proposal) rust.tf/libs363 *Un\-specialize impl ToString*

### (stalled change proposal) rust.tf/libs298 *Constructive/Destructive Interference Size Padding*

### (stalled change proposal) rust.tf/libs365 *ACP: \`TwoSidedRange\` trait*

### (stalled change proposal) rust.tf/libs210 *Add associated consts to f32, f64 for mathematical constants*

### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_