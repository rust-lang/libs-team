# Libs Meeting 2026-08-11

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Clar, Mark, Nurzhan, Chris, David, Josh T, The 8472, Amanieu

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

1 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3983 *\`f16b\` type* - (5 checkboxes left)

19 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/134915 *Tracking Issue for ByteStr/ByteString* - (2 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (3 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (3 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/160136 *Add \`Default\` implementation for \`std::sync::Once\`* - (3 checkboxes left)
    - Nia: Looks straightforward; missing one checkbox.
    - The 8472: Do we also stabilize the const?
    - Nia: This leaves it const_unstable.
    - Clar: const traits are unstable.
    - The 8472: That makes it easier.
    - ??: I don't see how we could implement it differently.
    - Clar: If we enabled Clippy, it'd lint on having `new` but not `Default`.
    - The 8472: What's the policy on unstable trait and ??.  I have an open PR where I used it, and I found it limiting.  (??)
    - Nia: That'd be nice.
    - Clar: Isn't the unstable trait mechanism for unstable bounds on stable traits?
    - The 8472: [The PR I have](https://github.com/rust-lang/rust/pull/160404) has a stable trait and unstable implementation. The macro uses `unstable_feature_bound` on the impl itself.
    - Clar: Assuming that works like how I think it does, I'd be down to do that in the future.
    - The 8472: The problem is virality, which makes it impossible to dogfood.
    - Nia: Worth pinging t-compiler. Do you think we should use it for Default here?
    - The 8472: It just came to mind when we looked at insta stablilization. For more complex traits, I think it'd be worth it to start with unstable impls and stabilize them later.
    - Nia: Happy to accept that as a policy for complex cases.
    - David: I think it's no longer the case that unstable trait implementations are unstable... (See `Pattern` trait.)
    - Clar: I forgot about that, yes.
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (2 checkboxes left)
  - merge rust.tf/160108 *Stabilize \`windows\_process\_extensions\_main\_thread\_handle\`* - (3 checkboxes left)
  - merge rust.tf/160534 *stabilize smart pointer map functions* - (3 checkboxes left)
  - merge rust.tf/160394 *Lang/libs coordination question: okay for libs to stabilize traits with \`final fn\` in them?* - (5 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[dtolnay (3)](https://rfcbot.rs/fcp/dtolnay), [the8472 (13)](https://rfcbot.rs/fcp/the8472), [nikomatsakis (3)](https://rfcbot.rs/fcp/nikomatsakis), [lcnr (1)](https://rfcbot.rs/fcp/lcnr), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [Amanieu (10)](https://rfcbot.rs/fcp/Amanieu), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [joshtriplett (8)](https://rfcbot.rs/fcp/joshtriplett), [BoxyUwU (1)](https://rfcbot.rs/fcp/BoxyUwU), [BurntSushi (4)](https://rfcbot.rs/fcp/BurntSushi)

### FCPs

12 rust-lang/rust T-libs FCPs

  - merge rust.tf/158186 *Guarantee 8 bytes of alignment of RawWakerVTable* - (3 checkboxes left)
    - Nia: no concerns
    - The 8472: Did we ask async folks if they have any? I don't know what people are doing with `RawWaker`. I guess additional guarantees won't break anyone.
    - Nia: We'll just be making a promise.
    - ()
    - Josh: Slightly torn between declaring that the actual requirement is being pointer-aligned vs. saying who cares about 2-bit platforms, just make it 8. I feel like 2 is winning.
    - The 8472: could we ask Embedded?
    - Josh: Embedded mostly uses 64 bit these days.
    - Nia: Happy to leave a note asking the author to ping Embedded. The point was to go through the easy FCPs.
    - Josh: There's a ping group for embedded-wg. At least there should be.
    - Nia: Couldn't find one.
    - The 8472: There's a wg-embedded in a [separate organization](https://github.com/rust-embedded/wg).
    - Mark: I'd ping the leads of the embedded WG.
    - Nia: Happy to reply with that.
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (3 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (3 checkboxes left)
  - merge rust.tf/157973 *Distinguish \`repr(C)\` ZSTs from others in ABI compatibility rules* - (4 checkboxes left)
  - merge rust.tf/158368 *std: make \`Instant\` a signed \`Duration\` on all platforms* - (3 checkboxes left)
  - merge rust.tf/157144 *Prevent mutating the global environment pointer in \`CommandExt::exec\` and opt to use execve and resolve path manually* - (3 checkboxes left)
  - merge rust.tf/160564 *volatile: allow accesses to non\-AM memory to trap* - (3 checkboxes left)
  - merge rust.tf/160436 *stabilize \`Box::take\`* - (3 checkboxes left)
  - merge rust.tf/160108 *Stabilize \`windows\_process\_extensions\_main\_thread\_handle\`* - (3 checkboxes left)
  - merge rust.tf/160229 *Extend \`dropping\_{references,copy\_types}\` lints to \`drop\_in\_place\`* - (3 checkboxes left)
  - merge rust.tf/160534 *stabilize smart pointer map functions* - (3 checkboxes left)
  - merge rust.tf/132484 *unix: Relax escaping in \`Debug\` impl on \`Command\`* - (0 checkboxes left)

[joshlf (2)](https://rfcbot.rs/fcp/joshlf), [tmandry (2)](https://rfcbot.rs/fcp/tmandry), [Amanieu (5)](https://rfcbot.rs/fcp/Amanieu), [BurntSushi (3)](https://rfcbot.rs/fcp/BurntSushi), [digama0 (2)](https://rfcbot.rs/fcp/digama0), [joshtriplett (8)](https://rfcbot.rs/fcp/joshtriplett), [the8472 (7)](https://rfcbot.rs/fcp/the8472), [thomcc (2)](https://rfcbot.rs/fcp/thomcc), [saethlin (1)](https://rfcbot.rs/fcp/saethlin), [nikomatsakis (2)](https://rfcbot.rs/fcp/nikomatsakis)



### (Prioritization Requested) rust.tf/160603 *SGX \`UnsafeList\` is unsound, \`WaitQueue\` can execute UB*

Nia: Looked at this briefly. Tempted to say this is a more high...

Josh: +1 for Medium. Woudn't call soundness issues low, but in terms of our inclination to do something about it...

??

Josh: High = close attention, critical = drop everything, medium/low = two flavors of "whatever".

??

Clar: It's unfortunately P-high...

Josh: This seems to be the same issue as the one from 3 years ago, which we're not treating as high.

??

Clar: I don't think we have a priority per team (P-libs-high), only per issue.

Nia: We could say libs is unlikely to deal with this, despite the work done on it.

Clar: Lang have specific drag labels, which we could adopt, but it's unnecessary for now.

Nia: Let's say P-high and reply that we want to wait more.

### (Prioritization Requested) rust.tf/160793 *Inappropriate \`panic!\`s in \`Thread::new\` on certain unix platforms*

Nia: Big deal; being worked on. High or critical? Every platform is susceptible, but the code is only triggered on certain ones.

The 8472: The fact that it doesn't happen in practice (or not) should factor into whether it's critical.

??: I think it's high.

(Reentrancy issues with Global allocator...)

Nia: We have a guarantee that small parts of the library won't touch the global allocator...

Clar: If you want your allocator to be portable at all, you'll want to use std. And we offer that. If we can't, it's going to be harder to impl allocators.

Chris: Do we need to add caveats to our guarantee that this is best effort?

??

Nia: Happy with high.

Josh: I expect this to be straightforward to fix.

Nia: It's getting fixed.

Chris: I'm concerned about any other issues lurking.

### (Prioritization Requested) rust.tf/160815 *\`Condvar\` and \`Mutex\` in \`std::sys::pal::unix::sync\` violate aliasing rules*

Mark: These are the same thing.

Nia: This is the same code. Has an open PR, looked like it was going the right way. Should both be P-high? To me, both are.

The 8472: Sounds good.

### (Prioritization Requested) rust.tf/160837 *More global allocator reentrancy issues in \`park\` and \`unpark\`*

Josh: More reentrancy issues.

Clar: Added A-global-allocator-reentry label so we can tag these

(Same as above.)

### (Nominated) rust.tf/57391 *Tracking issue for duration\_constants*

Nia: oli nominated this. Seems trivial.

Clar: Agreed.

Nia: I've tried typing out these constants in the past.

Clar: The only bikeshed is (something about days being between 23 and 25 hours).

Nia: Doesn't propose that.

...

Josh: We should've shipped this years ago, but we can do it now.

Mark: David raised concerns in 2019, are they relevant?

The 8472: `max` is already stable? Why is it here?

Nia: Probably the issue hasn't been updated.

Josh: I saw a recent comment on this saying "we should have Duration::ms"

Nia: That sounds like an ACP.

Josh: Yeah, just saying we should start stabilizing them.

The 8472: david's concern from 2019
https://github.com/rust-lang/rust/issues/57391#issuecomment-468103225
 
The 8472: archive link: 
https://triagebot.infra.rust-lang.org/gh-comments/rust-lang/rust/issues/57391#issuecomment-467966013
 
Clar: Re-exporting associated items (import millisecond as ms...)

Nia: Any objections to the FCP?

David: All my concerns were resolved.

The 8472: Something on documentation wasn't resolved?

Nia: Seems like it's being documented.

The 8472: About the constants themselves...

David: The concern was that the role of documentation is to establish the rationale behind the constant, not how to use it.

Josh: If you do add that test, please include the rustdoc directives to say this should be compiled, tested, but not run-tested. Otherwise tests will take 5 seconds.

The 8472: We might want to restart the RFCbot...

Mark: We could leave a comment saying we're happy to see a stabilization PR and then we'd kick off an FCP on it.

### (Nominated) rust.tf/160766 *Stabilise \`abort\_immediate\`*

Nia: We need this for abort in core that doesn't trigger the panic hook. There were concerns: naming (panic=abort_immediate or immediate_abort, which is not the same thing). The other was about behavior: the doc I proposed was "what this does is unspecified, just end the program as quickly as possible without calling a panic hook". Abort hooks and signal handlers might still be called sometimes, so maybe we should mention that in the docs.

Clar: On signal handlers, we don't have a good notion of what we guarantee if you add a single signal handler. We should probably put our foot down here and say we don't guarantee a particular signal happening. ??

Josh: "On some platform it's not possible to catch this; it might be, but don't".

Amanieu: When would you use this instead of rt abort?

Nia: No libc when no std.

Amanieu: Do we want this or do we want to panic that doesn't unwind?

Nia: There are use cases for this where RT abort might be unsafe.

Amanieu: panic handler should deal with these...

Nia:

Mark: ??

Nia: panic=immediate_abort calls std abort instead of abort_immediate...

Clar: No way to call intrinsic abort stably if you redefine the panic handler.

The 8472: Giving a distinct name to them would be good for searchability.

Amanieu: I feel like in most of the cases you want panic no unwind.

Nia: the naming is confusing. `panic=immediate_abort` invokes `std::process::abort`; `core::process::abort_immediate` invokes `intrinsics::abort` 

(..)

Amanieu: Once we have EII, we should just make the panic handler overridable.

Nia: It's worth offering a "crash and burn" intrinsic for core because there's no other way to abort.

Amanieu: You shouldn't be able to call rt abort from core.

Clar: `panic_nounwind` isn't exposed stably. People know that the internal machinery of abort_immediate exists, and usually it's an illegal instruction. To encourage portable code, having it as an intrinsic would help. Also, to implement a custom panic handler, you'd need access to it.

Amanieu: If it's a wrapper around the abort instrinsic, sometimes it lowers to a call to the abort fn in libc.

Clar: TIL. That should be documented.

Josh: 

Nia: 2 possible semantics for "lower to std process abort opportunistically, otherwise crash and burn" vs. "always crash and burn, do not attempt libc abort". Either would be useful for my use case.

Josh: +1.

Clar: also just to follow up on where we use this in core: I'm fairly certain that the places where we would use this, we just do panic_nounwind. I think at one point there was `intrinsics::abort` only in the ub checks, but it's now panic_nounwind 

Amanieu: In stack smashing situations, you still want to print a message to the user before aborting. libc/glibc does this.

Josh: Debatable.

simulacrum: I went looking and -Cpanic=immediate-abort calls the instrinsic (https://github.com/rust-lang/rust/blob/a04c7a037d59b6dedc9f921e84e391f13a257aa4/library/core/src/panicking.rs#L62). This is the same mechanism...

The 8472: https://github.com/rust-lang/libs-team/issues/442  proposed both abort and immediate

Nia: Seems like there are 2 directions. Amanieu, do you believe that there'd be value to "try to call libc abort and fallback to not doing it if unavailable"?

Amanieu: it's the job of the panic handler to decide how a message is printed and how the process is aborted

Nia: How does PH abort if in a ?? enviroment?

Amanieu: Depends on the env.

Nia: ??

Amanieu: We could have a helper fn under `core::arch`.

Josh: I don't think `core::arch` ... architecture specific.

Amanieu: It is arch-specific.

Nia: Do you think we need distinct arch-specific aborts, but not a generic one?

Amanieu: Anything calling libc shoudn't be in core. It should be a decision made by the panic handler. And it should be documented.

The 8472: People will ignore this.

Amanieu: Then they'll have aborts with no messages.

Nia:

Amanieu: There's a different solution, no_unwind attr on a fn or a panic guard.

Nia: If you do that, panic hooks could be called.

Amanieu: Don't install bad panic hooks.

Nia: Caller can install bad panic hooks.

??: Setting panic hooks is safe, so safe code could trigger UB...

()

Josh: Can we agree that we should have, under some name, the instrinsic for "die without ceremony", that compiles to a byte inst?

The 8472, Nia, Amanieu: +1

Nia: Would like for it to be cross-arch.

Amanieu: `core::arch` has some cross-platform stuff.

Nia: Perfect.

Chris: Windows fast fail?

Nia: There was some discussion in the thread.

Chris: It's an immediate abort.

Amanieu: ~3 instructions. You set some values in a register and do an illegal instruction. The kernel recognizes that as an abort rather than a crash.

Josh: Makes sense.

Nia: To summarize, `core::arch::*_abort` with explicit documentation. We're fine with shimming it to something like fast_fail, if it has access to the host env. What do we nae this? `abort_immediate` makes sense if we want to keep using the intrinsic of abort. Or `crash`/`die` (don't like `crash` because it implies structure).

Amanieu: On Linux we could raise SIGABORT using raw syscalls.

??: The only promise is that this will kill the process.

??: The most important thing is that this will not call into the panic handler, so it's safe for use in the panic handler impl.

Nia: I'll write a comment.

The 8472: function documentation:
https://media.istockphoto.com/id/1018231684/photo/statue-of-battling-titan-with-a-dagger-at-the-main-gate-of-hradcany-castle-in-prague-czech.jpg?s=170667a&w=0&k=20&c=NCRJDtlqqF0VFA3QKu-knC5s46CskEOFWY2q-aQO1cc=

### (Nominated) rust.tf/117693 *Tracking Issue for \`core\_io\_borrowed\_buf\`*

Nia: Concern from waffle.

Amanieu: It's resolved. There's a concern abour variance?

Clar: I'm reviewing a PR about variance.

Nia: waffle reached out to me privately saying that it doesn't believe that most of these concerns have been fully addressed.

Josh: ?? Starting a stabilization on `core::io::BorrowedBuf` and `ReadBuf`, splitting those FCPs and talking about them separately. There was enough discussion to cause confusion...

Nia: Should we put a concern on the FCP (it's completing tomorrow) saying...

Amanieu: We should raise a concern of waffle's behalf.

Clar: We could cancel the FCP here and then do it on the stabilization PR.

Nia: This makes more sense, I think.

Josh: I was mostly saying that we should separate the FCPs for `BorrowedBuf`/`BorrowedCursor` and `ReadBuf`.

Clar: I'm pro cancelling the FCP since raising a concern would cancel it anyway.

Josh: Works for me.

Amanieu: This is a good chance to switch to FCPs on stabilization PRs only.

Josh: +1 for doing that across teams.

Amanieu: How does someone know when it's appropriate to submit one?

Nia: I submit one when it seems like there's not much being discussed anymore. Stabilization PRs aren't that much work. If there's more to do, it should be done separately anyway.


### (Nominated) rust.tf/154046 *Tracking Issue for \`alloc::io\` and \`core::io\`*

Nia: Nominated 5 days ago with a big comment.

Clar: This is bushrat commenting that since we've merged core::io/alloc::io, whether we'd consider stabilizing them. I think it's reasonable to stable alloc::io. We can't move some traits to core. For alloc, there don't seem to be outstanding issues. One of them was Error repr on 16-bit targets — I merged a PR for that. Don't think there's anything concerning about satbilizing these besides Error.

Josh: Looks like only types are being proposed for stabilization, not traits. +1 for that. We can decide later on the rest.

Nia: Yeah, looks fine to me too.

Clar: Wouldn't be the first time we've decided on ??. There was a concern about OS-related things in IO, but I think we've gotten past that (if target includes OS, then core will include OS).

Nia: impl Error references the Error trait, which isn't marked for stabilization. Nevermind, we stabilized it. Should we FCP this or ask for a PR, given we have a thorough report?

Josh: Given our new policy, I think we should ask for a stabiliation PR.

Clar: I assume they have it written already and just haven't submitted it.

### (Nominated) rust.tf/154065 *Stabilize \`fN::BITS\`*

Nia: That's a big regression (crater).

Josh: It'd definitely be breaking. It's officially allowed, and the question is whether we're comfortable with it.

Clar: we would probably want to  coordinate with crate maintainers

The 8472: Didn't we notify the crate maintainers?

Josh: I think we have, a while ago.

The 8472: So it's just float math and ...

Nia: Sometimes people define their own thing, maybe with different types.

Nia: I'd give a cautious go ahead.

??: FCP has completed already.

Josh: Given. the FCP is completed, and we've communicated this to crates, we should just go ahead and merge this. We'll catch things from beta crater runs.

The 8472: Maybe the float MASK might be new.

Josh: The git repo no longer exists. This crate isn't used as a dependency.

Nia: Happy to comment +1 from us.

### (Nominated) rust.tf/158580 *std: map ENOTSUP to ErrorKind::Unsupported*

(Stale, in FCP.)

### (Nominated) rust.tf/160108 *Stabilize \`windows\_process\_extensions\_main\_thread\_handle\`*

(Stale.)

### (Nominated) rust.tf/160514 *Extend \`NumBuffer\` API to allow casting*

Nia: Looks new.

Amanieu: This is assigned to me for review. I nominated it. I don't remember why.

Nia: We can unnominate.

David: This impl has the issue that it will succeed in cargo check and fail in cargo build, right?

??

Amanieu: The old impl used the trait bound. The new one doesn't. I'll review this. Let's skip it.

David: Using the same integer casting trait bound?

Amanieu: Maybe not; it cares about sign changes. I haven't decided yet.

### (Nominated) rust.tf/160705 `P-lang-drag-1` *Remove \`From\<!\> for T\` \*reservation\* impl*

Nia: I've been following this vaguely and think this is a good idea.

Clar: The libs ask is, are we ok with this? The concern is about not having the reservation for `Infallible`.

Josh: It also turned out that the reservation impls had holes.

The 8472: Wasn't the idea that if anything overlaps with `!`, it's still `!` and can only be unreachable, so it doesn't matter?

David: It's possible to write code that will not compile if this impl starts to exist. I wonder if anyone's written such code, though.

??: There is such a case linked in the PR.

Nia: I'll say +1 with a slight sigh that it's broken.

Clar: I can comment on that.

Josh: I don't get the impression that types are saying this is too hard to do. It's more like, we're not aware of benefits that would be worth the difficulty.

Nia: We would like to have it, but...

Clar: I've replied.

### (waiting on team) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*

The 8472: What's the status?

Josh: Slowly making progress on some side items people requested. Not treating it as urgent. Attempting to have it used `fill_buf` instead of `fill_bytes`.

### (waiting on team) rust.tf/159963 *core: implement FusedIterator for StepBy*

### (waiting on team) rust.tf/160436 *stabilize \`Box::take\`*

Nia: This needs 1 checkbox for FCP.


### (Regressions) rust.tf/139730 `P-high` *A case of compound x86\_64 performance regression caused by LLVM 20 and #124810*

### (Regressions) rust.tf/53340 `P-low` *Performance regression in tight loop since rust 1.25*

### (Regressions) rust.tf/119560 `P-low` *\`cargo test\` performance regression on Windows on version 1.75.0*

### (Regressions) rust.tf/124216 `P-medium` *Performance regression: rustc failed to optimize specific x86\-64 SIMD intrinsics after 1.75.0*

### (Regressions) rust.tf/130320 `P-medium` *stdlib code size increase in nightly\-2024\-09\-01 for aarch64\-linux\-android*

### (Regressions) rust.tf/142119 `P-medium` *Linking a Rust staticlib unexpectedly changes C math functions from libm to bundled ones from compiler\-builtins*

### (Regressions) rust.tf/142345 `P-low` *\`pin!()\` macro no longer constant\-promotes \`&mut\` references to empty arrays*

### (Regressions) rust.tf/145424 *rustc output no longer reproducible if it crashes*

### (Regressions) rust.tf/149752 `P-high` *TrivialClone is not derived for generic types (Performance regression)*

### (Regressions) rust.tf/152942 *Query depth limit overflow due to nested async fn when building matrix\_sdk*

### (Regressions) rust.tf/153425 `P-medium` *\`&pin!()\` stopped being deref\-coerced in rust 1.88*


### (new change proposal) rust.tf/libs850 *ACP: Add Instant::epoch()*

### (new change proposal) rust.tf/libs849 *Tuple \<\-\> Array conversion helpers*

### (new change proposal) rust.tf/libs848 *ACP: Variadic \`core::cmp::{min, max}\`*

### (new change proposal) rust.tf/libs847 *map on homogenous tuples*

### (new change proposal) rust.tf/libs846 *ACP: Add BTF relocation macros to core*

### (new change proposal) rust.tf/libs844 *Add methods for bounds\-checked raw slice pointer indexing to \`SliceIndex\` and slice\-like types.*

### (new change proposal) rust.tf/libs842 *Setting a \`File\` non\-blocking*

Josh: This had some back and forth and comments from 8472. Wanted to confirm tif you're ok with additional flags be separate ACPs.

The 8472: Feels very piecemeal.

Josh: Do they need to block this ACP?

The 8472: No, it's fine.

Josh: The motivation was writing a small rust program and finding that the only thing besides std was the ability to pass a non-block.

(..)

Nia: Looking at this, I like it. I'd second it.

Clar: Can we use rfcbot for seconding?

Nia: Someone can just set the label.

Josh: compiler does; they might be using triagebot for it. I'm +1 for having automation for this.

Chris: Should we have a 10 day delay?

Nia: We put it as advisory, but there are cases where you can wait less, e.g. if discussed on zulip thoroughly or is trivial/could be easily reverted.



### (new change proposal) rust.tf/libs841 *Per\-thread panic hooks*

### (new change proposal) rust.tf/libs840 *Trait for safe all\-zero byte initialization (v2)*

### (new change proposal) rust.tf/libs838 *Add \`rem\_euclid\_unsigned()\` for signed integer types*


### (stalled change proposal) rust.tf/libs476 *Parse IPv6 zone identifiers*

### (stalled change proposal) rust.tf/libs124 *Integrate \`Error\` trait with panic interfaces*

### (stalled change proposal) rust.tf/libs420 *Add basic tempfile API to stdlib*

### (stalled change proposal) rust.tf/libs549 *Add \`uNN::checked\_sub\_nonzero\`*

### (stalled change proposal) rust.tf/libs298 *Constructive/Destructive Interference Size Padding*

### (stalled change proposal) rust.tf/libs523 *Add a deterministic constructor for \`RandomState\`*

### (stalled change proposal) rust.tf/libs331 *Support permanently skipping tests on a specific system*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_