# Libs-API Meeting 2026-07-14

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Nurzhan, BurntSushi, Amanieu, Josh T, Urgau

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

17 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/157226 *Partially stabilize \`box\_vec\_non\_null\`* - (1 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (4 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (5 checkboxes left)
  - merge rust.tf/156906 *Stabilize \`dyn Allocator\`* - (2 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (4 checkboxes left)
  - merge rust.tf/146954 *Tracking Issue for vec\_try\_remove* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[the8472 (11)](https://rfcbot.rs/fcp/the8472), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [Amanieu (8)](https://rfcbot.rs/fcp/Amanieu), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [dtolnay (4)](https://rfcbot.rs/fcp/dtolnay), [joshtriplett (8)](https://rfcbot.rs/fcp/joshtriplett), [BurntSushi (3)](https://rfcbot.rs/fcp/BurntSushi)

### (nominated) rust.tf/libs811 *ACP: convenient numeric conversions dependent on overflow\-checks*

Nia: We looked at this last week.

Amanieu: These two ACPs are follow-up work from a previous one ([#788](https://github.com/rust-lang/libs-team/issues/788)), which added a [set of integer casts](https://github.com/rust-lang/rust/issues/157388) (explains).

Clar: https://github.com/rust-lang/rust/issues/154330
 for widen casts 

Josh: We also have widen and narrow that we added.

Amanieu: I feel like narrow is superseded by the integer casts, which can widen or narrow.

Josh: The biggest problem with `as` is that it's a massive hammer. I think having more specific methods is better. 

The 8472: We discussed it, didn't we conclude that the methods are specific enough that they're unambiguous?

Amanieu: Widening is useful on its own. Don't think there's value in narrowing ones. The main issue with `as` is accidental data loss. With the new cast methods, you explicitly say how you want to handle data loss, which IMO is sufficient.

Josh: Data loss is part of it, but knowing what type you're going to get and constraining it for ease of reading is valuable. If I call `.cast()`, I expect ??. If I call `narrow`, I expect ??.

Clar: Just to Clarify, saturating/warppping/strict/unchecked... but also truncating/widening, and a third one (signed/unsigned). There isn't necessarily interplay between the three.

Josh: One of the things is value-preserving... Arguing that there should also be a version that doesn't change the signedness.

Amanieu: Disagree. Don't think it's worth duplicating API surface for such a minor difference. The important part is value-preservation.

Josh: The result will not necessarily be correct if something goes negative and overflow checks don't catch that.

The 8472: These things are turbofishable, so you can always make the target type explicit.

Josh: The specific reason this comes up is, (FFI/C stuff).

Clar: This feels like a lint issue more than an API one. As Amanieu said, you're going to run into a locality issue in the code...

The 8472: There's not necessarily a converting back if you're doing FFI...

Josh: +1.

Clar: I think linting could still help prevent that. We'd need to be careful with implementing that (?)

Josh: Indexing slices with u8... as long as it's value-preserving, bounds checks ??. The argument against it is, at indexing, you want to be warned about using the wrong type... We could have array types for which you could set the index type.

Josh: Not arguing against general casts. Just arguing that those shouldn't be the *only* thing we have.

Nia: Would you like to write an ACP for that? And then we can see how the proposals interact.

Josh: We have an ACP for several of these and have talekd through them. The issue is, what combo do we want to stabilize.

Nia: That makes sense. In that case, IMO we should have the broad cast, probably in combo with widen/truncate. I don't see the point in rejecting this, they go well together.

Amanieu: We've already accepted the ACP on saturating/etc casts. The question for this new ACP is, should we add a generic cast which when overflow_checks is enabled, is strict and otherwise is wrapping?

Nia: My stance is yes, we should.

Amanieu: My concern is people will think that cast implies wrapping.

Josh: People think that today about operations that break with overflow_checks.

Clar: Later today it will be helpful to collect all the existing methods into a meta tracker so we can all look at it and choose. It's 5-7 issues now, which is hard to look at.

Nia: That'd be useful, if you could prepare that for next week.

The 8472: Maybe a table including intersections with overflow_checks and stuff...

Clar: Would be helpful to switch my attention to this for a while.

### (nominated) rust.tf/libs810 *ACP: Missing floating point conversions*

Josh: Floats are so screwed up that applying any reason to them is futile. So we might as well...

Amanieu: There are 5 different ways of converting floats to integers. Not counting overflows, exceeding max, etc... Rounding toward ±inf, away/towards 0, to nearest even/odd(rare), ... Currently, `as` does towards 0.

Nia: Towards 0 feels like the nice one? And for the rest, use a library?

Amanieu: There are uses for all of these.

Josh: I don't mind having other casting modes, but we should have a default, which `as` does.

Clar: Would the compiler optimize a float-to-float rounding followed by an int cast?

Amanieu: I think it does, but not sure.

Nia: Sounds like it could be made to do that.

Clar: Maybe with a MIR optimization?

Josh: On a related note, clearly when we go to add all the various restricted-range types, like `NonNegative` and `NonNan`, clearly we should also cater to Rust's market and start an `OnlyNans`.

(..)

Nia: awful idea: const generic on to_int with a RoundingMode 

The 8472: I would love more of those to combat API explosion, but we need argument position const for that 

Nia: or, have that be a different method where to_int is implemented as `to_int_with<RoundingMode::ToZero>(val)`

Clar: Maybe float-float rounding conversion + exact-int method

Amanieu: Rounding to int where it doesn't fit into int. Currently it saturates. Given that they saturate, do we still want all these variants?

The 8472: Checked makes sense if you want to make sure it fits.

Clar: `1024.0.to_int_checked::<u8>() = None` makes sense 

Nia: Yeah, something like that.

Josh: Should we have to_int that either does strict or saturating depending on overflow_checks?

Nia: Proposal: `to_int` like `as`, and then `to_int_{strict, checked}`?

Amanieu: Personal inclination is to not have any of these, only `to_int` that saturates.

Josh: Mine is `to_int` with `overflow_checks` behavior.

Josh: Thoughts on the motivating example in the ACP (#811)?

Amanieu: Float-to-int conversions are not round-trippable and always give a valid value.

Josh: Why should that be the only thing we provide? This feels like the opposite argument you gave on integer conversions.

Amaneiu: I changed my mind. You're right.

Josh: Also curious, should we do the same in reverse if you do to_float, or should to_float return an infinity? `u64::MAX.to_f32`

Amanieu: I think infinity is correct.

The 8472: `.cast_finite()` 

Josh: I'm saying, should it apply to `to_float` in principle?

Amanieu: I don't think it makes sense. Float has limited range but infinite precision.

Clar: How about to_int doesn't cast at all? f64::to_int gives you an i64, etc? This gets rid of a lot of the discussion...

Josh: Even f32 can represent values wildly bigger than i64.

Clar: Note than converting to an integer... e.g., f64 to an integer, you expect an integer in a safe range... and you'd map infinity to i64::MAX. Past that safe range, you're just estimating, not really converting to an integer.
    
(..)

Clar: I would expect overflow checks to just check outside the safe integer range, not saturating or rounding (but rounding non-integers in that range) 

(..)

Nia: We have some agreement on to_int for floats. We can discuss to_float further.

Josh: I'm becoming convinced by Amanieu's argument. ?? so I think that's less of a concern given how unlikely it is.

Nia: Should we respond on the issue and move on?

Amanieu: Are we happy with the casts? Approving 811? 

Josh: We talked about to_int, which we're approving. Do we want `cast` for float-to-int?

Amanieu/Nia: `cast` is just for ints.

Josh: Ship it.

Nia: On 810...

Amanieu: We haven't discussed float-to-float conversions. I think we're happy with to_int, and happy with int-to-float (Josh: allowing to go to infinity).

Clar: float-to-float using default rounding method seems fine 

Josh: Is the default rounding mode for floats...

Amanieu: We enforced nearest. (There's hardware support for it.)

Josh: Ship it.

Amanieu: Not a fan of `to_float` (it's already a float).

Nia: ?? (cast?)

(Agreement on cast.)

Nia: Approve 810 and 811 with renaming float::to_float to cast?

(Yes.)

Nia: Happy to reply to both.

### (nominated) rust.tf/libs815 *ACP: Add \`CovariantUnsafeCell\`*

Nia: We discussed this a while ago. I'm leaning towards shipping it. There were concerns about ContravariantUnsafeCell, and where this leaves UnsafeCell. I think this is fine because even in the worst case we want to add ContraUnsafeCell, ?? is just an impl detail, so we'd have three types (Contra, Co, and normal UnsafeCell).

Clar: contravariant unsafe cell seems *extremely* cursed 

Josh: Did we have a use case for ContravariantUnsafeCell?

Nia: No, that's purely for completeness.

Josh: Yeet it into the sun?

(Something about `Sync`?)

Nia: Either we make CovariantUnsafeCell or have a default parameter on UnsafeCell.

Clar: CovariantUnsafeCell is fine.

Nia: What about Sync?

Amanieu: The only reason people want SyncUnsafeCell is putting stuff in statics...

Nia: You can trivially achieve it with ??.

Amanieu: LazyLock would impl Sync itself...

Nia: So we don't care about Sync.

Amanieu: Could we just have unsafe static that allows non-Sync?

Nia: That's called static mut.

Clar: Isn't there an RFC for that?

Nia: IDK.

Clar: https://github.com/rust-lang/rfcs/pull/2937

Nia: Let's say Sync is a non-issue and ACP accepted.

Clar: Ship it

The 8472: Seems suboptimal to make this part of the type if this could be done in a compositional manner (phantom)...

Nia: The only way to do that is to make the bivariant unsafecell... which is not okay because bivariance is a type system hole. And we don't need the contravariant case, or you can hack your way into it.

Amanieu: I'm inclined to accept as proposed.

Nia: I'll reply that we +1 it.

### (nominated) rust.tf/134915 *Tracking Issue for ByteStr/ByteString*

Nia: We should do this early

BurntSushi: AFAIK, we're blocked on whether this should `impl Display`. If you have it impl'd then you have `to_string`, which causes problems because it's lossy. Also related to `from_str`. Josh, do you agree?

Josh: 1) The original intended plan for ByteStr was introducing a new type (already heavy) and add many useful fns for conventional UTF-8 operations. I don't think it's unreasonable to stabilize the type before wee solve that. 2) For Display, I think that if we don't impl Display for ByteStr, we lose 3/4 value of having it.

BurntSushi: Disagree — I think Debug adds that value there.

Josh: Won't argue with that. Debug impl not being Vec's impl is useful. But I think Display makes it comfortable to deal with ??.

Nia: I assume Debug renders valid UTF-8 and escapes everything else?

Josh: Correct. ??

BurntSushi: bstr replaces invalid UTF-8 with the ?? codepoint.

BurntSushi: On Display, I agree with Josh, but the problem is the `to_string` method call. This is the same difficulty as `Path`.

Josh: I thought of that too. Since we often think "Display is fine but to_string isn't", maybe it makes sense to deprecate ??, and emit a warning? We can't break it, but we could deprecate ToString if it resolves to ByteStr's or Path's lossy impl.

BurntSushi: I think serde has ?? for building error messages.

Nia: I wonder if this will need type system hacks...

Josh: We could ping types and ask them to ??. "Keep a list of types for which this is the case, and when to_string resolves to that type, emit a warning"

The 8472: to_string is already specialized, so we can have something specialized for ByteStr?

Nia: Can we specialize and deprecate that?

The 8472: Spec happens at monomorphization, and you probably don't want to warn at that time.

Josh: A simpler thing: I don't know what TS does when you do this, but if you add an inherent method... ??

Amanieu: I think the private method will not work.

Nia: I'd suggest we ping types and see if they can help with some hack. And we can add the Display impl later, right?

BurntSushi: True. If we add the type, you'd probably want to display it, but there's Debug.

Josh: To be roundtrippable, Debug needs to escape backslashes. You don't want that for Display.

Nia: Right.

BurntSushi: Elaborate?

Josh: IIRC, Debug for string-likes prints them in a pseudo-roundtrippable way so you can get the same thing if you feed the result back.

BurntSushi: But usually you don't consume Debug output, humans do.

Josh: For ByteStr, we need some way to display it (not debug). No one should impl it by hand.

BurntSushi: Can we add that later? Are you saying the minimally viable stabilization thing needs Display?

Josh: Would be disappointing if we gave users a type that you can't usefully display in any manner. You'd have to handwrite it or not use the type.

BurntSushi: I don't think I've used ?? on bstr at all.

Nia: This hinges on the point that we need ToString to be roundtrippable. Do we really want that? I think it's fine...

Josh: ToString has never been roundtrippable. Display never made any guarantee like that. Neither did Debug, but we tried to anyway.

Nia: So we can just relax the policy of ToString being roundtrippable, only something that needs to display the same.

Josh: Not even that, see [this](https://doc.rust-lang.org/nightly/std/str/trait.FromStr.html#input-format-and-round-tripping). It's only meant for human consumption.

BurntSushi: Can we drop the FromStr impl, then?

Josh: I'd expect FromStr to work by taking an arbitrary str and turn it into a bstr.

The 8472: It could be a From?

Josh: We can put that off.

The 8472: FromStr can error. Converting str -> bstr is infallible, so it can be From.

Clar: `From` is required, `FromStr` is optional imho

Clar: I was going to reply to BurntSushi's comments on the type, but haven't yet. I'm not convinced that conversion is difficult. Having some conversion between bstr and ?? might be worth it to make Deref work. Is the plan to start an FCP after this meeting, or can this be done later?

The 8472: We'll probably need more than one Display impl. One that does question marks, one that escapes, maybe more. Do we want one canonical one + alternatives, or do we want to treat them as equal?

Nia: That'd require wrapper types.

BurntSushi: I think the replacement codepoint needs to be the canonical one. (Josh, Nia: +1)

Josh: 1) The 8472, I agree with BurntSushi that we want a canonical one. Other ones would be useful, and for them I'd want `.escaped()` or something, but it shouldn't be the default. Most of the time, people want to just print it and not explode if it's not UTF-8. 2) Clar, I think we'll want to try to FCP this soon-ish, but there are potentially multiple issues to address. Whether we can agree on a Display impl is only one of the issues.

Clar: I think that the canonical one being replacement characters is very reasonable, whether it's via a `display` method or `Display` impl is not necessarily an issue imho

Amanieu: On FromStr, last time we discussed it, I had the position that it's fine that FromStr doesn't roundtrip, but it has to parse correctly.

Josh: Display of ByteStr should always reparse, but not necessarily roundtrip.

Amanieu: So I think we should have a FromStr impl. ??

Nia: To summarize, seems mostly wee're on board with a Display impl. Is anyone against a FromStr? If yes, we can solve that later.

BurntSushi: I prefer no FromStr, we can discuss async.

The 8472: Same.

Josh: I want it, but would like to try stabilizing without it first.

Nia: Josh, can you FCP?

Josh: Yes, but I'd like to resolve the concern on opaque wrapper. Proposal: BurntSushi wants to have an async conversation with Clar on that detail. Can I start FCP, and if your discussion leads to ??, raise a concern?

BurntSushi: Sounds good.

BurntSushi: If we have this impl on ByteStr, we should have a way to issue a deprecation warning.

Josh: Yes, we should direct people to to_string_lossy or something.

(after a while)

Josh: I realized there's another blocker for stabilization.

Nia: Uh oh.

Josh: Currently, ByteStr derefs to its contents, so we're prevented from adding methods that shadow those.

Nia: Clar was about to talk about that.

Josh: We have to make an opaque wrapper or methods that shadow those.

Clar: Let's talk about that async.

Josh: Worth bringing up that it's a blocker. Might start FCP and file a blocking concern.

Nia: I think that's right.

Clar: I was typing a reply, will post later. If you want to reference that as a blocking concern, that's fine with me.

Josh: The main issue is I want this to be easy for people to use. `ByteStr` being similar to a `Vec<u8>` is convenient... Should ask BurntSushi how he handled that in bstr. Might have realized how: Historically, bstr has been an opaque type, and he rewrote it to deref to a Vec of u8. And there's a trait that provides all of the methods. Not sure if we want to use that structure, but it would solve the problem.

Clar: This is essentially what I wanted to write. Rust 1.39 is connected to the bstr rewrite (from opaque to not opaque)... It's been 7 years, and std changed since. I'll write it up.

Josh: Let's take it async then.

### (nominated) rust.tf/146954 *Tracking Issue for vec\_try\_remove*

Nia: This is in FCP. Not much to discuss.

(Unnominated.)

### (nominated) rust.tf/149103 *Tracking Issue for \`#!\[feature(ptr\_cast\_slice)\]\`*

Amanieu: Request for stabilization?

Nia: A few questions are remaining.

Clar: The main thing blocking this IIRC is similar to some other ptr cast... issues with inference on methods with pointers, and issues with method deref (interfering with Receiver work).

Clar: this one was blocked on similar concerns: 
https://github.com/rust-lang/rust/issues/74265

Clar: I think the main issue on these is because it's from slice to non-slice pointers.

Nia: Do these block each other?

Clar: Still says there's inference concerns, not sure if it's up to date.

Nia: Other than that, the other issues are...

Amanieu: Isn't everything PointeeSized? What's the difference from Sized?

Nia: cast_slice methods work on Unsized and not MetaSized...

Amanieu: Can't we defer figuring this out until later?

(..)

Nia: Do we const-stabilize? Policy says no, but IMO it's fine to. slice::from_raw_slice is const-stable already, so it makes sense for this to be too.

Nia: Where docs should redirect to...

The 8472: Whether we should duplicate or link.

Nia: I don't think it's for libs-api. I suggest we reply with, inference failure is intentional and fine, const is okay, ??, and docs are up to reviewer taste..

Clar: And if we're super worried we can bother opsem.

### (nominated) rust.tf/156906 *Stabilize \`dyn Allocator\`*

Nia: I suspect this might end up straightforward. Use cases blocked by this... (not going to do assoc type/consts). If this is the case, I'd motion to stabilize dyn allocator because the alternatives are worse.

Josh: The only real downside here is if we commit to dyn Allocator, then anything along the lines of "if allocator behaves like this, do that" has to rely on const fn rather than assoc const. And we can't use assoc types, but we agreed on a specific error type.

The 8472: const fns aren't possible in traits?

Josh: They're not stable, but will be. (something about const and dyn intersection)

Nia: If you enable fat LTO, you can get devirtualization and const unfolding with dyn.

The 8472: Doesn't dyn Allocator impl Allocator?

Nia: Yes.

The 8472: And what happens if ???

Nia: We're not const-stabilizing Allocator, so you can't get dyn Allocator in const context.

Josh: We can defer all of that because ??. We have a way of solving the problems assoc const would have solved.

Josh: Ship it.

Amanieu: Did we solve Clone and PartialEq issues?

Nia: Yes. Conclusion is different marker trait (Clone is always unsound, dyn has nothing to do with it).

Amanieu: Link to discussion?

Clar: the discussion is here: 
https://github.com/rust-lang/rust/pull/157428

Josh: We can't have an impl of a safe trait that depends on an unsafe trait ??

(..)

Nia: I'll update my refactoring PR for it(?) to be a dyn trait.

Nia: So we can resolve the concern about impl Clone.

### (nominated) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*

Nia: We should do this early

Josh: There's been a lot of discussion on whether we should do this at all. Three points of disagreement. 1) Should we handle errors? 2) What about targets that don't have rng? 3) What about interfaces like uninitialized_bytes and similar? Excluding "What about a free function?" because that seems like a derived concern, because people are asking for a free function in order to bypass the other concerns slowing stabilization of Rng/SystemRng.

Josh: On errors, I don't think there's anything stopping us from adding additional APIs for error handling. ??. If every instance of "rolling a D6" involves error handling, it gets cumbersome (e.g. for gamedevs). We should optimize for the common case.

Clar: games are a bit of a poor example since those tend to be quite averse to panics, but your example makes sense 😛 

BurntSushi: getrandom can build on top of what we want to stabilize. From discussion with them, sounds like they need a fallible interface to be able to do it. Josh, I agree that having an infallible interface is what people want most of the time and is useful sometimes. Why can't we stabilize both?

Josh: Part of the issue is which aspect of fallibility do we want to handle. Nobody argued that we should have an API for handling the case of "do you want an RNG". On linux there are various failure modes where you might not have randomness available. Everyone agreed on "let's have an API for that". We also don't want to make people handle errors that are incredibly rare (will probably never happen).

The 8472: ??

Josh: That infects every interface you call from it. The issue is that any interface derived from an ENG would need to decide if it's deriving from a panicking or non-panicking interface. They'd need to propagate the Result. ??

Clar: I've been thinking, what does the Rng trait offer over an adapter that has read? Right now, from that perspective, maybe we could add an adapter that implements Read, and then add Rng on top of that. These concerns should be answered by that. The main issue is whether we want to do that first.

BurntSushi: Rand has the same thing. It's built on top of getrandom and has fallible and infallible (which I usually use) interfaces. rand has proven successful in that regard.

Josh: Agree that it's possible. It's more about the extent to which the traits should be stacked on top of each other vs. be separate traits. People may want to build weird third-party RNGs that are variously fallible (e.g. fuzzer streams where the rng can fail if you exceed the allocated random data). rand's current structure with an assoc type and Rng: TryRng isn't viable because we want to support Rng for things that can fail. ?? We should document that SystemRng can't fail after the initial call.

Clar: it's `TryFrom` versus `From` all over again… 

BurntSushi: Sounds like we mostly agree? I have to leave, but want to make sure getrandom can built on top of what we have.

Josh: The only item that's still an issue is handling targets without Rng, about whether we can replace the SystemRng. ?? on the other end, don't replace SystemRng because then it's untrusted. My argument would be, we don't have to settle it immediately. We don't have to commit to either.

BurntSushi: I still have a concern registered on that. It might have been weakened, but I need to drop.

Nia: I assume discussion will happen async.

### (nominated) rust.tf/157226 *Partially stabilize \`box\_vec\_non\_null\`*

Nia: We should do this early

Nia: This was a naming point, whether we want from_parts or from_non_null_parts. There's a general sentiment that we'd like NonNull to be the natural thing to reach for. There's a world where in the future we'll want NonNull + aligned pointer. In that case, that name would bite us.

BurntSushi: There's potentially other ways to do this construction. Seems like having from_non_null and into_non_null_parts would be better, but it seems folks are convinced NonNull is canonical...

Josh: from_raw_parts is used extensively in FFI. It's common in boundary fns. I think if we give these a longer name, we'd be discouraging people from using it everywhere, but it's a better type to use at the boundary...

BurntSushi: Do you think it'd change if we got a type that also described alignment?

Josh: You won't be able to have that type be a canonical type for many pointers... Can't have `Option<NonNullAligned>` that...

Clar: presumably the type that reflects alignment would be `&'unsafe T` and not `NonNullAligned<T>` (syntax bikeshed) 

The 8472: You could have some pattern type where unaligned pointers are ??

Josh: ??

Josh: I was going to suggest adding a type for a pointer with everything except alignment (?)... That shouldn't stop us from saying that for the time being, NonNull is the canonical repr (can change over an edition later?). Handling null at FFI boundaries is common, ?? is less likely to happen accidentally... 

BurntSushi: I'm convinced.

Josh: Thanks for talking this through. If we didn't have a mechanism planned for migrating over an edition, I'd be more convinced by your argument.

### (nominated) rust.tf/157573 *std: simplify the random feature*

Nia: I think we can unnominate this because we're not going to do this?

Clar: I think the part about removing the trait is misunderstanding the problem...

Nia: Fair. I'll unnominate and reply.

Amanieu: Yeah.

### (nominated) rust.tf/158286 *Escape additional classes of characters in \`escape\_debug\_ext\`*

Josh: I thought we responded with +1 with some extras. Looks like there's some back and forth... Manish responds saying that the categories listed are fine, and....

The 8472: Part of the question is if we're going in the right direction. Escaping things that normal keyboards would emit could make text unreadable. The conversation stopped at some point, so what do we do now?

Josh: Feels like we need to answer the overall question of what we're trying to do with Debug.

The 8472: Seems like this is what Manish is saying too(?)

Josh: We know we want to escape things like `\r\n`, control characters... Jules proposes escaping everything that's not normalized, and the back-and-forth is on whether we want that.

The 8472: ??

Clar: The deprecated characters thing was probably fine because I don't think keyboards should be outputting those...

(Grapheme extenders?)

The 8472: Those are needed for emoji and maybe some Asian languages.

Josh: We already do some mangling... The family emojis consist of combinations of other emojis, that's why people joke about Rust breaking up families.

Clar: Maybe the emoji thing is a bit weird, but I'd push that if this is text you can see, we probably shouldn't escape it unless it will not affect the character (e.g. newline, zero-width space but not joiner).

Josh: I believe we already mangle non-English languages in other ways, e.g. Arabic.

The 8472: Is it totally unreadable or just ugly to the native reader?

Josh: Something in between, makes things incorrect. I get the impression it's noticeable.

The 8742: Then maybe we shouldn't be doing that.

Josh: On the one hand, Debug is meant to be a useful mechanism for... If I'm debugging something with Arabic in it, I don't want normal text to be mangled, but I do want control characters escaped. There are multiple use cases here. Which should you just get with Debug, and which should require an additional call?

Clar: The purpose of Debug is to output text that hopefully will not completely destroy the terminal... not, check for control characters that affect text rendering ??

The 8472: We can look into the history of this escaping, see if there are security implications or something like that, but for internationalization, we should avoid mangling scripts as much as we can.

Josh: Mostly agree, but my concern is that it's annoying if escape debug doesn't tell me about a zero-width joiner in the middle of my string.

The 8472: Maybe we could do the unicode confusion attack thingy where it tries to detect the surrounding script/mixed scripts?

Josh: Concrete proposal. This is nominated and waiting for FCP. This feels sufficiently solved to start an FCP. I don't think we will come to a conclusion here. We should have some general principle we should follow and try to follow it, probably in consultation with Manish. We should invite him for next week?

Nia: Makes sense.

Clar: Maybe we should consider ACPs that are just MCPs for the libs team because it might be hard to discuss even if we have an expert like Manish in the meeting.

Nia: Fair. I don't know what the right approach here, other than "needs more discussion"

Clar: Just defer to a future problem (?)

Nia: ?? and when there is a proposal, renominate it. There are conflicitng use cases, and I don't think there's a clear correct choice here.

Clar: I think Debug should find the bugs and tell what's wrong with the code for me

Josh: Clearly, Debug should escape emojis that represent insects :D

Nia: Let's defer and let people discuss.

Josh: I'll write a comment.

### (nominated) rust.tf/158654 *Variance's \`PrivateItems\` assoc const is accessible in subtraits*

Amanieu: This, I think, is resolved?

### (waiting on team) rust.tf/158271 *Add rem\_floor and rem\_ceil*



### (new change proposal) rust.tf/libs829 *Introduce something akin to \`Zip\` iterator but one that doesn't truncate the longer iterator*

### (new change proposal) rust.tf/libs828 *Separate \`Components\` iterator implementation into non\-prefixed & prefixed versions*

### (new change proposal) rust.tf/libs827 *ACP: add \`unsafe trait PureRef\<U\>\` to guarantee purity of the AsRef/Deref/Borrow traits*

### (new change proposal) rust.tf/libs826 *Add \`core::mem::size\_of\_field!\`*

### (new change proposal) rust.tf/libs821 *ACP: \`impl From\<&Rc\<T\>\> for Rc\<T\>\`*

### (new change proposal) rust.tf/libs819 *ACP: Remove 'static from Any itself, moving the bound to the operations themselves*

### (new change proposal) rust.tf/libs816 *ACP: Add \`fN::ordinal\`*

### (new change proposal) rust.tf/libs814 *\`OnlyNegative\` and \`NonNegative\` to compliment \`NonZero\` for signed integers*

### (new change proposal) rust.tf/libs810 *ACP: Missing floating point conversions*

### (new change proposal) rust.tf/libs808 *Type\-smart \`intersect\` on ranges*


### (stalled change proposal) rust.tf/libs314 *Add security\_attributes() to windows::OpenOptionsExt*

### (stalled change proposal) rust.tf/libs363 *Un\-specialize impl ToString*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*

### (stalled change proposal) rust.tf/libs457 *APC: split\_pattern on slices*

### (stalled change proposal) rust.tf/libs462 *impl fmt::Write for BufWriter*

### (stalled change proposal) rust.tf/libs452 *ACP: Implement \`TryFromIterator\<T\>\` trait to compliment \`FromIterator\<T\>\` trait.*

### (stalled change proposal) rust.tf/libs191 *Add LocalWaker support*

### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
