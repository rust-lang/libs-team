# Libs-API Meeting 2026-02-17

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Amanieu, The 8472, David, Nurzhan Saken, Tomas Sedovic, Chris Denton, Josh

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

2 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3874 *build\-std: always* - (11 checkboxes left)
  - merge rust.tf/rfc3892 *Complex numbers* - (5 checkboxes left)

15 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/139087 *Fallback \`{float}\` to \`f32\` when \`f32: From\<{float}\>\` and add \`impl From\<f16\> for f32\`* - (3 checkboxes left)
  - merge rust.tf/99301 *Tracking Issue for \`error\_generic\_member\_access\`* - (2 checkboxes left)
  - close rust.tf/136638 *warn on empty precision* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`Exclusive\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/111688 *Tracking Issue for ExitCodeExt on Windows* - (4 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/119639 *Tracking Issue for tcp\_deferaccept* - (3 checkboxes left)

[Muscraft (1)](https://rfcbot.rs/fcp/Muscraft), [Amanieu (5)](https://rfcbot.rs/fcp/Amanieu), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [dtolnay (2)](https://rfcbot.rs/fcp/dtolnay), [joshtriplett (6)](https://rfcbot.rs/fcp/joshtriplett), [0xPoe (1)](https://rfcbot.rs/fcp/0xPoe), [estebank (1)](https://rfcbot.rs/fcp/estebank), [traviscross (1)](https://rfcbot.rs/fcp/traviscross), [Mark-Simulacrum (1)](https://rfcbot.rs/fcp/Mark-Simulacrum), [oli-obk (1)](https://rfcbot.rs/fcp/oli-obk), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [the8472 (10)](https://rfcbot.rs/fcp/the8472), [epage (1)](https://rfcbot.rs/fcp/epage), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [weihanglo (1)](https://rfcbot.rs/fcp/weihanglo), [tmandry (1)](https://rfcbot.rs/fcp/tmandry), [BurntSushi (7)](https://rfcbot.rs/fcp/BurntSushi), [nikomatsakis (3)](https://rfcbot.rs/fcp/nikomatsakis)

### Project goals with unsafe guidelines

Josh: There are 2 goals proposing unsafe code guidelines. I tried to get them to reconcile. Pete just sent a patch to help clarify. One goal was to explore unsafe patterns and cross reference them. Patterns for synchronization, memory mapping, atomics etc. And goal 511 is to improve the documentation and guidelines in the standard library.

T8: The std one is about how we use unsafe?

Josh: I think it could affect how we do safety comments for libs and libs-api too. But I doubt they'll produce so many pull requests we'd have issues processing them. And if it is we could rate limit them.

T8: The `pal` module contains a lot of syscalls with many unsafe blocks without comments. It would require quite a bit of effort to document.

Josh: We could use the support level in the goal to say how much support we're willing to give them. There might not be enough bandwidth to cover the whole std, but enough to do a handful of places.

T8: You should tell them that.

Amanieu: one of the goals is not merged yet.

Josh: Because we haven't weighed in yet. If/when we do, it'll get merged and accepted.

Amanieu: Sounds good.

### (nominated) rust.tf/99301 *Tracking Issue for \`error\_generic\_member\_access\`*

FCP finished. Should be merged?

Amanieu: We need to clarify that we're moving this forward as it is.

T8: Did we reach a conclusion on which methods to have in the trait?

Amanieu: Yes.

T8: Is there a PR on that?

Amanieu: The MultiProvider thing.

Amanieu: I personally don't want it, but don't know how people feel.

T8: We should close it, if someone wants it, we can add it on nightly.

Josh: I agree. It's good to see that this can happen, but we don't want it right now. If people have a use case for multiprovider, we have the code. For *today's* use cases, it seems overengineered.

Amanieu: So we close the PR with a possibility of reopening?

Josh: Yes, with the possibility of iterating on this attempt.

T8: will reply to the multi member access PR

Amanieu: will reply to the tracking issue

### (nominated) rust.tf/143801 *Tracking Issue for \`core::cell::{Ref,RefMut}::try\_map\`*

Josh: We probably deferred here because we couldn't decide on Try vs Result?

Amanieu: No, that was different. This had two ACPs open. One's closed, the other's accepted. It can't use the Try trait because it's modifying the Error type. The Residual lets you to modify the success type.

Josh: If you want to augment the type you have to use a concrete Result. I am confused, or the ACP is confused on the API it's suggesting.

Amanieu: The ACP one does not return Self. It returns the same error type as the closure without returning Self.

Josh: In https://github.com/rust-lang/libs-team/issues/586 shows different result from the closure

Amanieu: Seems to be a typo.

Josh: both seem wrong to use impl Try because. Both should take the same generic saying that the closure takes `Try`  and returns the same `Try`

Amanieu: We have something similar for MutexGuard. There we have map and filter_map. For RefMut we have map, filter_map, try_map and map_split. I would then expect having try_map on MutexGuard as well.

Josh: I'd expect so.

Amanieu: filter_map is stable on RefMut but not MutexGuard. It has a closure returning Option, and parking_lot has a function returning Result with Self. We're stabilizing what's in there.

Josh: I'm confused, the ACP has a different signature.

Amanieu: But it matches what's in the tracknig issue.

Amanieu: There are two choices for the API shape. One is how it already is the closure always returs the `Result` and the outer result is the `Try` trait, the other: using the Try trait for the closure, the outer Result then only returns .. and you lose the RefMut.

Josh: another: not for RefMut but for Ref we can not bother returning the original reference with Err. You can just clone it if you need.

Amanieu: It is ref-counted, so making copies is no-op (probably gets optimized). We could .. weak guard?

Josh: This would be a simpler model and possibly make the API more usable. 

Amanieu: I'd like to keep the API consistent between RwLock and RefCell.

Josh: In general the API would be painful to use because .. not sure if people would want to do anything with the .. value. You'd have to do `.map_err(|e| e?)`

Amanieu: Sure, but that comes back to the proposal in the ACP. It returns the `Try` trait. BUt then you have two APIs that do half the thing and you don't have the general API that does what you want.

T8: Could we implement a custom error type that returns `Try` so you could use `??`?

Josh: Ignoring the `Try` trait here and using `Result`:

```rust
// #1
pub fn try_map_with_recovery<U, E>(
    orig: Ref<'b, T>,
    f: impl FnOnce(&T) -> Result<&U, E>,
) -> Result<Ref<'b, U>, (Ref<'b, T>, E)>

// #2
pub fn try_map<U, E>(
    orig: Ref<'b, T>,
    f: impl FnOnce(&T) -> Result<&U, E>,
) -> Result<Ref<'b, U>, E>

// #2 with Try trait
pub fn try_map<U: ?Sized>(
    orig: Ref<'b, T>,
    f: impl for<'a> FnOnce(&'a T) -> impl Try<
        Output = &'a U,
        Residual: Residual<Ref<'b, U>>,
    >,
) -> impl Try<Output = Ref<'b, U>>; 

// #3
pub fn try_map<U, E>(
    orig: Ref<'b, T>,
    f: impl FnOnce(&T) -> Result<&U, E>,
) -> Result<Ref<'b, U>, TryMapError<'b, T, E>>

impl From<TryMapError<E>> for E { ... }

// filter_map
pub fn filter_map<U, E>(
    orig: Ref<'b, T>,
    f: impl FnOnce(&T) -> Option<&U>,
) -> Result<Ref<'b, U>, Ref<'b, T>>
```

Josh: I'd say the one that doesn't support recovery (line 140) would be called in the vast majority of the cases.

Amanieu: The with recovery one is the more general one.

T8: But then you lose the error type which you could use for recovery.

Amanieu: .. the question is how often this actually comes up in practice?

Josh: if we have `try_map` would people ever want to use use `try_map_with_recovery`? If people need this, let's add it, but seems like it's not a common case. `try_map_with_recovery` is the more general version, and `try_map` could be implemented in terms of it, but ..

T8: We could have a custom error type and provide automatic conversion.

Josh: If we could work with .. types, it could be promising.

...

Josh: If you were already planning to have a conversion it might complicate things. But writing an additional trait impl to make that work automatically isn't a huge drag.

T8: You could call an additional .into() in the closure.

Josh: The version with a custom thing instead of a tuple .. sounds better than the tuple.

Amanieu: OTOH, I like that #2 works with the Try trait.

Josh: So is #3 what we're talking about here? It returns `TryMapError` with an appropriate `into` impl.

T8: Can we combine #2 and #3 to have the Try trait result type?

Amanieu: No, the Try trait can only work with #2.

Josh: #2 seems still broken because it's not the same type?

Amanieu: It probably is broken.

Josh: #3 seems good for usability reasons, at least it's better than #1.

Amanieu: Agreed. I prefer #2. The problem with #3 is TryMap would get you back Error ??

Josh: You're talking about #2 being the version which does not give the original Ref back if it fails?

Amanieu: In almost all cases you care about getting the error back or getting Self back.

The 8472: Sometimes you need both

Amanieu: Yes but in practice it's so rare.

Amanieu: I won't stop you if you want to go with #3. .. And if we do that, are we deprecating `filter_map`?

Josh: I wouldn't deprecate, people could still use it. The new one is just more general.

Amanieu: excluding #1 from the poll.

#2: Amanieu
#3: Josh, 8472 (weakly favor)

T8: What's the benefit of the Try approach?

Amanieu: It's very convenient to use.

T8: You mean for the types other than Result like ControlFlow, etc?

Amanieu: Yes, but in practice it will be Option. Let's go with #3.

Amanieu: Proposal: go with #3 and extend the tracking issue to Mutex and RwLock

### (nominated) rust.tf/147859 *reduce the amount of panics in \`{TokenStream, Literal}::from\_str\` calls*

Amanieu: I'm not familiar with this part of the codebase.

T8: Does the .. change API guarantees?

Chris: https://github.com/rust-lang/rust/pull/147859#discussion_r2732841594

David: There are 3 cases ..

Josh: That seems like behavior you should ask for. I see why it makes sense, but it's still confusing.

David: rustc's fixups are geared towards full .rs files. If you give it smal fragments of token streams from within macro output, the same fixups are not as good.

Josh: If you hand it something you expect to be e.g. an identifier, ..

David: or passing a value to an attribute

T8: Could rustc do better?

David: My preference is that this behavior that .. should be opt-in. There should be two options: one is from_str 

Josh: That part should be a breaking change.

David: Correct. This would be executing while proc macro is running.

Josh: In that case there would be no fixup to call.

Josh: ripping out the fixup behavior seems like the correct approach. Just fail there. For the panicking case, it's mismatched behaviors around closing parentheses. Seems fine, might warrant a crater run. If it doesn't scream, we should do this.

David: The only time it could go wrong is if the LexError is zero-sized.

Josh: I see, because it adds information to the LexError. I wouldn't expect compilation? to make assumption about the LexError. I worry about someone expecting a panic and receiving an error.

The 8472: Regarding the stability of Error Display -- we don't make any guarantees?

Josh: Exactly.

### (nominated) rust.tf/149169 *ptr::replace: make calls on ZST null ptr not UB*

Amanieu: This and rust.tf/152615 are related.

Amanieu: Read and Write on a null pointer is valid if you're reading/writing a ZST

Josh: The 1st issue is that we already made the rw not UB.

Josh: If we agree to have this behaviour we should be consistent.

The8472: Consistency is part of the issue. There were inconsistencies between what you can read and what you can turn into a reference. I think the 2nd one is saying you can't operate on null pointers in general. Except for specific methods that say those particular methods are okay. It's taking a bit of the guarantee back and making the scope of specific methods that are okay if it's the ZST back.

Josh: I was talking about the first one. If we do it for read and write we should make replace consistent with read and write.

The 8472: Okay.

Josh: The first one seems straightforward, trying to follow the second. The definition of valid for R/W needs to exclude zeroes in general because valid for read/write implies you can get a reference from it, instead we should say that .. 

Amanieu: I regret having null pointers for ZSTs. What was the motivation for that?

T8: Sort of generalization, because ..

Chris: I don't remember why, but there was a use case some people needed.

T8: Probably turning integers into pointers and .. operating on ZSTs

Chris: Something like that.

Amanieu: Seems like too much of a footgun to allow general null access for the ZSTs because it allows an accidental reference in the middle

Josh: Yes. Valid for read/write implies taking a reference

Amanieu: What are the exceptions?

Josh: Read, write, copy

The 8472: read_volatile and write_volatile too

Josh: These are saying "we allow you to handle ZSTs with value of zero and pretend to conjure it on the spot"

Amanieu: At least that one seems strictly beneficial, so we should go forward with it.

Amanieu: I'll start an FCP.

T8: replace already has an FCP.

Amanieu: I'll do the FCP on the other one.

The 8472: We should look at the wording. Ralf suggests that the current wording can confuse users: https://github.com/rust-lang/rust/pull/152615#discussion_r2809756298

Amanieu: Won't hurt to add a 'unless specified otherwise'.

The 8472: Yes.

Amanieu: I'll start the FCP.

T8: Does it affect opsem?

Chris: I don't think it changes behavior.

The 8472: It removes the text that for ZSTs every access is valid.

Chris: But that was wrong, wasn't it?

The 8472: It talks about memory references, not referencing places. Those are different operations.

Amanieu: It's the documentation changed, the actual behaviour didn't change.

### (nominated) rust.tf/152615 *refactor 'valid for read/write' definition: exclude null*

(see above)

### (nominated) rust.tf/149537 *f\*::min/max do not behave as documented for signaling NaN on aarch64, or with optimizations*

Amanieu: There's a [godbolt link](https://llvm.godbolt.org/z/7PcooGGEq) that shows how none of the options work for every platform. x86 is partularly bad. The only one producing good assembly is cmp+select

Amanieu: It's eight instructions on x86 to do a `min` (instead of just one). Differenvt platforms have different preferred min operations.

We would like min/max do the fast thing on each specific platform. And we have to change that if one of the inputs is `NaN` we either return the other value or a `NaN` nondeterministically.

T8: What were the concerns other than .. correctness issues?

Amanieu: I personally don't think it's controversial. 

Josh: This is a question on what the defaults are. Some people want the deterministic behaviour and others want whatever is fastest. The question is which one should be the default.

The 8472: Also, do we want subtle breakages for the existing functions?

Josh: True but I think that's inevitable either way. We should either change them to be fully deterministic or fully maximized for performance. Right now some platforms do one and others do the other. I'd happily support either alternative.

Amanieu: We could do this over an edition to do the edition-aware mechanism.

### (nominated) rust.tf/149926 `P-lang-drag-1` *Do not deduplicate captured args while expanding \`format\_args!\`*

### (nominated) rust.tf/152304 *stabilize new RangeToInclusive type*

### (nominated) rust.tf/152480 *\`format\_args!\`: only de\-duplicate captured identifiers that refer to places*


### (waiting on team) rust.tf/151603 *Stabilize \`str\_as\_str\`*

### (waiting on team) rust.tf/152092 *Stabilize \`refcell\_try\_map\`*



### (new change proposal) rust.tf/libs745 *ACP: Introduce something akin to reverse ancestor method/iterator for \`std::path\`*

### (new change proposal) rust.tf/libs744 *Const path separators*

### (new change proposal) rust.tf/libs743 *ACP: Opt\-in to calling GlobalAlloc in std's internals*

### (new change proposal) rust.tf/libs742 *ACP: add \`str::split\_twice\`*

### (new change proposal) rust.tf/libs740 *ACP: add \`str::version\_cmp\`*

### (new change proposal) rust.tf/libs733 *allow specifying whether \`Instant\` should be suspend\-aware*

### (new change proposal) rust.tf/libs711 *Add \`mem::needs\_clone\`*

### (new change proposal) rust.tf/libs710 *\`BinaryHeap::from\_raw\_vec\`*

### (new change proposal) rust.tf/libs709 *Add \`str::copy\_from\_str\` and other mutating \`str\` methods*

### (new change proposal) rust.tf/libs700 *Mapped Arc and UniqueArc*


### (stalled change proposal) rust.tf/libs525 *Vec::try\_remove*

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs462 *impl fmt::Write for BufWriter*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*

### (stalled change proposal) rust.tf/libs523 *Add a deterministic constructor for \`RandomState\`*

### (stalled change proposal) rust.tf/libs549 *Add \`uNN::checked\_sub\_nonzero\`*

### (stalled change proposal) rust.tf/libs204 *Integer Manipulation API*

### (stalled change proposal) rust.tf/libs262 *Add infallible variant of RangeFrom::next()*

### (stalled change proposal) rust.tf/libs452 *ACP: Implement \`TryFromIterator\<T\>\` trait to compliment \`FromIterator\<T\>\` trait.*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_

