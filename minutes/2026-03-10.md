# Libs-API Meeting 2026-03-10

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Josh, David, Chris, Tomas, Nurzhan, The 8472

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

1 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3874 *build\-std: always* - (3 checkboxes left)

15 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/152851 *Fix SGX delayed host lookup via ToSocketAddr* - (3 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/139087 *Fallback \`{float}\` to \`f32\` when \`f32: From\<{float}\>\` and add \`impl From\<f16\> for f32\`* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`Exclusive\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/111688 *Tracking Issue for ExitCodeExt on Windows* - (3 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/119639 *Tracking Issue for tcp\_deferaccept* - (3 checkboxes left)
  - merge rust.tf/149537 *f\*::min/max do not behave as documented for signaling NaN on aarch64, or with optimizations* - (4 checkboxes left)

[scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [Amanieu (3)](https://rfcbot.rs/fcp/Amanieu), [dtolnay (3)](https://rfcbot.rs/fcp/dtolnay), [BurntSushi (7)](https://rfcbot.rs/fcp/BurntSushi), [joshtriplett (6)](https://rfcbot.rs/fcp/joshtriplett), [nikomatsakis (2)](https://rfcbot.rs/fcp/nikomatsakis), [the8472 (9)](https://rfcbot.rs/fcp/the8472), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [weihanglo (1)](https://rfcbot.rs/fcp/weihanglo)

### (nominated) rust.tf/libs743 *ACP: Opt\-in to calling GlobalAlloc in std's internals*

(skipped)

### (nominated) rust.tf/149926 `P-lang-drag-1` *Do not deduplicate captured args while expanding \`format\_args!\`*

(skipped)

### (nominated) rust.tf/152480 *\`format\_args!\`: only de\-duplicate captured identifiers that refer to places*

(skipped)

### (nominated) rust.tf/152851 *Fix SGX delayed host lookup via ToSocketAddr*

Unnominated now that FCP is proposed.

### (nominated) rust.tf/152855 *Do not emit separator before elements for \`Intersperse\` on non\-fused iterators*

Nia: Both of the participants said that the first example makes more sense, there the initial `None`'s are ignored, and more after that are still interspersed.

Nia: Someone brought up an example of a restartable loop, where the first proposal would be undesirable.

Josh: What would be the use case for wanting the first behavior?

Nia: Try-receiving from a channel and ignoring it when nothing is received.

David: That's still implementable with this. Instead of:

```
let mut channel = (...).intersperse();
loop {
    for x in channel {
        ...
    }
}
```

You'd have:

```
let mut channel = ...;
loop {
    for x in channel.by_ref().intersperse() {
        ...
    }
}
```

Nia: That would also work. Given this, makes sense to merge this as is.

Josh: What behavior are we discussihng here?

Nia: Before this PR the behaviour is the same as the first example but it emits the separator. That's undesirable and this PR fixes it.

Josh: Agreed.

### (nominated) rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`*

Nia: We can skip this

### (nominated) rust.tf/153380 *stabilize new RangeFrom type and iterator*

Nia: This is a stabilization, waiting on review. Some of this seems to be stuff we've agreed on in the RFC. Not sure if anyone involves in this is here now. Is there any reason not to stabilize? I think it makes sense. There's the `RangeFromIter::remainder` that they were arguing about.

David: We talked about this last week. There are `Index` impls on the old types. We'd want them to exist on both old and new ones.

Nia: We can just say ok and let them proceed.

David: Including stabilizing `remainder`?

Nia: Yes, makes sense to have them together.

### (waiting on team) rust.tf/55794 *UdpSocket receive to short buffer behaves differently on Unix and Windows*

Discussed last week.

David: This is waiting for us to kick off an FCP, right?

Chris: We do not want any change to the Unix behavior.

Nia: It needs documentation. If nobody plans on doing it, I can create a PR.

### (waiting on team) rust.tf/149392 *UdpSocket: recv() and recv\_from() behave differently for zero sized buffers*


### (waiting on team) rust.tf/152092 *Stabilize \`refcell\_try\_map\`*



### (new change proposal) rust.tf/libs753 *Expose Float Masks*

Nia: The conversation is still active (last message from 1h ago). But let's take a look what this is about.

Josh: In principle, seems reasonable. The discussion seems new, so we should let it bake. It's a matter of bikeshedding, but sign, exponent and mantissa are the obvious name choices. The other potential useful one, when NaN-boxing, it's helpful to be able to expose the safe bits.

Nia: That one I'd leave people to write directly, but I wouldn't be opposed to it.

Josh: I wouldn't be opposed to adding it, but it should be a separate ACP.

Nia: I'll let them discuss for some time.

Josh: Should we let them know that on the basis of light discussion, we think it's a good idea? I'll let them know.

### (new change proposal) rust.tf/libs751 *Extend / Replace current Linked List (currently doubly linked list) Implementation in std collections*

Nia: I don't like this. They want to replace the impl of LinkedList with XOR Linked List, which is better in terms of memory but worse in terms of pointer chasing and soundness that joboet pointed out.

Josh: And debuggability. XOR LLs are too specialized for the std. +1 for closing this on the basis of perf tradeoffs, debuggability, and being too specialized.

Nia: I'll type something up.

### (new change proposal) rust.tf/libs750 *Direct Simple BitQueue for std collections*

Josh: This is effectively a queue of bits, not bytes, encoding chunks of bits. That's useful, but whether it's appropriate for std is questionable.

Nia: Yes. It's cool, but I don't see a common, straightforward implementation that would warrant inclusion in std. ??

Josh: It has use cases. It's worth considering, but it'd have to bake more. My first reaction is: build this as a crate, collect feedback from users you're representing (e.g. compression and crypto libraries). If you have a common design and converge on an API, we can revisit this.

Nia: ?

Josh: I'd love to see a BitSet in std, and I expect that to be more popular. There are several implementations in the ecosystem, and having a standard vocabulary would be nice.

Nia: I'll reply to that effect.

Josh: BitQueue and BitSet are both worth considering, but they should be separate proposals.

### (new change proposal) rust.tf/libs749 *ACP: Add non\-panicking versions of \`Duration::from\_{nanos\_u128,mins,hours,days,weeks}\`*

Josh: So e.g. if you called try_from_nanos_u128 with something larger than 2^64 you'd get Err rather than a panic.

Nia: Sounds correct. Do we not have any fallible conversions?

Josh: I would expect so.

Nia: We have `Duration::try_from_secs_f32` and `.._f64`. Is all we have? This is surprising.

Josh: Given we have those two, it makes me think we should add more. We should return `Result` rather than `Option` There return a `TryFromFloatSecsError`. We could have reused it if not for the name.

Nia: We could change the name and use a type alias.

Josh: You raise a good point.

Nia: I would be shocked to see code relying on `TryFromFloatSecsError` and `TryFromError` being distinct types.

Josh: They already are the same. I think it's appropriate to rename and alias. I can let them know.

### (new change proposal) rust.tf/libs748 *Add \`is\_disconnected()\` to \`std::sync::mpsc\` and \`std::sync::mpmc\`*

Josh: The only potential issue here is that some channel flavors in the ecosystem provide a way to construct a `Receiver` from a `Sender`. If you provided such a method, `is_disconnected` would be racy in one direction. ?

Nia: Fair point, but they're not mutually exclusive.

Josh: ?? BurntSushi's comment says "a method like this is very likely to be used incorrectly and in a way that leads to undesirable race conditions"

Nia: The motivating example is not great. It would make sense to add if you have control over both sides of the channel. You could use `is_disconnected` to cheaply check if the other side has disconnected before trying to send large data over the channel.

Josh: ?? once `is_disconnected` is `true`, it can never become `false`.

Nia: We could add this and document it clearly saying users almost always need something else.

Josh: That's valid. We could explicitly document the use case of checking only this direction, and also send a note to clippy. I keep thinking of whether to keep improving these or rewrite them.

Nia: ?? consensus.

Josh: It seems fine. We should confirm with BurntSushi.

Nia: Josh, do you want to send the reply?

Josh: I'm typing another one.

Nia: I'll do it after the meeting.

### (new change proposal) rust.tf/libs746 *ACP: Add \`TryFromIntError::kind\` method and \`IntErrorKind::NotAPowerOfTwo\` variant*

Josh: Presumably this would be `non_exhaustive`. I could see a couple of ways to solve this: 1) non_exhaustive, 2) .

David: This is proposing adding variant to the existing non-exhaustive IntErrorKind enum.

Josh: The existing struct is opaque.

Josh: Oh, we do have `IntErrorKind`. Yes, this proposes adding one more error kind to it. Given it already exists with 5 variants, that makes sense. `NotAPowerOfTwo` is valid for certain types. Other types might require a prime number. But I wonder if we should have a generic domain error (i.e. this isn't valid for the given target type)?

The 8472: Since it's not zero-sized, we could treat it as ??

Nia: That would make it difficult to

Josh: All of the other items are about parse failure, and `Zero` is about an invalid value. It sets an unfortunate precedent because it should have been `InvalidValue`. We could have reused that for `NotAPowerOfTwo`, `NotPrime`, etc.

David: `InvalidValue` would hurt error messages. Instead of having a single generic value, I'd like to set a specific error value where we can set that.

Nia: I'm not sure there's a way to generically express the domain-specific requirements.

Josh: I'm less concerned about niche cases like o non-power of two architectures.

The 8472: Alignment is not the only thing using `IntErrorKind`. Maybe we should go over the other usages?

Josh: `TryFromIntError` doesn't have any means of construction. You can't turn an `IntErrorKind` into `TryFromIntError`, which means you can't use it for custom types. If this is exclusively constructible by std, I'd withdraw the desire to make it generic.

Nia: There's nothing stopping anyone from creating an `IntErrorKind`.

Josh: It also doesn't implement `Error`. Would be awkward to reuse `IntErrorKind` in custom error types. We should reply saying we're happy to see this happen.

### (new change proposal) rust.tf/libs733 *allow specifying whether \`Instant\` should be suspend\-aware*

Nia: We've looked at this several times. There's no new activity since the last time. We suggested something, and someone replied off-topic. We can skip this.

### (new change proposal) rust.tf/libs710 *\`BinaryHeap::from\_raw\_vec\`*

Nia: We discussed this also.

Josh: We did, and I don't think we responded definitively. We have the go ahead. I will mark this as approved explicitly.

### (new change proposal) rust.tf/libs709 *Add \`str::copy\_from\_str\` and other mutating \`str\` methods*

Nia: We also discussed this.

The 8472: The author replied about adding APIs for consistency, and that they don't have much of a use case for them themselves. ??

Nia: And cut out `copy_from_within`, I think. We can thumbs up this.

The 8742: Partially.

Nia: I'll write something after the meeting.

Tomas: What about `swap_with_str` We talked about the first two, but not that one.

Nia: It's similar to `copy_from_str` we can get it too.

The 8472: What's the motivation for swap?

Nia: Consistency, and it's as easy to implement.

The 8472: We can ask if there's a use case, and go from that.
 
### (new change proposal) rust.tf/libs700 *Mapped Arc and UniqueArc*

Nia: We discussed this, and it's large.

Josh: It's not as big as it looks. The current state is: we discussed the naming. It's part of the Field Projection work (and we'd like ArcRef rather than Mapped). The other aspect was type erasure. The proposal uses the generics to point to the original type, but it can be erased. Generally, we're in favor of type erasure as the default impl. We delayed because we were waiting for field projection folks to look at this. I see that Benno has looked at this, so we can approve this with that name and saying that we want the type-erasure approach.

Josh: I'll write a response.

### (stalled change proposal) rust.tf/libs133 *Add fmt::Write to io::Write adapter*

Josh: We didn't generaly have any objections to this, but we haven't looked at it again since discussing the new `WriteFmt` trait. It interacts with that proposal. We need to look at it with that in mind.

Nia: Did we ever add the `WriteFmt` trait?

Josh: That's blocked on a couple of lang features to add compatibly. We decided this is non-urgent, so we haven't tried it since.

Nia: The author seems to have written a crate for this. Doesn't seem urgent.

Josh: We can comment saying we need to take a look at this, then look at it async.

Nia: I wasn't present for that discussion, so anyone who was is welcome to type a response.

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*

### (stalled change proposal) rust.tf/libs262 *Add infallible variant of RangeFrom::next()*

Nia: Not convinced that this should be a thing. If you know you're doing something infallible, that's what `unwrap` is for. There's talk about potential optimizations for infinite iterators.

Josh: Is this a version of next that panics instead of returning an error?

Nia: Yes.

Josh: I assumed "infallible" meant "a total function" rather than panicking on error.

The 8472: ?

Nia: I'm not convinced. The motivation just seems to be having a panic in code that's linting against an `unwrap`.

Josh: I'd call `unwrap`, but I'd get flagged, which calls for a method that would let me avoid calling `unwrap`.

Nia: I'd like to get this un-nominated. I'll reply to them.

### (stalled change proposal) rust.tf/libs347 *Context reactor hook*

### (stalled change proposal) rust.tf/libs540 *Add Unicode XID related functionalities to \`proc\_macro\` crate*

Josh: This is asking for bits in proc macro looking at xid and ??. Adding  `is_XID_start` and `is_XID_Continue` to `char` wouldn't be unreasonable.

Nia: I see how having these methods would make sense. I'll reply saying that.

### (stalled change proposal) rust.tf/libs131 *Add \`std::fs::rename\_noreplace\`*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs296 *ACP: Designing an alternative \`FromStr\`*

### (stalled change proposal) rust.tf/libs287 *ACP: Add \`FromByteStr\` trait with blanket impl \`FromStr\`*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
