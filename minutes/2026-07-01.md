# Libs Meeting 2026-07-01

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-ujepnbwg2lzqgt6wvrndwimi
**Attendees**: Amanieu, The 8472, Clar Fon, Chris Denton, Josh Stone, Mark, Josh Triplett, Nurzhan

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

7 rust-lang/rust T-libs FCPs

  - merge rust.tf/158186 *Guarantee 8 bytes of alignment of RawWakerVTable* - (3 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (4 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (5 checkboxes left)
  - merge rust.tf/158368 *std: make \`Instant\` a signed \`Duration\` on all platforms* - (3 checkboxes left)
  - merge rust.tf/158504 *stabilize \`extern "custom"\`* - (2 checkboxes left)
  - merge rust.tf/132484 *unix: Relax escaping in \`Debug\` impl on \`Command\`* - (0 checkboxes left)
  - merge rust.tf/149648 *Mark const SIMD intrinsics as indirectly stable* - (4 checkboxes left)

1 rust-lang/rust-forge T-libs FCPs

  - merge github.com/rust-lang/rust-forge/issues/1040 *Add an LLM policy for \`rust\-lang/rust\`* - (4 checkboxes left)

[thomcc (1)](https://rfcbot.rs/fcp/thomcc), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [dtolnay (1)](https://rfcbot.rs/fcp/dtolnay), [tmandry (2)](https://rfcbot.rs/fcp/tmandry), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [traviscross (1)](https://rfcbot.rs/fcp/traviscross), [joshtriplett (4)](https://rfcbot.rs/fcp/joshtriplett), [BoxyUwU (1)](https://rfcbot.rs/fcp/BoxyUwU), [BurntSushi (2)](https://rfcbot.rs/fcp/BurntSushi), [Amanieu (4)](https://rfcbot.rs/fcp/Amanieu), [scottmcm (2)](https://rfcbot.rs/fcp/scottmcm), [the8472 (5)](https://rfcbot.rs/fcp/the8472)



### (Prioritization Requested) rust.tf/158008 *\`Read::read\_to\_end\` performance regression*

Amanieu: We talked about this last week.

The 8472: We said Medium. Done.

### (Nominated) rust.tf/157144 *Prevent mutating the global environment pointer in \`CommandExt::exec\` and opt to use execve and resolve path manually*

Amanieu: We talked about this too, right?

The 8472: No? Maybe the week before?

Josh S: Last week was async.

The 8472: Yes, we discussed and didn't reach a conclusion.

Amanieu: AFAIK, Only macOS has this problem, and for it we can read the actual path(?), that part is open source.

The 8472: Do thy have execvpe?

Amanieu: They have execve but not execvpe (?? doesn't look up the path for you).

The 8472: Too bad. 

Amanieu: Can we try to use execvpe on the platforms where it's available?

Josh S: We wanted that last time and let the platform decide what it means when possible. And whether we want to use the same lookup logic (yes).

The 8472: I don't think I agree. We need to account for the possibility of chroot happening between forks(?)

Amanieu: Path lookup is purely string processing. We will split the path envvar into chunks, prepend these to the exec name, and try the ?? syscall until one of them works. No FS access happening. This happens after forking.

The 8472: Sounds okay.

Josh T: And it's not safe to do it earlier than that. TOCTOU, and someone can do an unusual FS setup.

The 8472: If it happens later, then it's probably ok.

Josh S: I don't know if TOCTOU matters...

Amanieu: The current approach is more efficient because you make fewer syscalls...

Josh: I mention TOCTOU because, say I have a pth with 3 dirs in it I have a foo var in the first ... then the valid things are execute the first or the second, invalid is returning it if it doesn't exist (??)

Amanieu: You shouldn't rely on that.

Josh: Sure, but I can imagine a scenario like that.

Amanieu: Are we happy to pursue this further (doing path processing on our end, not libc)?

The 8472: In cases where we can avoid it (?)

Clar: "We don't have to be consistent between platforms, only within one."

The 8472: Sometimes we call POSIX spawn, sometimes fork. If they agree, this doesn't happen, otherwise...

Clar: I'd keep in mind if someone's explicitly setting a path for the command.

Amanieu: This only happens if there are no slashes...

Clar: I mean the path variable...

Amanieu: Wait, no, execvpe reads the path from the global env...

The 8472: Before or after letting the user change the path variable in the child?

Amanieu: Before. It's wrong, but we'll change it over an edition.

The 8472: Aren't we already inconsistent with posix spawn, or do we lookup manually before?

Amanieu: Good point. I think that's why we're not using execvpe — we can't because we need to use the one we've already overridden. That's a mess.

Josh S: What are the cases where the mechanism of path lookup... The path being unset is weird...

Clar: Someone could unset all env variables, which would include the path.

Amanieu: I don't think there's a default path if it's unset.

Clar: Maybe it only allows an absolute path (?)

(..)

Josh S(chat): "The default search path (used when the environment does not contain the variable PATH) shows some variation across systems.  It generally includes /bin and /usr/bin (in that order) and may also include the current working directory."

(..)

Amanieu: Are we happy to pursue this? There's no other fix, so I think yes? The other question, ?? or should we let POSIX spawn to keep using ??. I think we already don't allow using posix spawn if you've changed the path (?)

Amanieu: Not change POSIX spawn? (The 8472: Yes.) Starting a libs FCP.

Josh S: Please be very explicit about what you're asking in the FCP.

### (Nominated) rust.tf/158368 *std: make \`Instant\` a signed \`Duration\` on all platforms*

Amanieu: We discussed this yesterday in libs-api and started an FCP for libs. I think that's done. Unnominating.

### (Waiting on team) rust.tf/154715 *split try trait in twain*

The 8472: Has been open for a while.

Amanieu: Mark nominated this.

Josh S: Shouldn't this be more of an API thing?

Amanieu: Yes. Marked as waiting on libs, not libs nominated.

Clar: Should be changed to waiting on libs-api?

Amanieu: Need someone to champion the Try trait. I feel like Scott...

Mark: There wasn't necessarily an API discussion to be had. The difficulties are with making sure we're happy how it looks, not the public-facing API...

Josh S: The Project goal is owned by Tyler. And you're the libs-api champion.

Amanieu: This needs a champion to decide what happens to the Try trait, and I don't think it's up for a team discussion yet. I don't think it helps.

Mark: I think ??

Amanieu: I think we cc Tyler as the PoC?

The 8472: Is that lang relevant?

Amanieu: He's the Goal proposer.

Josh S: He also champions from lang, so it's relevant.

The 8472: I mean the proposed change specifically. The motivation seems to be something with Option.

Amanieu: Try for &Option.

The 8472: Doesn't seem lang-related. More like which types we want to support `?` or `try` blocks.

Clar: This is difficult because the #1 decider would be inference. I don't know how this would affect that.

The 8472: They can write a test on how much additional annotation would make this case work. Existing tests didn't break as far as I can tell.

Clar: A crater run across the ecosystem would be a concern.

The 8472: That's easy to do.

Clar: Design is probably less of a concern compared to the effect.

The 8472: Start a crater run while Amanieu familiarizes himself with the issue?

Amanieu: I don't want to add more Try impls to the language. I think we have enough.

The 8472: The point is that people will be able to do it anyway?

Amanieu: Please don't?

The 8472: You won't get what you're asking for.

Clar: The change needs to be rebased so can't be cratered.

The 8472: We have the rights to do it.

Josh T: Sounds like you want to do it?

The 8472: I can do it.

Clar: It's breaking tests too.

Clar: Maybe ping Tyler and Scott and see what they say.

(Meeting ended here.)

### (Regressions) rust.tf/139730 `P-high` *A case of compound x86\_64 performance regression caused by LLVM 20 and #124810*

### (Regressions) rust.tf/53340 `P-low` *Performance regression in tight loop since rust 1.25*

### (Regressions) rust.tf/119560 `P-low` *\`cargo test\` performance regression on Windows on version 1.75.0*

### (Regressions) rust.tf/124216 `P-medium` *Performance regression: rustc failed to optimize specific x86\-64 SIMD intrinsics after 1.75.0*

### (Regressions) rust.tf/130320 `P-medium` *stdlib code size increase in nightly\-2024\-09\-01 for aarch64\-linux\-android*

### (Regressions) rust.tf/142119 `P-medium` *Linking a Rust staticlib unexpectedly changes C math functions from libm to bundled ones from compiler\-builtins*

### (Regressions) rust.tf/142345 `P-low` *\`pin!()\` macro no longer constant\-promotes \`&mut\` references to empty arrays*

### (Regressions) rust.tf/145424 *rustc output no longer reproducible if it crashes*

### (Regressions) rust.tf/149752 `P-high` *TrivialClone is not derived for generic types (Performance regression)*

### (Regressions) rust.tf/152942 *Query depth limit overflow due to nested async fn when building matrix\_sdk*

### (Regressions) rust.tf/153425 `P-medium` *\`&pin!()\` stopped being deref\-coerced in rust 1.88*


## Actions

- [ ] Reply to all issues/PRs discussed in this meeting, or add them to the [open action items](https://hackmd.io/Uehlc0qUQfWfvY1swYWRgQ).

_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_

