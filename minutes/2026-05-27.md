# Libs Meeting 2026-05-27

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-ujepnbwg2lzqgt6wvrndwimi
**Attendees**: Nia Deckers, Amanieu, Josh Triplett, Chris Denton, thomcc, The 8472

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

7 rust-lang/rust T-libs FCPs

  - merge rust.tf/155750 *Document that \`ManuallyDrop\`'s \`Box\` interaction has been fixed* - (4 checkboxes left)
  - merge rust.tf/156882 *alloc: stabilise \`Allocator\`* - (4 checkboxes left)
  - merge rust.tf/155697 *Stabilize c\-variadic function definitions* - (6 checkboxes left)
  - merge rust.tf/155114 *Add \`Step::forward/backward\_overflowing\` to enable RangeInclusive loop optimizations* - (3 checkboxes left)
  - merge rust.tf/132484 *unix: Relax escaping in \`Debug\` impl on \`Command\`* - (0 checkboxes left)
  - merge rust.tf/149648 *Mark const SIMD intrinsics as indirectly stable* - (4 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (3 checkboxes left)

1 rust-lang/rust-forge T-libs FCPs

  - merge github.com/rust-lang/rust-forge/issues/1040 *Add an LLM policy for \`rust\-lang/rust\`* - (8 checkboxes left)

[CAD97 (1)](https://rfcbot.rs/fcp/CAD97), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [lolbinarycat (1)](https://rfcbot.rs/fcp/lolbinarycat), [tmandry (2)](https://rfcbot.rs/fcp/tmandry), [BoxyUwU (1)](https://rfcbot.rs/fcp/BoxyUwU), [matthewjasper (1)](https://rfcbot.rs/fcp/matthewjasper), [saethlin (1)](https://rfcbot.rs/fcp/saethlin), [digama0 (1)](https://rfcbot.rs/fcp/digama0), [BurntSushi (4)](https://rfcbot.rs/fcp/BurntSushi), [scottmcm (3)](https://rfcbot.rs/fcp/scottmcm), [joshtriplett (3)](https://rfcbot.rs/fcp/joshtriplett), [Amanieu (3)](https://rfcbot.rs/fcp/Amanieu), [dtolnay (1)](https://rfcbot.rs/fcp/dtolnay), [nagisa (1)](https://rfcbot.rs/fcp/nagisa), [joshlf (1)](https://rfcbot.rs/fcp/joshlf), [the8472 (5)](https://rfcbot.rs/fcp/the8472), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [traviscross (1)](https://rfcbot.rs/fcp/traviscross)



### (Prioritization Requested) rust.tf/156951 *\`CommandExt::exec\` writes to the global environment pointer while holding only a read lock*

Amanieu: That's a somewhat of a security issue. I talked with peoble at the All Hands about this. I'll put this as P-High

Nia: It's not critical but it's bad

Amanieu: We decided it's not a real security issue. This is `exec` not `spawn`. We should still fix this.

Nia: Seems someone claimed this

Josh: +1 for P-High. Is the fix here more complicated than making sure to call execve?

Amanieu: It's not available on all platforms. `execve` is available everywhere but it doesn't do the path search.

Josh: I'm proposing calling `execve` and doing the path search ourselves.

Nia: That's reasonable.

### (Nominated) rust.tf/155816 *Libs proposal: Policy for constifying traits and trait implementations*

Nia: We discussed this previously, but have an actual proposal now.

Nia: We wanted input from the maintainer on whether this is reasonable.

Amanieu: Right

Nia: Mark seems happy with it

Josh: The policy wwould be: we've written enough things using const traits to demo they're useful. The proposal is to wait for const traits to stabilise before doing further nontrivial trait constification? And giving nightly users more const APIs is not sufficient reason to constify a trait

Amanieu: The implementations cause a lot of churn because iterators aren't const so need to be rewritten with foor loops

The 8472: There were potential unsoundness between constification and min specialization. Drive-by PRs might not be aware of those interactions. Restricting tem or looping in const-eval-wg to chechk if each PR is justified and checking edge cases would be good.

Chris: Scott took himself off of review queue because of all this const everything work

Nia: That seems like a good reason to sign-off on it

Josh: That seems like a reasonable policy. It allows us to approve things on a case by case basis where there is a good reason. +1 to that, it seems fine.

Amanieu: The main concern is around const hack. But if that's solved, is it still necessary?

The 8472: It's not just const hack, it's soundsess too. The feature is not complete yet.

Amanieu: My understanding is it's not complete. That's what lang's been saying.

The 8472: From the types point of view it's not complete. There are unsoundness. See e.g. theemathas's coment here:

https://github.com/rust-lang/rust/issues/148200

The 8472: And theres' been a PR where someone constified a trait with specialisation. Who knows whether that could cause unsoundness

Nia: As far as I know, the soundness stuff is resolvable if we stabilised ??

The 8472: This introduces new cases where we can't use it or where we have to be extra careful.

Nia: Types think they can ship const traits. The only blocker is syntax bikeshedding.

Amanieu: That's a big blocker -- it'll cause churn for us.

Nia: Yes, but less than if it was a soundness issue

Nia: Would people be okay with the policy as written? And if const traits are soonish, we can worry about that once they're stable. And then it's on types to figure out something so there's no big soundness hole all reviewers need to be careful about

The 8472: We should add a point to the guidelines to loop in the const-eval-wg for PRs by nonmembers and point to the specialisation issue

Nia: I like that. So accept with that addition?

The 8472: Yes

Nia: Everyone else okay with that?

...

Nia: I'll report this as a tentative feedback so far.


### (Nominated) rust.tf/156270 `P-lang-drag-3` *Vibe check: can \`Simd\<T, N\>: Receiver\<Target = T\>\`?*

Josh: The ask is: work working on portable-simd to make `Simd<T, N>` be a receiver type for `T` (e.g u32) so you could write a method on u32 whose self type is `u32, N`. The ask for lang was is there a fundamental reason why this can't be implemented? And we handed the answer to the 2nd question to libs-api. Libs-api will decide whether this is the structure we want for portable-simd methods.

Josh: Speaking for myself, I ahve sympathies for the problem. But my reaction is this isn't the way to do it. I don't think the right place to look for methods for simd type is u32. This won't improve documentation and won't be intuitive to people.

Amanieu: Is the solution to ask rustdoc togenerate separate pages for simd u32, etc.

thomcc: There should be something for rustdoc. I'm in favour of the macro solution because it's consistent. But it generated pages that would crash chrome. If you look at the issues for wrapping, youd' see the same issues simd used to have

Josh: We could try to make rustdoc better. We could say that there should be individual pages for monomorphisation of separate types. Thank you for that example. The simd page would be even worse.

thomcc: The secondary goal portable-simd group wants to support user-defined types.

The 8472: The issue doesn't link the actual documentation or show on which element it would show up. So the issue is not self-explanatory

Amanieu: I'd see this as a doc attribute that would say "generate separate page for these impl blocks". Maybe an attribute on an impl block would give it its own page.

Nia: Let's defer to async?

Amanieu: I'd defer this to Guillaume. Proposal: "we don't think this is the right solution but cc rustdoc people" and point at the wrapping examlpe

The 8472: You proposed having multiple pages -- one for each primitives. Are there differences between the primitives to make it worth them t ohave separate? The proposal in this issue would result in a single page.

Amanieu: The proposal would put all the simd u32 docs under the u32 page.

The 8472: Could it be not on something else? For `MaybeUninit` we have array methods on all arrays and all Ts ??

Amanieu: I think those are on the Array page?

Josh: There are two things going here: (1) we're reacting to the proposed solution (2) we're trying to solve the problem ourselves. Can we start by giving feedback on the baseline proposal?

The 8472: Yeah, sure

Amanieu: I'd like the rustdoc people involved to find a better solution for this. This is fundamentally a rustdoc issue

The 8472: If we can shape things to help rustdoc that would help on our side but we need to know what would reduce the clutter

Nia: I'll reply to this

### (Nominated) rust.tf/156496 *Redesigned \`Components\` iterator to use front and back indexing instead mutating and subslicing path field*

Chris: the nomination comment says it speeds up next_back but slows down netx. The conversation is ongoing

Nia: I'd defer.

The 8472: It's not just for performance. We had an ACP that wanted something with a reverse pass. Is this related to that?

Nia: I think it's the same author.

The 8472: So maybe it's feature work to unlock something else.

Josh: It'd be helpful to know which it is. If it's performance, then the metric is whether it improved performance. If it's to enhance capabilities, we'd evaluate it differently.

The 8472: Let's ask that first and make additional decisions later?

Josh: +1


### (Nominated) rust.tf/156920 `P-lang-drag-1` *\`dyn Allocator\` together with \`Allocator + Clone\` requirements is unsound, leading to UB with \`Arc\`*

Nia: I need libs and libs-api opion. I think we'll do marker traits for the first iteration. But I have an idea that's backwards compatible.

Nia: Look at this: https://internals.rust-lang.org/t/pre-rfc-unsafe-traits-conditionally-depending-on-safe-trait-behaviour/24360

Nia: I want a mechanism to say: "unsafe trait conditionally relies on another trait". I'd like wibecheck from libs. This is a footgun that we run into in libs. When we say "this unsafe trait requires certain behaviour of another safe trait". We also do this internally with trusted clone.

Josh: Except for this hole that allows you to generalte conflicting impl by relying on dyn.

Nia: The only thing that's preventing you from a bad impl is coherence. And people want to mess with coherence.

Nia: I spoke to lcnr and on the types level something like this is probably doable and useful. Lang will have to chew on it for a bit. For stabilisation for now we'll do marker traits. And we can model any attribute like this "if you say unsafe has unsafe precondition on clone, it will generate an unsafe trait". ??

The 8472: That's there in case you have multiple unsafe supertraits?

Nia: We may have multiple implementations of clone like that

thomcc: The marker has to go on `Clone`. So users that want to have their own unsafe trait that rely on the clone behaviour, this won't help them.

Nia: I'd like this to be stable. They can't do it on `Clone` but they could do it on their trait.

thomcc: So this would be only for the standard library. They have more flexibility because they can change their API. But I have reservations because this feels like it's only for std.

Nia: That's fair. I feel there must be a way to put this not on the `Clone` definition.

thomcc: You'll have to repeat this on the blanket impl. And the person doing the blanket impl can't know you have the clone precondition.

The 8427: But a person who wants an unsafe trait can't add it t oclone because they don't own clone

Josh: The person doing blaket impl of clone for some dyn trait only have to know the specific traits they've derived from. Trait NewTrait is an allocator, we build a blanket impl on `Box<dyn NewTrait>` but if you impl `NewTrait` you know about `Allocator`

thomcc: One of usecases where you'd ipmlemest a blanket impl on `Clone` is reference counted pointers. You'd still need some way of saying you've satisfied the Allocator precondition. The case is not the dyn subtrait case.

Nia: Yes.

Josh: I see. It seems like another case of `dyn Trait` implementing `Trait` was a mistake.

thomcc: Also, the person implementing blanket impl on clone can't know what some unsafe traits might have as an requirement. That's why I'm in favour of a marker trait that's specific on what property of `Clone` is being required

Nia: I think we should just do marker traits. I don't want to give up `Clone` and `PartialEq` semantics.

Josh: I feel marker traits feel isomorphic to what the final solution is. We need to have a way to specify that whovere's implementing these acknowledges that they upheld the requirements that are required.

Nia: That's exactly what I did: https://github.com/rust-lang/rust/issues/156920#issuecomment-4556585102

Nia: We use marker traits, we keep space for `dyn` compatibility. And we ship this without types and lang.

Josh: I like this plan. Let's do the marker traits and if something cooler comes along, we can map them to that.

The 8472: But we don't need the marker traits now because we marked it as `dyn` incompatible.

Nia: This is part of stabilisation, because otherwise we're relying on coherence rules

The 8472: Can we add the marker traits after stabilisation?

Nia: I think we can add them after stabilisation

thomcc: But then `Arc` is broken

The 8472: Didn't we exclude `Arc`?

Nia: We have excluded `Arc` temporarily. But we'll want that stabilised soon.

thomcc: I think BTreeMap is broken too. We should make it clear that people can't rely on the `Clone` promise as they did before.

The 8472: Seems we're still figuring things out, that could could make the stabilisation more complex. The only concern would be: do we need to stabilise anything now to avoid blocking any such future changes?

Nia: I have a plan, going to fix the docs and push something and request checkboxes afterwards. There is an FCP open on the `Allocator` stabilisation PR that Josh opened. If people are cool with it, please check your boxes.


### (Regressions) rust.tf/139730 `P-high` *A case of compound x86\_64 performance regression caused by LLVM 20 and #124810*

### (Regressions) rust.tf/147964 `P-high` *regression: the method \`as\_ref\` exists for reference*

### (Regressions) rust.tf/53340 `P-low` *Performance regression in tight loop since rust 1.25*

### (Regressions) rust.tf/119560 `P-low` *\`cargo test\` performance regression on Windows on version 1.75.0*

### (Regressions) rust.tf/124216 `P-medium` *Performance regression: rustc failed to optimize specific x86\-64 SIMD intrinsics after 1.75.0*

### (Regressions) rust.tf/130320 `P-medium` *stdlib code size increase in nightly\-2024\-09\-01 for aarch64\-linux\-android*

### (Regressions) rust.tf/142119 `P-medium` *Linking a Rust staticlib unexpectedly changes C math functions from libm to bundled ones from compiler\-builtins*

### (Regressions) rust.tf/142345 `P-low` *\`pin!()\` macro no longer constant\-promotes \`&mut\` references to empty arrays*

### (Regressions) rust.tf/145424 *rustc output no longer reproducible if it crashes*

### (Regressions) rust.tf/149752 `P-high` *TrivialClone is not derived for generic types (Performance regression)*

### (Regressions) rust.tf/152942 *Query depth limit overflow due to nested async fn when building matrix\_sdk*

### (Regressions) rust.tf/153425 `P-medium` *\`&pin!()\` stopped being deref\-coerced in rust 1.88*


## Actions

- [ ] Reply to all issues/PRs discussed in this meeting, or add them to the [open action items](https://hackmd.io/Uehlc0qUQfWfvY1swYWRgQ).

_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
