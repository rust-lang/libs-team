# Libs Meeting 2026-04-15

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-ujepnbwg2lzqgt6wvrndwimi
**Attendees**: Nia, Amanieu, The 8472, Josh Stone, Josh Triplett

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

6 rust-lang/rust T-libs FCPs

  - merge rust.tf/132484 *unix: Relax escaping in \`Debug\` impl on \`Command\`* - (0 checkboxes left)
  - close rust.tf/150933 *Improve precision of Duration\-float operations* - (3 checkboxes left)
  - merge rust.tf/149648 *Mark const SIMD intrinsics as indirectly stable* - (4 checkboxes left)
  - merge rust.tf/148799 *Switch the destructors implementation for thread locals on Windows to use FLS* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (3 checkboxes left)
  - merge rust.tf/153975 *remove forever\-deprecated and hidden \`f64\` methods* - (3 checkboxes left)

[thomcc (2)](https://rfcbot.rs/fcp/thomcc), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [the8472 (4)](https://rfcbot.rs/fcp/the8472), [traviscross (1)](https://rfcbot.rs/fcp/traviscross), [tmandry (1)](https://rfcbot.rs/fcp/tmandry), [BurntSushi (2)](https://rfcbot.rs/fcp/BurntSushi), [joshtriplett (4)](https://rfcbot.rs/fcp/joshtriplett), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis)

### (Critical) rust.tf/154988 `P-critical` *1.95 beta regression: "\`lua\` does not live long enough"*

Nia: I assume this is being addressed by the backport?

Amanieu: It's being addressed by the item right below

Josh Stone: It's also for 1.94 now.

Nia: Is this for 1.96 as well?

Amanieu: Yes, it's fixed in nightly and if we backport it, it'll be backported to 1.95 beta.

Amanieu: We have to approve the backport.


### (Backports) rust.tf/154994 *don't leak internal temporaries from \`dbg!\`*

Nia: This is addressed too.

### (Nominated) rust.tf/154107 *Use \`f128\` internally for \`Duration\`\-float methods*

Nia: What's the status here?

Josh: I put up an alternate PR that turns it into more of a doc change: https://github.com/rust-lang/rust/pull/155133

Nia: So this just waits for reviewer, otherwise it's fine?

Josh Stone: It doesn't prevent us from doing the other thing in the future.

Nia: I'd just do the documentation changes then.

Amanieu: That seems fine.

The 8472: The doctests contain specific numbers, we don't consider those guarantees, right?

Josh Stone: I would not consider them guarantees.

Nia: The doc says these are examples. We are allowed to change how this behaves within reason. Making it more precise would be fair.

Josh Stone: I am making the f32 one more precise now.

Josh Triplett: Sonuds good to me

### (Waiting on team) rust.tf/122408 *Use futex\-based synchronization on Apple platforms*

Amanieu: I'd like to propose a final resolution here: we will not be using private APIs. We'll never get a response from Apple that we're allowed to use private APIs. They may tolerate it, but they won't give us the permission. I think we should say "no on this one, but we're willing to dynamically depend on libc++ if it's available"

The 8472: Is that really a good thing? Then we'd need to switch implemenation at runtime.

Amanieu: Once our minimum apple version has the C++ version of atomic and ??

The 8472: So we'd do this unconditionally as a dynamic library dependencies?

Amanieu: On apple, yes.

The 8472: Someone will complain about that

Nia: It's a system library.

Josh Stone: We can defer that question to the point we're trying to do that. What we're saying now is we're not tryinng to do the private API

Amanieu: Yes.

The 8472: And we'd do this not just for mobile but also the desktop OS? People compile Rust there not just for the App store. It seems strange to apply app store policies to non-appstore code

Amanieu: We shouldn't use private APIs in general

The 8472: We do that on Windows.

Josh Triplett: I do wonder if macos targeting the iOS vs. desktop makes sense. And then we'd be able to separate what we do there.

Josh Stone: The app store 

Josh Triplett: we would depend on libc++ which would do this

Josh Stone: they could change libc++

Josh Triplett: Are we not allowed to statically link libc++?

(no one knows)

Josh Triplett: I'm not suggesting making another target, but I wonder if restrictions like that could help. If someone feels strongly about this and feels there's enough benefit they could propose a separate target

Amanieu: They may allow static-linking libc++. The appstore checker does't catch these specific symbols but it catches other symbols.

Nia: But at that point we're relying on implementation details of appstore checker.

Nia: We should not go with this and no use of private APIs in general.

Josh Triplett: I'd say unless the platform has the policies on not changing them e.g. on windows

Nia: That's fair, this is a difference between macos and windows

Amanieu: I can write it up. I can do an FCP close on it to show that the whole team is behind this.

---

Amanieu: none of the regressios seem new.

### (Regressions) rust.tf/139730 `P-high` *A case of compound x86\_64 performance regression caused by LLVM 20 and #124810*

### (Regressions) rust.tf/147964 `P-high` *regression: the method \`as\_ref\` exists for reference*

### (Regressions) rust.tf/53340 `P-low` *Performance regression in tight loop since rust 1.25*

### (Regressions) rust.tf/119560 `P-low` *\`cargo test\` performance regression on Windows on version 1.75.0*

### (Regressions) rust.tf/124216 `P-medium` *Performance regression: rustc failed to optimize specific x86\-64 SIMD intrinsics after 1.75.0*

### (Regressions) rust.tf/130320 `P-medium` *stdlib code size increase in nightly\-2024\-09\-01 for aarch64\-linux\-android*

### (Regressions) rust.tf/142119 `P-medium` *Linking a Rust staticlib unexpectedly changes C math functions from libm to bundled ones from compiler\-builtins*

### (Regressions) rust.tf/142345 `P-low` *\`pin!()\` macro no longer constant\-promotes \`&mut\` references to empty arrays*

### (Regressions) rust.tf/145424 *rustc output no longer reproducible if it crashes*

### (Regressions) rust.tf/149752 `P-high` *TrivialClone is not derived for generic types (Performance regression)*

### (Regressions) rust.tf/152942 *Query depth limit overflow due to nested async fn when building matrix\_sdk*

### (Regressions) rust.tf/153438 `P-high` *pin!() is unsound due to coercions*

### (Regressions) rust.tf/153425 `P-medium` *\`&pin!()\` stopped being deref\-coerced in rust 1.88*

---

Amanieu: We're running low on libs reviewers

Nia: I should probably put myself on that list

Amanieu: does anyone else have ideas of people we could invite?

Josh Triplett: if folkertdev had time, he'd be an awesome reviewer. If he wanted to, he'd be a great a person. We could invite a few people that may say no but we'd be happy to invite them.

Amanieu: Would it be helpful to get the people form the compiler team to join the libs implementation review?

Josh: If there's interest, I know compiler people who would be great.


## Actions

- [ ] Reply to all issues/PRs discussed in this meeting, or add them to the [open action items](https://hackmd.io/Uehlc0qUQfWfvY1swYWRgQ).

_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
