# Libs-API Meeting 2026-06-09

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia Deckers, Clar Fon, JoshTriplett, thomcc, Aamnieu, Christ Denton, Orson, BurntSushi, The 8472, Martin (martinomburajr)

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

15 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/155421 *Document panic in \`RangeInclusive::from(legacy::RangeInclusive)\`* - (3 checkboxes left)
  - merge rust.tf/157226 *Stabilize \`box\_vec\_non\_null\`* - (3 checkboxes left)
  - merge rust.tf/156906 *Stabilize \`dyn Allocator\`* - (2 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (2 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[dtolnay (3)](https://rfcbot.rs/fcp/dtolnay), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [joshtriplett (5)](https://rfcbot.rs/fcp/joshtriplett), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [Amanieu (6)](https://rfcbot.rs/fcp/Amanieu), [the8472 (10)](https://rfcbot.rs/fcp/the8472)

### (nominated) rust.tf/libs806 *Request: guarantee that \`Vec\` field order is independent of type parameter*

Nia: C++ interop relevant.

The 8472: They're asking for more guarantees. Not just that the offsets are the same but also that you can load the values verbatim. So we can't make any changes to the values in the future. They want to be able to transmute the `Vec`. Historically we said we didn't want to do that and that's why from/into_raw_parts exists.

Orson: We changed layouts in the past. E.g. `CoW` doeosn't need to make branches when accessing slices. That's the kind of change we wouldn't be able to do.

The 8472: They want this within 1 rust version. They want a guarantee that we wouldn't get polymorphic (mono differently depending on `T`)

Orson: Could ZST `Vec`s be 16 bytes?

The 8472: In the doc we say it'll always be 3 `usize` wide. But if that guarantee didn't exist we could make that change

Clar Fon: The main issue is the way C++ APIs work. If you're offering various field pointers to the type, C++ would do type erasure that Rust wouldn't.

The 8472: Programmerjake said that at compile time the layout information for each individual monomorphisation C++ can't query because C++ could run some const eval when instantiating the templates

Nia: I'm not sure what guarantees we'd lose from this change. What they're asknig is to be able to safely transmute `Vec<T>` to `Vec<U>` as long as you can safely transmute `T -> U`

The 8472: Just transmuting is insufficient. We could do things like type-specific memory tagging in a way that's not specific to the system

Orson: They requested `Vec`, but ther's a lot of other types. Would just Vec satisfy their needs?

The 8742: How does that fit in the "crabi" RFC? That faced a similar issue. Wasn't the goal there to have e separate type for FFI Vec?

Josh: That was a step further to having a stable cross-version ABI

The 8472: What was proposed there for the stdlib type, wasn't that an FFI vec?

Josh: The original point was to define how specific types were passed across the FFI boundary. The original point would be for Vec and String to be able to passed across FFI. But for e.g. HashMap you wouldn't define a layout. You'd get a pointer and some functions

The 8472: So it wouldn't solve monomorphization problem. You'd pass pointer

Orson: They want one vtable for all vecs, not one vtable per monomorphization

Josh: People assume they can do that already. I think it's reasonable to say "I have a `Vec<u32>`, I should be able to create a `Vec<f32>`".

The 8472: I think randomized layout would break that

Josh: That's not stable and I think this is an argument against ever stabilising 

Josh: I think it's reasonable to change an owned buffer from one type into another assuming the underlying types are compatible. And Vec is an own buffer

The 8472: We have methods to do this (into_raw_parts etc.)

Nia: I think the correct way around this is to give certain guarantees so people can rely on undocumented stuff. If we don't specify a layout but say it'll be consistent

Orson: That's what they're asking: same order across monomorphisations within a single Rust version. They mention `repr(ordered_fields)`

The 8472: The problem is it pessimises the layout if you add an allocator. They only care about Vec with Global, but we have other usecases.

Clar: I think it it would be reasonable to say we demonomorphise raw vec into the allocator, capacity and pointer. That's what the person wants with the exception for the length of the Vec.

The 8472: Capacity is also type dependent.

Clar: It is, but the actual position of the field is not.

The 8472: The layout algorithm looks at niches and if we get alignment of niches, it could be type dependent in the future

Orson: Then I guess we should reject it if it'd prevent doing this in the future

The 8472: The question is how important it is

Nia: I'd still argue in favour of this. I don't think future possibilities would outweight helping C++ now

The 8472: We'd need to designate this somehow

Orson: accesses can be inefficient

Nia: We're not being asked to do `repr(C)`. The only thing we're being asked to commit to is taht if there's an adequate `repr`, we make the guarante, the repr will be consistent with regards to T

The 8472: I think they need not just the guarantee order but they'll also need to be able to access the fields. THey'll need to implement the length and ?? pointer themselves. They want to mess with the internals of `Vec`.

Orson: Maybe we should get back to the author

Amanieu: I understand what they're doing. They're just generating a blob of suitable aligned bytes that can hold a Vec by extracting the size and alignment from rustc and using that from C++. So they want the layout of `Vec<T>` to not depend on `T`

The 8472: https://github.com/rust-lang/libs-team/issues/806#issuecomment-4636124447

> Assume that all invariants of the type will be upheld by any C++ code that exists. For the simplest case, imagine that `Vec<T>` is read-only and provides only const begin()/end() pointers, data(), and size().

The 8472: They need to look inside rust code

Nia: It's still possible to read through that

The 8472: If they use `Vec` methods we insert a bunch of type specific stuff and that could cause issues

Nia: Transiently instantiating a concrete function will under this proposal be a consisntent way without knowing what the underlying type is

Amanieu: You can transmute `Vec<T>` to `Vec<U>` as long as the layout is th esame?

The 8472: For this method, yes. But it still depends on a bunch of implementation details

Clar: I'm just looking at the layout of the type. This only applies if you have the same allocator. If you look inside RawVec, the only thing we'd really have to do is going to Vec to guarantee the Vec is not moved around. The layout is independently changed for every `Vec<T>`  which can change

The 8472: That's all based on the current implementation. When we stabilise the Allocator and decide to change the layout because it's suboptimal, all these assumptions will change

Nia: They just want to be independent of the allocator

Orson: you run into bugs if one type is ZST vs not

Nia: FFI code can check that. I think the guarantee we're being asked to give is a small enough one that doesn't hamstring us

The 8472: It might be worth it but it does constrain us

Nia: I don't think it constrains us enough that we're likely run into in the future. And even if we do, I still think this is worth it.

Nia: I don't want to make stronger guarantees. But guaranteeing just over type is relatively tiny one

Josh: I was going to say the same thing. That this is allowed only for Vec with the same allocator

Nia: Yeah

Amanieu: There's same discussion happening for `Box` which is lang nominated

Nia: I'd assume the same argumets appyl for `Box`

The 8472: `Box` is already specially treatead by the language. Vec is less special type and has more freedom at the moment

Orson: Someone mentioned alignment niches. If you have a highly-aligned type, you can end up with a large niche. If a Vec occurs within a larger struct, something might end up in there.

Clar: We don't use the alignment niches at the moment. If we do account for alignment niches, we'd emit 20 different impls for Vec. 

thomcc: Would they be allowed to call code methods?

The 8472: What are you going to do with the fields? Either you write your own logic which means you'll make your own assumptions on within the fields. Or they call methods but our methods are monomorphic which are type specific. So they'll have to rely on methods.

thomcc: I agree, they'll likely want to be able to call methods with the wrong types. And we shouldn't support that.

Nia: It doesn't seem like they want methods.

The 8472: Sounds like thay do 2 things: (a) for each rust version they generate custom code (length, pointer getter) to compute the offsets of the field.s They rely on field order 

thomcc: Allowing transmute across different types and calling methods sounds hard to support

The 8472: They're not calling methods, they're generating methods based on other assumptions

Nia: This seems we have a beginnig of better and nicer things

Nia: i would more broadly be *highly* in favour of saying if T can be transmuted to U then `Vec<T>` can be transmuted to `Vec<U>` 

Orson: That's not what they're asking for though

Nia: Yes, but this opens up for more dores than it closes

Chris: Can we bring the C++ interop group into this?

Nia: And opsem etc.

The 8472: We'd also need a support from the compiler for new wrapper types

Orson: I'd like to also see examples of what they're actually need


### (nominated) rust.tf/69835 *Tracking Issue for layout information behind pointers*

Nia: Nominated 2 days ago. Ralf saying we should stabilize it. It needs an FCP if we're doing that.

The 8472: There are a bunch of unresolved questions. It's also a bit older. Does the new Sized hierarchy change any of this?

Amanieu: No, these wouldn't be available to pointers to extern types

Nia: they're marked as `?Sized` so `MetaSized`

Amanieu: I think they should be unsafe

Orson: I think they should be safe if you're allowed to call them on valid pointers

Nia: If the metadata was a vtable pointer, that would have to be unsafe.

The 8472: If the vtable always has to be valid ??

Orson: Is it allowed a previous stdlib function to make unsafe safe?

Josh: I believe we have a mechanism for this and not warning that it's in an unsafe block for compatibility.

Orson: If we have that ability, I'd be in favour of stabilising as `unsafe` and relaxing it later

The 8472: Does this change the pointer type? Function pointers need the `unsafe` keyword to denote an unsafe function so it'd be a differnt type.

Josh: Last I saw I thought we settled the layout question of "do we require an invalid pointer to have a valid metadata to pass it around". I think we settled that if you have an invalid pointer, you still need valid metadata

Josh: I think there was a lang FCP saying that it's required.

Nia: The current safety required mechanism say this. It's the general policy. So yes +1, stabilize as is

Josh: The fact we expect you have these be valid, ??. You can in safe code make an invalid pointer and we ??

Nia: Is everyone okay with that? 

(no issues from the room)

Nia: We can start the FCP


### (nominated) rust.tf/117108 *Tracking Issue for \`mapped\_lock\_guards\` (\`MappedMutexGuard\`, \`MappedRwLockReadGuard\`, \`MappedRwLockWriteGuard\`)*

Nia: I had several people come up to me saying they'd like this mechanism. I'd like to stabilise this.

Amanieu: We'd like the MutexGuard map API to match the one in RefCell's ref and ref_mut. THey're logically the same thing.

Amanieu: We currently have: https://github.com/rust-lang/rust/issues/143801. Where we're trying to add TryMapError. And that's blocked on ??

The 8472: Isn't there work on pointer projections? would that be relevant here?

Orson: This is almost always a projection.

Orson: The tracking issue only has `map` and `filter_map`. It doesn't have `try_map` from RefCell.

Amanieu: `parking_lot` has an entire different set

The 8472: Arc also has a `map` API

Amanieu: That's fundamentally different from Cell

Orson: The `Arc` one is for owned values

The 8472: I though we had something that borrows something from Arc?

Josh: We're adding the `ArcRef` mechanism.

Amanieu: I think we don't need to worry about that. We can stabilise that later than the one in RefCell and Mutex

Nia: Either way, sounds there are some unresolved questions.




### (nominated) rust.tf/147368 *Add extra \`unchecked\_disjoint\_bitor\` implementations.*

(discussed last week, skipping)

### (nominated) rust.tf/154811 *SGX name resolution APIs*

Amanieu: The issue is that SGX doesn't believe that IP addresses are numbers. It believes they are strings. In SGX `SocAddr` should be a string.

Josh: What are they trying to do such that they care about that?

Amanieu: The syscall API takes a string. 

Orson: Can't we cretae a string at the moment we 

Amanieu: You have to give the original unresolved name to the syscall. You can't resolve the name

Orson: We have one platform that supports this

Amanieu: And this is specifically about that platform

Amanieu: on a normal system you'd call 'gethostbyname', on SGX you call `connect` on a string

Josh: Why is this something we can't work around? In SGX you connect to a given host and you assume whoever's responsible for you connecting to it 

(???): `connect` call in std is generic and supports anything taht supports ToSocAddrs. You can just pass in a string. IS this a question about the internal implementation?

Amanieu: The ToSocketAddr trait implementation would return the error type. And the implementation would recorver the address from the error type and connect to that

Clar: What is the thing we're asked to do here?

Amanieu: For `SocketAddr` on SGX to be represented as a string

Nia: If this doesn't touch another architecture, then that's fine

Josh: `std::net::SocketAddr` is an enum of `V4` and `V6`.

Amanieu: Proposal, what if on SGX we made a breaking change to the SocketAddr enum that only takes one variant which isa string

Orson: Alternative, we keep the two socket addr API. Use doc hidden. 

Josh: That's not far off from what we're doing. We're just doing it directly on the method that takes the string. The reason was this was filed and nominated was we asked for a solution that sucks less.

Josh: We currently have `std::net::SocketAddr`. It has `V4` and `V6`. We also has `std::os::net::SocketAddr` and `windows::net::SocketAddr`

Orson: Even if you never match on `SocketAddr` still implements `Copy`. A `String` inside would not let you do that.

thomcc: That's already a breaking change anyway, so dropping copy may be fine

Josh: It's obvious what representation we could use for this on SGX. The issue is with the APIs we currently have that expect two socketaddrs. Right now we're assuming you can take anything that's a ToSocketAddrs, pass it to a type and get an Iterator of SocketAddrs. This is a bad API because it doesn't allow other socket types.

Nia: We could leave it half-broken as is and a reason for doing sockets v2 or something.

Josh: We currently have a method that does a `connect` and it'll take the opaque string. We could try to add a separate `connect_opaque` method that takes the opaque string and converts it opaquely and end s up with `TcpSocket` or an error. The downside is you need to write code that needs to work on SGX.

Orson: Can we change the signature of `connect` to require it to accetp a `ConnectableBikeshed`. IT would accept the ToSocketAddr and we'd also implement it on a string.

Josh: ToSocketAddr isn't a sealed trait

Nia: This would also break inference

The 8472: ToSocketAddress already is a bunch of types

Nia: If we specialise on a concrete type, that would work but it would also be a half-assed solution

Amanieu: on SGX today the only way to make connect work is because it allows you to return a magic error types

Josh: To the extent we're just solving SGX's problem, it's not the worse thing we could do. The goal of trying to do a something different is "can we solve more general problem that's not just for SGX"

Nia: And for that we'd like an RFC for things that would support windowns path sockets or unix domain sockets. And until that SGX will deal with a hack.

The 8472: Which hack would be that?

Orson: The error type isn't great but it works

Nia: Might be worth writing a small ACP and see what's nice

The 8472: That's an implementation detail not a user-visible change

Josh: Yes, we should ask whether they can reduce the amount of platform-specific code. They already did something there already.

Nia: Then I'm happy saing we stick with that and every other improvement is dependent on a bigger overhaul

Josh: Right .One of the original asks I brought up was: SGX added support for doing other things with the opaque strings (named services, qualified service strings). Those are things we may want to support in a compatible fashien.

The 8472: That's a better DNS resolution, but also a completely separate topic

Josh: Yes, but now the opaque strings provide more functionality than just SGX's ability to connect to a socket

Nia: How should we respond?

Amanieu: I'm happy to do nothing and leave the magic error as it is. AS for a wider socket refactor, that's completely beyond scope of this issue and SGX should not be th ereason to motivate us

Nia: I agree

Josh: Agreed. But there would be a value to talk to SGX folks about being able to provide name resolution.

Amanieu: In SGX, a string doesn't just resolve to an IP address. IT resolves to IP address and a port

Josh: That's true for us too. Our SocketAddr is IP address and a port

Orson: SocketAddr::V6 is completely private fields and opaque (devolves into insanity)

Josh: SocketAddr::V6 can also accept an arbitrary string

Amanieu: It implements Copy

Amanieu: I'd mention the doc_hidden trait if they object to the special error type

The 8472: Are people actually using SGX?

(the room: yes)

Josh: one of Signal's servers uses it.


### (nominated) rust.tf/155421 *Document panic in \`RangeInclusive::from(legacy::RangeInclusive)\`*

(same, stayed nominated from last wk)

### (nominated) rust.tf/156252 *Make \`Error::type\_id()\` an inherent method on \`dyn Error\`*

Nia: Mark nominated this. This a pattern I ran into as well. dyn + something else doesn't necessarily iimplement a method if dyn implements it.

Josh: I thought I remember something about trying to make type_id final-ish

thomcc: We tried to stabilize it and then we had to back out of it right away.

Amanieu: If you make it final, it's no longer part of the vtable. This would return the type_id of the `dyn Error` rather than the underlying error type.

Clar: Does that mean this implementation doesn't work?

Amanieu: This one's not final, just hidden

thomcc: So the idea here is we'd keep the unstabl emethod in the vtable and we'd call the inherent method from `dyn Error`?

Orson: The explicit method is only implemented for `dyn Error + 'static + Send`

thomcc: Then there's no reason where it can't work

Amanieu: There is a ?? bound

Orson: I've long been a fan of putting the typeid into every single vtable. This would duplicate that. But if we ever end up doing that, this could forward to that so it's not a blocker. It's not just dyn Error where you want this, right?

thomcc: It's particularly useful for Error to get the concrete type of the Error without downcasting. I never needed it enough to impleeeeement it, but it was deemed important enough to be added to the vtable. Is there a reason not to do that?

Nia: Mark mentioned that it's a bit janky but we could live with it.

Nia: If you have `dyn Error + DynSync`  you don't have the dyn entry there. If we have a way of saying `impl for dyn Trait + ...` that would be nice. But for the time being you could add somenthing like that backwards compatibly. So the API would be fine ,just slightly ugly

Amanieu: Why does this have to be an inherent method? Could it be a final method called type_id that calls the hidden non-final type_id?

Orson: you can't stabilize final methods

Nia: You can

Josh: There's nothing preventing us from having a safe final method calling unsafe non-final method

Amanieu: I'd just make it hidden. The fact that the type_id intrinsic doesn't work is kind of an internal detail

Orson: If we're okay stabilizing a final feature. It would make us depend on depending on `final` for being in the language

The 8472: We'd have to ask lang

Josh: The only concern would be if we had to rip it out due to soundness issues and I don't see that happening

The 8472: programmerjake said this could be a separate trait and we don't have to reinvent this for every type that wants to return a type id? https://github.com/rust-lang/rust/issues/60784#issuecomment-511039223

Nia: I guess that would work

The 8472: Seems someone started an RFC based on that comment: https://github.com/rust-lang/rfcs/pull/2738

Orson: If we added that for more types than just Error, that would be nice. Wouldn't that essentially be called like `StaticAny`? Or the opposite (any dosen't imply static)

Amanieu: Are we okay making type_id method that calls a non-final one?

Nia: There are other proposals but I don't think those would be incompatible

Orson: IT would be incompatible. You could call Error::thing

Nia: And that would still work because `Error::TypeId` would be a supertrait

Clar: I don't think you can resolve child trait from a super trait

Nia: You can add it to the prelude

Nia: I'm happy to thumbs up the proposal as is now

The 8472: Wouldn't final cause a shadowing error?

Amanieu: Once we have supertrait item shadowing it will always prefer the supertrait

Amanieu: What's the advantage of the supertrait vs. the trait method restriction?

The 8472: The supertrait could be applied to other types than just Error

The 8472: Could we move this up to a supertrait of Any that works on more than just static?

Orson: Make TypeId with a method type_id that's a supertarit of Any.

Amanieu: that lets you add type_id to any type that needs it.

Nia: I'd like to see such a proposal in more detail

Orson: I can maybe cook something up for the next week. I'd suggest holding off on stabilizing this to see what we can do here that's more general.

Nia: Okay, we defer to next week pending a proposal

### (nominated) rust.tf/156906 *Stabilize \`dyn Allocator\`*

Nia: I tried to put up a hackmd doc as we discussed at the meeting. I ended up having more options in the hackmd than opinions written down. [I have a PR](https://github.com/rust-lang/rust/pull/157428) that has a separate trait for dynamic dispatch allocators. And I made it so effectively every Allocator is also a dyn Allocator and vice versa. The problem with this impl is a lot of `impl Allocator` for all the auto traits.

Nia: Other proposals were but up: https://hackmd.io/@nia-e/rk-cq53lMx

Nia: The most compelling one was what Josh had: we'd stabilise Allocator so it's dyn-compatible.

Orson: The associated constants on const allocator is the only thing we see that would be incompatible with dyn allocator

Orson: Should const fns ever become stable in traits? Those are never dyn compatible?

The 8472: How can they possibly be const and dyn at the same time?

Nia: This requires that const is maybe const so you can't call it at comptime

Orson: You wouldn't be able to call it from dyn but you'd be able to call it from concrete type

Josh: That would mean that const allocator and dyn allocator would be disjoint.

Nia: dyn Trait is a concrete implementor of Trait.

The 8472: How close to realisation would be those maybe traits?

Josh: We're expecting to ship them. It'll probably be a while. The feedback we got from types is it's not likely to stall out and be issues. But I expect this would mean we're taking some set of features and saing either they're blocked on maybe trait or we have some subtrait you d'nt necessarily have and if you have those methods you can't call them if all you have is a dyn allocator. You can only call them if you have the extension trait

Nia: Based on that I'm leaning towards having a separate DynAllocator trait.

Josh: We could also say these are not const in the sense you can use them in the const block. We're providing methods we expect to return const that are inlinable. And in practice we'd expect them to be monomorphised and const later. That would solve a lot of the "I wantto use these for optimisation" cases

Nia: That wouldn't solve everything, but it's interesting

The 8472: I'd prefer having theconst stuff. But if maybe traits are on the table and we have a fallback approach with the "not really cost" methods then maybe that's good enough

Nia: I think that's a fair compromise

Orson: last time I asked what's the usecas efor uninhabited allocators was zero-sized ??

BurntSushi: and "Cannot nicely express associated types in method signatures, notably the case of Err = !" ? 

Josh: We could provide that via a subtrait as well. We'd have an allocator that has a 

The 8472: But the subtrait doesn't help if you just want to plug an allocator into an existing collection. The collection is monomorphised based on the allocator type and all the error paths are unreachable

Nia: you could if you wrote a really ugly code

The 8472: HashMap isn't written that way. 

Nia: I'd like to pick one of the option

Orson: We agreed the aborting allocator would be the only reason for an uninhabited allocator

Nia: There is a reason: nmap-based allocators. It can reserve more memory than the system has and it'll only error when you use 

thomcc: nmap can still return an error. You have to handle that error so the branch exists

Orson: I'm not convinced by associated error type

Clar: The associated error was the main purpose of having a separate trait. But does making a separate trait ever prevent us from making a dyn allocator in the future?

Nia: If we have a dyn DynAllocator and we later decide that allocator should be compatible, then both dyn DynAllocator and dyn Allocator would implement allocator. You could do it, it's not technically a breaking change

Clar: I think the error would be useful, but it wouldnv't show up in that many places. We also have the ?? niche as well.

Nia: we have a larger niche. You can return an usize-sized error

Josh: we have the baseline Allocator which can't do associated constants, errors et.c and it's not dyn compatible. We could either say we support dyn Allocator and you have to wait for the other features. Or we can start supporting the features and if you want to have a dyn Allocator, we'd have to have a separate DynAllocator. So we need to pick what we're going to prioritise

Josh: either we say allocator is dyn compatible or we say we want to support all these other features

Nia: would anyone say that having an associated error type is blocking for stabilising allocator? IS adding an associated error a blocker for stabilising an allocator without it?

The 8472: It's a nice to have

Nia: Agreed, but at this point it would not be worth the hassle

Clar: it's not worth it

Josh: having an associated type rather than a fixed type is not worth it. Nobody's going to need an associated error type that isn't the ! error type. If a specific alloc error is sufficient for every other usecase 

Clar: The layout is almost always going to be bigger than the error we could fit in the niche for the error. Ultimately the response is you don't have enough memory. If you can't figure out the layout from that, ??

The 8472: The Linux kernel folks would have other errors

Orson: AllocError is an empty struct now. Can we make it into an opaque pointer and passe it along.

Amanieu: You'd have to have that pointer point to a thread local storage or to an allocation and you're paying the cost on every allocation of passing tahat pointer arround

The 8472: You could have a static table of possible errors

Orson: I meant a c-style API, truly opaque pointer

Clar: This is firing all the alarms of C code passing `void *` everywhere

Nia: Either way, it seems we have an agreement we dont't need AllocError be an associated type

Nia: Another point: should it return the slice or a thin pointer?

(agreement to talk about this next week)

thomcc: https://gist.github.com/thomcc/9a328dee5ad8c6e9086942fdb1e4c2b4


### (nominated) rust.tf/156983 *Add \`io::Read::read\_le\` and \`io::Read::read\_be\`*

Amanieu: It seems fine to me

### (nominated) rust.tf/157168 *Stabilize \`RandomSource\` and \`DefaultRandomSource\`*

Josh: We mostly finished discussing this last week. Someone raised the point that `Sys` is a nice abbreviation but we have no precedent for `Sys...` rather than `System...`. Worth asking

Orson: I strongly oppose an Rng trait. You said "I want this to be a trait otherwise we'd be committing to this being an EII on the basis they'll want to change what `fill` returns". That argument is not acceptable

Orson: If you say "the way to override randomness is passing around trait" you're forcing every library thatcurrently relies on getrandom, ??. Creating a true random number is as fundamental for the platform as allocation is. And that's an EII

Clar: There are still 2 option: true randomness (I think we can agree that doesn't need more than one source) but in terms of secure randomness 

Josh: We're trying to provide a trait for random number generation and a default trait for the system random implementation.

Orson: One is a  clear need that everyone wants. The other is highly opinionated, easily to get wrong. I don't we should do this now.

Josh: That's a generic argument for adding anything to std ever

Nia: The trait is nice piece of vocabulary for expressing "I'm a source of randomness" and one that will be used in std

Josh: The main goal for discussing this at the moment was to resolve the last piece of bikeshed around "Sys". Does anybody have concern about that particular detail? On deferring we've spent a lot of time in the past meetings

Orson: If you name it RandomSource I'd have a lot-less problem. That implies that it's for implementing random sources vs. implementing specific random number generators. Source gets randomness out of the environment.

Orson: I don't think PRNG should implement what you're calling Rng

Josh: When we first designed it and called it RandomSource, we meant it as "this is a random generator"

Josh: are there opinions on the system / sys?

Amanieu: If we have a `random` function, I'd expect that to source from the DefaultRng. As opposed to the SystemRNG that would be different form th edefault

The 8472: I don't think we should have a default. We should have System/Sys (I'd prefer sys as it's shorter)

Orson: If you want to follow the Allocation scheme of things, it'd be `GlobalRng`

The 8472: On modern linux, the impl is fast and secure and you can use it for both. But not all platforms are like that. Some have a slow secure and a fast maybe less secure userspace impl. I don't think we should be making sweeping statements for "default". "default forwhat purpase"? Global is the same issue "global for what purpose"

Nia: in terms of consistency with allocators I tihkn System is the right name

Josh: The problem that "system" vs. "default" has semanting meaning is helpful. To what extent are people treating the rename from "default" to "system"/"sys" is a semantic change. That didn't come up last week.

Nia: For the narrow purpose of naming "System" or "Sys" is better

Orson: System is what I'd say to. Global would be what I'd prefer if we have an overridable Rng with System being the default

Amanieu: For System vs. Sys there's preference for "System"

Josh: I agree. But we don't have a clear resolution about "default" vs. "system"/"sys"

The 8472: I'd say the precedent isn't that relevant. SystemAllocator is a standalone name.

(much further discussion on both semantics and bikeshed color)

Nia: abort, abort

### (nominated) rust.tf/157226 *Stabilize \`box\_vec\_non\_null\`*

Nia: I'd say sure

Amanieu: I disagree. If I want a pointer to my slice for read-only purposes, I should be able to get one

Nia: you can, just not `NonNull`

Amanieu: It should be `NonNull`. In order to call the slice version you need to call the `deref` impl, which requrires all of the elements to be initialized. The pointer you get after deref only has enough provenance to access the elements in the slice. It doesn't allow you to access the excess capacity.

Chris: With that in mind do you think NonNull makes sense?

Amanieu: I can see why you'd want this on slice as well

The 8472: For slice, how the reference is derived matters. For Vec it doesn't really. It goes into the underlying pointer. On Vec it's really more guardrails than strict safety requirements.

Amaniou: yes, but I don't think the guardrail is needed when it blocks you from doing useful things

Nia: Ralf points out that people will get NotNull and people will think they can mutate through that that's fine. But you get this through a shared reference and the type is Freeze, this is unsound

Josh: If we get this from a shared reference and you mutate through it, you're likely doing something wrong

Nia: You'd have to call `as_ptr` and cast as mut

Orson: `as_ptr` explicitly doesn't allow you to write through the pointer. It's clearly stated in the docs

Nia: Because as_ptr takes a shared reference

The 8472: It's not a provenance issue. The pointer is coming from the shared reference

Orson: you're creating a 

The 8472: If you have miltuple threads accessing your vec, you're likely to get aliasing violations

Orson: yes, but I know what I'm doing and the API doesn't let me do that. So with that in mind, I'd concur with amanieu that this shouldn't take an immutable reference

Amanieu: or have two versions

Nia: I'd rather see two versions

The 8472: This isn't doing stack borrows or tree borrows. It's not deriving the returned pointer through the reference. That's true fo rslices. But here you read through the reference, you retrieve it from somewhere in the memory and it's the same in both.

Amanieu: We can't agree so let's remove the method from the stabilization

Josh: We could defer that and the Box method too

Nia: I'd also defer this. We can look at the pointer stuff next week and ask Ralf whether the shared/mutable thing runs into the provenance thing


### (nominated) rust.tf/157515 *Tracking Issue for \`xdg\_basedir\`*

### (nominated) rust.tf/157573 *std: simplify the random feature*


### (waiting on team) rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>*

### (waiting on team) rust.tf/157428 *allocator: refactor for stabilisation*



### (new change proposal) rust.tf/libs812 *ACP: Add \`HashTable\` to \`alloc::collections\`*

### (new change proposal) rust.tf/libs811 *ACP: convenient numeric conversions dependent on overflow\-checks*

### (new change proposal) rust.tf/libs810 *ACP: Missing floating point conversions*

### (new change proposal) rust.tf/libs809 *ACP: library folder APIs for \`std::os::darwin::path\`*

### (new change proposal) rust.tf/libs808 *Type\-smart \`intersect\` on ranges*

### (new change proposal) rust.tf/libs807 *ACP: Add \`BTree\` to \`alloc::collections\`*

### (new change proposal) rust.tf/libs804 *ACP: Add allocation\-free owned downcasting for Any values*

### (new change proposal) rust.tf/libs803 *ACP: redesign \`proc\_macro\_diagnostic\`*

### (new change proposal) rust.tf/libs802 *ACP: give access to the inner iterator of \`std::iter::Peekable\`*

### (new change proposal) rust.tf/libs801 *ACP: add volatile operations to \`Atomic\*\` types*

...

The 8472: Do volatile operations participate in the same way in fence as reordering operations do?

Amanieu: Yes, the same way as relaxed order does

The 8472: The proposal has orderings now. Previously the proposal was the operation was relaxed. But follow-up discussion said you should be able to specify ordering into it

Nia: That's fine. I don't like it but I think we need to do that

Orson: Let's say I have 2 memory location: I do volatile read on 0 then volatile read on 1 and then volatile read of u16 on both

Amanieu: That is allowed. The spec on volatile comes from C and it says the behaviour is target specific.

The 8472: But here we have volatile atomics. Those are different

Amanieu: The behaviour of that is "implemntation defined"

Nia: If you do it on Orson's example, I don't think there's 

Orson: Do we currently support it on volatiles?

Nia: We want to allow the implementation and possibly restrict it later

The 8472: This is FCP. If Ralf says it's useful for implementation, then yes

The 8472: I looked some of the opsem discussions and people were confused by some of the stuff taht happnes there?

Nia: Do we want to accept this?

The 8472: Right now people have to do either. Sometimes you want to use both

Nia: Is that a light thumbs-up?

Amanieu: I think we should just give atomic properties to normal volatile load and store

Nia: I agree that because the way people use it, all volatile accesses should at least require relaxed ordering

Amanieu: Volatile atomics are used in a lot of places where normal atomics would be enough. And this is going to make it worse. People think they need volatile for IPC but they just need atomics.

The 8472: There are 2 reasons for volatile atomics: side-effects for memory access

Amanieu: That's MMIO

Nia: What's stopping a target saying "MMIO can be accessed through atomics"

Amanieu: Nobody does that

Nia: there are usecases like reading from procfs where what you write and read are corellated

The 8472: There may be an IPC usegase where one process is trusted and the other is untrusted. IPC with an untrusted remote side is where you want the volatile property because it's untrusted and the atomic property because conditioned on the other side being well-behaved you need a protocol that is acquire/release

Nia: I'm leaning in favour of the ACP

Amanieu: Let's just add an experiment. I'll complained at FCP time.


### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*

### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs544 *\`Arc\<impl !Sized\>\` constructors*

### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs314 *Add security\_attributes() to windows::OpenOptionsExt*

### (stalled change proposal) rust.tf/libs287 *ACP: Add \`FromByteStr\` trait with blanket impl \`FromStr\`*

### (stalled change proposal) rust.tf/libs549 *Add \`uNN::checked\_sub\_nonzero\`*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
