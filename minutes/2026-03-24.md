# Libs-API Meeting 2026-03-24

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia Espera, Mark simulacrum, Amanieu, David Tolnay, Josh, Chris Denton, Max Heller

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

1 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3874 *build\-std: always* - (3 checkboxes left)

15 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/139087 *Fallback \`{float}\` to \`f32\` when \`f32: From\<{float}\>\` and add \`impl From\<f16\> for f32\`* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`Exclusive\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/111688 *Tracking Issue for ExitCodeExt on Windows* - (3 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/119639 *Tracking Issue for tcp\_deferaccept* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (3 checkboxes left)
  - merge rust.tf/139690 *\`impl Default for RepeatN\`* - (3 checkboxes left)

[joshtriplett (7)](https://rfcbot.rs/fcp/joshtriplett), [nikomatsakis (2)](https://rfcbot.rs/fcp/nikomatsakis), [BurntSushi (7)](https://rfcbot.rs/fcp/BurntSushi), [Amanieu (2)](https://rfcbot.rs/fcp/Amanieu), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [dtolnay (2)](https://rfcbot.rs/fcp/dtolnay), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [the8472 (9)](https://rfcbot.rs/fcp/the8472), [weihanglo (1)](https://rfcbot.rs/fcp/weihanglo)

### (nominated) rust.tf/138099 *Tracking Issue for #138068: Add \`Result::map\_or\_default\` and \`Option::map\_or\_default\`*

Josh: It's been sitting for a long time. The original RFC is from 2021. It went trough an FCP recently. Finished FCP in May 2025. I merged the RFC yesterday. It seemed to have existed in unstable for ages. I think it's reasonable to start FCP to stabilize this. I assumed it must exist and got a message that it's an unstable feature -- that's how I discovered it.

Nia: I see there hasn't been any controversy.

Amanieu: This seems fine for me, 100% in favour. Do we need an FCP?

Josh: We FCPd the RFC. I propose we start FCP for stabilization. I'll start that.

David: Is it in scope for proposal to give guide to clippy to not propose this?

Josh: I think it's within scope for us to give a guidance for *when* the rewrite is appropriate. There may be patterns where it should be.



### (nominated) rust.tf/149926 `P-lang-drag-1` *Do not deduplicate captured args while expanding \`format\_args!\`*

Nia: In the middle of active discussion.

### (nominated) rust.tf/151379 *Stabilize \`VecDeque::truncate\_front\`*

Amanieu: This is just a stabilization request.

Amanieu: The length parameter feels a bit weird. There's `truncate_front` remove all elements from the index compared 

Josh: I'd agree, even with the regular truncate, I found it's better to do ??

Amanieu: 

Josh: With `truncate` there's a third interpretation.

Max: Would something like `truncate_before` make sense?

Josh: It might. `truncate_front` keep this many items and `truncate_before` remove elements before this one?

Josh: I think the proposal was to add this method too. We should go with this stabilization and also add an ACP adding this.

David: Couple months ago we were discussing a similar API where it would take a range.

Amanieu: That's basically `Vec::drain` which is available in `VecDeque`

Josh: That's interesting. Does that suggest people who would write `truncate_before` should use `drain(..NUMBER)`

The 8472: Drain has a bunch of branches that cover the cases in the middle, causes more move of data

David: Drain is monomorphized for each different range kind

Josh: Yes, I'd expect it to be as efficient as `truncate_before` would be

The 8472: But it does additional work to move elements into an iterator

David: Not really, you drop the iterator right away 

Max: I'm worried more about the awareness issue of the method.

David: For awareness we have the same issue with `truncate` already. But for discoverability, the documentation could point to `drain` if you want to truncate front/before.

Josh: I agree that the documentation should point to drain for more complicated usecases.

Nia: Agreed but I don't think we want to block this on the docs.

David: Are we moving with `truncate_front`?

Amanieu: They're not competing proposals. `truncate_front` is what I think most people will want.

David: I wonder if truncate is the right word here. We have the `retain` family of methods. I wonder if this fits better in that family. e.g. `retain_last(N)`. For truncating at the end the meaning is obvious.

Amanieu: At soem point it just becomes a `pop_front` in a loop.

The 8472: We could have a `retain_n` 

Nia: Which would effectively be a `drain`. None of that necessarily stops us from stabilizing this. We can have multiple methods taht do similar-ish. I think `truncate_front` is a reasonable name, but that doesn't prevent us from putting a `retain_n`

The 8472: I think David proposed that exact behaviour but under a different name

David: Yes. But I'm in favour of either one.

Amanieu: I prefer stabilizing this as is.

David: And at this point we're not deprecating anything, right?

Amanieu: No.

Amanieu: I'll start an FCP.


### (nominated) rust.tf/152480 *\`format\_args!\`: only de\-duplicate captured identifiers that refer to places*

Nia: Same as the previous format_args

### (nominated) rust.tf/153555 *Clarified docs in std::sync::RwLock + added test to ensure that max reader count is respected*

Amanieu: The RwLock has a maximum number of readers before owerflowing its internal counter. That's platform-dependent. In practice you typically run out of threads first. The counter is big enough to hold one RwLock per thread in the process. Except if you acquired it recursively it.

Mark: We don't guarantee that you can acquire RwLocks recursively. It's in the documentation.

Nia: In that case +1. It's a small change

Amanieu: Yes. We can add a small paragraph saying the limit exists, it's platform specific but you're unlikely to hit it.

Mark: Do we want to guarantee a number and have a test for that number?

Amanieu: I don't think we want to guarantee. We want to be able to switch out the implementation.

Nia: I'm happy to keep a vague guarantee of "a lot"

The 8472: Some embedded systems the number might even be small.

Nia: I like adding the paragraph on it being platform dependent.

Mark: Today, a "correct" library shouldn't expect it can spin on read locks if it doesn't have a RwLock.

Nia: Either way, this is still probably good to merge.

The 8472: The PR adds a test that it can have at least 2^30 readers. That's enough.

Mark: Sounds like we agree we'll basically approve the PR, we're okay adding the test. It's not a stable guarantee but people can take a look at it.

The 8472: At least we'll knowif someone adds a target that doesn't uphold it.

Mark: True.

### (nominated) rust.tf/153603 *Add argv\[0\] fallback for current\_exe() on Linux when /proc is unavailable*

Amanieu: If you're running on a system where `/proc` is not mounted, `current_exe` returns that it couldn't find the executable path.

Nia: There's another discussion here: https://github.com/rust-lang/rust/issues/152269#issuecomment-3864547768

Mark: It feels we should make a decision on whether we can and what we want to do before we tweak things. I vaguely recall security implications.

The 8472: Returning a path is always a security issue. It looks at the symlink and in the meantime you can change the mount structure, change symlinks and so on. The very moment you obtain the path it can already be wrong. The only reliable thing is to open the proc thing. And even then you need to verify you're looking at a proc FS, there's no bind-mount etc. A security-aware system shouldn't look at this. So this is more doing the simple thing that's not necessarily guaranteed to be correct.

The 8472: Returning an error if it doesn't work seems fine. And people raised issues that `argv[0]` can be renamed at program startup.

Amanieu: `argv[0]` can be an arbitrary string that can be unrelated to the exe path. It depends whether we want to guarantee to give you the correct answer or fail. Rather than give you the correct answer, or the next best thing and otherwise fail.

Max: The docs note it shouldn't be trusted.

Josh: I'd still not use `argv[0]` because it can be lied to. You could get the original thing you executed, but 

Amanieu: Does teh documentation of `current_exe` say it has to be absolute path?

??: It says it's the full FS path

Josh: And people use it that way.

The 8472: Is it even relative to the working directory, relative to the parent etc.?

Amanieu: Depends on how the program was invoked.

Nia: Given all this, I'd say "no". Feels there's too many weird edge cases.

Amanieu: I'm somewhat inclined to support the `getauxval` path.

Josh: I wouldn't object to `getauxval` to find out the path and report itc if it's absolute. But I wouldn't try to resolve it if it's not absolute. Just fail instead.

Nia: Agreed.

Josh: I have some experience with getting things into usptream linux kernel. Adding PIDFD_GET_EXE would probably be uncontroversial. If someone gets it into the upstream, we could start using it at some point.

Nia: I'm not sure I'm comfortable with the idea of trying to figure out the absolute path.

Amanieu: Can we get a quick survey of what the other programming languages do?

The 8472: We could ask someone on the issue to do that?

Nia: There's enough open things there for us to not accept it right now.

Nia: Do we have a consensus if we get an absolute path?

Amanieu: You'd get an absolute path in the `getauxval` if the executable in the system `PATH` env var.

The 8472: The PR also mentions a chroot environment. Does the path get adjusted for the chroot directory?

Amanieu: Based on the discussions, we just require `/proc` to be mounted. In practice it's always mounted. It's in containers, chroot.

The 8472: The author explicitly mentions containers where it's not.

Nia: When you do weird things, you have to adjust to weird circumstances.

Amanieu: It's unfortunate, but I don't see any better solution.

Nia: Or there's the option we get something in Linux.

Amanieu: At that point you're basically recreating `/proc` using syscalls

Josh: Which is exactly what PIDFD is doing, I'm sure they'd be happy to do more of it.



### (nominated) rust.tf/153990 *Decide and document where stdarch intrinsics are allowed to diverge from asm behavior*

Amanieu: I answered this. I think it's still undecided how much we're allowed to diverge. Let's skip this one, I don't think the discussion is making a lot of progress.

Amanieu: If there's no progress next week, we'll un-nominate it.


### (nominated) rust.tf/154003 *Make std::fs::File Send on UEFI*

Mark: There's an opsem question beingf discussed. But for Libs-API I don't think we have a poilcy on making things Sync or Send on Tier2 targets.

Josh: I think this should be FCPd. We go out of our way to put any cfg-conditional methods into extension traits rather than directly on a type. If we implement `Send` for a type conditionally on specific platforms and not other, that makes it very easy to write platform-specific code.

The 8472: `File` is already `Send` though.

Nia: I'm not a fan of the fact that TCPStream was made `Send` in [that other PR](https://github.com/rust-lang/rust/pull/150990). And making things `Send` only on some targets is a horrible precedent.

Mark: That's a different situation though.

The 8472: There's some sort of type registry where they have some UUIDs they need to call to get the functionality there.

Amanieu: In general we don't want to make `Send` conditional. Because docs will show it as implementing `Send` and that will be confusing.

Amanieu: Also, the standard library doesn't provide the async signal safety

The 8472: If we're rolling everything ourselves, we should say this specifically.

Amanieu: In general, anything in the standard library can allocate and allocation isn't async signal safe.

Amanieu: In UEFI you can have interrupt handlers, don't call the standard library inside interupct handlers.

Josh: There's nothing in the File datastructure in UEFI that is thread-specific in a fashion that would be broken. The only reason it wouldn't be `Send` is because you can't call it from other threads. Because the protocols themselves internal to the firmware don't expect to be called from outside the bootstrap processor. But nothing in the File oject is not sendable.

Josh: I don't think we should be taking a data structure in the standard library that's not ?? and making it send not Send because this platform doesn't have any threads. In here it makes sense only because it includes a protocol where that can't be called from another thread.

Mark: Isn't that the definition of not send?

Josh: I'm sying File is Send on other platforms. So you won't be writing non-portable code. Here you can start other CPUs on UEFI, but it's understood there's no OS on those other threads.

The 8472: We define the target as single-threaded in the target-spec, which also affects how Atomics are lowered.

Mark: Yes, UEFI is defined as single-treaded.

Mark: We need an issue tracking assertions (static asserts?) for all types implementing `Send` and `Sync`. And for this particular change we want FCP to implement `Send`? Or sholud we try to claw back TCPStream and not implement Send here?

Josh: In this platform I think we should ship this. But we should get people into a habit of saying they should be asking for approval.

Nia: Agreed.

Amanieu: Yes.

## Part 2

**Attendees**: Nia Espera, Amanieu, David, The 8472, Chris Denton

### (nominated) rust.tf/154004 *\`Alignment\`: move from \`ptr\` to \`mem\` and rename \`as\_nonzero\` to \`as\_nonzero\_usize\`*

Nia: They thing they need us for is [naming](https://github.com/rust-lang/rust/pull/154004#issuecomment-4089025442). Do we name it `non_zero` or `nonzero`?

Amanieu: Looking at examples.

Nia: clarfonthey [lists](https://github.com/rust-lang/rust/pull/154004#issuecomment-4093205627) PRs with naming examples.

Josh: There are precedents with no underscore, but they're all unstable.

Nia: ?? non_null is two words, but nonzero is one.

Josh: That's my intuition as well, but I can't explain it.

David: I agree with Nia: non-null and non-temporal would be hyphenated in English prose, whereas nonzero would be one.

Josh: ??

David: (?? camel case)

Josh: I think this is the right naming. On a different note, I don't think there's value in it being nonzero_usize, nonsize would make sense since usize is the only reasonable type.

Nia: It would be more consistent with other methods that have usize in the name, but I don't have the source to back this up. Will look for it.

Josh: I wonder if the methods were named as_usize?

Amanieu: Not putting an underscore in nonzero is clearer. as_nonzero makes it clear that ?? are a unit.

Josh: Do we all agree that nonzero > non_zero?

(The 8472 and Amanieu agree.)

Amanieu: The point of the fns is if you want a `NonZeroUsize`, otherwise just call `as_usize`.

Nia: The discussion was on [DATE].

Josh: Would be bad to override something we already agreed on in a past discussion. Does anyone recall any discussions?

David: There was a discussion [linked](https://github.com/rust-lang/rust/issues/102070#issuecomment-3979107415) in the issue.

Amanieu: https://github.com/rust-lang/rust/issues/154237

David: Let's override and say ?? is clearer.

Nia: Do we want this to be as_nonzero or as_nonzero_usize?

Amanieu: as_nonzero_usize is my preference.

Nia: Mine too, feels clearer.

Josh: Mild preference for as_nonzero, but I won't stop folks if they want the other one.

Nia: There's a world where we might want as_nonzero_isize.

Amanieu: No, we'll always want a usize in the allocation context.

Nia: Makes sense.

The 8472: Should we be using `to` prefix instead of `as`? For example to_be instead of to_big_endian.

Josh:

Nia: `as` seems right.

Amanieu: `as` seems right. The difference between as and into() matters for Copy types.

The 8472: I only suggested `to`

Josh: The difference between `as` and `to` is less clear. I've seen it in guides but it seemed obtuse.

Amanieu: `as` is a value-preserving transformation, you're only changing the type, not the value. `to` (The 8472: e.g. `to_uppercase`) does more.

The 8472: ?? `int::to_be`, `char::to_u8`...

Amanieu: There are value transformations, whereas `as` here (alignment is a special integer) changes the type and preserves the value.

Josh: That's my intuition as well, and maybe matches Nia's. You wouldn't say `str::as_lowercase`, you'd say `str::to_lowercase`.

The 8472: What about `Ipv4::to_bits`?

Amanieu: bits are a different value, but `as_bits` could work.

Josh: This was justifiable when ... platform... Even here, `to` is a conversion. We use `to_be` because it's a potential value conversion depending on the platform's endianness.

The 8472: We also have `to_int_unchecked`.

Nia: These are probably named inconsistently.

Josh: We have a combination of vague guidelines and intuition. We already have a as_usize method, and that's a precedent...

Amanieu: It's unstable.

Josh: Right, then it's not a precedent.

The 8472: Would we want to tell users that transmuting alignment to usize is okay?

Josh: Technically it would be okay.

Amanieu: We probably don't want to guarantee this.

Nia: ??

Josh: This type is repr transparent. We havent' documented that but at some point we did decide to mark it so. This might suggest that ??

The 8472: This could be considered an implementation detail.

Josh: But it means we had it in mind, so might be worth documenting. We don't have to decide this simultaneously.

Amanieu: I don't think we should do that.

Nia: I don't see a compelling case for it.

Josh: I see the value but we don't have to do it now.

Amanieu: It could be useful for FFI when it expects a valid alignment.

Josh: Exactly.

Nia: What's the consensus? No underscore. Do we want _usize at the end?

Amanieu: Yes.

Josh: Does anyone other from me not want `_usize`? Happy to go along with consensus if everyone else wants `_usize`.

The 8472: I on't think it's useful because what else could you want? But also, I don't think this method will be used so frequently that golfing the name is that important.

Josh: Valid. Let's go with `as_nonzero_usize` then. I'd like this to not serve as precedent for *other* such methods having to have an `_type` suffix if it doesn't make sense in those cases.

Nia: I'll respond to that effect.

### (nominated) rust.tf/154065 *Stabilize \`fN::BITS\`*

Amanieu: I started the FCP.

### (nominated) rust.tf/154237 *\`mem::Alignment\`: Naming the \`\-\> NonZero\<usize\>\` method.*

### (nominated) rust.tf/154238 *Add a note on iterator exhaustion to the \`iter\` module docs*

Nia: We talked about this. This is about documenting Iterator behavior after `next` returns `None`.

Josh: I read this PR when it was proposed. I think scott's documentation is accurate. It's long, detailed and goes into specific detail on what can go wrong. This seems like a perfectly reasonable documentation of what's already true.

Amanieu: Does it say anything about iterator adapters and how they react to exhaustion?

Josh: It just says to be careful when handling it.

The 8472: It depends on the specific adapter. We can't make a general statement.

Nia: I imagine reading this doc would prompt someone to check each adapter.

The 8472: Adapters generally don't make guarantees, so can't rely on that.

Amanieu: The doc seems helpful, but doesn't address adapters, like how does intersperse react to exhaustion. Maybe we should document that adapters don't handle exhaustion and relying on that should be avoided?

The 8472: I think that's going too far.

Josh: Posted a [suggestion](https://github.com/rust-lang/rust/pull/154238#discussion_r2983194182) on that.

The 8472: Someone could follow the documentation and still violate something.

Amanieu: I still think that adapter results after exhaustion should be unspecified.

The 8472: I agree, but for adapters that do make guarantees it should be okay.

Amanieu: Are there unfused iterators that guarantee behavior after exhaustion?

Josh: TryIter from mpsc.

Amanieu: sigh.

Josh: I wish it wasn't so too. The mpsc case is probably a mistake.

The 8472: Even if the std didn't have an example...

Josh: This is a Rust 2.0 kind of thing. Right now, Iterator makes no guarantees. This documentation seems like a good way to cope with that.

Amanieu: Can we extend this to discourage people from relying on this?

Josh: I'd love to add a sentence "please don't make iterators that aren't inherently fused"

Amanieu: "Don't use iterators after they've been exhausted" is the recommendation I'd make, "unless they're explicitly fused".

The 8472: If people already use it, we can't really tell them to stop.

Amanieu: Sure, but we could discourage people from making new ones.

The 8472: We could do a more nuanced wording.

Nia: We could address the adapter thing. If you use an unfused iterator, you shouldn't expect adapters to handle exhaustion well.

The 8472: We could say it doesn't compose well with generic code. 

Nia: That's a good idea.

The 8472: You can have such iters, but you'd be restricting yourself to a subset of things that explicitly supports it. E.g. a Resumable trait with async support, where a Waker wakes it up when items are available ??.

Amanieu: Sure.

Nia: We can try to write something to that effect.

Amanieu: The core text is fine, we'll just add a few bits.

Nia: Volunteers?

The 8472: I can do it.

Nia: Thank you.

### (waiting on team) rust.tf/149392 *UdpSocket: recv() and recv\_from() behave differently for zero sized buffers*

Nia: We agreed that we'll document the differences. I made a PR yesterday on it. It's a simple 8-line change. Double-checking if everyone's okay with it.

The 8472: Does the existing doc say which syscall it's calling?

Nia: Nom but most platforms have ?? syscall.

The 8472: But the impl could call the more general aPI and not the direct syscall.

Josh: There may be platforms that call read instead of receive.

Nia: I left it vague but I can specify. But then we'd have to guarantee that.

Josh: And we probably shouldn't.

Nia: Right.

The 8472: ??

Josh: They can do that, but then they'll find that we don't guarantee anything for that platform.

Nia: That leaves specifying more for the future.

Nia: What's the preference of the team?

Josh: I think we could write more, but we should merge what we have now.

Nia: I'll wait for Mark to look at it. We can unnominate it for now.

### (waiting on team) rust.tf/152092 *Stabilize \`refcell\_try\_map\`*

Amanieu: It's waiting on author. Unnominating.

### (new change proposal) rust.tf/libs764 *Add \`str::split\_first\` and others*

Josh: split_first definitely. I want to look at the rest, but that one and split_last seem useful.

The 8472: It's actually proposing first_char. If you look at the solution sketch. Otherwise it's not clear that you're splitting off bytes.

Amanieu: We have split_first on slices today. One of the motivations seems to be const support, which is blocked on const traits.

Nia: It's reasonable. A bit ??. Not sure how I feel about tuple ??.

Josh: That's a standard shape we use for split methods.

Nia: Nevermind then.

Josh: It does generally seem like rsplit methods have a property of splitting into Option.

Nia: Yeah, I usually look at split_at which panics.

David: Do they explain why not split_off?

Josh: Presumably, you could have split_off_first_char?

The 8472: You could call it in a loop and mutate the slice instead of reassigning the tail. split_off is useful.

Josh: I'm confused by something. They're writing:

> `split_off{,_first,_last}{,_mut} -- no, uses a trait`

It's not obvious to me what trait this was using.

Nia: split_off_first and last is const, even if unstable, reducing the amount of useful ??

The 8472: const is under heavy development, so not sure if it's a strong motivation. Unstable is not an argument because this would also be unstable.

Nia: Unless they propose to stabilize (?)

Josh: They don't seem to.

The 8472: There's also a [const tracking issue](https://github.com/rust-lang/rust/issues/138539) where someone asked for stabilization in December.

Nia: What do we do with the ACP? Say no?

Josh: The usual thing if we think it's useful and ??, then we ?? and also do something.

Nia: Isn't this redundant if we have split_off?

Josh: We have that for slices, not str.

Nia: Nevermind. Then this is good.

Josh: I agree.

Nia: Then we approve and ask them to make a tracking issue. Unless there are objections.

Chris: We have to ?? for String.

Josh: We don't have `split_off_first`. The fact that we have those for ?? is a precedent for having it for String.

Josh: It would be useful to have that for `String` that shifts the reference (?)

The 8472: We'll probably need `split_off_ref` or something. ??

David: You actually won't need(have?) shadowing.

Josh: That's an argument for why `??_in_place` are useful.

Nia: The conclusion is we want this ACP? Okay, I'll reply.

### (new change proposal) rust.tf/libs763 *ACP: Mark \`NonNull::with\_exposed\_provenance(\_mut)\` as const*

Nia: This is straightforward.

Josh: Has anyone in wg-const-eval said explicitly that they don't have a problem with this?

Nia: We have std::ptr::with_exposed_provenance, it's const already.

Josh: Ship it.

The 8472: Doesn't const have magic pointer stuff where they don't behave like null pointers? (Checks.) Okay, that probably works.

Nia: This is the same as making a pointer with exposed provenance ...

(Accepted.)

### (new change proposal) rust.tf/libs762 *Simple \`bool\` Implementation for \`keepalive\`, \`set\_keepalive\` on \`TcpStream\`*

Amanieu: There was discussion on a really old PR for keepalive from 2020. The option is present on Linux and Windows and probably other platforms as well. The ?? the keepalive interval isn't available on Windows.

Nia: That's a big issue.

The 8472: Is it? Can you set it?

Amanieu: You can set it on ?? but you can't read it back.

The 8472: Doesn't sound so bad because they are separate methods which we can expose as (not?) portable.

Nia: Fair.

Amanieu: https://github.com/rust-lang/rust/pull/154025

The 8472: https://docs.rs/socket2/0.6.3/socket2/struct.Socket.html#method.set_tcp_keepalive

...

Josh: I'd expect that the only way to make it portable is having set_keepalive and get the defaults, or set a specific ?? and expect that one of them will override the other. ?? Given the discussion that Tokio can't do it portably, this seems reasonable. ??

Amanieu: I agree. It's reasonable to have a boolean as a portable option.

Josh: I propose we document that calling ?? may override ??, and if someone wants a specific time, ??, and one day we might give them a method if they want it. Ship it.

Amanieu: Ship it.

### (new change proposal) rust.tf/libs761 *Add constructor of \`std::fs::Metadata\` from \`struct statx\`*

Amanieu: Tokio traditionally implemented async ?? metadata by doing ?? in a thread pool which returns metadata asyncronously. Recently they moved to io_uring ??, and now they can't construct metadata out of thin air (?)

Josh: Presumably they're asking for a linux-only mechanism that would convert ...

Amanieu: ??

Josh: The statx structure has a mask telling you what fields were filled. If we don't have all fields, do we fill them with dummy values?

Amanieu: We'd error.

(??)

Amanieu: Not a fan of this because it limits the API evolution.

The 8472: statx is the swiss army knife of ??. If that doesn't contain the metadata...

Amanieu: ??

Josh: What would we do on Linux platforms that don't have the syscall to get that new information?

Nia: ?? partial results ?? what happens in the case where we return everything?

(??)

Josh: I found [an argument](https://man7.org/linux/man-pages/man2/statx.2.html) for doing this. We should be able to cope with not having all data using the statx structure.

(??)

Josh: The kernel guarantees that anything you asked for will be filled with dummy value. We could explicitly state in our conversion interface that the kernel makes this guarantee and we rely on it. If you want to pass this data structure to us, you also have to respect the guarantee.

Amanieu: I'm not comfortable with this because it's only for Linux.

(??)

Josh: The API design here would need to be careful when dealing with unset mask values and fields.

Josh: We could explicitly say that you shouldn't construct one of these by hand, only use ones provided by the kernel.

The 8472: Basically, rely on kernel's behavior and we don't support manually constructing...

Amanieu: We rely on all fields containing at least a dummy value.

The 8472: I'd say we rely on the API guarantees on the man page.

(Agreement.)

Josh: ?? the mask determines the size of the structure? That suggests they might not be able to extend the length of the structure. They reserve one value...

(??)

Amanieu: We can add this as unstable and see how it works.

Nia: This will have more discussion around it anyway.

Josh: Let's bring this to the Rust for Linux meeting this week to find out whether there are any API guarantees that will prevent us from evolving statx. Maybe RFL folks could track down whether ??.

Nurzhan to include this in the upcoming RfL agenda.

### (new change proposal) rust.tf/libs760 *ACP:  Adding \`exp2\_m1\`*

The 8472: It provides extra precision when(of?) doing it the naive way.

(??)

(Approved.)

Nia: I'll reply. If there are more similar function, we can invite people to open more ACPs.

Josh: They could be grouped so that we don't have to deal with them one at a time.

Josh: I'm fine approving this alone, but also with `log2_1p`.

Amanieu: Why not add the rest of the functions?

Josh: We haven't reviewed them all yet.

Nia: People are welcome to submit more ACPs.

### (new change proposal) rust.tf/libs757 *Implement FusedIterator for core::iter::StepBy*

### (new change proposal) rust.tf/libs733 *allow specifying whether \`Instant\` should be suspend\-aware*

### (new change proposal) rust.tf/libs709 *Add \`str::copy\_from\_str\` and other mutating \`str\` methods*

### (new change proposal) rust.tf/libs699 *Bring \`hashbrown::Equivalent\` into std*

### (new change proposal) rust.tf/libs691 *\`std::io::Read\` provides byte\-order reading trait*


### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs379 *Combine, an iterator adapter which statefully maps multiple input iterations to a single output iteration*

Josh: This looks complicated. They're trying to build an arbitrary iterator adapter combining elements of Iterator and producing a new Iterator. I'm trying to figure out how doable this is.

Amanieu: Is this similar to fold?

Josh: Not sure if this is folding things or sliding a window.

Amanieu: Is there a crate for this?

The 8472: The issue references Java's Stream API (Gatherer).

Josh: I would definitely refer to Java if I wanted to design elegant Stream APIs ;)

The 8472: They have methods producing aggregate results in a single pass. There are some good designs.

(??)

Amanieu: We discussed this in libs-api and [proposed](https://github.com/rust-lang/libs-team/issues/379#issuecomment-2122991696) to prototype this in itertools. Has this happened?

(No.)

??: Let's close this and reiterate the proposal to implement this in itertools.

Amanieu: Sounds good.

The 8472: Anyone replying?

Josh: I can write it.

Amanieu: Thanks.

### (stalled change proposal) rust.tf/libs322 *\`AssertThreadSafe\` (name TBD) – a more general API for lifting conservative \`!Send\` or \`!Sync\` implementations*

### (stalled change proposal) rust.tf/libs420 *Add basic tempfile API to stdlib*

### (stalled change proposal) rust.tf/libs287 *ACP: Add \`FromByteStr\` trait with blanket impl \`FromStr\`*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs204 *Integer Manipulation API*

Amanieu: I love this.

Josh: Yes please. In particular, having traits for `.truncate()` and `.reinterpret()` would rid us of `as`.

Amanieu: I'd like variants of `truncate()` (saturating, normal, etc.).

The 8472: Multipliers :(

Amanieu: No, this would be only for truncate.

Josh: What's the value of checked_truncate over try_into?

Amanieu: I want conversions to be explicit. I think `into` is a mistake.

Josh: We should fix `From`/`Into` (?).

Josh: +1 for truncate and other variants. Should it be saturating_truncate or truncate_saturating? The arithmetic methods are prefixed. So checked_truncate and  saturating_truncate?

Amanieu: `zero_extend` could just be `extend` and not allow changing the sign.

The 8472: sign_extend would support zero_extending a signed integer.

Amanieu: I think that behavior is surprising. I think you should extend, then change the sign or vice versa.

The 8472: We should check if many people need both done together. Verbosity might be annoying.

Josh: zero_extend shouldn't exist for signed types. They're already saying the correct thing...

Amanieu: https://github.com/rust-lang/libs-team/issues/204#issuecomment-2028447344 (num-conv crate) has the API feel we should have, plus variants.

Josh: We could implement something like `TruncateTarget` using a macro.

(??)

Amanieu: I don't agree with how isize is handled here...

Josh: I'm hearing agreement not to use...

The 8472: I'm concerned about noise in situations where they are common conversions.

Josh: Users could write helper methods.

Amanieu: I think `sign_extend` and `zero_extend` should just be `extend`.

Amanieu: For truncation, I think you should be able to `truncate` to a type with a different sign. (Josh talks about why this might be a bad idea.)

Amanieu: let's have these APIs:

- `extend`: same signed/unsigned only
- `truncate`: same signed/unsigned only
  - plus saturating/checked versions
- already exists: `cast_signed`/`cast_unsigned`
  - drop `cast`

Amanieu: I can write this up.

Josh: num-conv's approach with a separate trait might not be how we should do it.

Amanieu: They just needed an extension trait.

```rust
impl iX {
    fn extend<T: ExtendTarget<Self>>(self) -> T;
    fn truncate<T: TruncateTarget<Self>>(self) -> T;
    fn checked_truncate<T: TruncateTarget<Self>>(self) -> Option<T>;
    fn saturating_truncate<T: TruncateTarget<Self>>(self) -> T;
    fn cast_unsigned(self) -> uX;
    fn checked_cast_unsigned(self) -> Option<uX>;
    fn saturating_cast_unsigned(self) -> uX;
}
```

Josh: Do we want to seal these traits or leave them unsealed?

Amanieu, The 8472: Start with sealed.

Josh: They should also be implemented for `NonZero` pairs. Caveat: need to be careful when implementing `truncate`, but `extend` should just work.

Amanieu: `extend` is value-preserving, so yes.

Josh: `checked_truncate` could exist, but if these are all on the same trait, then `truncate` can't.

Josh: `saturating_truncate` would work, right?

Amanieu: Yes, because ...

Josh: We have limited support for NonZero types. You can go from `NonZero<smaller>` to `NonZero<larger>` and you can `From`/`Into` between `NonZero<same_size>`, but not ...
    
(??)

The 8472: Should we make the traits unnameable or unstable?

Amanieu: unstable (?)

Josh: lang team has been discussing phasing out `as`. Doing that required APIs from libs. We should bring up this being accepted and ask lang to plan ahead. (Nurzhan to do that.)

### (stalled change proposal) rust.tf/libs347 *Context reactor hook*

### (stalled change proposal) rust.tf/libs296 *ACP: Designing an alternative \`FromStr\`*

### (stalled change proposal) rust.tf/libs210 *Add associated consts to f32, f64 for mathematical constants*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
