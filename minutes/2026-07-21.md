# Libs-API Meeting 2026-07-21

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, The 8472, David, Amanieu, Josh T, Nurzhan

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

15 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/134915 *Tracking Issue for ByteStr/ByteString* - (3 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/158303 *Always escape grapheme extenders in \`str::escape\_debug\`* - (3 checkboxes left)
  - merge rust.tf/156160 *feat: add symmetric PartialEq impls for Vec, &\[T\], &mut \[T\] versus Cow\<'\_, \[T\]\>* - (3 checkboxes left)
  - merge rust.tf/156999 *Implement FusedIterator trait on StepBy if the underlying iterator is fused* - (3 checkboxes left)
  - merge rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`* - (2 checkboxes left)
  - merge rust.tf/126840 *Tracking Issue for \`mpmc\`* - (4 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (0 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (0 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (2 checkboxes left)

[dtolnay (4)](https://rfcbot.rs/fcp/dtolnay), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [joshtriplett (7)](https://rfcbot.rs/fcp/joshtriplett), [Amanieu (7)](https://rfcbot.rs/fcp/Amanieu), [nikomatsakis (1)](https://rfcbot.rs/fcp/nikomatsakis), [the8472 (10)](https://rfcbot.rs/fcp/the8472)

### (nominated) rust.tf/libs831 *saturating float to int method always matching our current \`as\` semantics*

Nia: None of the cast we put it would have the same semantics as `as`. Do we want to make this ACP redundant by ??

Amanieu: NaN to zero or NaN panics? I think we should go with NaN to zero because that's where most hardware is converging to.

Nia: I think it was #810. If it's only x86 that has nice handling for NaN panics, then I'd be in favor of ?? and making this one redundant.

Amanieu: Arm has the right behavior, but RISC-V returns -1, X86 returns something, and ?? emits some fixup code for overflows.

Nia: What does it do for NaN?

Amanieu: X86 is int::min on NaN. Everyone does something different.

Nia: In that case, unless there's an explicit reason to have this behavior, I'd say adjust the previous ACP (it's an open question there), and let's not have another method to emulate `as`. I don't feel very strongly about this. Suggested names on #831 are bad.

The 8472: clearly we need a method `.as()` that does the CPU-specific thing, not what `as` currently does 😃 

Amanieu: NaN on X86 = minimum (0x1000...); Arm = 0; RISC-V = maximum.

Nia: That's terrifying. We can just do NaN to zero, and if someone wants trapping behavior, we can handle that. Would everyone be okay with that?

Amanieu: LLVM instrinsic says "return 0 on NaN".

Amanieu: Do we guarantee 0 on NaN with `as`?

The 8472: I think the Reference says NaN returns 0.

Amanieu: Given that, I think we should ?? NaN to zero.

The 8472: Do we need a name?

Nia: We already have one from the previous ACP.

David: Reasonable name for the existing behavior would be try_to_int_saturating that returns Option, which you can unwrap, but I don't see anyone asking for that.

Nia: I'll reply stating that and close the ACP.

### (nominated) rust.tf/libs833 *Meta\-ACP: Integer cast methods*

Nia: Thanks to Clar for putting this together. The suggestion is that we've come up with a patchwork for integer casts, and they've proposed to add more. It looks reasonable.

Amanieu: Should we look at the first or second table?

Nia: First one is the status quo, second is the proposed API.

Amanieu: Bottom part, saturating cast — we already have cast_signed and cast_unsigned, so this completes the matrix. It always keeps the same... Confused about them, because it looks like it would change the sign.

Nia: cast_signed shows up for uN -> iN...

Amanieu: Is this proposing to change cast_signed/unsigned to change width as well?

Nia: It's arguing that cast_signed is meant to cast to a signed type (so it's present for signed types too).

Amanieu: I thought cast_signed only went from unsigned to signed to avoid turbofishing.

David: +1.

Josh: +1.

The 8472: Might be useful for macros.

Amanieu: You could just do the general `cast` method and tell it what you want to cast to.

Amanieu: `cast` is a superset of everything except for width-changing methods.

Nia: And saturation, etc. Seems like Clar mostly suggests filling out the bottom half. The table looks longer. A delta would have been nice.

Amanieu: The current (stable) behavior of cast_signed/unsigned is the same as wrapping_, unfortunately.

Nia: Right, but that's not the semantics of wrapping_cast_signed? Ah, this requires changing stable behavior. That would need to be an edition change.

Josh: No, that's wrong. The point of these methods is to reinterpret the bits.

Nia: Agreed.

Josh: We could do one thing, this table would be half the size if we ripped out the proposed changes to cast_signed/unsigned and left the remaining changes to widening/narrowing/etc. ??

Nia: Need to look at the proposal you mentioned.

Josh: #??

Amanieu: I still think we need to only have `cast` and not `narrow`, but let's not relitigate that.

Josh: Appreciate that it links to all the proposals, but we may not want all the recommendations.

The 8472: Is it actually accurate? Looking at saturating_cast_unsigned, and it doesn't have narrowing?

Josh: I don't think i2N to u2N exists?

Nia: Seems like it only works for the same bit-width.

Josh: IMO, the two mentions of widen-narrow overlap (narrow shouldn't exist for noop case) and sign interplay (width and signedness shouldn't change together) are the things we shouldn't be changing.

Josh: `narrow` should be able to do an identity conversion, and same for `widen`.

Nia: I'm partial to either "they can be an identity fn" or not...

Josh: There's a strong reason for `widen` to have identity because ??.

Nia: Looking at the top table, I'm not convinced we need to add much more to it. I suggest to reply that some of these suggestions aren't where we want to go...

Amanieu: Would you like me to write up a counter-proposal table? I want widen to be able to change signedness if it's lossless... remove narrow and ??. The idea is that `widen` is lossless, `cast` is any but you specify handling of out-of-bounds. `narrow` is a subset of that.

Josh: We have reopened the bikeshed. Open a proposal and let's discuss it then.

Amanieu: I feel strongly about this, and I'll block stabilization of `narrow`.

Josh: Sigh.

Nia: Let's see the counter-proposal. And in the meantime, I'll leave this issue open because this table is really useful.

### (nominated) rust.tf/libs834 *Guarantee \`handle\_alloc\_error\` never unwinds*

Nia: This is mine. It's because allocating anything while unwind is happening is bad. We've had unsoundnesses due to random unwinds, especially BTree and Arc. On stable it's impossible for handle_allocator to unwind. The default hook aborts. I'd suggest to use some mechanism to guarantee that it will never unwind and will abort. If we ever get first-class unwind blocks, we could switch to that, and use some guard in the meantime.

Amanieu: https://github.com/rust-lang/rust/issues/51245, the motivation was to be able to recover from unwinding for allocation to continue.

Nia: This would require rewriting a bunch of library code to account for the possibility.

Amanieu: We closed https://github.com/rust-lang/rust/issues/43596... The motivation was, if users wanted to ?? they should use ?? hook.

Josh: The proposal to be able to recover from alloc errors, presumably they wanted to do it by freeing some memory?

Amanieu: Unwinds a thread, that drops a lot of objects, then other allocations can succeed.

Amanieu: If a server request allocates a lot of memory, OOM...

David: Need to define realloc behavior in case of unwind.

Amanieu: realloc -> Error, which guarantees that the old allocation is still there. After Error you should call error hook, at which point you're still in the original allocation.

Nia: Addressing alloc issues was by saying unwinds should be aborts because otherwise it would require deep surgery across std. I don't want to deal with safety issues and CVEs.

Nia: ??

The 8472: How does that help? If you have the hook, does that tell you if library code is okay with it?

Nia: ?? becasue most unsafe code isn't written with consideration for unwinding.

The 8472: ?? entry point

Nia: ?? that would be compatible with my suggestion.

Josh: Can we not do it?

Nia: A lot of usages of allocator would be scoped behind...

Josh: what are the chances we could say "you can't unwind on this"

Nia: I suggest we guarantee that an attempt to unwind will turn to an abort...

Amanieu: https://github.com/rust-lang/rust/pull/147725#issuecomment-3407721042

The 8472: Doesn't that already happen in no_std?

Amanieu: In no_std allocator calls ?? handler but there's no way to unwind on no_std.

Josh: Don't want to do design here. Broadly, is there another way to handle the use case without unwinding?

Nia: No. Their use case is library code that doesn't want to handle alloc errors and halts in some way on unwind/abort. They want all aborts to turn into unwinds...

Josh: Horrific.

Nia: I'm fine with handle_alloc_error and slap a warning.

David: I brought up realloc after finding this code -.- 
https://github.com/dtolnay/cargo-tally/blob/1.0.75/src/alloc.rs#L121

Nia: Right. This would be UB if handle_allocator could...

David: Because the original allocation is already gone...

Nia: I think it's fair for lib authors to say "I don't support unwinding".

Amanieu: https://github.com/Amanieu/brie-tree/blob/main/src/node.rs#L628

(..)

The 8472: If we introduce a new mechanism, the ecosystem may migrate to using that...

Nia: Where do people stand on this?

Josh: Even if we say "the ecosystem isn't required to handle this, only specialized code is", people will still push libraries to deal with this, and the net result will be flakiness. I'm hesitant to inflict this on us. People are going to push us to handle allocation error unwind...

Amanieu: We can say allocator handler can unwind, deal with it. Alternatively, two separate methods, one can unwind, the other can't. The problem is, if your code can locally handle this, your users may not if they expect your clone method to panic.

The 8472: The RFC on unwind from drop is an abort, the conversation (https://github.com/rust-lang/rfcs/pull/3288#issuecomment-4993393828) went to code blocks or lints where implicit drops or stuff that would break unsafe code break, unexpected unwinds might make such code more robust? Maybe that RFC would help with Josh's concern?

Nia: My take is a bit more optimistic. I was hoping that having an explicit unwind method would push downstream to make code unwind-resilient, since they're thinking about unwinds already. Amanieu brings up the thing about lib consumers relying on panics happening or not, we have unwind-safe, which means nothing because people don't document panics properly. ?? would be a semver-breaking change because of soundness. I'd still like allocator to not unwind. Would like to experiment with an unwinding form of it... On the nominated item, I think it's necessary for justifying the current soundness promises we make.

Josh: At least if we're going to do this, we should talk to cloudflare. The current "let's remove OOM = panic", which they're using, if we do that, they might not have an alternative, and we shouldn't inflict this on them. They might have to pin some nightly and complain until there's a replacement.

David: That would happen if we enforce abort. But we could place the burden on libraries and users, who'll have to choose dependencies that...

Josh: That's semivalid, but if we don't enforce it... 

Nia: Agree with David, but most people won't set the hook (??)

The 8472: I suspect the safety people will protest becasue it makes it difficult to check whether libraries are safe or not.

Amanieu: FYI once EII is more mature we will likely be removing alloc error hooks in favor of just allowing users to override the standard library's `#[alloc_error_handler]`. But that doesn't change anything for the purposes of this conversation. 

(..)

The 8472: Doesn't cloudflare use tokio and most of the ecosystem, so this will result in them not being to use anything?

### (nominated) rust.tf/157168 *Stabilize \`Rng\` and \`SystemRng\`*

Josh: FCP open, I'm looking through the concerns. We don't need to bikeshed some of these (`TryRng`) on day 1 so this isn't a blocker. After talking to BurntSushi, landed on the point of "we can ship `TryRng` if we don't expect people to *have* to use it". Several of these need discussion or addressing (doc changes for 3 of them).

Nia: So we wait for now?

Unnominated for now.

### (nominated) rust.tf/158286 *Escape additional classes of characters in \`escape\_debug\_ext\`*

8472: Looked at one script from east asia, ran that through debug_assert and it mangled it pretty badly. We shouldn't do that.

David: On stable none of these get escaped?

(explaining)

8472: This mangles things someone would write with a regular keyboard, we're escaping too much.

David: Agreed.

Nia: Also agreed to reject this, especially for unnormalized characters.

Josh: So long as we take care to avoid having a pile of ASCII, if we still provide the escapes, I'd be all for us tuning the escaping to escape less of things that are just orphaned combining chars that don't combine as opposed to invis characters.

8472: Invis chars are used in some scripts for ligatures.

Josh: True, but typical text uses these pretty sparingly. While not perfect, debug escaping mostly works. If there are whole languages that get mangled, that seems worth trying to fix, but this is `Debug` not `Display`, it's not meant for human readability for an end user, it's output for the dev with the understanding that they're trying to understand what's going on in their program.

8472: My error messages are debug printed and they're human-written.

Josh: There's a diff between a human dev who wrote info in their own msgs & needs to see what they wrote, for which we need to repro non-western languages, but it's not the same as this being perfect for end user presentation, just not gratuitously mangled if it's not ascii. Balance that with flagging things this is intended to escape like noticing there's a 0 width joiner in the middle of your ASCII.

8472: Worth seeing if there's any script that does use those a lot, apparently quranic arabic.

Nia: Quranic arabic is still used.

8472: There might be more examples, we need to strike a balance and deal with stuff like LTR override and unprintable chars. But we don't want to make languages unprintable. I'm not sure if that's easily representable with unicode properties or if we need to maintain our own table or do context analysis.

David: Might be a good time to solicit expert input, and leave it to someone who can make sure i18n is held up.

Josh: Happy to write some principles that we can use to guide us. So far expert feedback has told us we need to either escape *more* or *less* but we both mangle and don't escape enough for it to successfully help.

Nia: Okay, what principles do we want to pick?

Josh: We should say "please give us an idea of the tradeoffs", but if you grab a random sentence from parts of wikipedia you ought to get something readable enough to be interpretable and not garbage like the Lao script word 8472 tested was. If the word has an escape every other char that's not okay. We also want invisible or difficult to interpret marks in binary not to mess up the terminal or produce unreadable text.

8472: Are we also concerned about confusion attacks?

Nia: Doubt that could be malicious.

David: Talked to the rust team at amazon, currently they're putting together a list of ecosystem problem areas not served by any library (canonical example being yaml), doing this in collaboration with the RCN folks & coming to a consensus with various companies. This is the kind of thing that would belong there, to increase visibility for a library that could escape the right cases here.

I bring this up because it's an option becoming available to us to say "please put this problem area on your list" and we can check later if they've developed a library for this later. Same way url works, we want a URL library developed by browser experts not vec/string experts.

8472: Do we have companies interested in minority languages or just american ones?

Nia: I think a lot care about i18n. Sure, let's poke RCN.

Josh: Also let's poke the unicode experts like Manish who can help coordinate this. If we're going to hand this off we should have someone from our side too.

8472: For this PR do we close or partial accept or?

Josh: I don't think we know enough to say what to do.

Nia: Agreed. Keep as is and let's poke RCN?

8472: Turn this into an issue, then poke.

8472: Will write one.

Unnominated.

### (nominated) rust.tf/159298 *Tracking Issue for \`bool::toggle\`*

Nia: Super short time, but it's a tiny API surface.

8472: Leave it for a release so ppl can get a FCW?

Josh: I agree in principle to let things bake but this doesn't need very long. Conflicts are reasonably unlikely here, someone might have a trait method but those override inherent methods.

David: No need to tie the FCP to immediate stabilisation, we can just delay it. Let's kick off FCP and only land stabilisation no sooner than 1.whatever.

Nia: +1

FCP started.

### (nominated) rust.tf/159431 *1.98 regression: "expected \`std::ops::Range\<usize\>\`, found \`std::range::Range\<usize\>\`"*

Nia: Can also slap priority on this while we're at it.

Josh: I see, shadowing.

Amanieu: Is there anything we can do about shadowing?

8472: Give it a different name?

Amanieu: Means we can never add inherent methods because of possible trait methods.

8472: We can add it still if we think the impact is minimal. There was discussion about freezing reserved names but that's speculation.

Amanieu: Author already published a workaround.

Josh: Unfortunate breakage, we appreciate the author, we can just say that.

Nia: +1.

Amanieu: Close it.


## 

Josh: If we're gonna do ACPs async, we should try to externalise our institutional knowledge into the ACPs so ppl can process them async. Anything where we have said "here's why we're deferring this" or "here's what we're actually looking for", where we have live context and we should either finish dealing with that or post a comment. There are cases where an ACP comes up and we go "oh that one again".

Nia: How would you suggest that? Keep an eye on the ACPs during meetings?

Josh: We can handle it incrementally.

Nia: I think it's mainly the numeric casts which we're handling in meetings anyways bc they're controversial.

Josh: One of the ACPs that gave that impression was #830 and the response looks pretty reasonable, would be worth us taking a look at.

### (new change proposal) rust.tf/libs830 *ACP: \`std::fs::UserDirs\` or similar*

Josh: Looks pretty reasonable. I don't know what `take` does, is it `clone`? Or is it leaving it all `None`?

Nia: I assume the latter. It's *interesting*...

Josh: Is it necessary?

8472: If it impls default... ??

Josh: Yeah this should impl Default

Nia: Uhh this shouldn't have platform-specific defaults

(consensus that it's an empty initialiser)

Nia: Then just replace `take` and `empty` with a default impl.

Josh: Tradeoff of builders, it'll be 3 lines instead. I'll write a response.


### (waiting on team) rust.tf/158271 *Add rem\_floor and rem\_ceil*



### (new change proposal) rust.tf/libs835 *Rename \`VecDeque::truncate\` to greatly reduce ambiguity*

### (new change proposal) rust.tf/libs832 *Type\-smart \`intersect\` on ranges \-\- v2*


### (new change proposal) rust.tf/libs828 *Separate \`Components\` iterator implementation into non\-prefixed & prefixed versions*

### (new change proposal) rust.tf/libs827 *ACP: add \`unsafe trait PureRef\<U\>\` to guarantee purity of the AsRef/Deref/Borrow traits*

### (new change proposal) rust.tf/libs826 *Add \`core::mem::size\_of\_field!\`*

### (new change proposal) rust.tf/libs821 *ACP: \`impl From\<&Rc\<T\>\> for Rc\<T\>\`*

### (new change proposal) rust.tf/libs819 *ACP: Remove 'static from Any itself, moving the bound to the operations themselves*

### (new change proposal) rust.tf/libs816 *ACP: Add \`fN::ordinal\`*

### (new change proposal) rust.tf/libs814 *\`OnlyNegative\` and \`NonNegative\` to compliment \`NonZero\` for signed integers*


### (stalled change proposal) rust.tf/libs210 *Add associated consts to f32, f64 for mathematical constants*

### (stalled change proposal) rust.tf/libs365 *ACP: \`TwoSidedRange\` trait*

### (stalled change proposal) rust.tf/libs191 *Add LocalWaker support*

### (stalled change proposal) rust.tf/libs544 *\`Arc\<impl !Sized\>\` constructors*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs363 *Un\-specialize impl ToString*

### (stalled change proposal) rust.tf/libs525 *Vec::try\_remove*

### (stalled change proposal) rust.tf/libs298 *Constructive/Destructive Interference Size Padding*

### (stalled change proposal) rust.tf/libs347 *Context reactor hook*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
