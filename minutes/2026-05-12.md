# Libs-API Meeting 2026-05-12

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Amanieu, Nia, The 8472, Josh, Tyler B and Ben S, Nurzhan, Tomáš Šedovič

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

20 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/155750 *Document that \`ManuallyDrop\`'s \`Box\` interaction has been fixed* - (5 checkboxes left)
  - merge rust.tf/155989 *Update \`transmute\_copy\` to ub\_checks and \`?Sized\`* - (3 checkboxes left)
  - merge rust.tf/152193 *Tracking Issue for \`nonzero\_from\_str\_radix\`* - (4 checkboxes left)
  - merge rust.tf/147946 *Tracking Issue for \`strip\_circumfix\`* - (3 checkboxes left)
  - merge rust.tf/147455 *Tracking Issue for \`path\_set\_times\`* - (3 checkboxes left)
  - merge rust.tf/127544 *Tracking issue for CommandExt::show\_window* - (3 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`SyncView\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`* - (3 checkboxes left)
  - merge rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`* - (3 checkboxes left)
  - merge rust.tf/153990 *Decide and document where stdarch intrinsics are allowed to diverge from asm behavior* - (3 checkboxes left)
  - merge rust.tf/126769 *Tracking Issue for \`substr\_range\` and related methods* - (3 checkboxes left)

[scottmcm (2)](https://rfcbot.rs/fcp/scottmcm), [Amanieu (7)](https://rfcbot.rs/fcp/Amanieu), [BurntSushi (13)](https://rfcbot.rs/fcp/BurntSushi), [dtolnay (1)](https://rfcbot.rs/fcp/dtolnay), [the8472 (14)](https://rfcbot.rs/fcp/the8472), [joshtriplett (8)](https://rfcbot.rs/fcp/joshtriplett), [nikomatsakis (2)](https://rfcbot.rs/fcp/nikomatsakis)

### (nominated) rust.tf/151379 *Stabilize \`VecDeque::truncate\_front\`*

FCP finished. Should be merged?

The 8472: Last week, we had `truncate to range`, and we discussed whether we wanted to reconsider `truncate_front`. The question is if we want both or just one. The author brought up that the two have different semantics. One keeps the ??, and with the other you need to provide a range. Converting between the two would be difficult.

Nia: I reviewed truncate_to_range and it uses truncate_front internally, which is ...

Josh: `truncate_front` is more convenient for "I want to keep 100 elements". The key issue is, if you want to keep the last 100 elements, that's not trivial to write without truncate_front. The remaining concern is if truncate_front is the best name for "keep N in tail".

The 8472: Yeah. I think we've talked about this and didn't come up with anything better.

Josh: Somebody (The 8472) said `keep_tail`. The last comment said `truncate_front_to_len`, but eh.

The 8472: That's a rather indirect way to express what it does, depending how you look at it.

Josh: truncate makes logical sense, whether you think of length or number of elements. truncate_front doesn't have the advantage of being ambiguous(?). It seems like keep_tail or keep_in_tail would make more sense because its point is to keep only N elements and drop the rest.

Nia: Makes sense.

Josh: One factor affecting that is when we discussed this first, we didn't have the idea of a range. Now we do, and the symmetry with ?? loses its value. So I think it's fine if it's not named consistently with truncate as long as we pick a sensible name.

The 8472: So you'd be ok with renaming it?

Nia: I think so.

Amanieu: No opinion on this.

The 8472: Then is the precise name `keep_tail` ok?

Nia: It feels like the most obvious name.

Josh: Either that or `keep_n_tail`...

Amanieu: `retain_last_n`.

Josh: The reason we avoided retain is because usually that takes a closure.

The 8472: ??

Josh: By that argument, we could call it `retain_n_tail`. I lean towards `keep_n_tail`.

Nia: `keep_` feels better.

Amanieu: I think we need an `n` in there.

Josh: Do we need it on the basis of having n as an argument, or ??

The 8472: The only case where we have `n` is in `repeat_n`... Do we imagine adding other `keep_tail` functions?

Amanieu: I'm inclined to switch back to `truncate_front`... ?? is not too bad. Do we have precedents in other languages?

Josh: You said "not too bad", but the better argument would be if it would be better. I agree that using "keep" when you actually get rid of things makes sense...

The 8472: But retain is essentially the same.

Josh: Fair.

Nia: Semantically, we're building a list and doing things on it...

Amanieu: C++ doesn't have a method for this.

Josh: Python has a `max_len` argument that you could add on each `append`.

The 8472: ArrayList has `retain_range`, `retain_all`...

Amanieu: Java doesn't have anything.

Josh: ?? has `erase`.

Amanieu: And it's different.

The 8472: Ruby has `shift` where you can shift an amount of elements.

Amanieu: So basically, nobody has a fn like this.

The 8472: JavaScript also has `shift`.

Josh: There's a whole family of names based around removing (trim, etc.), keeping (retain, etc.)...

(...)

Amanieu: Does drain do the same thing as truncate_to_range?

Josh: Yes, you could call drain... and truncate... and it should give you the same answer.

Nia: ?? something with `last`?

Josh: You could do `last_n`, but method names like this sound like they return something, and this mutates in place.

Amanieu: Still inclined to leave `truncate_front` and point users in the documentation to `drain`.


(...)

Amanieu: `retain_tail`? I feel like retain is the right word to use in this context.

The 8472:

- truncate_front
- retain_tail
- retain_back
- retain_end
- retain_last
- retain_range (for the range version)
- keep_last
- keep_last_n

Nia: Not a fan of `retain_tail` because of alliteration, but that's a nit.

(...)

Josh: +1 to retain as the verb. We shouldn't assume closure usage with that.

Nia: I like `retain_back`, seems like the most obvious to start typing if you don't know the method.

Amanieu: In the vecdeque docs, there's 29 instances of "back", 2 instances of "tail" and 3 instances of "last"

Amanieu: Do we throw `n` in there?

Josh: We could do `retain_left` and `retain_right`.

(Arguments against `left` and `right`.)

Josh: `retain_back` seems right.

Amanieu: `retain_back_n`...

Josh: What else would it take besides `n`?

Amanieu: Yeah, doesn't make sense to have the `n`.

Nia: So `retain_back`, `retain_range` and a doc alias for `retain_front`.

Amanieu: I'd also add an alias for `truncate_front`

Nia: Great. Happy to reply unless someone already is.

### (nominated) rust.tf/154330 *Tracking Issue for integer extension and truncation methods*

Amanieu: I just replied to this. My reaction to renaming "extend" to "widen" is sure, but then we need to do "narrow" too.

Josh: What are people's reactions to rename "extend" to "widen"?

Nia: Yes, I like it.

The 8472: Both seem fine to me. What about "signed extension"

Amanieu: They always extend with signed

Josh: extend_from is a generalization of sign_extend and zero_extend. But we also have "widen" in the language. We have `widening_mul`. Both are reasonable but having something that does'nt conflict with the "extend" on containers makes sense. "widen" only makes sense on numbers.

Josh: Should we have these methods on floating point types?

Amanieu: I could see "widen" but not narrowing.

Josh: Agreed. In integers there's value in integer truncating but not in floats.

The 8472: Is there a CPU instruction for widening?

Amanieu: Yes.

Nia: For integers I like the idea of widening

Josh: Sounds we have consensus to change `trim_extend` to `widen`.

Josh: Next: truncate vs. narrow. Both make sense but this is a lossy operation (as opposed to widen which is lossless). My instinct that "truncate" has more obvious indication that it's a lossy operation.

The 8472: Look at Amanieu's comment: https://github.com/rust-lang/rust/issues/154330#issuecomment-4431942165

Josh: you're suggesting that the current "truncate" operation would be called "truncating_narrow"?

Amanieu: Yes.

Josh: I'm using this a lot, please don't. It's two words and an underscore.

The 8472: Are you talking about `u64` to a `usize` on a 64-bit system?

Josh: Yes.

Amanieu: There you can also widen.

Josh: I can't because we took that one out. But I could use "narrow".

Amanieu: The plan is to put `usize` to all the conversions but put lints in

Josh: I could be convinced to rename "truncate" to "narrow", but I'd not be in favour providing no short operations here and providing no variants on "narrow".

Nia: We could do: "truncate", "saturating_narrow" and "checked_narrow"

Josh: We have "truncate", `saturating_truncate` and `checked_truncate`, right?

Amanieu: Yes

Josh: Any objections to changing "truncate" to "narrow" in the options we have now?

Nurzhan: Would you consider "shorten"?

Amanieu: `narrow` should be the opposite of "widen"

Josh: If we have a thing called "widen" and we expect people to come up with an antonym, most people would come up with "narrow" rather than "shorten"

Amanieu: These verbs come from SIMD operations

Nurzhan: +1

Josh: In other places I'm converting from `u64` to `u32`.

Amanieu: And you're asserting the value fits?

Josh: I've already done the math that the value fits.

The 8472: OR you don't care about the top bits because of bit bashing reasons.

Josh: Almost every use of `as` in my current codebase is cast. I count 81 `as` casts in this rust file. I'd love to get rid of all of them. A lot of `as u16`, a lot of `as u32` and a lot of `as usize` in 1200 lines. None of this predates `into()`

Amanieu: In my codebases 3k `as` casts.

Nia: Either way, that's too many.

Josh: I'm in favour of "narrow" and changing the three methods that have "truncate" to "narrow"

Amanieu: In some codebades I have u32 that contains bits, I need take it to a struct and then put it back to u32

The 8472: We're not going to do overflow checks in debug builds, right?

Amanieu: We can have "narrow" and "truncating_narrow". The first one would assert.

Josh: It's the difference between using `.wrapping_add(..)` instead of `+`. Same here: if you say in debug mode and `overflow_checks` on, .. I'd like to just be able to say "just chop off the bits"

Amanieu: What about "narrow" and "truncating_narrow"?

Josh: Then I'd be stuck with "truncating_narrow". I'd like to call it "narrow"

Amanieu: You told us that you've already asserted that the value never overflow. So you should call "narrow"

Josh: I see, so you'd say "call `narrow` to chop off bits and if you didn't do it right, panic in debug mode". 

Amanieu: The fact you can't immediately tell whether this truncates bits is an argument to have both variants.

Josh: I see your point. None of them are unintentionally losing bits, but you have a point.

The 8472: You could have `narrow` and `truncate`. You don't have to call it `truncate_narrow`

Josh: The downside is that a lot of people currently using `as` are trying to truncate. And a the fact I don't 

Josh: When you make something bigger, `.into` or `.widen` is about the same length as `as sometype`. `.truncate()` is already a little longer. `.narrow()` is nicer. But if it's called `.truncate_narrow()` I see a bunch of C programmers they'll go "ah, you mean `as`". There are areas where it's extremely common.

The 8472: If shortness is key, then we should use two different words.

Amanieu: It's important to call out cases where you'd lose bits.

The 8472: That'd be `truncate`

Amanieu: I'd feel `truncate` feels weird when we have all the narrowing operations.

The 8472: We can have `saturating_narrow`, `checked_narrow`, `unchecked_narrow` and `truncate` would be a bit of a special case. Not part of the family, just to keep it short.

Amanieu: I don't like breaking the naming conversion just to keep something short.

The 8472: We have the `+` operator and `.add()` method. We don't have to be super consistent everywhere.

Josh: you're suggesting having 2 different menthods: one applying overflow checks and the other just truncating. I can see the argument for that. Suppose we inverted the naming: `narrow` is the thing that chops off bits and `truncating_narrow` to do overflow checks.

Amanieu: I think the first one 4people would reach for to be the value preserving ones. I expect most of these use cases would be converting u64 to usize. It's a bug if the conversion doesn't preserve the value. In a lot of places where people are using `as` casts today it's a bug if the cast isn't value preserving. But they're not getting any checks.

The 8472: I agree. In the code I write it's converting IO types (u64) and memory types (usize)

Josh: That's half of my cases. The other is a data structure that's mirrored on disk. And it's indexed by `usize`. If I could index it by `u32` I'd use that.

Amanieu: And it's a bug if the conversion loses the value.

Josh: I'm thinking about this. IS it actually more Rusty to have the check here? ... Yeah, it probably is

Nia: It makes sense for now to try to let it cook and look later?

Josh: It might be worth doing what Amanieu is proposing. And if somebody really wants this to be short, we can address later.

Nia: Same.

Josh: My reaction to this: because of this being a more unusual proposal, because it's more different than what people might expect coming from C, I'd let it bake a bit longer on unstable. Let's run it by Rust for Linux folks, see what they think. But it's growing on me.

Amanieu: I'll reply. We're okay with landing this on nightly?

Josh: Yes.

Nia: We'll take another look at this when we stabilise this.

The 8472: And we can suggest that `truncate` could be a possibility if a shorter name is needed.

### (nominated) rust.tf/154374 *Rename \`MetaSized\` trait to \`SizeOfVal\`*

The 8472: We tried to discuss this on Zulip, but it didn't go far. I proposed something for the All Hands. I'd be against `SizeOfVal`.

Nia: What happens to our meetings next week?

(everyone: we cancel them)

The 8472: We can check for critical issues but otherwise cancel.

Amanieu: This is standar for big conferences.

Nia: Thought so, wanted to check.

### (nominated) rust.tf/155924 *Make \`Infallible = !\`*

Amanieu: We FCPd it, forgot to un-nominate

### (nominated) rust.tf/156068 *Tracking issue for release notes of #156063: Map \`WSAESHUTDOWN\` to \`io::ErrorKind::BrokenPipe\`*

Josh: We talked about this last week. The issue was we wanted to handle the case when you shut down the write side fo the socket on Windows.

Amanieu: From the last week we agreed "nothing for us to do, we just wanted to have th ediscussion"

Nia: I'll un-nominate then.

### (nominated) rust.tf/156477 *Tracking issue for release notes of #154647: change \`c\_double\` to \`f32\` on \`avr\` targets*

Nia: This was insta-stabilised without us looking?

Josh: The target platform for `avr` for `c_double` is `f32`?

Nia: It apparently can be set to be `f64`, but the default is apparently `f32`

Josh: That implies we might have a avr-double at some point

Amanieu: It's a complier flag. But in practice nobody uses the floating point operations on 8-bit ?? controllers.

Josh: Are both `float` and `double` f32 bits?

Amanieu: Yes, they're both f32

Josh: We need to change this to match the C behaviour. We should document this thoroughly and note this is a breaking change.

Nia: AVR is tier-3 so we're not breaking something that's used a lot. We're also breaking floats on 8-bit ?? controllers.

Josh: We should mention in the release notes and add a blog post mention

Josh: I'll write it up.

The 8472: The type itself already says "some 16-bit systems use f32 for example".

Amanieu: I'm happy with this change. Do we FCP?

Josh: We should do an FCP any time we do a breaking change.

Amanieu: We can FCP this on the release notes.

JOsh: I've editid the release notes to haev a blog post paragraph and compatibility notes.

### (waiting on team) rust.tf/155114 *Add \`Step::forward/backward\_overflowing\` to enable RangeInclusive loop optimizations*

Nia: Mark said this needs an FCP

Amanieu: Yes, it does need an FCP. It was conditioned on a crater run with 400 regressions. No true regressions apparently. So it should be fine.

Amanieu: It's nightly code manually implementing `step`.

Nia: Theer's stuff that's implementing `std::IterStep`. `Step` is not stable, right?

Amanieu: Yes, it's not stable.

Nia: In that case we need the FCP. Is any of this controversial?

The 8472: I'm not sure if it's necessary?

Amanieu: This is FCP because of the change to equality for old ranges. What does an "exhausted range" equals to behaves differently. It doesn't happen for the new ranges (because you can only exhaust range iterators and those are not comparable). We explicitly say values are unspecified after an exastion.

Josh: This is consistent with the fact we panic if you try to convert after exhaustion. Seems good to me. I agree we need an FCP but it seems fine.

The 8472: Yes.

Amanieu: I'm happy to start an FCP here.

Josh: I'm looking forward to having an optimized code here. We should do a perf run on this.

The 8472: There's no perf run yet, I'll start one.


### (waiting on team) rust.tf/155989 *Update \`transmute\_copy\` to ub\_checks and \`?Sized\`*

Nia: This should be set to `waiting-on-fcp`

### (waiting on team) rust.tf/156025 *Support if\-let guards in \`matches\` macros by using \`:guard\` matcher*

Nia: It would be adding stuff that would be insta-stable

The 8472: It's a different macro pattern type

Amanieu: Why is a crater run needed?

Nia: Is it?

Josh: When we introduce a new mechanism for matching things, it's possible to end up making an obscure breaking change in an arbitrary macro. But Idon't see how this could be a breaking change. Anythnig that now compiles would have previously fail.

The 8472: If you have a macro that feeds some of its input into this macro it doesn't match the guard pattern?

Josh: That would cause a compilation failure

Amanieu: I'm confused about the change to 

The 8472: Before this change it matched on a keyword and an expression. What's the `guard` macro type? If we have an outer macro that supplies an `if` keyword plus an expression, that wouldn't match this?

Amanieu: We should ask for clarification

Nia: That's reasonable, I don't understand what this is doing.

Josh: We should ask. But it introduces a new insta-stable behaviour. So we should FCP. If someone wants to block that on a crater, they can raise a concern.

Amanieu: I'd ask for clarification and start FCP next week when we have the response.

The 8472: The guard pattern itself is still unstable. Should we be exposing an unstable language feature?

Josh: The unstable language feature is just parsing guards. It could simply parse guards itself and that wouldn't be using an unstable language feature.

Nia: I'd still say waiting is the safe option.

Amanieu: It's not urgent. Although, we're not meeting next week.

Josh: That was my point, we're not meeting next week.

Amanieu: There's no way this makes it into the next beta anyway.

Nia: Given it's two weeks it's reasonable to FCP.

Nia: I'll ask for clarifications.

### (new change proposal) rust.tf/libs791 *Add \`kill\_process\_group\` to Unix \`ChildExt\`*

Amanieu: It's opened 10 hours ago

Nia: I wanted this

Josh: Me too. We can at least give a vibe check.

Amanieu: Sure but what's the difference between `kill_process_group` and `send_process_group_signal`?

Josh: Currently `std::os::unix::process::ChildExt` has ??. The proposal is to add `killpg()` and the other ??

Nia: Do we have a `kill`?

Josh: We have a `kill` but that's portable so it's in `Child` not `ChildExt`

The 8472: The signal numbers aren't standard

Josh: The signal numbers aren't portable, no

Nia: Are we not defining them in the standard library?

Amanieu: No. They're in libc.

Josh: And if you have to pull in libc anyway, you're one unstable block from just calling `killpg`. The reason for this is you can just call `kill_process_group`

Amanieu: I don't feel too strongly about this

Nia: It's reasonable.

Josh: Yes

The 8472: Yes

Amanieu: Is a child part of its own process group? How do you ensure the child starts in a separate process group so you don't end up killing yourself?

The 8472: There's a `process_group` command

Josh: You call `.process_group(0)`. We should document this.

Nia: We can just +1 the top comment, right?

***

Josh: The 8742, you kept banging the drum about using fd-based directory functions. There was a thread from the systemd folks on the fedivers complaining about not having this.

The 8472: We do have it unstable: `std::fs::Dir`. We have tracking issues for more stuff but the barebones version is here.

### (new change proposal) rust.tf/libs790 *Implement Iterator for \`Range\<NonZero\<U\>\` and \`RangeInclusive\<NonZero\<U\>\>\` where \`U\` is unsigned*

### (new change proposal) rust.tf/libs789 *ACP: \`std::time::StopWatch\`*

Amanieu: It's sort of the instant clock stuff again. We want to use a different clock for timing code that may not be suspend-aware vs. the main `Instant` we want to be suspend-aware. We want to use something faster on Windows.

The 8472: Unlike instant there's no way to add/subtract a `Duration`. Why? I'd prefer to have one `Instant` being generic over clock source

Amanieu: What happened to that clock discussion?

The 8472: The ACP is accepted?

Josh: We'd like to make `Insant` generic over clock, we asked for an experiment and see if it doesn't break inference.

Amanieu: I don't think we can make `Instant` generic over clock. What woudl `InstantNow` do?

Josh: `InstantNow` would be defined with a default clock. 

Nia: Yes, we'd try to make it generic and if that doesn't work we'd create a separate type that `Instant` would alias.

Josh: We should have Chris here to tal kabout this. But it sounds like the specific requirement is the ability to call `QueryPerformanceCounter`. I'd ask to try and use the more generic instant from the other FCP.

Nia: Yes. IIRC, `QueryPerformanceCounter` was one of the motivations for the previous FPC discussion.

The 8472: It's linked from this FCP, but Chris is not talking about it here. We should ask him.

Nia: I'll ask.

Josh: And maybe we can evaluate this with him if the generic instant isn't sufficient.

### (new change proposal) rust.tf/libs788 *Add missing integer casts*

Josh: This is effectively asking for a "just let me cast everything, I don't want to think about which way to cast". This goes against our narrow/extend etc. direction. My inclination is to say "no, this is againts the diraction we want."

Josh: Saturating_truncate removes the need for it.

Amanieu: It's weird to do saturating cast and not knowing whether it's widening or narrowing.

The 8472: maybe in generic code

Amanieu: Then use `.into()` or `as`

Amanieu: But there is a reason to do lossless widening of an unsigned type to a signed type. And narrowing of an unsigned type ot a signed type.

Nia: But having the same kind of method for that could be a footgun.

The 8472: There's also the motivation that `try_into` doesn't take a turbofish. So you need to define a temporary variable (motivating example #1)

Josh: There's a dozen crates in the ecosystem that are effectively `into` but you can turbofish it.

The 8472: Can lang fix this?

Josh: Fix what?

The 8472: That you can't turbofish `try_into`

Josh: We could do it if you give it a different name. We could have a `to` method that has a turbofish. But you'd have to make it a separate thing that takes a generic argument that is the target type.

The 8472: Can we have a generic method with a `T` or `U` and have the `T` be a `TryInto`?

Nia: You could. But that's a different question from this ACP. IF someone wants to sketch that out, sure.

Nia: This ACP is way too broad but I'm not sure what the right way to narrow this is. They made it as a crate.

The 8742: They mention future work. So what we talked about (improvements to extend and narrowing)

Nia: They make a good point that a saturating conversion is arguably always valid.

Amanieu: There's also a case for `saturating_extend` when you're changing a sign. But that's weird.

Nia: It feels like a case of a differnet trait that you'd bring into the scope if you want this. But then that's the case they made.

Amanieu: There could be the case for "narrow should be able to change the signedness of the type" but not for "extend".s

Nia: Once we have `truncate_extend` you can go between arbitrary integer types with at most two method calls, right?

Josh: Yes.

The 8472: But this goes back to the length of the names. If you're chaining two lengthy methosds, you're typing half a sentence to just change the numbers' type. Keeping them short becomes moer important.

Nia: If you want to do that, we could have an alias for `.s()` for cast signed and `.u()` for unsigned cast. I do like the fact that evereything is explicit currently and than casting between signed and unsigned is something you have to think about it. And if you don't want to think about it, you can use `as`

The 8472: they're proposing more API surface than just `as`.

Nia: This feels it could be a whole project of reworking the integer primitives and conversions between them.

Amanieu: We need a matrix of all source and conversion types and then ask people to fill the table in.

Nia: Anything going from signed to unsigned would have to be "checked" or "saturating". Widening unsigned is always possible and you can set the sign.

Nia: usize is weird. How do you fit it into this?

Amanieu: For usize I'd like it to only go to u32 on 32-bit platforms and to u64 on 64-bit platforms and it being warn by default.

Nia: And if you want to avoid the lint, you'd convert usize to u128. 

The 8472: There are also usecases where you don't want the platforh specific behaviour but you can say "i don't care if it's 32 or 64 bits, I'll just convert it and panic if it's out of range"

Amanieu: Currently usize doesn't extend to anythnig.

The 8472: It wouldn't really be an extension on the platform where it's the same size. So sometimes it's an extension and sometimes a pass through. And this proposal handles that better. It on platform where it's no-op it just works and on a platform where there's a lossy conversion you can choose if it's panicking or locking. It picks the appropriate strategy based on what you told it to.

Amanieu: Is `.cast()` in this proposal basically `.into()`

Nia: Yes

The 8472:  But with `usize` and in a platform-specific way

Amanieu: As soon as we can figure out how to make a lint when you use that impl, we'll add `usize` to `.into()`.

Nia: There's massive difference between 32 and 64 bits and 16 and 128 bits.

Amanieu: There would be a range of lints.

Josh: We'd have lints for what sizes we assume on the platfroms and allow the others
 
Amanieu: lossless is always a widen and saturating always applies to narrowing

The 8472: If you don't know the direction between the types and specify how to resolve a potential issue

Amanieu: I agree but if you don't know which direction you're casting, your code is questionable. I'd like to have a motivation to justify this.

Nia: I agree. Either way, what do we do with this ACP?

Amanieu: I'll read through it and write out a counter-proposal that matches the widen/extend API. I don't want to decide on this right now.

(The meeting ended here.)


### (new change proposal) rust.tf/libs787 *Add fN::replace\_nan*

### (new change proposal) rust.tf/libs786 *\`std::ops::RangeBounds::{start,end}\_bound\_inclusive\`*

### (new change proposal) rust.tf/libs784 *Float Exp Bias Constants*

### (new change proposal) rust.tf/libs783 *Add std::range::DecreasingRange\* structs*

### (new change proposal) rust.tf/libs779 *floating\-point NaN payload functions*

### (new change proposal) rust.tf/libs778 *ACP: Add \`char::to\_u32\` to access underlying value*


### (stalled change proposal) rust.tf/libs210 *Add associated consts to f32, f64 for mathematical constants*

### (stalled change proposal) rust.tf/libs322 *\`AssertThreadSafe\` (name TBD) – a more general API for lifting conservative \`!Send\` or \`!Sync\` implementations*

### (stalled change proposal) rust.tf/libs347 *Context reactor hook*

### (stalled change proposal) rust.tf/libs501 *ACP: Add floating point representation conversions*

### (stalled change proposal) rust.tf/libs334 *Customizing \`#\[derive(Debug)\]\`*

### (stalled change proposal) rust.tf/libs207 *\`parse\_line\` method for \`Stdin\`*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*

### (stalled change proposal) rust.tf/libs452 *ACP: Implement \`TryFromIterator\<T\>\` trait to compliment \`FromIterator\<T\>\` trait.*

### (stalled change proposal) rust.tf/libs331 *Support permanently skipping tests on a specific system*

### (stalled change proposal) rust.tf/libs420 *Add basic tempfile API to stdlib*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
