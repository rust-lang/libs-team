# Libs-API Meeting 2026-03-17

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Nia, Chris, Josh, Amanieu, The 8472, David, Nurzhan

## Agenda

- libs-api representative for Leadership Council
- Triage
- Anything else?

## libs-api representative for Leadership Council

Josh: The new representative will do it for 6 months, so ?? rather than have someone appointed.

Nia: ?? because I'm a neutral bystander. Does anyone want to have the position or nominate someone?

Josh: I'd like to self-nominate. But this is not something we should be doing via a meeting; this is a process we should run async on Zulip to ensure that the broader libs team set gets a chance to participate.

Nia: I'll ping everyone involved.

Amanieu: Josh Stone is also interested, and he'll come to tomorrow's meeting.

## Triage

### FCPs

1 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3874 *build\-std: always* - (3 checkboxes left)

14 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/152851 *Fix SGX delayed host lookup via ToSocketAddr* - (3 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/139087 *Fallback \`{float}\` to \`f32\` when \`f32: From\<{float}\>\` and add \`impl From\<f16\> for f32\`* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`Exclusive\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/111688 *Tracking Issue for ExitCodeExt on Windows* - (3 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/119639 *Tracking Issue for tcp\_deferaccept* - (3 checkboxes left)

[BurntSushi (6)](https://rfcbot.rs/fcp/BurntSushi), [Amanieu (3)](https://rfcbot.rs/fcp/Amanieu), [the8472 (8)](https://rfcbot.rs/fcp/the8472), [weihanglo (1)](https://rfcbot.rs/fcp/weihanglo), [joshtriplett (5)](https://rfcbot.rs/fcp/joshtriplett), [jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [nikomatsakis (2)](https://rfcbot.rs/fcp/nikomatsakis), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [dtolnay (2)](https://rfcbot.rs/fcp/dtolnay)

### (nominated) rust.tf/libs743 *ACP: Opt\-in to calling GlobalAlloc in std's internals*

Nia: Waiting on author to submit PR.

Amanieu: We should unnominate it then.

### Allocator project goal?

The 8472: Should we include the message from oli?

Nia: If someone wants to work on allocators, I have some time to do reviews.

Amanieu: The issue is less review work and more reworking the composition of the allocator working group.

Nia: Makes sense. I don't know exactly the people oli was talking about.

Amanieu: The name was Cetra Free who's written a blog post and seems interested in moving this forward.

The 8472: Since they're proposing a goal, do they need review bandwidth from us? We'd need to approve/review/support the work, which would take up time.

Nia: We should ping people in the allocator working group and see if  anyone has bandwidth for it.

Amanieu: We could do a public post asking that and invite the interested people. It could be on zulip.

### (nominated) rust.tf/134021 *Implement \`IntoIterator\` for \`\[&\[mut\]\] Box\<\[T; N\], A\>\`*

Nia: Waffle posted a [description](https://github.com/rust-lang/rust/pull/134021#issuecomment-4075591631) justifying the PR.

Josh: Is this a proposal to implement it for `Box<[T; N]>` and B type?

Amanieu: Do we have that for Box<&[T]>?

(Yes, we do.)

Josh: There's an IntoIter for Box<&[T]> and ...

Amanieu: This should more efficient because of `N`

Nia: (... negative impls)

Amanieu: Why are negative impls necessary?

Josh: It's necessary because ...

The 8472: We never do ?? on collections themselves.

Amanieu: I don't see an explanation on why !impls are needed.

Josh: We really should have an explicit documentation on it.

The 8472: (Explains that this is documented somewhere.)

The 8472: I assume there'll be a follow-up with a more efficient impl, but it's not relevant to API.

Josh: ??

Amanieu: Right, so users won't need to deal with this for their own custom types.

Josh: Users couldn't have written those negative impls, so there's no need for a crater run.

Amanieu: This is a stabilization request, so looks fine to me.

The 8472: Do we want Clone on this? (no)

Amanieu: I can start the FCP.

### (nominated) rust.tf/139690 *\`impl Default for RepeatN\`*

Nia: This is an older item that got nominated now.

Amanieu: I've never liked Default on iterators.

The 8472: I introduced that because I found myself putting iterators in structs, and having ?? is useful.

Amanieu: It's a way of creating a repeat N that's empty. Should we have an explicit method for that instead of Default?

The 8472: I've avoided impl Default for cases where it's ambiguous, but in this case empty is the obvious choice.

Amanieu: I don't disagree with the behavior, just whether it should be a function instead of Default impl.

Nia: This seems reasonable. Not sure if it should be done with Default.

Amanieu: It's probably fine. I'll FCP this.

### (nominated) rust.tf/149926 `P-lang-drag-1` *Do not deduplicate captured args while expanding \`format\_args!\`*

(No progress, skipping.)

Josh: There's been discussion on whether there should be magic requiring the macro to be part of the knowing the implementation details of the compiler, but no progress.

### (nominated) rust.tf/152480 *\`format\_args!\`: only de\-duplicate captured identifiers that refer to places*

### (nominated) rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`*

Nia: Waiting on author. Unnominating.

### (nominated) rust.tf/153622 *remove concept of soft\-unstable features*

Nia: This is a compiler feature.

The 8472: What's the ask from us?

Josh: Are we ok with this being removed? There hasn't been new uses of this old, rarely used mechanism, so t-compiler wants it removed. This seems like a heads up rather than an ask.

Nia: Seems fine. I haven't run into this recently.

Amanieu: The only thing that comes to mind is assert_??_total_eq that can be marked as soft unstable.

Josh: We could also do a crater run.

Nia: I'd be in favor of removing this since it saves t-compiler a lot of headache.

Josh: I think it's reasonable to go ahead with this.

Amanieu: What did we do with assert_??_total_eq?

Josh: (PR). FCW is the new mechanism to do this.

?: All the more reason to remove this.

Amanieu: Thumbs up.

Nia: I'll reply to them.

Amanieu: You can use the example of assert_??_total_eq.

### (nominated) rust.tf/153990 *Decide and document where stdarch intrinsics are allowed to diverge from asm behavior*

Amanieu: The issue with stdarch is: some archs behave differently than what we expect. E.g. on 32-bit arm if you do a simd op using stdarch intrinsics, it causes normals to be treated as zero, which isn't normal behavior for floating arithmetic. For these cases we can't use ??. Min/max on some archs have different behavior for signalling NaNs and ?? NaNs. Questions: 1) what difference is acceptable? 2) ??; 3) subnormal values don't work properly when ??. Thoughts?

Nia: Inline asm is stable if you need to do things the way the platform does, right? (Amanieu: Yes.) So as long as we put a big disclaimer, this should be fine. This is just asking to document what we've been already doing, not change it. On this ground, I'd give it a thumbs up.

Amanieu: Yes. No guarantees on ?? payload and ??. For subnormals, we want the intrinsics to do the platform thing, not mathematically correct thing. I can write this down and start an FCP.

Nia: That's reasonable.

### (nominated) rust.tf/153677 *Remove string content from panics*

Nia: The argument is privacy. If you're writing a server, and something panics it might log something you don't want to.

Josh: It's not useful as well to say that there was a panic processing some ??

Amanieu: I don't think anyone relies on it, but it's useful for debugging. It's helpful to know what the string(?) was.

Nia: I'm not sure. Leaning towards approving this.

Amanieu: I'm leaning towards rejecting this.

Chris: Can we ??

Amanieu: I don't think so. The string is sensitive user data.

Nia: Someone had an idea to tie this to cfg debug_assertions. It seems to be a compromise, but I'm not sure how I feel about that. It would let panicking with different content depending on the cfg.

Amanieu: We can't because the function generating the panic isn't inlined, so there's only one copy of it in the std.

Amanieu: I'm inclined to reject this and telling people to filter ?? using regex, which was suggested on the users forum.

Josh: ??

David: Usually it's not about bad character data, but the user having to process the data in some way (??)

Josh: ??

wffl: The reason for this PR is not very sensible. You still need to ?? panic messages. You still can leak ??.

Josh: I agree. It's not useful for the originally stated purpose. I'm suggesting it's a usability improvement. We should make the change despite it not being useful.

Nia: I think it makes sense. Realistically, the current panic message is not that great.

Josh: Getting the same message ... is not useful.

Nia: This would also (tangentially) mostly address the privacy argument because it makes it less likely to spill user data.

wffl: It addresses it in the case of bad indexing. But all panics can do something similar.

Nia: Yes.

Josh: I propose we respond to this saying we want to do it as a usability improvement to make errors more useful/understandable. We don't agree that this will make privacy/sanitization better. Does that address your concern, Amanieu?

Amanieu: Yes. I had the same concern as Waffle.

Josh: I'll write it up. We don't need an FCP for this because this isn't an API. We don't need full team alignment.

Nia: Does this sound reasonable to everyone?

(Waffle and Amanieu agree.)

Nia: Should we remove the ?? nomination?

Amanieu: It's more of a libs question than libs-api. Let's consider it discussed.

### (nominated) rust.tf/153873 *deprecate \`std::char\` constants and functions*

Amanieu: We did this for fp constants? We've only deprecated the integer constants. The float ones are still under `f32::consts`.

Chris: We've soft-deprecated them with a warning.

Amanieu: This looks fine. Any objections?

Nia: No.

Amanieu: It proposes to make them associated constants?

Nia: Feel free to thumbs up.

Amanieu: I'll start the FCP.

### (nominated) rust.tf/153975 *remove forever\-deprecated and hidden \`f64\` methods*

Amanieu: If crater is clean, we accept it.

Nia: Yes, seems trivial.

Amanieu: I'll start the FCP.

### (waiting on team) rust.tf/149392 *UdpSocket: recv() and recv\_from() behave differently for zero sized buffers*

Nia: This is waiting on libs-api. The conclusion last time was to document this as is.

Amanieu: Why does this happen? Is it the underlying OS API?

Nia: Yes. (Opening last meeting minutes.) No, this wasn't discussed last week. Two weeks ago I was about to make a PR into docs.

### (waiting on team) rust.tf/152092 *Stabilize \`refcell\_try\_map\`*

### (new change proposal) rust.tf/libs757 *Implement FusedIterator for core::iter::StepBy*

Nia: This is straight forward. If you have a `FusedIterator` and put `step_by` on it, it should also implement `FusedIterator`. Is there a trait bounds that could cause issue?

The 8472: We need to ACP this as it's insta-stable.

Amanieu: I think this is fine.


### (new change proposal) rust.tf/libs756 *ACP: \`BinaryHeap::as\_mut\_slice\`*

The 8472: Did we already approve the other one?

Nia: We did.

The 8472: We need to exactly specify the heap invariant so it can be maintained.

Nia: +1.

Josh: Yes. We specified that we won't change this heap into another heap. We can mark it as accepted, add a tracking issue and close it.

Nia: I'll write it.


### (new change proposal) rust.tf/libs755 *ACP: Introduce \`alloc::io\`* 

Josh: Reasonable icremental step. There's been desire of moving parts of `io` into `core`. There are compliactios with `io::Error` requiring Box allocation. But adding `alloc::io` seems like a good step.

Amanieu: We're happy to see this happen and if teer are issues, we'd find out during implementation phase?

JOsh: Exactly. Id' be happy to write this up: love to see this as incremental step. Would love to see `core::io` but don't wantto block on that.

The 8472: Doesn't `io::Error` having OS-specific issues?

Josh: We don't call out to the OS for formatting the individual specifi variants to `io::ErrorKind`. If you have an underlying formatting OS error, we need to format those. But if you have those, you need to have an `std::io` to create it. This one doesn't ??

Amanieu: We could do this as an externably implementable item.

Josh: Or not allowing OS error in alloc.

Nia: both are fine

Josh: I think they're on teh right general track.

Josh: I'll type it up.


### (new change proposal) rust.tf/libs754 *Add common modules, such as \`fmt\`, \`fs\`, \`io\`, and \`env\` to the prelude.*

Nia: I think we can close this. I don't see a reason for this and it'd break a lot of stuff.

Josh: THere's a lot we'd like to add to the predude but the top-level modules no. This does'nt seem worth it, and it'll just add to confusion. We'll end up acting as if we have 10-12 top-level modules rather than just `std`, `alloc` and `core`.

The 8472: This could be done in rust-analyzer. It could turn a fully-qualified import into a module import. Maybe we can point them towards that if writing this is annoying.

Nia: Sure.

Josh: There are common cases we should solve by adding ot prelude: writing impls for `Debug` and `Display`. You can use/derive them implicitly but if you want to impl either trait you need to import `fmt`. In their top-list of people import, the top one is `fmt`. And I wonder how much of that is `fmt::Display/Debug` in part of an `impl`.

The 8472: `fmt::write` too.

Josh: You're right. You need `fmt::write` and `io::write`.

Josh: I think we can safely close this one.

Amanieu: I agree with what's been said.


### (new change proposal) rust.tf/libs753 *Expose Float Masks*

Nia: This is aout exposing constants for `f32` and `f64` to expose signs and mantissa. We wanted it to cook. People don't seem to have issues with the proposal.

Amanieu: This seems fine to me. We could also add exponent bits and mantissa bits

Josh: Are you saying the number of bits?

Amanieu: we do have digits -- in binary.

Nia: Is there a tracking issue for that?

Josh: Should we deprecate `mantissa_digits`?

Amanieu: Yes.

Nia: I can reply, saying we give our final thumbs up.

Josh: Sounds good. Do we have an issue we can file for that?

Nia: Doesn't seem so. I'll file one up.


### (new change proposal) rust.tf/libs751 *Extend / Replace current Linked List (currently doubly linked list) Implementation in std collections*

Nia: We looked at this, we need to close it.

Josh: I closed it.

### (new change proposal) rust.tf/libs733 *allow specifying whether \`Instant\` should be suspend\-aware*

BIKESHEEDDDDDD

Josh: This is about whether `Instant` should continue ticking over a suspend or not. Some systems provide clocks for that, others don't.

Nia: Last time we discussed this is: it's not feasible ...

The 8472: It's not feasible to have either. We can't force the OS to provide one or the other. We need to keep an OS clock that does whatever the OS can provide. In some embedded environment you may have a clock and don't know whether it's suspend-aware or not. Anything beyond this should be platform/runtime-dependent. Any additional clock should be optional.

Josh: The proposal as written (making instant general over continouous clock) could be feasibel. But making `ContinuousClock` and `SuspendingClock` is that we can't guarentee each platform providing both.

Amanieu: Yes, and BSD claims to provide both but only provides one.

The 8472: So we could do a runtime check or put this in an OS module. Windows and MacOS have hard guarantees that this should be required.

Josh: I believe the oldest linux we suport does it now too.

The 8472: So this covers the big three. So it's mostly embedded/hobby systems that may not have this. So it would be a semi-portable API?

Josh: It sounds like on Mac and Linux you can have both suspending and continuous clock.

Amanieu: Definitely on linux.

Josh: If that's the case on both, that's helpful. For Windows

Chris: It does provide both APIs.

Josh: In versions we support?

Chris: Yes.

Amanieu: We should always provide the types and say the implementation is best-effort.

The 8472: *groan*

Nia: That's a bit scary. People might run into weird heisenbugs.

Amanieu: If it's best effort, by definition there's nothing you could have doen.

The 8472: The proposal iis to make `Instant` generic over the clock. Another option wolud be for the clock source to not be encoded in the Instant type. ALternatively you could have different Clock types.

Josh: Let's try to narrow down what we could do here. Does anyone have concern around introducing a generic parameter on INstant?

Nia: It's a bit icky but I'm not opposed.

Josh: The other alternative would be a generic type and be `Instant` a type alias to that specific type.

Nia: Yes, I'm not convinced that just reyling on a default type parameter wtould work .Would love to see crater.

Josh: Alternatively `struct ClockInstant<C>; type Instant = ClockInstant<C = OsClock>; `

The 8472: Then you risk comparing different Instants and getting garbage.

Josh: If we agree on wanting that, we need to settle on the default.

The 8472: Regarding best effort proposal. Alternative: if a user can implement a custom `Clock` structure, they can implement one with fallback with their preferred clock and fall back to whatever's available.

Chris: Could we do something with the usecases over INstant? A performance time being different

Amanieu: C++ has a difference between a steady clock and a high precision clock.

Josh: Your' proposing that instead of naming them about the behaviour over suspend, we'd name them based on the semantic behavior of the clock and that would define whether it's suspend-aware or not?

Chris: Yes.

The 8472: We don't provide an epoch for `Instant` so you can't compare it against anything. We don't know when it started.

Chris: It's guaranteed for  performance counter

Josh: There's enough reasons for clocks so this is probabyl not feasible to enumerate them all.

The 8472: You want your performance counter to be fast. In some virtual machines it's not available, the kernel falls back to something slower. The API is the same and the behavior differs.

Josh: Continuos Clock and Suspending Clock are both things we can provide on Win, Mac & Linux. But not on every platform we could run std on.

Amanieu: Yes

Josh: We could provide them both as best effort. Or provide them on specific OSes. We could do this with `cfg` (we do have things like target `has_atomic`)

Amanieu: I'd prefer to support them as best effort. If you don't haev it, you're probably on an embedded and you want best-effort.

Josh: That's my leaning too. SuspendingClock should always work in that either you don't have suspend or you don't have os-aware suspend or you should provide the clock.

The 8472: Some virtual machines might get frozen.

Josh: And then provide a hypervisor clock that shows back the current time?

Nia: Yes.

Nia: I'd push back against that because there are big non-embedded situations where this is an issue. E..g BSDs where continuous clock don't work there. If you wrote a server code that depends on this and now it stops working on BSD, that's impossible to debug.

Josh: I'd agree. The other alternative is your code won't compile on the BSD.

Nia: But that's a decent option.

The 8472: We can provide the user with hard guarantees and they can then build something with cfgs. The issue here proposes a continous and suspending clock. But there would also be the third (default/os clock) one. The user could cfg win/macos/linux to use these clocks and on other OSes use the default clock.

Josh: Ther eis an obvious inverse of this approach: we could provide the best-effort version and os-specific modules for guaranteed clocks. You can write the portable code that way and opt-into better clocks on the OSes that support them.

The 8472: That's the same thing, just instead of having it in the crate we'd have this in the std.

Amanieu: My prefereerd alternative is to have both always available and then have the support in the documentation and mention that it can fall back to default clock on other platforms.

Josh: WE'd need to look into how much effort people would have to go to. If we say everyone needs to create a new crate, we've created a dichotomy of crates that do the cfc thing themselves vs. importing a crate. We could solve this once for all in std

Nia: That's fair, I'm convinced.

The 8472: Doesn't have to be in a separate crate. Can be just we name them.

Josh: Yes, we could have `std::os::linux::continuous_clock` and e.eg. `std::time::best_effort_continouous_clock`

Nia: Do we want to have two of the best-effort ones?

The 8472: We need a default that provides whatever 

Josh: What it would take if not? IF we just have continous and suspending (no default). We'd have to declare the default is continuous (change default behaviour on macos and linux) or declare th edefault is suspending (change default on windows).

The 8472: It's also about which clock is consistent with sleep and timeouts and so on. Is it always the suspending or always the continouso clock that's consistent with sleeping? Considering BSD it cannot be?

Josh: What's the freebsd issue?

Amanieu: They have CLOCK_BOOTTIME and CLOCK_MONOTONIC. Tehy're separate clocks, but in the kernel they're both wired to the monotonic clock. They will implement CLOCK_BOOT_TIME differently in the future but right now it's aliased to CLOCK_MONOTONIC

Nia: *groan*

Nia: In that case it still makes sense to have a default clock.

Josh: Either of the behavior we pick, we'd have to say "our default is a best effort". Separately, there's the qusetion of consistency with sleep. Idon't think that a clock benig best effort makes it unsuitable as a default. IT would be fine to have a best-effort continuous clock. But if that's inconsistent with the ability to define sleeps... What are they typically defined in terms of

The 8472: We'd need to survey that across OSes.

Josh: Linux give you the option to choos eth eclock.

Amanieu: In `std::thread::sleep` we pick the default clock -- the monotonic. The one `sleep` syscall uses. If you want a specific clock, the `Clock` trait can provide sleep. There's a reasonable default on Mac as well.

Amanieu: Sleep on windows uses `ticks` I believe.

Chris: Yes, it has an interupt timer.

Amanieu: Is there a way to get a high-precision interupt timer?

Chris: Yes.

Amanieu: Then that should be the default timer.

Chris: It's slower than the performance timer.

The 8472: That's another argument about which one should be the default? Sholud it be the fast one? Seems there's conflict between having the fast one vs. consistent with sleep on Windows.

Chris: Yes.

The 8472: What are we using today?

Chris: We're using the performance timer today.

The 8472: So we'd have to document that at the very least too. But I see the argument you used before. But in many cases `Instant` us used to measure short time delays and there you don't care about suspend, you need the fast thing.

The 8472: Would I have to move all my code over to some typedef taht'd pick a different clock on windows and linuxs. I sprinkle `Instant::elapsed` around my functions. I want it fast and I don't care about continouis and suspending.

The 8472: The QueryPerformanceCounter is the fast one, but it's not consistent with sleep. The interrupt one is consistent with sleep but it's slow.

The 8472: We should get a table that covers consisten with sleep, is it fast?, does it include suspend? What does it map to on each OS?

Nia: That's reasonable.

Amanieu: We can't change `Instant`. It must be queryPerformanceCounter because peopele are using that.

Josh: Yes

Nia: Yes

The 8472: Yes. So the default clock would not be consistent with sleep.

Amanieu: Yes, default would be consistent with what `Instant` does now.

Josh: On Windows, which one is the fast one?

Amnieu: queryPerformanceCounter

Josh: And does or does it not tick over suspend?

Chris: IT does include time during suspend.

Josh: The fastest clock on windows does tick over suspend. It is a continouous clock. On linux it does vDSO. So what is it on Macos?

Amanieu: On Macos it uses clock_monotonic_raw. It counts over suspend.


```
Windows
QueryPerformanceCounter: fast, ticks over suspend, not consistent with sleep
QueryUnbiasedInterruptTimePrecise: slower, ???, consistent with sleep

Linux
CLOCK_MONOTONIC: fast, does not tick on suspend, consistent with sleep
CLOCK_BOOTTIME: fast, ticks on suspend, not consistent with sleep

Macos
uses CLOCK_MONOTONIC_RAW, which is fast, and (unlike the Linux clock of the same name) does tick over suspend
MAC_ABSOLUTE_TIME aka CLOCK_UPTIME_RAW: does *not* tick over suspend
```

Josh: Should the default be the best-effort continuous clock?

CLOCK_BIKESHED: ticks slower when we're talking about naming things

Josh: So we basically want exactly what they proposed. Except that adding a generic parameter to `Instant` would likely add type inferrence errors across the ecosystem. We'd rather see `ClockInstant<C>` and have `Instant` be a type parameter to that where the type parameter is the `OsClock`.

Josh: We can suggest running a crater run to see if this would cause a wide inference failure. And list this as an alternative that doesn't cause type inference failure.

Nia: Does it still make sense to make the OS/clock table?

Josh: If there's value in OS-specific clocks, we could add them under `std::os::time...`.

Nia: I'll summarise this.


### (new change proposal) rust.tf/libs709 *Add \`str::copy\_from\_str\` and other mutating \`str\` methods*

### (new change proposal) rust.tf/libs699 *Bring \`hashbrown::Equivalent\` into std*

Amanieu: We wolud want to have the `Comparable` trait at the same time as equivalent. IT lets you check if two values of differnt types are equivalent without either of them having to implement `Eq`

Amanieu: It's basically the same idea as the proposal for `Equivalent` at the bottom: https://github.com/rust-lang/rust/pull/144506

Nia: If we already approved the `Comparable` trait this makes sense. Is there a reason we wouldn't?

The 8472: Was it ever approved?

Nia: `Comparable` has been approved. It has a tracking issue.

The 8472: I don't see anything linking to an ACP. Maybe the author just opened the tracking issue without an ACP.

Nia: In that case, do we want `Comparable`?

The 8472: I'd like to see the API and how it's used.

Nia: It's in the tracking issue: https://github.com/rust-lang/rust/issues/145986

Nia: And I assume we'll also want to have impls for types that are `Ord` or `PartialOrd`

Amanieu: We have a blanket impl based on `Borrow`

Nia: This looks like a reasonable API.

The 8472: I think people asked for somethinng more general. Custom comparators.

Amanieu: With `Comparable` you can't have a reference to custom data. People would want that.

The 8472: If we're extending the API, maybe we should consider the `Comparator` trait instead. It also allows you to construct collections with different ordering without having to wrap everything in a new type. Have a `BTreeMap` with custom sorting.

Amanieu: That may be a better and more general solution. We'll have to talk to Josh Stone (cuviper) -- we could do it in the libs meeting tomorrow. He's the maintainer of the crate that provides the equivalence.

Nia: I can also message him and ask him if he wants to join the meeting next week.

The 8472: He commented on the ACP already, maybe we can talk about it there.

Nia: It does seem to have received excitement and I don't see people complaining about it. I don't see anyone expressing problems.

The 8472: Yes, I'm just saying it's not general enough.

The 8472: There was an internals thread about someone wanting a comparator: https://internals.rust-lang.org/t/pre-rfc-add-comparator-api-for-ordered-collections/15889

The 8472: It's not the only such case.

The 8472: What are we going to do about this?

Nia: It'd be useful to get his input. We can talk about it tomorrow -- there should be time in the libs meeting.



### (new change proposal) rust.tf/libs691 *\`std::io::Read\` provides byte\-order reading trait*


### (stalled change proposal) rust.tf/libs314 *Add security\_attributes() to windows::OpenOptionsExt*

### (stalled change proposal) rust.tf/libs287 *ACP: Add \`FromByteStr\` trait with blanket impl \`FromStr\`*

### (stalled change proposal) rust.tf/libs322 *\`AssertThreadSafe\` (name TBD) – a more general API for lifting conservative \`!Send\` or \`!Sync\` implementations*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*

### (stalled change proposal) rust.tf/libs452 *ACP: Implement \`TryFromIterator\<T\>\` trait to compliment \`FromIterator\<T\>\` trait.*

### (stalled change proposal) rust.tf/libs420 *Add basic tempfile API to stdlib*

### (stalled change proposal) rust.tf/libs552 *Provider\-style API for \`Context\`*

### (stalled change proposal) rust.tf/libs111 *Restructure ptr\_metadata to minimal support*

### (stalled change proposal) rust.tf/libs520 *Add out of the box support for mapping custom error types that implement \`std::error::Error\` to \`Box\<dyn Error\>\`*

### (stalled change proposal) rust.tf/libs296 *ACP: Designing an alternative \`FromStr\`*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
