# Libs-API Meeting 2026-03-03

###### tags: `Libs Meetings` `Minutes`

**Meeting Link**: https://meet.jit.si/rust-libs-meeting-crxoz2at8hiccp7b3ixf89qgxfymlbwr
**Attendees**: Amanieu, Tomas Sedovic, Nurzhan Saken, Nia Espera, Chris Denton, laurmaedje, The 8472, Josh, David

## Agenda

- Triage
- Anything else?

## Triage

### FCPs

2 rust-lang/rfcs T-libs-api FCPs

  - merge rust.tf/rfc3874 *build\-std: always* - (4 checkboxes left)
  - merge rust.tf/rfc3892 *Complex numbers* - (4 checkboxes left)

14 rust-lang/rust T-libs-api FCPs

  - merge rust.tf/80437 *Tracking Issue for \`box\_into\_inner\`* - (1 checkboxes left)
  - merge rust.tf/106418 *Implement \`PartialOrd\` and \`Ord\` for \`Discriminant\`* - (2 checkboxes left)
  - merge rust.tf/116258 *Tracking Issue for explicit\-endian String::from\_utf16* - (1 checkboxes left)
  - merge rust.tf/139087 *Fallback \`{float}\` to \`f32\` when \`f32: From\<{float}\>\` and add \`impl From\<f16\> for f32\`* - (3 checkboxes left)
  - merge rust.tf/98407 *Tracking Issue for \`Exclusive\`* - (0 checkboxes left)
  - merge rust.tf/140808 *Implement Default for &Option* - (1 checkboxes left)
  - merge rust.tf/141994 *add Iterator::contains* - (1 checkboxes left)
  - merge rust.tf/76314 *Tracking Issue for atomic\_from\_mut* - (1 checkboxes left)
  - merge rust.tf/149926 *Do not deduplicate captured args while expanding \`format\_args!\`* - (1 checkboxes left)
  - merge rust.tf/111688 *Tracking Issue for ExitCodeExt on Windows* - (3 checkboxes left)
  - merge rust.tf/109737 *Tracking Issue for \`once\_cell\_try\`* - (3 checkboxes left)
  - merge rust.tf/121641 *Tracking Issue for \`OnceCell/Lock::get\_mut\_or\_init\`* - (3 checkboxes left)
  - merge rust.tf/119639 *Tracking Issue for tcp\_deferaccept* - (3 checkboxes left)
  - merge rust.tf/149537 *f\*::min/max do not behave as documented for signaling NaN on aarch64, or with optimizations* - (4 checkboxes left)

[jackh726 (1)](https://rfcbot.rs/fcp/jackh726), [traviscross (1)](https://rfcbot.rs/fcp/traviscross), [BurntSushi (6)](https://rfcbot.rs/fcp/BurntSushi), [weihanglo (1)](https://rfcbot.rs/fcp/weihanglo), [nikomatsakis (3)](https://rfcbot.rs/fcp/nikomatsakis), [the8472 (9)](https://rfcbot.rs/fcp/the8472), [Amanieu (2)](https://rfcbot.rs/fcp/Amanieu), [tmandry (1)](https://rfcbot.rs/fcp/tmandry), [scottmcm (1)](https://rfcbot.rs/fcp/scottmcm), [spastorino (1)](https://rfcbot.rs/fcp/spastorino), [dtolnay (3)](https://rfcbot.rs/fcp/dtolnay), [joshtriplett (6)](https://rfcbot.rs/fcp/joshtriplett)

### (nominated) rust.tf/libs743 *ACP: Opt\-in to calling GlobalAlloc in std's internals*

Nia: The original motivating examples was provided by Laurenz, but I wasn't 

Amanieu: Last time we looked at your use case, and it seemed like you wanted to replace the system allocator, not the global one. What you seem to trying to do is to use the same allocator for everything including C code.

laurmaedje: My impression from the documentation was that the glob alloc is what did that. I ran into a case where that didn't work but I was able to patch that. The C code wasn't a problem because we don't link to it anyway, but I think some way to overrinde everything is useful, but I'm not sure whether the GlobalAllocator is the thing to do that.

Amanieu: Wasm-unknown is the only target where we don't link to c and still provide an allocation. Maybe we could change the default allocator to malloc symbol to provide the allocation, or would that expose implementation details?

laurmaedje: It would help if I shared my screen. I forked the std allocator and hacked it such that there's some dead memory at the beginning that my hacked allocator will use.

laurmaedje: The spin lock will sometimes spin.. as long as I can override alloc, alloc_zeroed, dealloc and realloc, it's fine. You don't have to design this around my use case, I'm just providing a motivation.

Amanieu: You mentioned running into spinlock issues with webkit. The way wasm memory work is that you ask for a certain number of pages, and it returns the old memory siez. You're always guaranteed to have a chunk of memory that's owned by you. Why does it matter that there's another memory allocator running in the background?

laurmaedje: If you ?? alloc with ?? the grow_internal is ?. I'm trying to use preallocated memory, initializing the module with a set amt of memory, and the allocator will redistribute. `wasm::memory_grow` was broken on Safari WebKit. It's been fixed, but I'm keeping it for backwards compatibility. It's not a huge deal on macOS because ?

Josh: My impression from this: it seems like if we gave a way to override the system allocator rather than the global allocator, that'd be better for you?

L: I'm confused about the two-layered thing. Even if I use the system allocator, I suppose some software will be able to define its own allocator would that be a guarantee?

The 8472: If some C code would call `wasm::memory_grow` you would have the same problem, right?

Josh: There's a three-layer thing: 1) GlobalAllocator, 2) SystemAllocator, the Rust wrapper over the underlying sys alloc; 3) ?? the memory grow mechanism. On Unix we can override malloc, which overrides all three layers. On webassembly we can't stop you from calling memory_grow. We'd need a mechanism for that. One approach is to tell the standard library to use the System allocator, not the Global allocator...

The 8472: There's no way that would late you to override the wasm grow_memory call

L: It's only broken on specific WebKit versions

Nia: It's on them for shipping a bad version

L: Yes. To have one way to override what std's doing seems simpler than having two layers. 

The 8472: You telling people not to use System is kind of a problem because System is supposed to be usable. Why have it if people are not to use it?

L: ?? I'm not telling people not to use it.

Amanieu: Would it make sense for you to upstream your implementation to the standard library? This way you could make webkit work on all browser.

L: You would have to preallocate memory which is not really something Rust programmers would like to do.

Nia: I think having this availlable would still be useful. There are cases where someone would want to write a global allocator, but .. It might still be useful in the majority of cases where this is not happening.

L: I think it would work.

The 8472: But if you override, you'd have to write platform-specific code.

Nia: Yes. That's what's happening here.

Amanieu: Trying to think of alternative solutions. What if you allocate ?? then immediately free it? I don't like having two ways to override the allocators.

laurmaedje: Overriding Global is something you do in leaf crates. 

Amanieu: The alt solution is opting into threadlocal storage .. 

L: In the end, you don't need to do anything, this is just a design consideration.

Nia: This is a design consideration for libs so it makes sense to be brought up. If overriding the System allocator feels too magicky, we're back to having a method. And we just bikeshed which name we'd use. Is there anyone who doesn't want to do the thing proposed in the ACP?

The 8742: Feels limited if the only motivation is to have a workaround for a broken system.

Josh: There's another use case for this: things like tracking every memallocation, e.g. profiling. There's value in interposing every allocation. These have the same effect, but are different in how we teach them to users

The 8472: The user code is not obligated to look at the allocator flag and so this can change and affect the instrumentation code.

The 8472: If it wants to bypass something fancy that the allocator is doing.

The 8472: Do we promise it's libc compatible or not?

Amanieu: We make no promises.

The 8472: The main use case is bypassing the system allocator.

Josh: yes.

Nia: There are cases where the global allocator is provided by the main library, but you might want to use an allocator from a different library.

Josh: I'd expect all these to be set at the top level. These are presented in two different ways and provide two ways of control. One is a trait implementation that says "use me for system allocations too". The other is the root crate that would override the allocator by using the #[global_allocator] that also wants to override the System allocator. Which one do we present? One has more .., the other can catch more things.

Nia: This brings us back to providing a proper way to provide a system allocator.

The 8472: ?

Josh: (Yes, I meant the method.)

Amanieu: I'm not too opposed to having this on nightly for people to experiment with. I don't think there's a lot of evidence that this would be useful on stable.

Nia: I agree.

The 8472: What we just discussed is different from the proposal. The proposal talks about reentrancy and threadlocal ??, but we talked about overriding the systemallocator.

Nia: We could close this ACP and ?. ?? the related ACP, but we need to check if they're orthogonal. Is there a reason to have both the method and trait implementation?

Josh: I don't think you've missed anything.

Nia: Should we do this as a nightly thing?

Josh: The question is what this should look like.

Nia: An attribute, like #[global_..]?

Josh: Should it be the same macro, or a different one?

Nia: This is bikeshedding, but it would make sense to have different attributes.

Josh: Presumably, they'd rely on the same trait. You'd have to be an implementation of ??.

L: One other design is to have a bool `reentrant` .. or `non_reentrant` but that's not nice.

The 8472: We don't have a stable Allocator API, so this would also be unstable. It makes sense in the abstract sense, but in practice people tend to override malloc itself, so this only matters in wasm and ??.

Amanieu: ??

L: The weak symbol is also an option, right? Another malloc symbol that could be overriden?

The 8472: The weak symbol is more linker level. The issue is Wasm doesn't have a widely agreed on interface. It would be Rust-specific.

Nia: I'm not sure if we should pursue this then, otherwise we'd end up in halfway-land.

L: This is how wasm `__TLS` feel like. It feels less bad in the public API.

Nia: Do others have preference?

Amanieu: I hate all of these equally. The bool stuff seems like we're exposing a semi-stable API in a weird way, and the other stuff feels like we're trying to solve a problem that only exists in WASM.

Josh: I think there's still a valid use case for overriding system alloc, but we need to be careful in ??. Not that this is a necessary use case, what version of WebKit is this so we know when that's going away?

laurmaedje: macOS/iOS 26.2, now on 26.3

Nia: So that's not going away soon.

Josh: Still, it's a point release so hopefully a lot of people would upgrade.

Nia: We've broken WASM on a point release of macOS.

The 8472: The other workaround is to detect the bug and switch to single-threaded just on those systems.

L: In the application or Rust?

The 8472: I think we can detect multithreading inside Rust, can't we?

L: Not really, ?? the application wouldn't work ??

Nia: Given everything, going for overriding the SystemAllocator seems like the proper approach that fixes wasm and potentially issues on the other platforms.

The 8472: What advantage do you see doing on the Rust level rather than overriding malloc?

Josh: I suggested systems where it's hard/impossible to override malloc and you don't want to play linker games, and you want to catch any Rust code that calls System. 

Amanieu: I don't like overriding SA because it gives a false impression that that it will intercept the malloc symbol and let you override it for C dependencies as well, which is not what it actually does.

Amanieu: Now we have a stable API for symbols on wasm. I'm proposing what's proposed in the PR.

Nia: ?

Amanieu: I'd not provide overriding System, only a flag to use the std allocator like in the PR.

The 8472: This will not help many people because the standard libary crates will be using system.

Amanieu: No third-party crates should use System, unless ..

The 8472: We're not telling people that.

L: Amanieu is right, if a crate is using System and I want to override that, then those two things are incompatible and I shouldn't be using such a crate.

Nia: We can go for that. Rename the ACP, document that if you are using System you'd be playing with fire. We can respond and/or continue discussing next time.

L: Thank you for considering my issue in such detail.

### (nominated) rust.tf/149926 `P-lang-drag-1` *Do not deduplicate captured args while expanding \`format\_args!\`*

Nia: This seems to be blocked by Niko on lang?

Josh: It's more complicated that that, having both lang and libs purview on it. The concern is to keep that space.

Nia: This seems to be about struct fields in format args?

Josh: We're using struct fields as an example, but the discussion is mostly around if it's valuable. Maybe we should let it percolate in the background and move on to another item. It's not urgent.

Josh: We should keep an eye on it, but the discussion can be kept async until we need to sync.

### (nominated) rust.tf/152480 *\`format\_args!\`: only de\-duplicate captured identifiers that refer to places*

### (nominated) rust.tf/152851 *Fix SGX delayed host lookup via ToSocketAddr*

Josh: This was the SGX hack we discussed last week where we tried to figure out the right approach.

Nia: We asked for some clarification, and for the next item as well.

Josh: The author has updated this to make it simpler as requested; I'd talked with them and tried to figure out something simpler with them.

---


Josh: 1) Do we want to do this at all? 2) This particular hack affects other targets too much. We've not settled (1), but for (2) the original version was too invasive, and the current is as minimal as possible in the sense that we don't ?? the cfgs outside of the ?? crate. ?? is instead providing a helper to do lookup host strings. Factoring that out of the use cases such that each target uses a standard impl, and SGX provides its own implementation. Every sys crate just uses that standard implementation. That seems like a reasonable refactoring strategy, and a least invasive way to preserve this hack. Do we want to keep this hack at all?

Nia: For now we should at least preserve this hack. There's a test for this. Beyond that, removing this hack on SGX would be annoyingly breaking. If not many people depend on this, we might fix them manually.

The 8472: Weren't people already broken on stable?

Nia: Yes, but ??

(Amanieu joins)

Josh: 8472, this isn't tied to specific crates, unfortunately. The point is from SGX point of view was that if he hack this into the standard library, we won't have to change every crate. That was a bad idea, and the fact that it's unilaterally extending what std does on every platform was a bad idea. We should communicate that people shouldn't be doing that again, but also keep the hack.

The 8472: What does this actually do?

Josh: The author gave us the pointer to the fortanix documentation. _DNS names ?? and that kind of thing aloows you to lookup what the ? port is on the system and ? it. Service names given as names rather than ports (:ssh rather than :22). This would require a lookup on toher services but not SGX.

Amanieu: The proper fix is to add a ? value for SocketAddr, which is ??, to say that it's an alternative way of representing the address.

Josh: 1) SGX itself won't be able to use it because they're trying to work with software not written explicitly for SGX

Amanieu: Yes, but that way ToSocketAddr works normally and doesn't do the Err hack. ??

Josh: You're talking about the internal type?

Amanieu: No, [SocketAddr](https://doc.rust-lang.org/nightly/std/net/enum.SocketAddr.html).

Nia: But that would still go against the point ??

The 8472: The enum is not `non_exhaustive` 

Amanieu: The correct design is somewhere in that space. Somehow generate the SocketAddr

The 8472: On Unix, the sockaddr type gets pun into different types, it's just inet sockets that use `sockaddr_in`... other sockets can use strings, e.g. unix sockets.

Nia: Alternatively, we can merge this as is, ping whoever was in charge of ?? SGX, and ask them to create an RFC to make this workable instead of a hack.

Josh: I also think we should merge this, saying we want to preserve this hack to prevent breakage -- but also say they unilaterally extended the API surface of the standard library and we'd like to find a better way of handling that, and what would that look like.

I originally thought, ??, and in that scenario I would've thought, ??. In this case they want to support additional name resolution, and they want to hand that out to the runtime because it can do name resolution and the insides can't. The long-term solution is that ?? should support this format for str. ?? We could extend it to support _ services. Then everybody gets better, not just SGX.

Amanieu: When you ??, you have to way of knowing how to parse it. There's overlap between ??, and you can't disambiguate it from just a string.

Josh: You can parse the service name. You can say that `hostname:service` you can figure out it's the service name rather than the port.

The 8472: what SGX is doing is just doing additional lookups and converting it to IP addresses and ports?

Josh: It's ultimately converting the string into something you can connect to.

The 8472: So it's not passing the string directly to `connect`?

Josh: It's not. I'ts not passing the string to connect. It's doing a resolution into something that can be passed to connect.

The 8472: So it's doing something that should be separate steps in one function call.

Josh: ?? They're generally useful. We want to have reviewed this before the API was extended, but I think there's value in extending that API.

Nia: That's fair. 

Josh: Concrete proposal: I'd like to see feedback from joboet, whose position is "I don't approve but libs-api can override my opinion". We can override it but have to be careful so I think we should do a libs-api FCP merge. I think we should register that this was a unilateral API extension that we weren't aware of, and going forward we'd like any API extension run by libs-api.

Nia: Agreed.

The 8472: I'd prefer we have a deadline before this gets removed, otherwise we say something, and nothing happens. We will still eventually break them.

Josh: If we fix this in the way I'm suggesting, we'll ??

Amanieu: But then we've changed the behavior on every platform ??

The 8472: Is ` _example._tcp.example.com` something standard?

Josh: Yes. This is doing DNS resolution.

The 8472: With A records?

Josh: With a combination of A and service records.

The 8472: Do browsers support this?

Josh: Yes, browsers currently support for HTTP3. You're supposed to look up example.com and `_tcp` as a service, and then ?? to ??.

The 8472: The whole thing looks like a security surprise. We should be careful with introducing this behavior on every platform.

Nia: I'd prefer this to be a separate API and us being loud about this change. It's a bit scary from the security standpoint.

Josh: We shouldn't design it in this meeting. We should ask ?? to design this through an ACP or RFC, or some team discussion. I'm only suggesting a strategy.

Josh: We should make the removal conditional on doing something. Removing this could go several ways. We should remove the regression and come up with a better solution.

The 8472: We'll still need to make that decision down the road. My concern is that we fix the regression now and keep the hack forever.

Nia: ?

Josh: Yes, we should make a reminder to come up with a solution or ??

Amanieu: I'll separately reply to their suggestion that the proper approach `_socket_addr`. I don't see any other way to make this work.

Josh: That is an implementation detail of how you get there.

Nia: It seems like the consensus is to FCP merge.

Josh: I'm starting it.

### (nominated) rust.tf/152855 *Do not emit separator as before elements*

Nia: What was the conclusion of the last discussion?

Amanieu: We're fine with making this change for now, but we'd like to see tests. https://github.com/rust-lang/rust/pull/152855#issuecomment-3953688865

The 8472: The author asked for clarification on which of the two variants we want.

Nia: I'm not certain we addressed the ?? case.

The 8472: Is None in the middle really special? The None in the beginning could be from the previous iterator.

Nia: I'd argue that the first thing (emitting a sep after every Some) is the intuitive solution.

The 8472: Not sure if it makes sense because a lot of loops will stop after the first None.

Nia: This is for non-fused iterators.

The 8472: You can use non-fused iterators as restartable loops. They start, you hit None. Then you can continue the loop. If you consider them as disconnected streams or characters, or batches, I'm not sure. I think the None in the middle can be meaningful.

Nia: Then the second makes more sense.

David: I agree that the first case makes more sense. I don't know that restartable loops are commonly used.

The 8472: I've done this once or twice; it's fairly uncommon to rely on unfused behavior. Not sure which one's better.

Nia: We could ask the person proposing this for an example. Can we reach out to more people for this? I'll reach out to the author.

### (nominated) rust.tf/153261 *Partially stabilize \`ptr\_alignment\_type\` as \`alignment\_type\`*

Amanieu: Right now the Layout type takes a size as a usize, and alignment as a usize. There's no way to encode that the alignment is valid. A valid alignment is a power of two.

Chris: It's been suggested that we move it to `mem` instead of `ptr`.

Amanieu: Layout is now in `alloc`.

Nia: If `align_of` is in `mem` then we probably want to use `mem`

Nia: So we can sign off on moving it to `mem`?

Amanieu: Are we happy with this? What does this actually stabilize?

Nia: There are a couple of methods on Layout, non-fallible versions of pre-existing mem size align

The 8472: When I try to have a struct with ?? it fails to compile. This is not ?? but a compile-time restriction.

Amanieu: There was an issue that statics ...

The 8472: Should we have a restriction that ...

Amanieu: I expect that restriction to disappear. Linux as a year ago respects ?? on elf headers.

Josh: Good. That makes me wonder if that article on making the smallest ELF executable is slightly broken now.

Amanieu: It's definitely broken. Someone tested it.

The 8472: Still, the compiler imposes limits. We should have an API that behaves like the compiler.

Amanieu: I disagree. We should have something that just works for every kind of layout. The restriction on the compiler only exists ??

The 8472: the off_t already does it automatically. At least we could add a comment.

Amanieu: I'm uncomfortable stabilizing this without the associated stuff in Layout, or with a plan for stabilizing it.

The 8472: Scott has been working on layout stuff, maybe we should ask for his opinion? I see, he proposed a partial stabilization.

Amanieu: What do people think about the Layout methods, which remain unstable? If we commit to adding this, then we should also commit on layouts.

Nia: Might still make sense to do this partial stabilization, since this is useful on its own. But the other methods are useful too. They seem fine, though the names could be bikeshedded. Not sure `align` and `alignment` mean different things.

Amanieu: Rather than deprecating from a version, what are your thoughts on deprecating in the next edition?

Josh: Would there be some way to opt into getting the warnings early if you want to migrate early?

The 8472: I don't see why this should be limited to editions. This is a new API.

The 8472: I still don't see how it's tied to an edition. If we deprecate it in five versions...

Amanieu: That also works. For this stabiliation request I'd like to stabilize the Layout methods as well.

Nia: Are we happy with the methods as is?

Amanieu: I'll have to check. Some of these are still unstable on Layout. Nevermind, we've stabilized all of them.

Nia: Then it makes sense to stabilize everything. We can ping teh author to expand the scope. Who would like to do that?

The 8472: There was some discussion on the tracking issue that pushed for partial stabilization. I wasn't following what the motivation was.

Amanieu: Scott suggested the partial stabilization [here](https://github.com/rust-lang/rust/issues/102070#issuecomment-3978905896)

Nia: They were talking which module to put it on (`ptr` or `mem`), which would be annoying for stabilization. We seem to have consensus on `mem`.

The 8472: If it's just names or modules, then it's fine. If there are other concerns, we sohuld understand their motivation.

Nia: I don't see any concerns besides that. So do we say we put it in mem?

Amanieu: Yes, seems like the right place.

Nia: Ok, we can reply with that. I can do it myself.

### (nominated) rust.tf/153272 *Add \`Path::absolute\` method as alias for \`std::path::absolute\`*

Amanieu: This one sounds fine. I can write up the response.

### (waiting on team) rust.tf/55794 *UdpSocket receive to short buffer behaves differently on Unix and Windows*

Nia: This is an old thing, Chris put a label on it.

Chris: ? truncate on Unix. Is that the behavior we want?

Amanieu: Both truncate, but Linux returns success and Windows returns an error?

Chris: Yes.

The 8472: I've used partial messages, UDP messages knowing the data was there, and it got truncated... Is it proposing that the whole buffer was filled? The truncated message can still be useful for logging etc.

Chris: There was a proposal to report the truncation in some way. They're waiting for libs to weigh in and apparently they never did.

The 8472: Receiving a truncated message on UDP is somewhat useful. We shouldn't turn it into a generic error when there's data in your buffer.

Chris: Do we have ?? where this could be used?

Amanieu: Is it silently truncating, or also returning a length that's bigger than the buffer? So you can detect that the extra data was sent and discarded.

Chris: That seems to be a test case, they don't ??

The 8472: The Linux syscall `recvmsg` has a flag called `MSG_TRUNC` which is set if additional bytes were discarded, but not the amount of truncation. Is it a standard flag? Yes, it's part of POSIX. Does Windows have recvmsg equivalent? Can we tell it not to return an error and set a flag instead? 

Chris: we can catch it, yeah

Amanieu: On Linux it will receive a message and ?? even if it was longer than passbuffer

Josh: It would tell you how long it needed to be to not be truncated.

Chris: Sounds it shouldn't have been truncated then?

Josh: That's not great behavior but you can increase the buffer size. And if not, there's a reason teh transfer is called unreliable

Amanieu: If we changed the behavior to an error, that would open up ?? for vulnerability.

Nia: I agree, it's more of a case where we put documentation ??

The 8472: Can we just extend the buffer and get the full message?

Josh: Not portably, unfortunately. I've looked into this a lot. You can do that easily as long as you don't care about portability. We can't change this to "return an error on Unix" because there are people are expecting this behavior, and their code would be broken. And it's not their code's fault. It's debatable whether we sohuld change the Windows behavior to match the Linux behavior -- but we definitely cannot change Linux behavior to match Windows.

The 8472: The impl for UDP socket [says](https://learn.microsoft.com/en-us/previous-versions/windows/desktop/legacy/ms741687(v=vs.85)#dwflags) ? that it'd be cut off. Are we using this API or why doesn't it return an error?

Josh: Reading the docs. It does give you the data and it produces an error code. We could turn that into an indicator that you're getting truncated data.

Nia: We are respecting the platform's behavior. Are we saying in our documentation that we say one thing over the other?

Josh: We do. In our docs, we say it will get truncated. Our API doesn't appear to have a way to get the data and the error indication.

Amanieu: The only way I see is to return a buffer length that's larger than the provider data ..

Josh: But on Windows we don't know how large the buffer is.

The 8472: We could just return a large number.

Josh: If it's truncated, it wouldn't fit in the buffer. The length returned indicates of how large the buffer should be for the message to fit in without being truncated. Chris, is there a way to figure out the proper length?

The 8472: It seems there is a way. LP number of bytes received

Chris: I've not tried it.

Josh: Does that give you the number in the buffer or the full length?

Amanieu: "A pointer or the number of bytes received by this call".

The 8472: Even if we get the data out, our stdlib API signature is a `Result`. We can communicate either `usize` or an error.

Josh: But the Error also is unambiguously indicated if the size is bigger than the buffer.

Nia: So should someone knowledgable looks into if this is possible for Windows? I prefer documenting it as is. It was brought up in 2018 and it doesn't seem to have been an issue with this.

The 8472: Given our API guarantee says that it's truncated and a lot of people write code for Unix but if it compiles on Windows is nice but not critical. But I'd be in favor of handling this if possible.

Nia: If there's a nice way then yes.

Amanieu: I'm looking through the wine code... On wine, it doesn't pass the message trunk.

Josh: So if you pass message trunk it gives you the total?

Amanieu: The underlying unix syscall doesn't return the buffer length.

### (waiting on team) rust.tf/149392 *UdpSocket: recv() and recv\_from() behave differently for zero sized buffers*

Nia: Is this platform specific?

The 8472: It's about a difference between `recv` and `recv_from`. I'm not sure the proposed fix makes sense.

Nia: They seem to want it to be the same behaviour. Does that make sense?

The 8472: In IO APIs, normally a 0-length read isn't a bug. But I'm not sure about UDP. It's not that unusual when streaming.

Chris: It does seem strange that they do different tings.

The 8472: They probably use different syscalls.

(the meeting ended here)

### (waiting on team) rust.tf/152092 *Stabilize \`refcell\_try\_map\`*

### (new change proposal) rust.tf/libs748 *Add \`is\_disconnected()\` to \`std::sync::mpsc\` and \`std::sync::mpmc\`*

### (new change proposal) rust.tf/libs746 *ACP: Add \`TryFromIntError::kind\` method and \`IntErrorKind::NotAPowerOfTwo\` variant*

### (new change proposal) rust.tf/libs733 *allow specifying whether \`Instant\` should be suspend\-aware*

### (new change proposal) rust.tf/libs710 *\`BinaryHeap::from\_raw\_vec\`*

### (new change proposal) rust.tf/libs709 *Add \`str::copy\_from\_str\` and other mutating \`str\` methods*

### (new change proposal) rust.tf/libs700 *Mapped Arc and UniqueArc*

### (new change proposal) rust.tf/libs699 *Bring \`hashbrown::Equivalent\` into std*

### (new change proposal) rust.tf/libs691 *\`std::io::Read\` provides byte\-order reading trait*

### (new change proposal) rust.tf/libs688 *Initial state once cell value*

### (new change proposal) rust.tf/libs659 *Generalize ExactSizeIterator to QuantifiedIterator*


### (stalled change proposal) rust.tf/libs347 *Context reactor hook*

### (stalled change proposal) rust.tf/libs296 *ACP: Designing an alternative \`FromStr\`*

### (stalled change proposal) rust.tf/libs462 *impl fmt::Write for BufWriter*

### (stalled change proposal) rust.tf/libs246 *ACP: replace use of \`Pointee\` trait with a \`ptr::Metadata\` type*

### (stalled change proposal) rust.tf/libs322 *\`AssertThreadSafe\` (name TBD) – a more general API for lifting conservative \`!Send\` or \`!Sync\` implementations*

### (stalled change proposal) rust.tf/libs262 *Add infallible variant of RangeFrom::next()*

### (stalled change proposal) rust.tf/libs298 *Constructive/Destructive Interference Size Padding*

### (stalled change proposal) rust.tf/libs287 *ACP: Add \`FromByteStr\` trait with blanket impl \`FromStr\`*

### (stalled change proposal) rust.tf/libs192 *Report allocation errors through the panic handler*

### (stalled change proposal) rust.tf/libs314 *Add security\_attributes() to windows::OpenOptionsExt*


_Generated by [fully-automatic-rust-libs-team-triage-meeting-agenda-generator](https://github.com/rust-lang/libs-team/tree/main/tools/agenda-generator)_
